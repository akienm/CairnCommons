{
  "id": "probescan-resolves-the-real-shim-not-a-discovered-one",
  "schema_version": 2,
  "date": "2026-08-14",
  "owner": "CC owns the code and the proofs. One bound is not CC's to move: the fix may NOT be to make `DiscoveredShim._start_device` return something. That refusal is deliberate and documented (discovered.py: 'NO PAGE. This shim deliberately does not implement _start_device: it fronts a device for the BEAT, not for the web surface') and softening it to turn a column green is exactly the plaster the 2026-08-14 rule forbids.",
  "node_class": "code-seam",
  "state": "code-seam@v2: THINKME -> [TICKETME] -> BUILDME -> PROVEME -> WATCHME -> PROVED (cast 2026-08-14 by hand; TICKET is an entry point and is not yet a skill)",
  "intention": "`bin/cmd/probescan`'s `can_receive` must resolve the shim the real delivery path would resolve — the device's own registered shim when it has one, a DiscoveredShim only when it does not — instead of constructing a DiscoveredShim for every device unconditionally.",
  "why": "MEASURED 2026-08-14, while dispositioning probescan's RED for an unrelated crossing. `can_receive` opens with `shim = DiscoveredShim(device_id)` for every id, and `DiscoveredShim._start_device` raises NotImplementedError by design. So the rung it reports is a property of that one class and not of the device: probescan is STRUCTURALLY INCAPABLE of returning `3-resolved` for anybody, ever, and its whole device column is a constant.\n\nThe reading that proves it is not an argument about the code: probescan reports `1-no-start-device` for the LIBRARIAN, and LibrarianShim()._start_device() returns a LibrarianDevice whose `receive` is callable. The one device on the roster that demonstrably can take mail is reported as unable to.\n\nWHAT MAKES THIS WORTH A TICKET RATHER THAN A SHRUG: probescan's own docstring states the contract it breaks — 'Mirrors BaseShim.deliver step for step so the answer is the answer a real envelope would get: resolve a shim for the id, wake the device, look for the receive face.' It does not resolve a shim for the id. And `GroundLoopDevice._reconcile` prefers the real shim when both exist, so the path probescan claims to mirror already picks the other one.\n\nAND THE PART THAT MATTERS MORE THAN THE FALSE RED: a check that cannot pass teaches nobody anything when it fails. probescan reds 23 of 23 today, it red 18 of 18 on the day it was built (dc16351), and a reader cannot tell from that number whether the world got worse, whether a device lost its receiver, or whether the instrument was never able to say otherwise. The genuine standing gap — nothing on the roster takes mail — is ticketed at `mail-arrives-and-what-cannot-marks-itself`, and THAT is the finding this constant is currently drowning.",
  "traces_to": [
    "Law 3 — nothing is known until measured. A column that reports the same value for every input has measured nothing, whatever it prints.",
    "Law 9 — red is the default and green is earned. A red that cannot be earned green is not Law 9, it is a stuck needle: it carries no information about distance from the spec, which is what red is for.",
    "Law 10 — nothing in the system is immeasurable. The real shim is resolvable from the roster the heartbeat already owns; nothing had to be built to read it right.",
    "Akien 2026-08-14 — 'we keep on until this whole skills and mechanical workflow is solid'."
  ],
  "gates": "(1) RESOLVE: `can_receive` asks for the device's registered shim first — the same resolution `GroundLoopDevice._reconcile` performs, borrowed rather than re-implemented, because a second idea of which shim wins is the drift this ticket is made of — and falls back to DiscoveredShim only when no real shim exists. (2) REPORT THE FALLBACK AS ITSELF: a device reached through a DiscoveredShim is not 'broken'; it is a device with no page and no receiver, which is a different sentence from 'its shim cannot wake it'. The rung vocabulary gains that distinction or the report keeps conflating a deliberate design with a defect. (3) STILL DO NOT DELIVER: the existing refusal to invoke `receive` stands — a check that delivers mail has written to a record of truth.",
  "falsifier": "DONE when probescan reports `3-resolved` for the librarian and for ground_loop, reports harbor_master's genuine lack of a receiver as its own rung distinct from 'cannot wake', and its verdict line can therefore CHANGE when the world changes. WRONG INTENT if the fix reaches into DiscoveredShim to make _start_device return something, or if the rung vocabulary is collapsed so that 'no receiver by design' and 'wake failed' print the same — either one buys a green by deleting the distinction the instrument exists to draw.",
  "horizon": "One build. The resolution logic already exists at `GroundLoopDevice._reconcile`; this is a call site pointed at the wrong constructor, plus the rung the report is missing.",
  "dependencies": [
    "Reads on `mail-arrives-and-what-cannot-marks-itself` (TICKETME) only for the ORDER of the two: that ticket is the real gap (nothing on the roster takes mail), this one is the instrument that has been unable to say so. Neither blocks the other — fixing the instrument first is what lets the other one's progress be seen."
  ],
  "provenance": "Surfaced 2026-08-14 while dispositioning probescan's RED at the WATCHME->PROVED crossing of `the-loop-names-its-own-staleness-instead-of-benching-a-device`. The staleness build added the 23rd probe to a queue that was already 22 long and already red; the red is not that build's and is not this ticket's parent. The measurement that separated the two is recorded in this ticket's `why`.",
  "distinctions_recorded": [
    "A FALSE RED AND A STUCK NEEDLE ARE NOT THE SAME COMPLAINT, and the second is the worse one. The false red on the librarian is what made this visible, but a probescan that red every device correctly would still be uninformative if it could never do otherwise. The defect is that the output does not vary with the world.",
    "THE VERDICT ON harbor_master IS CORRECT AND THE ROAD TO IT IS STILL WRONG. harbor_master genuinely has no registered shim, so 'cannot receive' is true of it. Right answer, wrong reason — and a check that reaches a right answer by a road that cannot reach a wrong one has not confirmed anything."
  ],
  "intent_berth": "none, because this is a bug: editing lines of code fixes it, and TICKET is the direct entry point for a bug (ruling 2026-08-02, three entry points). No new intention is being born — probescan's own docstring already states the intention this code fails to meet."
}
