{
  "id": "73c9d3093973",
  "title": "an-emission-is-one-file-named-by-its-pointer",
  "date": "2026-08-19",
  "owner": "CC builds; Akien owns the filename's spelling",
  "owning_intention": "cairn/tools/base/intention+why.json",
  "intention": "A diagnostic emission is ONE FILE, named so its identity is readable without opening it \u2014 and the pointer becomes thin enough to be part of that name. TWO HALVES, ONE GRAIN. (1) THE SHAPE: cairn/tools/base/breadcrumb_log.py stops appending lines to a single per-instance diagnostics.jsonl and writes one JSON object per emission, at a name carrying the UTC-normalised microsecond stamp, the source device, and the crossing \u2014 Akien's `timestamp.inference.task.json`, shape ruled, spelling his. The reader contract does not move: BreadcrumbLog.records() still returns the trail in write order, so cairn/machines/diagnostic_inspector/inspector.py composes over it unchanged, which is the evidence that the grain moved and the contract did not. (2) THE POINTER: cairn/devices/inference_domain/domain.py passes `pointer=canonical` at all three of its emit sites \u2014 the whole canonicalised request, median 1,171 chars and max 10,078 \u2014 and passes a canonical DIGEST instead, the full text staying where it always was, in the inference_calls row's own canonical column. THE JOIN THAT MAKES THESE ONE NODE AND NOT TWO: the filename carries the task, so the file cannot be named until the pointer is short enough to BE a name. A 10,078-character pointer is not a filename on any filesystem here.",
  "why": "AKIEN, 2026-08-19, verbatim: 'logging emissions are supposed to be single json emissions. why are we adding rows to diagnostics.json?' \u2014 then, correcting his own framing in the next breath: 'i was expecting to see timestamp.inference.task.json or some such. and i have to be clear, i just created a contradiction. and just noticed it. each call is one task ticket recieving multiple emissions... but for a single inference call.' THE CONTRADICTION HE FLAGGED WAS MEASURED AND DISSOLVES, which is why this ticket is a grain change and not a redesign: across 140 emissions and 68 distinct pointers the gate sequences are only ever miss->hit (66 pointers) or miss->hit->hit->hit (2), and the gaps between two emissions sharing a pointer run min 3.2s, MEDIAN 282s, max 4,348s, with ZERO under one second. Those are separate calls minutes apart asking the same question, not one call emitting repeatedly. One inference call = exactly one emission, always. What made it read as 1:N is that the pointer is the PROMPT rather than the call \u2014 it names the question, and a question outlives the calls that ask it \u2014 which is the same fact the second half of this ticket fixes. AND HIS OWN STATED WHY IS THE ARGUMENT FOR THE CHANGE: the reason he gave for the one logs root on 2026-08-18 was retention, 'easy to delete everything in the logs tree over 30 days old in one go'. A single appended trail CANNOT expire a record \u2014 mtime belongs to the file, so the sweep takes a device's whole history or none of it \u2014 while a file per emission gives per-record expiry for nothing. breadcrumb_log.py's own 'WHAT IS DELIBERATELY ABSENT' paragraph deferred rotation and evaporation on build-minimal grounds, correctly as a policy, and in doing so filed away the fact that the chosen grain could not express that policy when it arrived.",
  "traces_to": [
    "Law 1",
    "Law 3",
    "Law 4",
    "Law 5",
    "Law 7",
    "Law 9"
  ],
  "falsifier": {
    "proves_green": "PROOF GATE: tester \u2014 proofs beside the code, run twice (never trust the first green), sealed under TesterDevice().run_proof(path, caller=..., isolation='netns'), VALIDATION persisted beside the code through the store's own door. Two proof addresses, because the ticket has two code owners: cairn/machines/diagnostic_inspector/proofs/test_breadcrumb_log.py (the grain: one emission one file, write-order preserved through records(), per-record mtime expiry demonstrated in BOTH directions) and cairn/devices/inference_domain/proofs/test_an_inference_call_logs_that_it_started.py (the pointer: thin, and joinable to a real standing row by hashing that row's canonical). RED FIRST IS REQUIRED on both: each proof must fail on the unmodified tree on exactly the arms the defect owns and pass the rest, or the green is evidence about the fixture. THE ISOLATION CLAUSE IS NOT BOILERPLATE HERE and is bound explicitly because this ticket's own proofs write into the tree they read: every arm must ride set_diagnostic_roots over a temp roots table with a before-witness that the directory did not exist, never a substituted receiver (which would prove the receiver instead of the default), and the standing-rows join in clause (3) must read rows it did not write \u2014 the population is 15-of-19-written-by-the-test, the exact defect sibling ticket a-proof-cannot-seed-the-tree-it-reads was cast against. ENTRY GATE: buildme_rides_the_chart \u2014 a berthed validate packet claiming this ticket, or the emit chokepoint refuses BUILDME with EntryGateRed. EXIT GATE: proved_answers_the_chart \u2014 a complete passing verdict artifact against the claiming validate berth's criteria, or PROVED refuses with ExitGateRed. EMISSION GATE: the WATCHME below is in the workflow string and is not skippable, so the probe must be ARMED at its declared berth before the crossing or WatchmeEmissionRed fires. CHARTER GATE: the shape is stated as a promise in prose at cairn/tools/base/diagnostic.py:24 and inside cairn/devices/inference_domain/intention+why.json falsifier clause (9); both are charter deltas owed IN THE SAME ACT as the code, each poking cairn/tools/intentions_model_compiler/recompile_gate.sh. FEEDS BACK TO ORIGIN ON FAILURE (Laws 3 + 8): a red at any of these is a finding routed to this ticket, never a paragraph explaining it away.",
    "proves_red": "FALSIFIER \u2014 DONE when all five hold, and each is read off disk rather than off a return value. (1) ONE EMISSION IS ONE FILE: a real inference call (not a fixture) leaves exactly ONE new file under ~/.cairn/logs/inference_domain/0/, that file parses whole as a single JSON object, and no diagnostics.jsonl anywhere gains a line in the same window \u2014 measured by a before/after directory listing, never by asking the object what it wrote. (2) PER-RECORD EXPIRY IS REAL, which is the property the single trail CANNOT have and therefore the clause that makes this ticket worth its cost: stamp two emission files' mtimes at 31 and 1 days back, run one `find <logs root> -type f -mtime +30 -delete` against the ONE root, and exactly the old one is gone while the young one stands. Under today's shape that same command either deletes a device's entire history or nothing, and the proof must assert BOTH halves so the green is about expiry and not about deletion. (3) THE POINTER IS THIN AND STILL JOINS: inference_domain's emitted pointer falls inside the corpus band measured 2026-08-19 (every other trail's pointer median is 9-70 chars; inference_domain's is 1,171 with a max of 10,078 and accounts for 85% of that trail's 273,421 bytes), AND a record's pointer resolves to its inference_calls row by hashing that row's canonical column \u2014 proved against REAL rows already standing in the store, not against rows the proof wrote, because a join proved on self-written rows proves the hash function and not the join. (4) THE READER CONTRACT SURVIVES: BreadcrumbLog.records() returns the same records in the same write-order over the new shape, and cairn/machines/diagnostic_inspector/inspector.py reads them with no change to its own logic \u2014 the strongest available evidence that the grain moved and the contract did not. (5) THE AGREEMENT TOOTH STILL BITES: the count of gate='miss' emissions equals the count of verdict='miss' rows in inference_calls over the same window, matched by canonical DIGEST rather than canonical text \u2014 the substance of the sibling ticket's clause (c), preserved through the vocabulary change rather than quietly dropped by it. WRONG INTENT IF, and both directions are named because the grain can fail either way: reading one device's trail now costs a directory walk measurably slower than the single-file read it replaced at 30 days of records (the measure: time `records()` over a 5,600-file tree against the same records as lines \u2014 if the reader got worse, the picture got worse, and that is the parent's own 'logging happened, the picture did not' failure wearing this ticket's face); OR the digest makes a record unjoinable in practice \u2014 a pointer nobody can resolve back to its question is worse than a fat one, because a fat pointer is merely expensive and an unresolvable one is a record of truth that has stopped being a record. HORIZON: the next real inference call plus the first time a mind opens a trail to diagnose a red \u2014 needs no clock and no new instrument, because inference_domain is the sole path to every host so any real traffic tests clause (1), and clauses (2) and (3) are testable the moment the writer lands.",
    "horizon": "the next real inference call plus the first time a mind opens a trail to diagnose a red \u2014 needs no clock and no new instrument, because inference_domain is the sole path to every host so any real traffic tests clause (1), and clauses (2) and (3) are testable the moment the writer lands. Bounded above by the parent's own crossing: every-boundary-crossing-lands-in-the-logs-tree cannot cross PROVED before its children, and this is child (5)."
  },
  "children": "none, because the census says leaf rather than the wish saying so, and the census is the reason the two halves are ONE ticket rather than two. MEASURED 2026-08-19 across the corpus: 37 live emit sites in 18 files (proofs excluded), and the pointer-weight distribution splits them cleanly \u2014 bus 32 chars median, charter 29, ground_loop 9, tester 70, trouble 44, and inference_domain 1,171 with a max of 10,078. THE FAT POINTER IS A POPULATION OF ONE, at three call sites in a single module (cairn/devices/inference_domain/domain.py:319, :373, :408, all passing `pointer=canonical`). So the 'fix the fat pointer' half is not a corpus-wide sweep that would want children \u2014 it is three lines and their probe. And the 'file per emission' half has exactly one writer (cairn/tools/base/breadcrumb_log.py, RECORD_NAME + receive_diagnostic) and exactly one reader contract (records(), consumed live only by cairn/machines/diagnostic_inspector/inspector.py). THE JOIN THAT MAKES THEM ONE NODE: the filename Akien asked for carries the task, so the file cannot be named until the pointer is short enough to BE a name \u2014 a 10,078-character pointer is not a filename on any filesystem here. Splitting them would cast a child whose only falsifier is 'the parent can now proceed', which is a dependency wearing a ticket's clothes. The one thing that looked like a child is NOT mine to spawn and is named rather than folded: the 30-day sweep over the one root is the parent's uncast child (3), the-logs-tree-forgets-after-30-days, and this ticket deliberately stops at making per-record expiry POSSIBLE \u2014 it rides out in the bullets to the parent's owner instead.",
  "linked_ideas": [],
  "questions": [
    {
      "q": "distinctions",
      "a": "FOUR, and the first is asked to be ratified rather than merely recorded. (1) A PROVED NODE'S FALSIFIER MAY BE RE-SPELLED BY A LATER NODE WITHOUT BEING RETRACTED, provided the later node's own falsifier carries the earlier substance forward explicitly. an-inference-call-logs-that-it-started proved on 2026-08-18 naming diagnostics.jsonl by name and matching trail-to-meter by canonical TEXT; both spellings change here, its record is not edited (Law 7), and clause (5) of this falsifier is where its substance is carried. Without such a rule the corpus either freezes at its first spelling or a proof quietly stops meaning what it proved. I could not find this written anywhere. (2) BUILD-MINIMAL DEFERS THE POLICY; THE CHECK IT DOES NOT PERFORM IS WHETHER THE SHAPE CAN STILL HOLD THE POLICY LATER. This is the general form of the 'WHAT IS DELIBERATELY ABSENT' miss, and it is a real addition to build-minimal rather than a criticism of it: deferring rotation was right, and the grain that made rotation inexpressible was chosen in the same breath with nothing asking the question. (3) FILE-PER-EMISSION IS MORE APPEND-ONLY, NOT LESS. It reads like a conflict with breadcrumb_log.py's 'PURE APPEND, ON PURPOSE, AGAINST BOTH PRECEDENTS' and is the opposite: a written file is never reopened at all, where an appended trail is reopened by every later write. (4) A SHAPE THAT CANNOT EXPRESS EXPIRY IS NOT THE SAME AS A SHAPE THAT HAS NOT CHOSEN AN EXPIRY. Easy to lose, and losing it is how the deferral in (2) became invisible."
    }
  ],
  "constraints": [],
  "how": "ONE writer, ONE reader contract, THREE call sites, and a blast radius counted before the build rather than discovered inside it. WRITER: cairn/tools/base/breadcrumb_log.py \u2014 RECORD_NAME and receive_diagnostic's open(..., 'a') become a per-record path; records() sorts the directory instead of splitting lines, and must tolerate BOTH shapes for one retention window because the 1,295 standing records are not migrated (cache-class; they age out where they lie). The stamp in the filename is NOT the record's `ts` verbatim: `ts` is an ISO string carrying an offset and does not sort lexically as time does across offsets, so the name carries a UTC-normalised YYYYMMDD.HHMMSS.uuuuuu \u2014 the shape the launcher's boot log already uses, borrowed as a pattern and cited, not grafted. POINTER: domain.py:319, :373, :408 pass a digest of `canonical`; the store keeps the text (domain.py:91, `canonical text NOT NULL`), so any reader joins by re-hashing that column \u2014 deterministic, no schema change, no migration. THE ONE CONSUMER THAT MUST MOVE IN THE SAME ACT: cairn/devices/inference_domain/probes/an_inference_call_logs_that_it_started.py:104 counts `str(rec.get('pointer'))` against store canonicals today; unchanged, it would compare two vocabularies and report a disagreement that is an artifact of this ticket. PROSE IS THE LARGER HALF: `diagnostics.jsonl`/RECORD_NAME is spelled at 30 sites, few of them code \u2014 cairn/tools/base/diagnostic.py:24 states the address as a promise and cairn/devices/inference_domain/intention+why.json states it inside falsifier clause (9); both are charter deltas owed in the same act, each poking cairn/tools/intentions_model_compiler/recompile_gate.sh.",
  "links": [
    "parent: c6017f156442"
  ],
  "priority": "",
  "node_class": "code-seam",
  "workflow_and_state": "code-seam@v2: THINKME -> TICKETME -> BUILDME -> PROVEME -> WATCHME(an_emission_is_one_file_named_by_its_pointer) -> [PROVED] (proved 2026-08-26: 24/24 green, sealed under netns, emission gate clean \u2014 probe armed at probes/an_emission_is_one_file_named_by_its_pointer.py; clearance by CC onto cairn/tools/base/proofs/test_emission_gate.py; commit 67d32c9)",
  "watchme": {
    "object": "an_emission_is_one_file_named_by_its_pointer",
    "trigger": "each firing of the probe over the whole logs tree \u2014 it rides the device walk that already exists (ground_loop discovery enumerates class-space device folders, the same walk the parent's probe rides) rather than a clock, and it needs no live host and no inference: both populations it reads are files on disk.",
    "enough": "NOT satisfied by the corpus it was written against, and the reason is that this ticket's failure mode is REGRESSION BY NEW WRITER rather than breakage of the writer it ships. Enough when the invariant has held across at least TWO emitting components that landed AFTER this ticket \u2014 each writing one file per emission with a pointer inside the corpus band, without its author having read this ticket \u2014 because the thing being watched is whether the grain is carried by the floor or by the discipline of whoever writes the next emit site. A fat pointer is the specific way this decays: it needs no new code and no bad intent, only a caller passing the payload it already has, which is exactly how the one fat pointer in the corpus got there.",
    "carrier": "a verdict artifact against THIS ticket's falsifier, carrying (a) the count of trail files against the count of records they hold \u2014 equal is the invariant, and any file holding two records NAMES that file; (b) the pointer-length distribution per device with the corpus band beside it and every out-of-band device NAMED, because 'the median rose' is a finding nobody can act on and 'inference_domain is at 1,171 against a band of 9-70' is one somebody can open; and (c) the count of surviving diagnostics.jsonl files, which should fall to zero within one retention window and whose failure to fall is itself the finding.",
    "nexus": "the survey tree \u2014 this is a census of what the corpus actually writes against what its floor promises, which is the same shape as the parent's coverage measure and belongs in the same memory.",
    "consumer": "cairn/tools/base's owner \u2014 the floor owns the record shape, so a device drifting off it is a fact about the floor's reach and not about that device's manners.",
    "probe": "cairn/tools/base/probes/an_emission_is_one_file_named_by_its_pointer.py"
  },
  "deposits": {},
  "sorted_berth": "/home/akien/.cairn/devices/skill_block/0/berths/sorted/sorted-20260819T103100-48f476d1392a.json",
  "intent_berth": "none, because this node arose from Akien's direct challenge to a shipped shape on 2026-08-19 rather than from an /intent firing; its durable referents are the sorted berth above and the measurement recorded in `why` (140 emissions, 68 pointers, median inter-emission gap 282s).",
  "chart_claim": {
    "validate_berth": "/home/akien/.cairn/devices/chart/0/packets/validate-20260828T064953-ab011d17ee1b.json",
    "verdict_berth": "/home/akien/.cairn/devices/chart/0/packets/verdict-20260828T064953-13ae288aee7e.json",
    "chain_complete": true,
    "filled_by": "CC autonomous chain run 2026-08-28"
  },
  "schema_version": "v0",
  "dependencies": "NONE BLOCKING, and the two that look like dependencies are not. (a) the-logs-tree-forgets-after-30-days (the parent's uncast child (3)) CONSUMES this ticket rather than gating it \u2014 this makes per-record expiry expressible and performs no sweep. (b) a-proof-cannot-seed-the-tree-it-reads (sibling child (4), PROVED 2026-08-18) is already landed and is what makes this ticket's clause (3) honest: the join must read rows it did not write, and that discipline exists because of it."
}
