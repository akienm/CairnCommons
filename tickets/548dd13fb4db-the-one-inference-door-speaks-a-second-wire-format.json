{
  "id": "548dd13fb4db",
  "title": "the-one-inference-door-speaks-a-second-wire-format",
  "node_class": "code-seam",
  "task_or_ticket": "ticket",
  "workflow_and_state": "code-seam@v2: THINKME -> [TICKETME:waiting] -> BUILDME -> PROVEME -> WATCHME(an_agent_retry_gets_a_fresh_sample) -> PROVED",
  "from_idea": "2026-09-11-akien-2026-09-11-across-one-conversation",
  "intent_berth": "/home/akien/.cairn/devices/skill_block/0/berths/intent/intent-20260911T135455-fcafad8cb58f.json",
  "sorted_berth": "/home/akien/.cairn/devices/skill_block/0/berths/sorted/sorted-20260911T135710-8f7ba3507769.json",
  "intention": "inference_domain grows a SECOND WIRE FORMAT at its inbound door — an OpenAI-chat-completions-shaped endpoint — so an unmodified third-party agent harness becomes a metered, trailed, cached tenant of the one inference path without a line of its code being edited.",
  "why": "Law 6 (the sole path to the inference host — a third-party agent configured to dial a provider directly IS a second door, and the only way to close it is to offer a door it already knows how to speak to); Law 3 (metering is complete only because there is no uninstrumented path — a harness outside the door is inference this system cannot measure); Telos 1 (demonstrate inference compilation — a second heavy consumer is the first real test of the yield); Law 7 (the trail records what the call did).",
  "how": "A machine under inference_domain that accepts an OpenAI-shaped request, translates it into a domain request, calls domain.resolve(), and translates the answer back. It COMPOSES the existing resolver and opens no socket of its own, so the sole-path mesh (import_sieve, Law 6) is untouched by construction rather than by promise. Needs one new thing the charter does not carry: a zero-horizon lane for interactive agent traffic, because an agent's automatic retry sends BYTE-IDENTICAL prompts and a cached sample makes the retry a no-op.",
  "falsifier": "DONE when an UNMODIFIED third-party harness, configured with a base_url pointing here, completes a multi-turn session; every call lands both a row in inference_calls and an emission line in ~/.cairn/logs/inference_domain/0/, matched by canonical digest; and a byte-identical retry inside an agent loop returns a FRESH sample rather than the stored one. WRONG INTENT if the adapter turns out to need its own connection to a host (that breaches sole-path and means the seam is in the wrong place), or if agent traffic has to be exempted from metering to be usable at all — an exemption there would mean the door cannot serve the consumer it was built for.",
  "traces_to": "Law 6 (the sole path to the inference host — a third-party agent configured to dial a provider directly IS a second door, and the only way to close it is to offer a door it already knows how to speak to); Law 3 (metering is complete only because there is no uninstrumented path — a harness outside the door is inference this system cannot measure); Telos 1 (demonstrate inference compilation — a second heavy consumer is the first real test of the yield); Law 7 (the trail records what the call did).",
  "watchme": {
    "object": "an_agent_retry_gets_a_fresh_sample",
    "trigger": "every inference_calls row landed by a request that arrived through the OpenAI-shaped door",
    "enough": "when 50 agent-door calls have landed and the byte-identical-retry rate is measured rather than assumed — at that point the zero-horizon lane is either earning its keep or demonstrably unnecessary",
    "carrier": "a verdict artifact against this ticket's falsifier clause (c): a byte-identical retry returns a FRESH sample, not the stored one",
    "nexus": "the inference_domain meter — the same store the yield report reads, so the watch costs no new table",
    "consumer": "CC, and Akien through the operator inbox if the rate says the lane was the wrong shape",
    "probe": "cairn/devices/inference_domain/probes/an_agent_retry_gets_a_fresh_sample.py"
  },
  "gates_bound": [
    "proof gate — proofs beside the code, run by the tester under netns isolation; teeth a hollow build could not pass (Law 8).",
    "feeds back to origin on failure — a red routes to this ticket's own voyage, never to a paragraph (Law 3 + the deterministic-red rule: fixed, or carrying a ruling from Akien).",
    "durability gate — not done until proven AND committed.",
    "emit chokepoint — every crossing rides cairn.tools.base.transitions.emit at the component's own address carrying this ticket id; BUILDME refused without a berthed validate packet claiming it, PROVED refused without a complete passing verdict artifact."
  ],
  "owner": "CC builds it; cairn/devices/inference_domain owns the door, the wire format and the horizon lane",
  "owning_intention": "cairn/devices/inference_domain/intention+why.json",
  "depends_on": "none. This ticket is independently valuable — it turns ANY third-party agent into a metered, trailed tenant, and hermes is only its first user.",
  "cast_checks": {
    "assumption_check": "ONE LOAD-BEARING ASSUMPTION IS STILL UNSTATED AND IT IS NAMED RATHER THAN RESOLVED: that a third-party harness accepts an arbitrary base_url for an OpenAI-compatible endpoint. Measured so far is only that hermes-agent supports OpenAI, OpenRouter, Anthropic and local providers and switches with `hermes model`. The build's FIRST act is to check that config surface, because if it is absent the seam is still right and its first consumer is not hermes. A second assumption, weaker: that OpenAI chat-completions shape maps onto the domain request without loss — roles and multi-turn message arrays have no counterpart in a single canonical prompt string, so the translation is where loss would hide.",
    "missing_check": "Yes, one point was asserted wrapped and is not. The intent says 'a zero-horizon lane for interactive agent traffic' as though the lane's SELECTOR were settled; it is not. What distinguishes agent traffic from any other caller — an explicit request field, a domain vertical row in stacks/domains.json, or the OpenAI door itself always requesting horizon zero — is undecided. My read, recorded so the builder is not left guessing: the door requests it, because the door is the thing that knows its callers are agents, and a request field would let any caller opt out of compilation which is the whole thesis. Not ratified.",
    "falsifier_check": "The intent's falsifier stands and is checkable at three named instruments: (a) an unmodified harness completes a multi-turn session; (b) every call lands BOTH an inference_calls row and an emission line in ~/.cairn/logs/inference_domain/0/, joined by canonical digest — this is the exact agreement tooth an_inference_call_logs_that_it_started already implements, so the instrument exists; (c) a byte-identical retry returns a fresh sample. HORIZON: the first real multi-turn agent session. Wrong-intent tell: the adapter needing its own host connection, which import_sieve would red at proof time without anyone having to notice.",
    "collision_check": "Checked the compile-once thesis (Telos 1) and it is a DISTINCTION, not a collision. The why of each side: compile-once exists so a SETTLED answer is not re-bought; an agent's retry is by definition asking for an UNSETTLED one. They are not exclusive, and the store already carries a per-answer horizon, so zero is a legal value rather than a carve-out. Also checked Law 6 sole-path: composing domain.resolve() rather than opening a socket means this ADDS no door, and import_sieve enforces that at proof and build time. Recorded distinction: inference_domain has anticipated a second protocol OUTBOUND (the gemini residue); this is the first inbound one.",
    "builder_check": "Wearing the cheapest-builder hat, these are answered so nobody infers them at build time. (1) WHERE: a machine under cairn/devices/inference_domain/machines/, not a new device and not a tool — it is glue over an existing primitive. (2) IT OPENS NO SOCKET: it calls domain.resolve(); if a builder reaches for httpx or requests, that is the wrong turn and import_sieve will say so. (3) WHAT SERVES IT: RULED AND CLOSED 2026-09-11 — the door opens its OWN service port and does not route to web_server. Akien: 'the web server rule was because during theigors, you were creating seperate web servers on port after port for each device. akien hereby allows the exception because this is not the User Interface web server, this is a service port.' The one-web-server rule governs the USER INTERFACE, not sockets: a UI surface faces a person and arrives as a declared pane; a service port faces a machine. The test is who is on the other end. This port exists because a third-party harness can only be configured with a base_url — a measured constraint of the consumer, not a builder convenience. It may NOT serve HTML to a human at that port; that would be the TheIgors failure exactly. Ruling: CairnCommons/decisions/2026-09-11-the-one-web-server-rule-governs-the-ui-not-service-ports.json. WAS: undecided and it matters — an HTTP listener is needed for a base_url to point at, and this system's standing rule is one web server with surfaces in the shim, never a device with its own server. THIS IS THE ONE QUESTION A BUILDER CANNOT ANSWER ALONE and it routes to web_server's owner, not to a fresh listener. (4) TRANSLATION LOSS: OpenAI sends a messages array with roles; the domain canonicalizes a request. The mapping must be deterministic and total, because canonicalization is what the cache keys on — two different message arrays flattening to one canonical string would serve one agent another agent's answer. (5) SHAPE OF THE ANSWER BACK: OpenAI's response envelope carries usage counters, and the domain already returns real cost and provenance additively, so the counters are available rather than fabricated. Do not invent them."
  },
  "challenge_at_birth": {
    "better_approach": "Two smaller ways, both looked at and both rejected on measurement. (a) Make the agent speak ollama instead — that requires editing the agent, which is precisely the cost this whole direction exists to avoid (922MB .git, upstream at PR #107,000, a permanent merge burden). (b) Stand a translating proxy up OUTSIDE inference_domain — rejected because it would be a SECOND thing that dials a host, which is the exact breach Law 6 and import_sieve exist to prevent. Inside the domain is not the elaborate option, it is the only one that does not open a door.",
    "prior_art": "Consulted the compiled model after poking recompile_gate.sh: grep over CairnCommons/intentions-congruency-lab for openai / wire format / base_url returned ZERO. Nearest standing art is inference_domain's own filed residue that 'the gemini transport does not exist yet' — a second protocol on the OUTBOUND side, already anticipated. This is the same idea turned around to face INBOUND, which is a distinction to record rather than a collision.",
    "hidden_assumption": "That hermes-agent — and harnesses generally — accept an arbitrary base_url for an OpenAI-compatible endpoint. UNVERIFIED. What is measured is only that it supports OpenAI, OpenRouter, Anthropic and local providers and switches with `hermes model`. If an arbitrary base_url is not reachable through its config, this intent's value falls back to whatever other harnesses allow, and the seam is still right but its first consumer is not hermes.",
    "real_collision": "None terminal. The nearest is the compile-once thesis itself (Telos 1): a lane that must return a fresh sample on identical bytes LOOKS like an exemption from compilation. It is not — it is a HORIZON OF ZERO, and a per-answer horizon is already a field the store carries. The why of each side agrees: compile-once exists so a settled answer is not re-bought, and an agent's retry is by definition asking for an UNsettled one. Distinction recorded.",
    "back_up": "proceed"
  },
  "children": "none, because the seam is one machine over one existing primitive; the HTTP listener question is a dependency on web_server's owner, recorded in builder_check (3), not a child of this ticket",
  "schema_version": "ticket-v1",
  "date": "2026-09-11",
  "created": "2026-09-11",
  "author": "CC",
  "cast_under": "Akien, 2026-09-11: 'i'd say we're ready to move to both intentions and ticketing already' — said of the three seams named in this conversation, after he declined the fleet build-out and ruled hermes runs here beside CC rather than on Hex or akiendell.",
  "measured_at_cast": {
    "date": "2026-09-11",
    "by": "CC, in session 7a3cf7ce",
    "host": "akiendelllinux (10.0.0.229) — CONFIRMED by hostname; this is the box the database lives on and the box both agents would share",
    "ram": "free -g reports 15 total / 9 available. The TheIgors registry at /home/akien/TheIgorsProject/consolidated_2026-06-29/.TheIgors/local/machines.json claims ram_gb 32 for this host and is WRONG. CC has OOM-killed this box three times at 12-14GB.",
    "fleet_reachability": "akiendell 10.0.0.99, yoga9i 10.0.0.90, yogai7 10.0.0.71 all answer ping; ollama:11434 is CLOSED on all three. Only hex.local and this box serve inference. That registry was exported 2026-04-16 and does not list Hex at all, while the live rules stack (cairn/devices/inference_domain/machines/route/stacks/) does not list those six.",
    "hermes_scale": "/home/akien/dev/src/hermes-agent — 12,502 files, 5,967 .py, 3,977 test files, 1.1GB of which 922MB is .git. MIT, github.com/NousResearch/hermes-agent. A model-agnostic HARNESS, not a model.",
    "prior_art_grep": "grep over CairnCommons/intentions-congruency-lab for openai / wire format / base_url returned ZERO — no standing intention collides with the inbound-protocol seam."
  },
  "rulings_during_cast": [
    "2026-09-11-the-one-web-server-rule-governs-the-ui-not-service-ports — closes cast check (3); the door may open a service port"
  ],
  "measured_before_build": {
    "_why": "The door's wire spec is no longer my memory of the OpenAI schema. hermes-agent 0.21.1 was installed editable on 2026-09-11 and pointed at a throwaway listener; these are what it PUT ON THE WIRE. The instrument is CairnCommons/measurements/2026-09-11-hermes-openai-door-spike.py — a SPIKE, deliberately not in class-space, and not a component.",
    "client": "openai python SDK 2.24.0 (User-Agent 'OpenAI/Python 2.24.0', X-Stainless-* headers)",
    "endpoint_used": "POST /v1/chat/completions and GET /v1/models — nothing else was dialed",
    "request_top_level_keys": [
      "messages",
      "model",
      "tools"
    ],
    "no_temperature_sent": "hermes sends no temperature key at all; the door must default, not require one",
    "streaming": "hermes prefers streaming for every turn, but honors `model.streaming: false` in ~/.hermes/config.yaml, documented at cli-config.yaml.example as the escape hatch for self-hosted OpenAI-compatible servers. With it set, no `stream` key is sent. SO STREAMING IS NOT A BLOCKER FOR v1 — it is a later capability.",
    "tools_are_not_optional": "25 function tools ride EVERY request (browser_exec, terminal, read_file, write_file, patch, memory, skill_manage, tool_search, ...). host.ollama_resolver's chat branch sends no 'tools' key, so the one door as it stands today cannot serve an agent at all. This is the ticket's central build item, measured rather than assumed.",
    "system_prompt_size": "10930 chars on turn 1 — it is a stable prefix, which is exactly what makes the cache pay",
    "TWO_TRANSLATIONS_THE_DOOR_OWES": {
      "outbound": "ollama returns tool calls as {'function': {'name', 'arguments': <object>}}; OpenAI clients need an 'id', a 'type': 'function', and arguments as a JSON STRING.",
      "inbound": "THE ONE THAT BIT. OpenAI echoes those arguments back as a JSON STRING in the assistant turn; ollama /api/chat wants an OBJECT and refuses the string with HTTP 400 \"Value looks like object, but can't find closing '}' symbol\". Measured against hex.local qwen3-coder:30b on the SECOND turn of a tool-using conversation — turn one passed, so a one-shot test would have read green. A tooth for this belongs in the proof."
    },
    "validated_messages_refuses_agent_turns": "cairn/devices/inference_domain/host.py:184 requires every turn to carry a non-empty string 'role' AND 'content'. An agent's assistant turn carries tool_calls with content '' and its tool-result turn carries a tool_call_id; both are legal OpenAI and both are refused today. The check is right for the chat verb and wrong for the agent verb — this is a second checkable shape, not an exemption (per the 2026-09-11 gate-health ruling).",
    "IT_RAN": "With those three gaps closed in the spike, hermes completed a full agent loop through inference_domain on 2026-09-11: prompt -> tool_calls -> terminal tool executed -> tool result returned -> final answer 'akiendelllinux'. Metered rows carry model qwen3-coder:30b and the 25-name toolset in provenance.",
    "THE_CACHE_ALREADY_PAYS": "Measured in the same session: the retry after the inbound fix served turn 1 from cache — verdicts 'hit' at cost 12175 and 262 avoided. An agent replays a stable prefix constantly, so this is the compile-once gate doing its job on a tenant it was not built for.",
    "tools_belong_in_the_cache_key": "The spike puts `tools` into the request dict domain.resolve canonicalizes, because the same turns under a different toolset are a different question. Confirm this at build — it is my read, not a ruling.",
    "the_meter_counted_the_failures": "The three failed retries landed as verdict 'refused' rows at cost 0. Law 7 held without anything being added."
  }
}