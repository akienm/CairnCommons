{
  "id": "chart-tree",
  "schema_version": "ticket-v1",
  "date": "2026-07-28",
  "owner": "CC (code+proofs); Akien (the go: 'sounds good' on the tree-stratum + dial recommendation, 2026-07-28)",
  "intention": "STONE 2 of the chart device: the TREE STRATUM + THE DIAL, one stone with two proof surfaces. (1) THE TREE: the orient nexus's graph tree — its own table (chart_orient_nodes, owner 'chart') through db_domain's gate, reached via the librarian's tools generalized to carry an owner (the ratified unification: 'we'll use the same tools with the librarian'). Between floor and ceiling the /chart protocol now WALKS: counsel(request-vector) surfaces prior orientation learnings above the resolution floor (imported from the librarian, 0.65, its n=1 label carried visibly); the ceiling folds counsel in and marks fields it takes from the walk with provenance 'tree'. After the berth, DEPOSIT-BACK: the packet's intent lands as a node whose provenance names the berth path — so the same class of request next time is a walk, not a re-assembly (Law 1 as the nexus's own runtime, inherited from nexus #0). (2) THE DIAL: the ruling's measurement (held-nexi-everywhere: the puppet stops being forbidden by prose and becomes detectable by instrument) — a read-only compile over the berthed packets reporting, per nexus and in time order, the fraction of authored fields filled by floor vs tree vs claude. Reports only; the judgment stays with its readers.",
  "why": "The parent's why, plus the 2026-07-28 ruling's own demand: the hardware->tree->ceiling ladder makes the tree the PREREQUISITE for the ceiling (the deposit path that makes inference temporary), so chart's designed Claude ceiling stands on this stone or it stands illegally. And the dial is what makes the ruling's anti-puppet claim a measurement instead of a hypothesis (Law 3): 'floor+tree fraction not growing' can only red a nexus if someone can read the fraction. The first live packet (orient-20260728T110828) grounds exactly this request — its four unknowns (table grain, resolution floor, embed cost, home-field fencing) are this ticket's four settled answers.",
  "traces_to": [
    "Law 1 — a recurring orientation resolves from structure before inference is spent; deposit-back moves the stratum boundary down",
    "Law 3 — the dial makes the compilation claim measurable; the resolution floor travels labeled (a guess, n=1), never laundered as settled",
    "Law 4 — the deposit door validates the packet BEFORE seeding the tree (a malformed packet must not become a permanent resident); ownership is db_domain's physics, not manners",
    "Law 6 — chart's tree is chart's table through the one door; the librarian's tools generalize by owner-parameter, opening no grant side-door; each tenant reaches only its own gate",
    "Law 7 — an unreachable embed host at /chart time is recorded loudly in the packet's unknowns, never silently skipped; the dial reports unreadable packets by name",
    "Law 8 — the counsel is COUNSEL: the tree surfaces candidates, the ceiling decides what applies; the nexus is never scored by its own tree's similarity (home-field fence, manufactured resolution measured at 0.8295)"
  ],
  "node_class": "code-seam",
  "state": "code-seam@v1: THINKME -> TICKETME -> BUILDME -> PROVEME -> LEARNME -> [PROVED]   (cursor at PROVED 2026-07-28, same day as the cast; 11 teeth sealed twice, layer below re-sealed, live fire through the real seam: the paraphrased request walked to the deposited intent at 0.8342 over the 0.65 floor; the dial's first reading is the all-claude baseline)",
  "gates": "proof gate: cairn/chart/proofs/test_chart_tree.py under the tester's netns seal (the DB rides the Unix socket, the documented asymmetry), validation persisted beside-code; the librarian's trees proof re-green after the owner generalization; test_chart_orient re-green. Live gate at LEARNME: the first live packet deposited back through the real embed seam, then a paraphrased request's counsel walk ranks it — the tree answering a question it was never verbatim-asked.",
  "falsifier": "RED on any of: chart's table born owned by anyone but 'chart', or a non-owner write passing (db_domain's gate is the proof, not chart's manners); a malformed packet seeding the tree; a deposit whose provenance names a berth that does not exist on disk (a pointer to nothing as a permanent resident); counsel returning nodes below the floor as 'above'; the floor's n=1 label stripped in transit; chart/tree.py importing db_domain or the network directly (the librarian's tools are the ONLY door — the composition lesson, again); dial fractions that do not sum to 1 over covered fields; an unreadable packet silently skipped by the dial; the deposit-back re-embedding at walk time (the tree is consulted, not rebuilt, per crossing).",
  "horizon": "re-read when /moreabout lands (the learning door reads this same tree); when a second nexus wants its table (the naming convention proves or breaks); when the dial has enough real packets to show a trend (the ruling's falsifier becomes live); when the tenure loop lands on the librarian (chart inherits the standing upgrade).",
  "distinctions_recorded": [
    "THE FOUR UNKNOWNS OF THE FIRST LIVE PACKET, SETTLED: (1) table grain = per-nexus table, owner 'chart' — the charter's literal words ('its own table'), honored at the tenant grain Law 6 actually gates; (2) resolution floor = the librarian's RESOLUTION_FLOOR imported, n=1 label carried — a per-nexus floor is earned by tuning evidence, not minted at birth; (3) embed cost = one metered+cached embed per /chart crossing plus one per fresh deposit-back, all through inference_domain (yield_report reads it); (4) home-field fencing pre-tenure = three fences: nodes born hypothesis (inherited physics), counsel-not-verdict (the ceiling decides applies), and the dial measuring usage never quality.",
    "THE OWNER PARAMETER IS THE UNIFICATION LANDING, NOT A FORK: trees.py's own docstring filed 'the grant model for other owners' trees' as an edge; the need arrived and resolved simpler than a grant — each owner its own table through the same tools. The librarian's defaults are untouched; its proofs must stay green to prove it."
  ],
  "parent": "chart",
  "children": [],
  "dependencies": [
    "chart-orient (the packet, the berth, validate_orient) — PROVED 2026-07-28",
    "librarian trees + loop (deposit/nearest/tree_state + RESOLUTION_FLOOR) — PROVED 2026-07-27",
    "db_domain (owned tables through the one door) — PROVED 2026-07-18",
    "inference_domain + host (the live embed seam, metered and cached) — standing",
    "tester (netns seal) — standing"
  ],
  "provenance": "Cast 2026-07-28, the day after the nexi-everywhere ruling was ratified, on Akien's 'sounds good' to the recommendation that the tree stratum and the dial are the stone the ruling was implicitly pointing at: the ladder forbids chart's ceiling until the tree exists, and the ruling's anti-puppet claim stays a hypothesis until the dial reads. The completeness check surfaced four assumptions (per-nexus table; intent-as-node-content; owner-parameter-as-unification; dial-as-read-only-instrument) — all recorded above.",
  "filed_edges": [
    "(a) LANDED 2026-07-28, same day (ticket moreabout, PROVED): /moreabout v0 — the learning door over this same tree, scoped walk + the signal written back in one act; the no-re-run rule is import physics and the under-floor lesson is a tooth",
    "(b) node content beyond intent: a packet distills to ONE node today (build-minimal); when /moreabout needs finer grain (per-field nodes), the deposit-back grows against that need, not ahead of it",
    "(c) the DIAL's trend verdict: today it reports fractions and leaves the judgment to readers; when enough packets exist, 'floor+tree not growing over N crossings' becomes a checkable shape — a build_inspector filter candidate (arriving through the nexus loop, per the graft tickets)",
    "(d) per-nexus floor tuning: the imported 0.65 stays until real counsel evidence moves it — the move is an installation (hardware ruling), dated and ratified"
  ]
}
