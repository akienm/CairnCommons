{
  "id": "librarian-graph-trees",
  "schema_version": "ticket-v1",
  "date": "2026-07-27",
  "owner": "CC (code+proofs); Akien (design: the embedding-is-the-path model, the ownership rules)",
  "intention": "The librarian's spine: the graph-tree store as db_domain's first tenant. A node carries content + vector + provenance + standing; the deposit door refuses what cannot be traced; edges are DERIVED from vector proximity, never stored — 'the embedding IS the path through the graph trees.' Retrieval is a walk: nearest-by-cosine from a query vector, or the derived neighbors of a standing node.",
  "why": "Every one of the librarian's verbs (learn, summarize, chat) sits on this store — it is the one part with zero unmet dependencies today (db_domain seated, the embed seam seated, the library address ratified) and the part whose absence blocks all others. Akien's founding facts, settled 2026-07-22: 'the embedding IS the path' (edges derived from proximity — O(n) vectors, paths fall out); the librarian OWNS the graph (Law 6). And the extractor's discipline generalizes to the door: a node nobody can trace is fabricated attribution wearing a graph.",
  "traces_to": [
    "Law 1 — deposited structure is the settled; a walk re-derives nothing (and a duplicate deposit writes nothing)",
    "Law 3 — every node is born a hypothesis; standing is a field because tenure is a later measurement, not a birthright",
    "Law 6 — librarian owns librarian_nodes; db_domain's owner-gate is the physics under the deposit door",
    "Law 7 — refusals at the door are loud and complete; a walk of an empty tree is an honest empty, not an error",
    "Law 8 — a hollow store (edges stored, provenance optional, dimensions unchecked) cannot pass the teeth"
  ],
  "node_class": "code-seam",
  "state": "code-seam@v1: THINKME -> TICKETME -> BUILDME -> PROVEME -> LEARNME -> [PROVED]   (cursor at PROVED 2026-07-27, same day as the cast; component history/state beside the code at cairn/librarian/)",
  "gates": "proof gate: cairn/librarian/proofs/test_librarian_trees.py (13 teeth) under the tester's MEASURED netns seal — validations/test_librarian_trees.json, green, sealed; the proof needs postgres and the Unix socket crossed the seal (db_domain's documented asymmetry, exercised for real). Build gate: passed clean at the PROVEME exit (4 filters). Durability: committed + pushed.",
  "falsifier": "RED on any of: a deposit without provenance landing; a stored edge table appearing (the derived-edges claim gone hollow); a dimension mismatch answered instead of refused; a non-owner write landing; a deposit crossing without a breadcrumb. WRONG-SHAPE SIGNAL: if the store is live for weeks and every node still stands at 'hypothesis', the tenure loop never landed and standing is a decoration, not a measurement.",
  "horizon": "re-read when the core loop (parent edge a) lands on top of it, or when scale pulls the proximity walk into the database (pgvector — filed edge).",
  "distinctions_recorded": [
    "THE FIRST WALK RANKED BY MEANING, MEASURED: the live fire deposited the librarian's first real nodes — Akien's own verbatim design lines, each carrying provenance into the record it came from — and a query sharing almost no words with any of them ('what should the chat interface do...') ranked the chatbot-goal sentence nearest at 0.7473, the payback-meter line last at 0.3721. The embedding IS the path, on real vectors (nomic-embed-text, 768-dim), not fixtures.",
    "DOUBLE PARSIMONY ON THE RE-FIRE: the second run was all DUPLICATEs (the deposit door wrote nothing — Law 1 at the table) AND all cache HITs (the embed seam never touched the host — Law 1 at the meter): misses frozen at 8, avoided-tokens 955 -> 1165. The same interplay the extractor measured, one layer down.",
    "THE SEAL AND THE SOCKET, EXERCISED TOGETHER: this is the first proof that needs the DATABASE run under the netns seal — 1.1.1.1 unreachable inside, postgres still reachable through the Unix socket. db_domain's documented asymmetry stopped being prose and became a measurement.",
    "THE UNTRACEABLE NEVER LANDS: the deposit door requires provenance naming a source — the extractor's fabricated-attribution discipline carried one layer down, where a fake would be a permanent resident, not a refused draft."
  ],
  "parent": "librarian",
  "children": [],
  "dependencies": [
    "db_domain (create_owned_table / write / read; the owner-gate) — PROVED 2026-07-22",
    "inference_domain embed seam (vectors as a metered service; live wiring only — the module stays import-pure) — PROVED 2026-07-26"
  ],
  "provenance": "Cast 2026-07-27, the first stone of the librarian-as-chatbot chain, immediately after Akien ratified the ordering ('yes! go go go!').",
  "filed_edges": [
    "(a) proximity is computed in Python over the tree's rows — O(n) per walk, correct now; pgvector (proximity computed where the vectors live) when a real load pulls it",
    "(b) a duplicate deposit returns the standing node and DROPS the new provenance — corroboration is evidence the tenure loop will want; when that loop lands, a duplicate becomes a provenance append, not a discard",
    "(c) the tenure loop itself (standing: hypothesis -> earned) — unbuilt; nodes are honestly born hypotheses and honestly stay there until it exists",
    "(d) trees-index-into-commons grants (private-down default, grant-up explicit) — one table with a tree column today; the grant model opens when a second owner exists"
  ],
  "stage_needs": {}
}
