{
    "id": "constrain-filters",
    "schema_version": "ticket-v1",
    "date": "2026-07-28",
    "owner": "CC (code+proofs); Akien (the ordering ruling 2026-07-28 — 'we set up it's inspector filters first. then build the constraints module... a higher order build the test first' — which is the ratification of these two admissions; he can red either on review)",
    "intention": "THE JUDGES BEFORE THE JUDGED: install the build_inspector filters that will gate the constrain brick's output BEFORE the constrain module exists. Two filters, minimal by design, each encoding a measured failure that PRECEDES the module: (1) constraint_traces — every constraint in a berthed constrain packet names a source that resolves on disk (law/charter/ticket/component); an invented constraint is fabricated attribution wearing a bound's costume. (2) constraint_bounds_complete — a constrain packet declares BOTH in-bounds and out-of-bounds, non-empty; an empty 'out' is exactly the founding failure (bounds-checking that never ran to completion). Both run through the packet jurisdiction the wire built, AND both are exposed as a pure judge (judge_constrain, owned by the inspector) that the future constrain berth door COMPOSES — one implementation, two mouths: the door refuses at berth time, the gate re-judges at promotion. The judge lives in the inspector and constrain imports it, never the reverse — the module structurally cannot shape its own acceptance criteria.",
    "why": "Akien, 2026-07-28, verbatim: 'for constraints, we set up it's inspector filters first. then build the constraints module. what do you think? it's a higher order build the test first.' The deep why is the home-field fence by construction: the judge lands in a different component behind a different write-gate than the thing it judges, so constrain is born under criteria it cannot soften. Tooth 10 survives test-first honestly: the filters' provenance failures exist BEFORE the module — the web-server carrier miss (CC-- 2026-07-28: premature convergence, bounds-checking never ran to completion) seeds bounds_complete; the fabricated-attribution class (2026-07-26: an echo label attesting an unhappened push; the librarian-ruling misattribution) seeds constraint_traces. The module that doesn't exist yet has no failures; the failures that made it NECESSARY do.",
    "traces_to": [
        "Law 4 — the acceptance criteria are physics before the behavior exists; constrain grows INTO an installed gate",
        "Law 6 — the judge is the inspector's, behind the inspector's write-gate; constrain composes it and cannot reach in",
        "Law 8 — the module's first proof must pass a gate it did not write (the anti-home-field form of build-the-test-first)",
        "tooth 10 — both filters carry dated-failure provenance in their docstrings; a filter nobody was taught by stays refused"
    ],
    "node_class": "code-seam",
    "state": "code-seam@v2: THINKME -> TICKETME -> BUILDME -> PROVEME -> [PROVED]   (cursor at PROVED 2026-07-28, same day as the cast; 16 teeth sealed twice; the live gate fired: the constrain brick built UNDER these judges and its first real packet passed them at the door AND at promotion — one implementation, identity-pinned; judges-before-the-judged held end to end)",
    "gates": "proof gate: new teeth in test_inspector.py (each filter fire-and-stay-quiet on synthetic constrain berths; judge_constrain pure and identical through both mouths) under the tester's netns seal. Live gate: the chart-constrain ticket builds UNDER these filters — its first berthed packet must pass judges it did not write.",
    "falsifier": "RED on any of: a constrain packet with an unresolvable source passing either mouth; an empty bounds.out passing; the two mouths disagreeing (door passes what the gate reds, or vice versa — one implementation is the claim); the inspector importing constrain (the direction inversion that would let the module shape its judge); a filter admitted without dated-failure provenance; the pre-installed set growing speculatively beyond the two measured failures (build-minimal holds — the rest arrive through the brick's loop as reality teaches).",
    "horizon": "re-read when chart-constrain builds under these (the dependent ticket, the live gate); when constrain's reality teaches a third filter (it arrives through the stackable-learning-brick loop with its own dated failure, by installation); when a non-chart device berths constrain-class packets.",
    "distinctions_recorded": [
        "HIGHER-ORDER BUILD-THE-TEST-FIRST (Akien's coinage for the move): not unit tests of the module's code (the proof does that) but the EXTERNAL acceptance gate for the module's output, installed before the module — the judge in another component, behind another write-gate.",
        "TEST-FIRST AND LEARNS-FROM-FAILURE ARE COMPATIBLE HERE, NOT IN GENERAL: these filters install only because their seeding failures were measured before the module existed. A filter for a module with no failure lineage would be a speculative contract — that stays red.",
        "STACKABLE LEARNING BRICK is the ratified name (Akien, 2026-07-28, this session) for what the records to date call a question nexus — the constrain brick is the first component NAMED under it."
    ],
    "parent": "chart",
    "children": [],
    "dependencies": [
        "packet-inspector-wire (the packet jurisdiction these install into) — cast same day, in build",
        "build_inspector (FILTERS registry, tooth-10 admission) — PROVED 2026-07-27",
        "tester (netns seal) — standing"
    ],
    "provenance": "Cast 2026-07-28 in the same act as packet-inspector-wire and chart-constrain, on Akien's ordering ruling. Second of the three stones; the constrain module may not cast BUILDME until these are PROVED.",
    "filed_edges": [
        "(a) constrain's third-and-later filters arrive through the brick's loop (failure corpus -> ceiling proposal -> installation), never pre-installed",
        "(b) the same judges-before-judged move is a candidate pattern for every future stage (survey, decompose, ...) — pattern, not rule, until a second instance proves it"
    ],
    "migrated_from": "code-seam@v1 — the dissolved fifth summons stripped by sweep 2026-08-03 (Akien's ruling: the words should have been scrubbed when the version was minted). The dead token is deliberately not reprinted here; tickets/watchme-emits-a-probe is its one record.",
    "chart_claim": "verdict-20260828T061847-f82313416ca8.json"
}