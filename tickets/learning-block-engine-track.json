{
  "id": "learning-block-engine-track",
  "date": "2026-08-02",
  "owner": "cairn/learning_block",
  "node_class": "code-seam",
  "intention": "The Learning Block gets its inner engine in code — one reusable candidates-loop + evaluation-loop that runs any block from its declared domain knowledge and constraints, state-logged so recorded runs compile into deterministic Python + graph tree, with 'cheaper and still right' measured, never hoped.",
  "why": "WHAT AKIEN WAS EXPERIENCING, 2026-08-02: 'as soon as we switch from fable to opus, we start spiralling down. I CANNOT keep track of all those details.' Same day, he ran the design conversation with Haiku — the smallest model — successfully, because he fed it one bounded question per turn and carried the state himself. The engine exists to give every block that property with the ARTIFACT as the carrier instead of Akien or the model's attention. This is the first code step of I-the-system-is-a-learning-block ('we will have to implement that in code once we have sorted out how' — the Haiku transcript, ~/.akien/20260802.AkienOnWhatIWantTheProjectToDoLONGVERSION.txt, is the sorting-out, and its checklist reproduces that intention's corpus almost item for item). Cashes the corpus lines 'Demonstrate Inference Compilation into graph trees and code' and 'Demonstrate stackable learning brick concept — perform at least some of the build outside of the LLM'.",
  "state": "code-seam@v2: THINKME -> [TICKETME] -> BUILDME -> PROVEME -> PROVED (cast 2026-08-02 at /sorted from intent berth; parent — children prove before it crosses)",
  "gates": "proof beside the code, sealed under the tester; entry to BUILDME rides the chart chain and this intent berth; exit to PROVED rides the answered chart",
  "intent_berth": "/home/akien/.cairn/devices/skill_block/0/berths/intent/intent-20260802T164045-791a38fb5a13.json",
  "children": {
    "engine-runs-one-block": "cast now (2026-08-02) — the uniform inner engine, run end-to-end on ONE existing block",
    "infer-compile-reads-the-traces": "NOT cast yet, by design: casts when child 1's watch reports >=5 training-typed traces for one block — its design depends on what those traces actually look like, and casting it now would bake guesses (build-minimal)",
    "proof-validate-compiled-vs-model": "NOT cast yet, by design: casts when infer-compile emits its first floor — there is nothing to compare before then"
  },
  "falsifier": "DONE when one existing block runs through the engine end-to-end with a full state log, its recorded runs compile into a deterministic floor, and proof:validate shows the compiled floor agreeing with the model path on FRESH tasks at measurably lower cost. HOLLOW-PROOF GUARD (Law 8): a compile that memorizes must FAIL validate on fresh tasks. WRONG-INTENT TELL: if a second block requires per-block special cases inside the engine, the 'one engine, different data' hypothesis is falsified — stop and reconcile, do not accrete cases. '5-10 traces suffice to compile' is a HYPOTHESIS (Law 3) and is what child 2's cast tests first.",
  "horizon": "while build sessions require the largest model to hold — done looks like build work that holds on a smaller model because the state travels in the block's log",
  "watchme": "none, because the track's efficacy measure IS child 3 (proof:validate) — a watch here would duplicate a child's whole purpose as a probe; the concrete watch rides child 1, where the trace corpus it feeds actually accumulates"
}
