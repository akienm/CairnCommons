{
  "id": "learning-block-engine-track",
  "date": "2026-08-02",
  "owner": "cairn/machines/learning_block",
  "node_class": "code-seam",
  "intention": "The Learning Block gets its inner engine in code \u2014 one reusable candidates-loop + evaluation-loop that runs any block from its declared domain knowledge and constraints, state-logged so recorded runs compile into deterministic Python + graph tree, with 'cheaper and still right' measured, never hoped.",
  "why": "WHAT AKIEN WAS EXPERIENCING, 2026-08-02: 'as soon as we switch from fable to opus, we start spiralling down. I CANNOT keep track of all those details.' Same day, he ran the design conversation with Haiku \u2014 the smallest model \u2014 successfully, because he fed it one bounded question per turn and carried the state himself. The engine exists to give every block that property with the ARTIFACT as the carrier instead of Akien or the model's attention. This is the first code step of I-the-system-is-a-learning-block ('we will have to implement that in code once we have sorted out how' \u2014 the Haiku transcript, ~/.akien/20260802.AkienOnWhatIWantTheProjectToDoLONGVERSION.txt, is the sorting-out, and its checklist reproduces that intention's corpus almost item for item). Cashes the corpus lines 'Demonstrate Inference Compilation into graph trees and code' and 'Demonstrate stackable learning brick concept \u2014 perform at least some of the build outside of the LLM'.",
  "state": "code-seam@v2: THINKME -> TICKETME -> [BUILDME] -> PROVEME -> PROVED (cast 2026-08-02 at /sorted from intent berth; parent \u2014 children prove before it crosses; child 1 PROVED 2026-08-02, children 2/3 await their named casting conditions)",
  "gates": {
    "prove_gate": "proofs beside the code at cairn/machines/learning_block/proofs/, sealed under the tester (netns). Teeth: the engine runs one block end-to-end with a full state log (child 1 PROVED); recorded runs compile into a deterministic floor (child 2); the compiled floor agrees with the model path on FRESH tasks at measurably lower cost (child 3). A compile that memorizes FAILS validate on fresh tasks (Law 8).",
    "entry_gate": "BUILDME refuses without a berthed chart chain claiming this ticket plus intent berth on file.",
    "parent_crossing": "children prove before the parent crosses \u2014 child 1 PROVED 2026-08-02; children 2 and 3 cast when their named conditions are met (>=5 training-typed traces for child 2, first compiled floor for child 3).",
    "durability": "committed and pushed to both repos."
  },
  "intent_berth": "/home/akien/.cairn/devices/skill_block/0/berths/intent/intent-20260802T164045-791a38fb5a13.json",
  "children": {
    "engine-runs-one-block": "PROVED 2026-08-02 \u2014 the engine runs the measured tenant (intentions_model_compiler) end-to-end both ways (current -> no_op, stale -> regenerate_whole), live corpus at 2 engine runs / 3 rejected candidates / five questions all answerable; WATCHME(engine-trace-corpus) armed, enough at >= 5 complete runs casts child 2",
    "infer-compile-reads-the-traces": "NOT cast yet, by design: casts when child 1's watch reports >=5 training-typed traces for one block \u2014 its design depends on what those traces actually look like, and casting it now would bake guesses (build-minimal)",
    "proof-validate-compiled-vs-model": "NOT cast yet, by design: casts when infer-compile emits its first floor \u2014 there is nothing to compare before then"
  },
  "falsifier": "DONE when one existing block runs through the engine end-to-end with a full state log, its recorded runs compile into a deterministic floor, and proof:validate shows the compiled floor agreeing with the model path on FRESH tasks at measurably lower cost. HOLLOW-PROOF GUARD (Law 8): a compile that memorizes must FAIL validate on fresh tasks. WRONG-INTENT TELL: if a second block requires per-block special cases inside the engine, the 'one engine, different data' hypothesis is falsified \u2014 stop and reconcile, do not accrete cases. '5-10 traces suffice to compile' is a HYPOTHESIS (Law 3) and is what child 2's cast tests first.",
  "horizon": "Child 1 is PROVED. Child 2 casts when child 1's watch reports >=5 training-typed traces. Child 3 casts when child 2 emits its first floor. The parent crosses when all three are proved. The horizon is the trace corpus accumulating \u2014 deliberately gated on real data, not forced.",
  "watchme": "none, because the track's efficacy measure IS child 3 (proof:validate) \u2014 a watch here would duplicate a child's whole purpose as a probe; the concrete watch rides child 1, where the trace corpus it feeds actually accumulates",
  "filed_edges": [
    "[AKIEN] WHAT IS THE SECOND BLOCK? The wrong-intent tell is 'a second block requires per-block special cases inside the engine' \u2014 which cannot be measured until a second block exists, and none is named. The choice matters: pick one shaped like the compiler and the tell stays silent by construction; pick one deliberately unlike it and the engine is actually tested. His call, because it is a sequencing choice about what gets built next.",
    "[MEASURED] 2026-08-05 \u2014 THE ENGINE IS THE PART THAT SURVIVED THE ANATOMY CORRECTION. engine.py:12 reads 'input -> candidates loop -> evaluation loop -> decide or escalate -> output', and those two loops ARE the 'two loops inside, distinct' Akien described from memory the same evening without being able to name them. His guess (one input-side, one output-side) is right in shape: candidates generates, evaluation kills against the constraints. So this track is UNAFFECTED by the conflation that invalidates the organ list around it \u2014 what was wrong was hung on the engine's exit, not built into it."
  ],
  "schema_version": "ticket-v1",
  "dependencies": [
    "engine-runs-one-block (child 1, PROVED 2026-08-02) \u2014 the engine runs the measured tenant (intentions_model_compiler) end-to-end both ways.",
    "The trace corpus from child 1's WATCHME \u2014 >=5 training-typed traces casts child 2.",
    "inference_domain (the ceiling stratum) \u2014 the engine's Claude ceiling calls through the one inference door.",
    "librarian trees (the tree stratum) \u2014 each block's graph tree uses the shared deposit/nearest/tree_state tools.",
    "db_domain (each block's tree is a tenant table through the one door) \u2014 PROVED."
  ],
  "sorted_berth": "none, because cast before the sorted door existed (ticket dated 2026-08-02, door landed 2026-08-03)",
  "chart_claim": "none, because no chart chain was run \u2014 backfilled by ticket inspector 2026-08-26. BUILDME entry gate (buildme_rides_the_sorted) is where this is answered or the chain is run.",
  "traces_to": [
    "Law 1 \u2014 the resolver is spent on the novel: one reusable engine, not re-deriving the loop per skill",
    "Law 4 \u2014 enforced by physics: the candidates-loop and evaluation-loop are structure, not prose"
  ]
}
