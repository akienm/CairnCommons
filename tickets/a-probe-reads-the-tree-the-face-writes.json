{
  "id": "a-probe-reads-the-tree-the-face-writes",
  "node_class": "code-seam",
  "state": "code-seam@v2: THINKME -> [TICKETME] -> BUILDME -> PROVEME -> PROVED",
  "date": "2026-08-10",
  "owner": "the librarian (the probe berths beside its code at cairn/librarian/probes/; Law 6)",
  "cast": "2026-08-10",
  "cast_by": "direct TICKET entry — ruling 2026-08-02-three-entry-points-and-structural-refiled, recorded in CairnCommons/tickets/_charter+why.json `entry`. Editing one line fixes it, which is that ruling's own test for bug-rather-than-structural.",
  "priority": "P1",
  "priority_why": "Not from a trouble, so it earns P1 on the charter's other clause: an issue on the LIVE SYSTEM NOW. The probe is armed and sealed on the running system today, and what it reports is a false green — silence that reads as 'nothing to see' when the instrument is pointed at an address nothing writes to. Law 9: a green that was never earned is worse than a red.",
  "intent_berth": "none, because this is a direct TICKET entry rather than a birthed intention — the three-entry-points ruling (2026-08-02) makes TICKET a door of its own, and a bug does not pass IDEA or INTENT. The resolvable referent for how it was found is the survey berth below.",
  "sorted_berth": "none, because direct TICKET entry — see intent_berth. The finding stands measured in /home/akien/.cairn/devices/chart/0/packets/survey-20260810T133527-4e19d02b69fc.json (absences[4]), which is the packet that produced it.",
  "from_idea": "none, because this was not imagined — it was measured while surveying the territory for revision-with-receipts. Referent: the survey berth named in sorted_berth.",
  "chart_validate_berth": "none yet, because the chart chain has not run for this ticket. buildme_rides_the_chart (cairn/build_inspector/inspector.py:1259) reads chart berths by ticket id, so the BUILDME crossing REFUSES until a validate packet claims 'a-probe-reads-the-tree-the-face-writes'. Recorded as owed rather than silent — and NOTED as friction worth Akien's eye: a one-line bug fix carries the same seven-brick preamble a whole behaviour does, which may be correct (the gate is one rule, uniformly applied) or may be a place the bug door deserves its own shape. Not decided here.",
  "parent": "the-tenure-loop",
  "intent": "Point the tenure loop's armed WATCHME at the tree the live face actually writes, so the watch can fire. cairn/librarian/probes/standing_moves_under_live_use.py line 44 sets _TREE = 'commons' and line 88 censuses only that tree; the librarian's live face constructs its session with tree='library' (cairn/librarian/shim.py:35). The probe therefore surveys an address the running system never writes to. The fix is one constant; the ticket exists because a bug enters at a door, not at an edit.",
  "why": "The lived symptom: the-tenure-loop's WATCHME was armed and sealed at PROVED on 2026-08-09, and on 2026-08-10 it can report nothing — not 'the loop is healthy', not 'the loop is decorative', nothing at all — because its census returns zero rows and its trigger needs >= 20 post-era crossings to fire. A watch that cannot bite is arming theatre: the voyage closed on a gate that checked the probe EXISTS at its declared path, which it does, while the thing it was built to observe went unobserved. This is the exact failure the WATCHME summons was inverted to prevent (node_classes/code-seam.json v2: 'optional to carry, MANDATORY TO SATISFY once carried') — the crossing was satisfied by a module that imports, not by an instrument that measures. It also has a second cost, which is how it surfaced: the probe is the template every later librarian probe copies, so a dead substrate propagates by imitation. revision-with-receipts was specified to copy this shape and had to be steered off it by hand.",
  "measure": "store.read('librarian_nodes', where=\"tree = 'commons'\") = 0 rows on 2026-08-10. Full census 78 rows at survey time, tree distribution Counter({'library': 74, 'founding': 4}); after three live crossings the same day, 87 rows, still none in 'commons'. Probe source line 44 _TREE = 'commons'; line 88 reads WHERE tree = %s with that constant; _ENOUGH_CROSSINGS / _trigger requires >= 20 crossings, unreachable on an empty tree at any horizon.",
  "falsifier": "Two ways this ticket is wrong. (1) MISAIMED: if 'commons' is the intended home of the librarian's live corpus and the defect is really at the writing end — shim.py:35 writing 'library' when it should write 'commons' — then the one-line fix belongs at the other end and this ticket points the wrong way. Discriminator: which tree does the librarian's charter name, and which one do the ratified library-fold and summary nodes sit in? (2) INSUFFICIENT: if after the fix the probe's survey function still returns zero rows against the live store, or its trigger arithmetic is still unreachable, then the tree constant was not the whole defect and the ticket closed on a symptom.",
  "horizon": "at the fix plus one live crossing: the probe's survey function called directly against the live store must return a NON-ZERO row count, and its trigger must be evaluable against real arithmetic rather than an empty set. Re-check within one voyage — this is the instrument for a component under active build, so a stale answer here is expensive immediately.",
  "gates": {
    "proof": "a tooth beside the code asserting the probe's census against a seeded fixture returns the seeded rows — i.e. the instrument reads where the face writes. A hollow build passes 'the module imports'; it does not pass 'the census sees the rows the live face deposited', which is the tooth this ticket owes.",
    "feeds_back_to_origin_on_failure": "a kick-back re-enters BUILDME on the-tenure-loop's probe, since the artifact under repair is that ticket's WATCHME (Laws 3 + 8)."
  },
  "watchme": "none, because the artifact under repair IS the watch. A watch on a watch has no independent instrument — the probe's own census returning non-zero against the live store is both the fix's proof and its ongoing observation, and it is already the horizon above. Resolvable referent: cairn/librarian/probes/standing_moves_under_live_use.py, whose repaired census is the standing measure.",
  "known_adjacent_debt": "NOT scoped into this ticket, recorded so the one-line fix is not mistaken for the whole truth: the librarian has three disagreeing default trees live at once — loop.py:189, chat.py:143/193, summarize.py:67/123 and trees.py (5 sites) default 'commons'; shim.py:35 and library.py:181 default 'library'; live.py:53 TREE = 'founding'. So 'the librarian's tree' is not a well-defined address for any probe or census, and this bug is one symptom of that. Making them agree is a refactor across five modules and wants its own ticket; fixing the constant here does not retire it.",
  "notes": "Found 2026-08-10 during the /chart survey for revision-with-receipts, whose own probe was specified to copy this one's shape. Carried to Akien as a measured finding rather than repaired in passing; he ruled the sequence: ticket it now, sail revision-with-receipts, then sail this. Deliberately NOT fixed before revision-with-receipts sails, because that ticket's validate criterion 5 uses this probe's zero-row census as its contrast measure."
}
