{
  "store": "tickets",
  "role": "store-charter",
  "schema_version": "v1 — 2026-09-01, hex ids, merged falsifier, engineer-readable",
  "what": "Cast nodes — an intention that has passed /sorted, been typed to a node-class and bound to that class's gate-set. The universal work unit.",
  "why": "Workflow rides on node-class, not on the node's identity, so a ticket carries no domain — an 'inference ticket' and a 'concept-piece ticket' are the same artifact with a different class field. Everybody has tickets. Crucially, the ticket's `workflow_and_state` field IS the pipeline instance: no orchestrator holds a second copy of where the work stands, so the state of the tickets is the state of all things, always (MAP.md 'Gates, not supervisors'). Written when skill:/sorted casts a node.",
  "owner": "each ticket has exactly one owner who gates writes to it; the owner alone advances its state through the bound gates (Law 6). The tickets store owns the collection; the emit chokepoint validates records against the template.",
  "filename": "<hex-id>-<title-slug>.json — hex is the stable machine identifier, slug is the human handle",
  "template": {
    "id": "string — 12-char hex, the stable machine identifier (uuid4 minted at cast time)",
    "title": "string — the human-readable one-liner (kebab-case slug)",
    "date": "ISO-8601 absolute date",
    "owner": "who gates writes (terse — a name, not a paragraph)",
    "owning_intention": "path to the intention+why.json that owns this ticket",
    "intention": "what this node is FOR (the WHAT)",
    "why": "why it exists — required (traces upward)",
    "traces_to": "which Law/principle this traces to",
    "falsifier": {
      "proves_green": "what the proof gate checks — the conditions that mean it worked",
      "proves_red": "what would show this node wrong — the conditions that mean it went wrong",
      "horizon": "when to re-check (optional — omit if testable now)"
    },
    "children": "array — child ticket hex ids, or 'none, because <X>'",
    "linked_ideas": "array — idea references (was from_idea)",
    "questions": "array of {q, a} — the challenge answers and open questions",
    "constraints": "array — what the build must respect",
    "how": "implementation sketch (terse)",
    "links": "array — parent tickets, decisions, cited_by references",
    "priority": "when and why (merged field)",
    "node_class": "the bound class (address in node_classes/): skill | concept-piece | code-seam | …",
    "workflow_and_state": "the pipeline instance — the full workflow cursor string",
    "watchme": "object — the probe spec the emission gate reads, or null",
    "deposits": "object — {stage_name: stage_output} where the chart pipeline writes its cargo",
    "sorted_berth": "path — the sorted door's berth for this cast",
    "intent_berth": "path — the /intent firing's berth",
    "chart_claim": "path — the chart chain's claim, or null",
    "release": "string — the release for the pickup phase on workflow_and_state's cursor, or absent when the phase is 'waiting'. queued names the ticket it waits on, blocked the question or trouble, hold the condition that ends it. Written and CLEARED by cairn.tools.base.transitions.set_phase in the same act as the phase (ruling 2026-09-08-a-fleet-of-one-reshapes-the-pickup-phase)",
    "phase_writes": "array — append-only record of every pickup-phase write on this ticket, one entry per act: {act, actor, phase, release, standing, workflow, at}. The SAME body the component's history.json carries for that act, minus the history's own 'seq' envelope — one clock, two addresses (ticket 9e7867aa1056). Written only by transitions.set_phase; a phase on the cursor with no record naming it here was written by hand"
  },
  "entry": {
    "ruling": "2026-08-02-three-entry-points-and-structural-refiled (confirmed)",
    "the_three_doors": "IDEA (pre-intention design) → INTENT (/intent births and traces) → TICKET. This store is a DIRECT entry point, not only /sorted's output: a bug enters here without passing IDEA or INTENT.",
    "what_makes_it_structural_instead": "LEVEL, not size or effort. Akien verbatim: 'if we have to edit lines of code to fix it, then it's not deeply structural... but if new information breaks the harmony of the system (eg an intention was implemented in a way contrary to the actual intention) with that version you and i have to reconcile it. that's structural.' A structural bug is reconciled with Akien; it does not enter here unexamined.",
    "why_the_test_is_shaped_this_way": "The cost heuristic CC reaches for by default — big, scary, wide blast radius — is a judgment call, so it drifts. Akien's test names an OBSERVABLE: look at where the fix lands. Same move as intention+why.json forcing the why into the filename (Law 4 in miniature)."
  },
  "notes_on_the_store": [
    "TROUBLES DRAIN HERE. troubles/ is a pre-ticket inbox, not a parallel place work lives; a trouble that never becomes a ticket is a bug with no workflow. The trouble record stays (it is the account of the live symptom); the ticket is what moves through the gates.",
    "A ticket is never terminal: `validated` = best guess still carrying its falsifier and horizon; the back-edge (build → deconstruct) is the universal shape.",
    "A node with no falsifier does not cast (the /sorted completeness gate).",
    "Ownership stays put; what moves phase-to-phase is which peer is invited to act next, through the owner's gate — not a transfer of ownership.",
    "NOTHING ENFORCES THIS TEMPLATE. It is prose describing a shape, and no door reads it: a ticket may carry a key that is not here, or omit one that is, and nothing reds. Measured 2026-09-08 while adding 'release' and 'phase_writes' — the two fields the write door had already been writing for a voyage before this file mentioned them, which is the drift the absence of a schema makes invisible. Law 4: this is an IOU, not a resting state."
  ]
}
