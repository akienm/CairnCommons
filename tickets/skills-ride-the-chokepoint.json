{
  "id": "skills-ride-the-chokepoint",
  "schema_version": "ticket-v1",
  "date": "2026-07-29",
  "owner": "CC (code+proofs); Akien (the queue is his ratified plan, 2026-07-29)",
  "intention": "SKILL VOYAGES GET A WORKFLOW — the chokepoint learns class skill@v1. Today a skill node cannot journal a voyage: the emit chokepoint refuses skill-class crossings with 'no registered workflow for class skill' (found live at /sail's first real crossing, filed as sail ticket edge (d)). This stone registers the skill@v1 workflow (the stages a skill node's life actually has — drafted, proved-by-crossing, challenged — whatever constrain/decompose settle them to be) so skill nodes ride the same record of truth as code-seams: journaled crossings at the one door, state compiled from history, gates able to see them.",
  "why": "Skills are load-bearing devices (the whole liturgy lives in them) but their voyages are invisible to the record — a skill 'proves out' today only in prose. The sail charter's own proof section says PROVED-by-first-crossing with no journal to show it. Every rule the gates enforce assumes the crossing exists; a node class that cannot cross cannot be gated (Law 4 needs the door to exist before physics can stand at it). Found-in-the-world provenance: the chokepoint itself named this gap at /sail's first crossing.",
  "traces_to": [
    "Law 5 — a skill's voyage and proofs should share its address like every component's; today its history has nowhere to append",
    "Law 4 — the /challenge cadence and prove-by-crossing are prose until the class has journaled stages a gate can stand on",
    "Law 3 — 'PROVED by the first real crossing' is currently narration; a journaled crossing is the measurement",
    "Law 1 — one workflow registry, one door; skill nodes compose the existing chokepoint, never a parallel journal"
  ],
  "node_class": "code-seam",
  "state": "code-seam@v2: THINKME -> TICKETME -> BUILDME -> PROVEME -> [PROVED]   (built + proven 2026-07-29: class skill@v1 registered in node_classes/skill.json ALONE — cairn/tools/base/transitions.py never touched, last modified by stone A's a3127d2, absent from this stone's 7f3fd96 entirely, so the registry-is-the-door claim holds by measurement. 34 checks green twice byte-identical unsealed, then green/sealed twice under TesterDevice netns; the VALIDATION persisted at 12:32:19 and reproduced twice at close. /sail's own record born at skills/sail/{history,state}.json: one record, skill@v1 THINKME->BUILDME forward, entry gate clean, state == project(history) verified directly. All seven stage berths deposited (confirmed duplicate:True, node_ids on the verdict). Builder was Sonnet, stopped mid-close by imminent usage exhaustion (NOT a design pause — Akien 2026-07-29); the close finished inline on Opus. TWO FINDINGS AT CLOSE, both carried below in stage_needs.findings)",
  "gates": "proof gate: teeth beside the code — a skill-class node journals a full voyage through the chokepoint (the exact stage names per the settled workflow); the refusal that seeded this stone no longer fires for class skill; unknown classes still refuse loudly; existing code-seam crossings untouched (every test_transitions tooth still greens). Tester netns seal. Live gate: one real skill node (sail or chart) journals its first real crossing.",
  "falsifier": "RED on any of: the chokepoint still refusing class skill after the stone lands; a skill voyage journaled outside the one door (a parallel journal is the defect, not a workaround); an unknown node class now passing (the fix must not widen the door); a registered workflow whose stages nobody charted (stages invented in code rather than settled through the chain).",
  "horizon": "re-read when a third node class wants registration (the second instance proves the registration pattern, the third earns a shared shape); when the concept-piece class (quorum-signature gates, /sorted's human-proved path) wants its workflow; when skill state/history berthing (class-space vs instance-space) is settled by the chain.",
  "parent": "base",
  "children": [],
  "dependencies": [
    "emit chokepoint + workflow registry (cairn.tools.base.transitions) — the door being taught the class",
    "sail ticket edge (d) / sail charter proof note — the dated refusal that seeded this",
    "CairnCommons/node_classes/ — the class charter the workflow must match (survey checks what exists)",
    "tester netns seal — standing",
    "model: sonnet — assigned 2026-07-29: the survey collapsed the build to a one-file registry amendment (the code-seam template mirrors over), teeth on existing fixture worlds, and one real first history; fully specified, solid tier suffices with the gates watching. Akien's option exercised: a model as a dependency, per position in the flow"
  ],
  "provenance": "Cast 2026-07-29 on Akien's 'go go go!' ratifying the budget stage-split plan. Seeded by the chokepoint's own live refusal at /sail's first crossing 2026-07-28. Cast BEFORE the chart run.",
  "chart_notes": {
    "constrain": "what the skill@v1 stages ARE (the workflow string) and where a skill node's state/history berth — skills live in class-space (repo skills/) so the record must too, but which address gates writes; also whether /challenge is a stage or a back-edge",
    "survey": "what CairnCommons/node_classes/ already charters for class skill; what the workflow registry's shape demands of a new class; which skill nodes exist today and what their first crossing would honestly journal"
  },
  "findings": [
    "FINDING 1 — BUILD_INSPECTOR CANNOT SEE skills/ AT ALL. Criterion 2's named instrument was `python3 -m cairn.machines.build_inspector.inspector` on the sail address; it REFUSES, loudly and correctly: 'no component skills/sail under <root>/cairn — the census sees [base, build_inspector, bus, chart, charter, db_domain, diagnostic_inspector, ground_loop, harbor_master, inference_domain, intention_extractor, intentions_model_compiler, librarian, orient, sudo_relay, system_rackmount, tester, trouble, web_server]. A gate that silently inspects nothing passes everything (Law 8).' orient's device_census is scoped to cairn/, so EVERY filter regime — charter_on_disk, proofs_exist, silent_device, state_is_projection — has zero jurisdiction over skills/, the very class this stone just taught to cross. The instrument was not substituted away: the invariant it measures (state == project(history)) was verified directly through cairn.tools.charter.projector and holds. But the criterion as charted was not runnable, and that is recorded rather than smoothed. CONSEQUENCE FOR ticket nothing-rides-loose, cast the same day: its crossing-half filter would not cover skills/ either — the census scope is a prerequisite that ticket does not yet name. Carry it there.",
    "FINDING 2 — THE 'SAME ACT' RECOMPILE POKE WAS NOT SATISFIED for the sail charter delta. H3 expected 'both charter deltas landed with the recompile poked in the same act'; in fact both charter edits landed at ~12:32 (builder) and the model write-door was poked ~3h later at close. The INVARIANT never lapsed — the gate is on-read, and /intent pokes before consulting, so any reader in that window refreshed first — but the procedural expectation as written was missed. Recorded as the honest partial it is, not passed silently. The distinction worth keeping: 'same act' is stricter than the invariant it protects (model current AT READ), which is the [[proof-over-live-data-assert-invariants]] shape — pin the invariant, not the procedure."
  ],
  "migrated_from": "code-seam@v1 — the dissolved fifth summons stripped by sweep 2026-08-03 (Akien's ruling: the words should have been scrubbed when the version was minted). The dead token is deliberately not reprinted here; tickets/watchme-emits-a-probe is its one record.",
  "forwarding": {
    "cairn/chart/intention+why.json": {
      "to": "skills/chart/intention+why.json",
      "why": "the chart MACHINE dissolved on 2026-08-13 (cairn bf80d5d) and its charter moved WHOLE into the skill that calls the chain: skills/chart/SKILL.md says so in as many words -- 'there is no separate device charter: the chart machine dissolved on 2026-08-13 and its charter moved here whole'. Git records the last rename as cairn/machines/chart/intention+why.json, which was then deleted rather than moved again, so the derived follow correctly declines: its endpoint is not in the world. A hand is the only thing that can cross a content merge."
    }
  }
}
