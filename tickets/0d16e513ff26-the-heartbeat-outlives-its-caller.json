{
  "id": "0d16e513ff26",
  "title": "the-heartbeat-outlives-its-caller",
  "date": "2026-08-18",
  "owner": "CC",
  "owning_intention": "launchers/intention+why.json",
  "intention": "The ground loop outlives the terminal that started it: a heartbeat spawned by an entry point is parented on the user manager, not on the caller's cgroup, so killing the window or the session leaves it beating.",
  "why": "The heartbeat is the one resident process every callback in the system hangs off, and today its lifetime is an accident of which window launched it. MEASURED 2026-08-18: the kernel oom-killed the claude binary at 14:57:31; systemd marked app-org.kde.konsole-b1d62b21cfc74a57b8e15d874590bdc6.scope 'Failed with result oom-kill' at 14:57:43; the loop's own trail at ~/.cairn/logs/ground_loop/0/diagnostics.jsonl carries exactly three loop starts that day - 14:52:55, 14:58:01, 15:33:34 - so the heartbeat died in that scope kill with nothing between. It died the same way on 2026-08-10 ('74,209 beats, then nothing, and nothing was loud about it'). A thing declared ALWAYS UP whose survival depends on a terminal staying open is policy wearing physics' clothes.",
  "traces_to": "Law 4 first - a rule that matters is enforced by physics, not policy. 'The ground loop is always up' (Akien, 2026-08-11) is enforced today by nobody closing the window. Law 3 - the survival was never measured, only intended, and the word 'setsid' in the spawn's own comment was read as evidence of it. And the founding aim of getting Akien out of running the system: a heartbeat that dies with a terminal is a thing only a human notices, and on 2026-08-18 a human did.",
  "falsifier": {
    "proves_green": "code-seam invariant, transcribed: PROVED, and feeds-back-to-origin-on-failure (Laws 3 + 8). PROVE GATE: the tester runs launchers/proofs/test_ground_loop_survives_its_caller.py, verdict and seal from the same hand. Its non-hollow tooth is that it asks the KERNEL - /proc/<pid>/cgroup for the spawned loop, then kills the caller's whole process tree and reads the loop's own trail for beats on both sides of the kill - rather than reading flags back out of the launcher script. The precedent is launchers/proofs/test_superclaude_memory_scope.py, which drives the real launcher through a pty and asks the kernel for the limits. DURABILITY GATE: not done until tested AND committed to git.",
    "proves_red": "DONE when the loop, started through the launcher, sits in a cgroup that is neither the caller's nor a descendant of it - asked of the kernel, never read off the launcher's flags - AND survives its caller's entire process tree being killed, with the loop's own trail showing an unbroken run of beats across that kill, AND the losing-claimant refusal still lands in ~/.cairn/logs/ground_loop_nohup.log rather than only in the journal. WRONG INTENT if: (1) anything becomes system-level startup - an installed unit file, an `enable`, anything surviving a reboot (Akien's 2026-08-11 ruling); (2) the launcher grows an opinion about whether a loop is already running, re-deriving the singleton guard at the caller (Law 1, and guard.py's own docstring names this caller); (3) the spawn can abort, delay or fail a launch (the prime directive: from parse to exec, nothing may stop a session starting).",
    "horizon": "one launch. Every clause is answerable at the first superclaude boot after the change - no clock, no live host, no wait."
  },
  "children": "none, because it is a leaf: one seam (the spawn at launchers/superclaude:229), one owner (the launcher), one proof. The sibling that could look like a child is named rather than absorbed - the sudo relay died in the same scope kill with the same parenting defect, but it has a different start path and is Akien's privileged act, and folding it in would put a more specific thing inside a less specific one (his ruling, 2026-08-18). It wants its own node.",
  "linked_ideas": [
    "2026-08-18-the-heartbeat-dies-with-the-terminal-window"
  ],
  "questions": [],
  "constraints": [],
  "how": "Replace the `setsid python3 -m cairn.devices.ground_loop` spawn at launchers/superclaude:229 with a transient user unit: `systemd-run --user --unit=cairn-ground-loop --working-directory=<repo> -p StandardOutput=append:$HOME/.cairn/logs/ground_loop_nohup.log -p StandardError=append:<same> -- python3 -m cairn.devices.ground_loop`. It lands at /user.slice/user-1000.slice/user@1000.service/app.slice/cairn-ground-loop.service - a SIBLING of the Konsole scope and of the session scope rather than a child of either. Everything else about the spawn is unchanged: still spawn-blind (the singleton guard decides), still under the prime directive with a guarded fall-through to today's setsid spawn when systemd-run is missing or its probe fails, still `|| true`, and the boot log names which branch it took, exactly as the memory-scope block already does at :291/:302. The StandardOutput redirect is load-bearing, not decoration: a unit does not inherit the shell's append redirect, and without it the losing claimant's refusal - the line that distinguishes 'refused, one is running' from 'broke' - vanishes into the journal.",
  "links": [
    "parent: none - it is not a child of an-entry-point-starts-the-loop-only-once. That node owns WHETHER a second loop starts and is PROVED; this one owns how long the first one lives, and neither contains the other."
  ],
  "priority": "",
  "node_class": "code-seam",
  "workflow_and_state": "code-seam@v2: THINKME -> TICKETME -> BUILDME -> PROVEME -> WATCHME(the_heartbeat_outlives_its_caller) -> [PROVED]",
  "watchme": {
    "object": "the_heartbeat_outlives_its_caller",
    "trigger": "every beat - the loop reads its own /proc/self/cgroup, which it is already awake to do; no clock of its own, no scan, one file read of ~200 bytes. Deliberately not a launch-time check: a launch-time check answers 'was it started right', and the question is 'is it parented right NOW', which is the one that was false and unnoticed.",
    "enough": "NOT satisfied by the launch that installs it. Enough when it has seen at least one caller die while the loop kept beating - until then the watch has confirmed only that the parenting is what the launcher said, never that the parenting does what it is for.",
    "carrier": "a verdict artifact against this ticket's falsifier: the cgroup path the kernel reports, whether it is a descendant of a terminal or session scope, and the beat number - so a reader can join a survival to the kill that preceded it.",
    "nexus": "the ground_loop device's own tree - the same nexus its liveness and staleness watches teach, this being another fact about the loop's residency rather than about any caller.",
    "consumer": "the trouble lane on a red (a heartbeat that has quietly become a child of a terminal again is exactly the class of thing that stays true and unnoticed for days), and the ground_loop pane for the standing green.",
    "probe": "cairn/devices/ground_loop/probes/does_the_heartbeat_outlive_its_caller.py"
  },
  "deposits": {},
  "sorted_berth": "/home/akien/.cairn/devices/skill_block/0/berths/sorted/sorted-20260818T154253-5b136a0160b6.json",
  "intent_berth": "/home/akien/.cairn/devices/skill_block/0/berths/intent/intent-20260818T154154-e726610d506b.json",
  "chart_claim": "validate-20260818T160010-a6527557b941 \u2014 chart chain complete with real content; verdict berthed at verdict-20260818T170101-73e0c8c5fa51",
  "schema_version": "v0",
  "dependencies": [],
  "open_at_cast": "UNPROVEN and carried into the build as its one open question: that `systemd-run --user --unit=<name>` fails harmlessly when the unit name is already taken. The spawn is deliberately blind and the singleton guard is what refuses a second loop; if systemd-run instead errors before the guard can run, the spawn's blindness changes shape. A five-second measurement at BUILDME, written into the falsifier rather than left in prose.",
  "open_at_cast_resolved": "MEASURED AND THE ANSWER WAS NO \u2014 the cast's one open question, closed at BUILDME. `systemd-run --user --unit=<name>` does NOT fail harmlessly when the name is taken: it exits 1 with 'Unit <name>.service was already loaded or has a fragment file' and starts nothing, so the singleton guard never runs and never gets its chance to refuse. The spawn's blindness did change shape, exactly as the cast said it would. TWO CONSEQUENCES, both now physics. First, a unit that exits NONZERO stays loaded in `failed` holding its own name \u2014 and the singleton loser exits 3 BY DESIGN, so without SuccessExitStatus=3 every ordinary losing launch would wedge the name against every later one; the proof's wedge arms are that measurement kept. Second, a HELD NAME must not fall through to the setsid spawn, because a held name MEANS a loop already holds it and the fall-through would be a second claimant racing the first (ticket the-beater-holds-the-claim). Only an UNREACHABLE systemd is a legitimate fall-through. The two failure modes look identical at the call site and are opposite in what they license \u2014 which is why the launcher now distinguishes them by systemd-run's own message rather than by an exit code that cannot tell them apart.",
  "chart_feedback": {
    "score_is_the_count": "225 tool calls between the validate berth and done; 104 for the chart chain itself; 329 for the session. Read DOWNWARD, per the 2026-08-14 ruling \u2014 a good packet is one you barely had to go looking behind, and 225 is not that. Counted from the transcript rather than estimated. WHAT THE NUMBER HONESTLY CONTAINS, said so the next reader does not over-read it: a mid-build REDESIGN (three round trips down to one) driven by a cost measurement the chart asked for but did not take; an n=15 interleaved timing harness built from scratch, run three times because the first two runs were confounded; cleaning up 14 loops that harness leaked; and two gate refusals with their fixes. A thinner packet and a harder build produce the same number, which is the metric's known limit and the reason the named falsifications below exist.",
    "named_falsifications": [
      "CONSTRAIN FLOOR, and this one is a defect in the machine rather than in my packet: charter_constraints came back [] for every ref under launchers/, bin/ and skills/, because address.component_of is rooted at <repo>/cairn and cannot see a component outside it. Three whole rungs of the tree are invisible to the constrain floor. The bounds I wrote were read by hand from charters the floor should have surfaced. Owed: a ticket.",
      "CRITERION 1's INSTRUMENT specified `kill -- -<caller pgid>`. A killpg cannot prove the claim \u2014 setsid has already left the process group, so it spares the loop before the fix as much as after. The proof kills the caller's cgroup.procs. The chart named the wrong weapon for its own claim.",
      "CRITERION 6's INSTRUMENT assumed the probe's __main__ took a pid. It accepted argv and IGNORED it, so the two-pid reading the criterion asks for was not performable as written. Fixed in the build; the criterion was written against a surface nobody had run.",
      "CRITERION 7's INSTRUMENT \u2014 'grep, and every hit is the helper or an import of it' \u2014 is falsified by its own output: 10 hits, 9 of them prose, comments, the proof's own search pattern, and a bash one-liner. A text grep cannot tell a mention from a read. The claim survived; the instrument had to be replaced with a python-read census carrying declared exemptions and a staleness check.",
      "DECOMPOSE's writes_to needed correcting mid-build.",
      "SYSTEMD-RUN --user HANDS THE UNIT THE MANAGER'S ENVIRONMENT, not the caller's \u2014 nothing in the chain anticipated it, and without --setenv=PATH the loop loses ~/.cairn/venv/bin and cannot import itself. A survey holding that would have caught this does not exist.",
      "THE deletegate HOOK refuses any rm whose command contains the literal substring '.cairn', including scratchpad paths that merely mention it. A real false positive, met twice this voyage; the door is bin/cmd/trash.",
      "THE HARBOR'S CLEARANCE GATE was not in the chain at all. Nothing in orient, survey or validate knew that a forward crossing into a REST needs harbor_master.clearance.clear, nor that it needs a machine-readable gated_by on the component's charter. Both discovered by being refused at the door \u2014 which is the door working, and a hole in the survey.",
      "TWO PROOF BUGS the red-before caught: a prime-directive arm that stripped PATH of every directory holding systemd-run (on this host /usr/bin), leaving the launcher without `script` \u2014 isolation that removes the world reads exactly like the defect; and a one-reader census that counted a bash one-liner as a python reader.",
      "TWO MEASUREMENT-METHOD FINDINGS, kept because they are general: sequentially-run A/B arms measure the drift BETWEEN the arms as if it were treatment (it reported 1 round trip as slower than 3 until the arms were interleaved); and a measurement taken while your own leak is running measures the leak (the first per-stage breakdown read 125ms where the same commands later read 30ms \u2014 load average 18.7 vs 3.5, and the 18.7 was my own 14 orphans)."
    ]
  }
}
