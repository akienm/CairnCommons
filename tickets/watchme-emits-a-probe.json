{
  "id": "watchme-emits-a-probe",
  "schema_version": "ticket-v1",
  "date": "2026-07-30",
  "owner": "CC (code + proofs + the migration); Akien (the design rulings of 2026-07-30: WATCHME keeps the -ME grammar and means 'emits a probe'; every intention gets a keep-learning step BY DEFAULT and 'none, because X' is the only other legal answer; a verdict artifact is the DEFAULT carriage and not the only one; and the bootstrap-vs-product distinction below, which is his and is load-bearing on the whole design; and the CALLBACK -> PROBE rename, ruled in the same conversation: 'that IS what it does PROGRAMMATICALLY... but what it DOES FUNCTIONALLY is: it's a probe')",
  "intention": "A NODE EMITS THE WATCHER THAT MEASURES ITS OWN INTENT. The state `LEARNING` is replaced by the summons `WATCHME`, whose act is to EMIT A PROBE — the immutable worker that gathers data about whether this node's intention actually did what it claimed, in the world, after it rests. Being in the workflow is what obliges the ticket to SPECIFY, at ticketing, everything needed to emit that probe: how it accumulates (the trigger), when it clears (the enough-condition), what rides back (the carrier), where that lands (the nexus), and who consumes it. Default carriage is a verdict artifact written against the ticket's own falsifier — the same schema, validator, deposit door and kick-back routing the build-time verdict already uses — but carriage MAY be something else (a metric accumulating for a consumer orthogonal to this intention) provided the spec names its shape and its consumer. A probe carries NO AUTHORITY: it deposits and it pokes; the back-edge on a failed efficacy verdict is the owner's act (Law 6).",
  "why": "THE FALSIFIER BECOMES EXECUTABLE. Every cast node in this system already carries a falsifier and a horizon — /intent question 5 demands it, /sorted's completeness gate refuses a cast without it, and a PROVED node is described as resting 'still carrying falsifier+horizon'. That field has been PROSE on every ticket since the beginning: written, never fired, never measured. Law 4 says a rule that matters is enforced by physics, not policy, and until it is it is a tracked debt. This node is the payment. A probe emitted at WATCHME IS the falsifier and horizon written in a form that fires.\n\nWHY THE OLD SHAPE HAS TO GO RATHER THAN BE EXTENDED. `LEARNING` was minted as a terminal state before probes existed as machinery, to stand in for exactly this: a place a node could sit while data about its efficacy accumulated. Probes now exist (cairn/base/probe.py — immutable, trigger-as-open-predicate, carrier-as-open-callable, evaluated where the data is owned). So the state has nothing left to do that the worker does not do better: keeping it would mean the node DWELLS while its watcher runs, which is wrong at the tail — a proved intention's efficacy data can only accumulate AFTER it rests, so a node that waits for it never rests and never gets measured. Emitting instead of becoming lets the node rest at PROVED and the watcher stand. That deletes a state rather than adding one.\n\nWHY LEARNME AS IT STANDS IS THE INVERSION OF THE INTENT. Measured 2026-07-30: code-seam@v1 and skill@v1 both carry LEARNME in `path` with `skippable_summons: ['TICKETME']`, so LEARNME is MANDATORY TO CROSS for every node of both classes — and it carries NO GATE (the build gate fires at PROVEME->forward, the exit gate at ->PROVED). It is the one summons in the path that is forced and unchecked. Akien's rule is the exact inverse: optional to CARRY, mandatory to SATISFY once carried. Under the forced-and-unchecked shape the crossing gets filled with whatever is at hand — on ticket a-node-holds-one-claim (2026-07-29) it was filled with close-bookkeeping (depositing that voyage's own berths), which is not efficacy data at all and involved no accumulation; the node never dwelled. /sail step 8 has quietly redefined LEARNME as 'deposit the learnings at the close', and MAP.md's gloss ('learn *from* me — I am material, harvest me into the trees') reads the node as passive corpus. Akien's reading is active: the node causes learning to occur. Same word, two meanings, and the passive one is what got built.\n\nLEARNING WITHOUT AN OBJECT IS INERT (Akien, 2026-07-30). A bare token in the path cannot state what is being learned, so nothing can check it. The object is forced by structure the way `intention+why.json` forces the why — a WATCHME that cannot name its accumulation is a parse error, not a blank field.\n\nWHY ONE FEEDBACK CONTRACT AND NOT ONE PER THING (Akien: 'We do NOT want to be creating learning contracts for each thing individually'). We already built the general one and had not noticed it was general. cairn/chart/verdict.py carries a schema with teeth (claim / instrument / outcome / evidence per claim; validate_verdict refuses narration, refuses an unattributed verdict, refuses incomplete coverage), a rendering into ANY nexus (verdict_node_parts -> deposit_learning(nexus, content, vector, provenance) — one node per claim, bare, content-hashed), and a return route that is ALREADY PHYSICS (a failed criterion is a kick-back, not a crossing; the exit gate enforces it today). So the standard interface for a question nexus / Stackable Learning Brick is one sentence: it accepts a verdict artifact naming one of its claims, and it emits one. It scales down without a second contract — a thing that is not a whole SLB emits a ONE-CLAIM verdict, and n=1 is not a special case of the schema, the coverage check simply has less to cover.",
  "traces_to": [
    "Law 4 — the falsifier is the oldest prose-standing-in-for-physics in the system; this is its enforcement, and CLAUDE.md's rules-awaiting-physics section is supposed to shrink monotonically.",
    "Law 3 — an intention whose efficacy is unmeasured is a hypothesis and must be labeled one. Today every PROVED node is exactly that and nothing says so.",
    "Law 1 — one feedback contract compiled once, not a learning agreement re-derived per component; and the resolver is not spent re-answering 'did that work?' by hand.",
    "Law 6 — a probe is authority-free by construction, which is what lets it stay immutable and shareable; delegated writes go through the owner's gate, never ambiently.",
    "I-learns-its-gates (2026-07-16, founding) — the system learns its gates from evidence. This is that intention at the NODE level; CLAUDE.md's learning-as-a-pattern IOU is the same intention at the COMPONENT level (see distinctions in provenance)."
  ],
  "node_class": "code-seam",
  "state": "code-seam@v2: THINKME -> TICKETME -> BUILDME -> PROVEME -> WATCHME(does-optional-mean-never-carried) -> [PROVED]",
  "gates": "proof gate: teeth beside the code in cairn/base/proofs/ (transitions + probe), extending test_transitions.py and test_probe*.py rather than paralleling them; the chart deposit teeth (cairn/chart/proofs/test_chart_verdict.py) must all still green under the widened validate_ref; sealed under TesterDevice().run_proof(isolation='netns') with the validation persisted beside the code. PLUS the cairnmap recompile gate, twice over: this build edits a model source (intentions-other/I-heartbeat-probes-and-bus.md) and edits skills (/sail, /sorted), so cairn/intentions_model_compiler/recompile_gate.sh is poked in the same act as each write. Feeds back to origin on failure: a red at PROVEME back-edges to BUILDME; a red in the MIGRATION (an at-sea node whose string will not convert) back-edges to TICKETME, because that is a decomposition error not a build error.",
  "falsifier": "RED on any of: (1) a WATCHME crossing is accepted that emitted no armed probe — the gate must check EMISSION (the probe exists, is armed, and carries its trigger, enough-condition, carrier, nexus and consumer), which is the clause that makes 'not optional once present' physics rather than prose. (2) A probe is armed and never fires, and nothing is loud about it — a watcher emitted into a heartbeat nobody runs learns nothing while LOOKING like learning, which is the silent-failure disease wearing our own badge; an unfired watcher past its horizon must surface at a diagnostic surface (Law 7). (3) A ticket carrying WATCHME casts with any part of the spec absent — unspecifiable is never a silent omission: no conceivable efficacy measure is a RED at /sorted's completeness gate (the falsifier is unwritable, so the node is not ready to cast), and specifiable-but-not-yet DROPS A TICKET and carries the IOU. (4) `LEARNING` or `LEARNME` survives anywhere as a LIVE state after the migration — a grep must find them only in history, superseded records and the migration's own record (records of truth are never edited to hide the old shape, Law 7). (5) A probe moves a node's state directly — that is an ambient authority leak (Law 6) and it must be structurally impossible, not merely avoided. (6) A verdict written by a probe cannot pass validate_verdict, or needs a second schema to do so — one contract or the design failed. (7) A node casts with neither a WATCHME nor a recorded 'no keep-learning step, because X'; silence is not a legal answer. (8) The spec's `nexus` or `consumer` cannot name a target outside the toolchain — see the bootstrap distinction; a design that only works when the watched thing is one of our own devices has solved the wrong problem.",
  "horizon": "Re-read when: the heartbeat actually fires devices' probes in anger (until then clause (2) is the whole risk surface and this node's watchers are armed into a mechanism whose liveness is itself unmeasured); the FIRST efficacy verdict comes back failed and back-edges a resting node (the return route is the least-exercised half of this design and the only part with no existing physics); the first NON-verdict carriage lands (a metric accumulating for an orthogonal consumer) and shows whether 'name the shape and the consumer' is a sufficient obligation or wants its own schema; and the first ticket that is NOT about building the toolchain carries a WATCHME — that is the measurement that says whether the bootstrap distinction was honored or merely written down.",
  "parent": "state-machine-physics",
  "children": [],
  "dependencies": [
    "PIECE (a) — WHERE A COMPILED PROBE BERTHS. RULED BY AKIEN 2026-07-30, and it corrects two wrong turns worth recording so neither is re-taken. FIRST WRONG TURN: cairn/base/probe.py's header justifies class-space FROM A MISAPPLIED WORD — 'because it is DECLARATION, it lives with the device's CODE'. A probe is NOT the declaration. The DECLARATION is the intent, and the intent is on the TICKET; the probe is an EXPRESSION of that intent (when this happens, call this, with this). So the placement argument in that header has nothing under it and must be rewritten with the rest of piece (e). SECOND WRONG TURN (CC's, twice): first placing a per-ticket watcher beside its ticket's component, then collapsing everything into instance-space. Both wrong on the same axis.\n\nTHE RULE: A PROBE IS COMPILED INTENT — EXACTLY AS CODE IS COMPILED INTENT — AND IT BERTHS WITH WHAT IT WATCHES, NOT WITH THE TICKET IT WAS COMPILED FROM. So a probe's folder is routinely nowhere near its ticket, and probes CLUSTER BY SUBJECT: a device's time-related watchers share a folder in that device, as do the watchers over one piece of code. Three destinations, chosen by the subject:\n  (i) WATCHES THE TOOLS -> IN THE REPO, beside the monitored code, CHECKED IN. Someone who installs this repo needs these to have working tools — they are part of what the tool IS.\n  (ii) WATCHES THIS INSTANCE'S RUNTIME, or is a standing ask from this user -> instance-space (Akien's example, fictitious: 'watch for parsimonious and AI appearing close together' -> ~/.cairn/devices/librarian/0/librarian/probes_for_learning/).\n  (iii) WATCHES A DOWNSTREAM PROJECT -> that project's own repo, because that project's ticket workflow lives there (Akien's example: an Acurite weather app built with these tools carries its own probes in its own repo, on our work process) — and ITS user-specific learning points at that user's own runtime path, outside both of our roots entirely.\n\nDELIBERATE EXCEPTION TO LAW 5, recorded so it is not 'fixed' later: intent, voyage and proofs share an address, but a probe is none of the three — it is an INSTRUMENT, and an instrument berths at what it measures. A probe far from its ticket reads as drift and is not.\n\nEXTENDS THE THREE-ROOTS TEST rather than breaking it: CLAUDE.md asks 'does the intention have ONE code address?'. For a compiled probe the question becomes 'what does it WATCH, and where does THAT live?' — same question, different subject.\n\nAND IT ANSWERS THE INSTALL QUESTION, which is the half Akien opened this with: a stranger cloning the repo must get the tool-watching probes as checked-in code that ARMS ITSELF on their host, with none of our work-watchers firing on their box. That makes a shipped probe a HOST-SEAM in shape — a declaration that must apply and verify against a host it has never seen — so it lands on ticket host-seam-installer's known gap (host-seams declare apply/verify as PROSE with no machine-readable `applies-to` field). The two are COUPLED, not sequential.",
    "PIECE (b) — THE CLASS TABLES AND THE GRAMMAR. Mint code-seam@v2 and skill@v2: LEARNME leaves the path, WATCHME may appear ZERO OR MORE times at any position, and a bare `WATCHME` with no spec is refused at parse. This is a NEW VERSION and not a widening of v1 (recorded as CC's read, see provenance): the path itself changes, versions are immutable and version-stamped, and migration-by-find-and-replace is the mechanism MAP.md already names for exactly this. cairn/base/transitions.py's exact-sequence conformance refuses a zero-or-more state today, so the parser is real work. Every at-sea node's string migrates; the migration is journaled, never silent.",
    "PIECE (c) — THE SPEC AND THE EMISSION GATE. The WATCHME spec (trigger, enough-condition, carrier, nexus, consumer) becomes a required structure at ticketing, and the emit chokepoint refuses a forward crossing out of WATCHME unless the probe it names is ARMED — emission, not accumulation. THE DWELL FALLS OUT ELSEWHERE AND IS NOT A SECOND MODE (settled with Akien): a mid-workflow node that must accumulate before continuing dwells because the NEXT summons is gated on data that has not arrived, not because WATCHME holds it. One state, one act — emit.",
    "PIECE (d) — THE FEEDBACK CONTRACT, GENERALIZED. `validate_ref` admits a second provenance form (`falsifier@<ticket-id>`) so a verdict whose criteria come from the ticket's falsifier rather than from a berthed chart validate packet is legal. `nexus` becomes a specified parameter rather than the hardwired `hypothesize`. Carriage beyond a verdict artifact is legal when the spec names its shape and consumer. The return route reuses what exists verbatim: outcome=pass deposits and rests; outcome=fail is a kick-back, which is already how the exit gate routes a failed criterion — the ONLY new part is the rest-state form (a fail must MOVE an already-resting node backward, where today a fail refuses a crossing), and per Law 6 that move is the OWNER's act on reading a failed verdict, with the probe only depositing and poking.",
    "PIECE (e) — THE PROSE THAT MUST MOVE WITH THE PHYSICS, each pairing its write with a recompile-gate poke. intentions-other/I-heartbeat-probes-and-bus.md lines 95-99 ('LEARNING is a state; a probe is the worker... a node IN LEARNING can SET a probe') — SUPERSEDED, this is the exclusive-and-terminal collision. MAP.md's 'Two rests, both inside the one loop' section — rewritten; there is one rest, and the standing work lives in the probe, which is not a node state at all. /sail step 8 stops being a LEARNME crossing (the close-deposit is bookkeeping the close does, not a summons). /sorted casts the WATCHME spec by default and records 'none, because X' when there isn't one. cairn/base/probe.py's own header carries the superseded sentence and must move with it.",
    "cairn/base/probe.py — the primitive is BUILT and needs no extension: triggers are open predicates (trigger(now, context) -> bool), carriers are open callables ((context) -> dict) merged over a static body, a broken carrier does not swallow the poke. This design adds no primitive; it adds an obligation and a gate.",
    "cairn/chart/verdict.py — validate_verdict / verdict_node_parts / write_verdict, the contract being generalized. Its teeth (18 checks) are the regression surface for piece (d).",
    "cairn/base/transitions.py — the emit chokepoint, where pieces (b) and (c) become physics.",
    "The heartbeat + shim path (cairn/ground_loop, cairn/base/shim.py) — falsifier clause (2) is unmeasurable until a device's probes demonstrably fire on a beat. NOT a blocker for the build; IS a blocker for believing the result.",
    "PIECE (f) — THE VOCABULARY: CALLBACK BECOMES PROBE. RULED BY AKIEN 2026-07-30, folded into this build rather than cast separately, and done FIRST so nothing is built on the old word. HIS FORMULATION: 'that IS what it does PROGRAMMATICALLY... but what it DOES FUNCTIONALLY is: it's a probe.' The rename is not cosmetic — the thing had ALREADY become a probe and the name lagged. We ruled earlier the same day that it carries NO AUTHORITY (it deposits and it pokes; the owner acts), and its own header already describes firing as 'kick off a short-lived process that sends a message to the target's shim, then terminates'. So it is structurally incapable of anything but OBSERVE A CONDITION AND REPORT WHAT IT SAW. 'Probe' names that; 'callback' named the plumbing by which the report is delivered.\n\nWHAT FALLS OUT FOR FREE, which is the tell that the rename is real: (1) piece (a)'s placement rule stops needing an argument — 'a probe berths at what it measures' is self-evident where 'a callback berths at what it measures' required a ruling; the name forces the content, which is the naming stone working as designed. (2) The bad justification in piece (a) cannot even be WRITTEN about a probe ('because it is DECLARATION, it lives with the device's CODE'). (3) 'WATCHME emits a probe' reads as one idea, where 'WATCHME emits a callback' read as a state emitting a function pointer.\n\nSCOPE (a find-and-replace while this node is hours old and nothing is built on it — after the build it would be a migration): cairn/base/probe.py and its class; its three proofs (test_probe.py, test_probe_carry.py, test_probe_crossing.py); cairn/base/shim.py, cairn/bus/bus.py, cairn/base/diagnostic.py, cairn/ground_loop/, cairn/system_rackmount/, cairn/librarian/shim.py, cairn/diagnostic_inspector/; CairnCommons/intentions-other/I-heartbeat-probes-and-bus.md (the FILE renames too — it is a model source, so the write pokes the recompile gate in the same act); MAP.md; and this ticket, already done at the cast.\n\nTHE ONE PLACE THE OLD WORD SURVIVES, deliberately: describing HOW a probe is delivered programmatically, in the implementation's own docstring. The word is not erased from the code's vocabulary — it stops being the name of the concept. Recorded because a later reader finding 'callback' in probe.py must be able to tell a survivor from a straggler."
  ],
  "provenance": "Cast 2026-07-30 through /sorted, at the end of a design conversation Akien opened the morning after ticket a-node-holds-one-claim proved, with 'noce. so what is the role of LEARNME?' — a question about a state I had just crossed without being able to say what it was for. The arc: LEARNME is a data-gathering step (Akien) -> it is optional to APPLY but not optional if the data is needed -> 'LEARNING by itself is meaningless, it must CAUSE learning to occur' -> and since probes now exist as machinery, the state should EMIT one rather than BE one. Ratified stepwise, ending 'i think we're in accord? ... /SORTED'.\n\nFOUR DISTINCTIONS RECORDED AT THE GATE (none of them a collision to resolve; all of them things a future reader will otherwise re-derive):\n\n(1) SUPERSESSION, NOT COLLISION, with the parent's open child. state-machine-physics is live at BUILDME and names 'the LEARNING driver-terminal migration + workflow-migration-by-find-replace' among its remaining children. This node SUPERSEDES that child: the driver-terminal is DISSOLVED rather than built, and the find-and-replace half survives as piece (b). code-seam@v1's own `why` files the same migration against itself and is superseded the same way.\n\n(2) NODE-LEVEL vs COMPONENT-LEVEL LEARNING — a distinction, not a duplication. CLAUDE.md's rules-awaiting-physics carries 'every component's charter answers how does this component learn? — it doesn't, because X is a valid answer; silence is not' (ticket learning-as-a-pattern). That is the COMPONENT's standing answer; WATCHME is the per-NODE instance. They are the same intention at two levels of compilation and will likely converge; recorded so the convergence is a choice later rather than a surprise.\n\n(3) THE VERSION RULING IS CC'S READ, following from a ratified premise. Versions are immutable and version-stamped (MAP.md), and this changes the PATH, so it is v2 — not a widened v1. Flagged because the alternative (rule that widening a fork inside v1 is not a semantic change, since no existing node's crossings become illegal) was put to Akien and not explicitly ruled on. If he rules the other way, piece (b) shrinks and the migration disappears.\n\n(4) THE BOOTSTRAP DISTINCTION — AKIEN'S, 2026-07-30, AND IT CONSTRAINS THE DESIGN RATHER THAN ANNOTATING IT. 'Our first problem is to build our tools. We're using the tools on themselves. But we're designing for a world where we won't be constantly designing the tools — we'll be building products with our tools.' Two consequences the build must honor. FIRST: `nexus` and `consumer` take NO DEFAULT drawn from the toolchain. Every WATCHME cast so far would name one of our own nexi and one of our own devices, which would make a toolchain-shaped assumption invisible until the first product ticket — hence falsifier clause (8), which fails the design if a target outside the toolchain cannot be named. This is also WHY carriage had to be open beyond a verdict artifact (Akien's correction): a metric accumulating for a consumer orthogonal to the intention that spawned it is the PRODUCT-world shape, and a verdict-only contract would have quietly encoded the bootstrap world as the only world. SECOND: a tool watching its own efficacy is circular — it is both the instrument and the subject. We DO want the tools learning as we build them (Akien: 'we do want the tools to be learning as we build them!'), so the answer is not to forbid self-watching but to keep the ground truth OUTSIDE the tool's own opinion: the build's proof, the measured run, the artifact on disk. Kin to the standing cocoon note that ground truth stays the build's proof, not CC's opinion.\n\nAkien's closing frame at the cast: capture the rest durably, then he and CC dive into this one together — so this record is written to be the input to that dive, not a summary of it.\n\n(5) THE FORWARDING ORDER — A FINDING THIS VOYAGE PRODUCED BY SUCCEEDING, disposed in physics rather than argued past. 2026-07-30, at this node's own WATCHME crossing, the emit chokepoint's build gate refused with five findings: [charted_refs_resolve] naming cairn/base/callback.py, and [survey_holdings_resolve] for holdings 0, 1, 2 and 18 naming cairn/base/callback.py, cairn/base/proofs/test_callback_crossing.py and intentions-other/I-heartbeat-callbacks-and-bus.md. Every finding was correct and none of them was drift: those are the three addresses THIS TICKET'S OWN decompose piece (f) renamed, run first by the chart's own triage order precisely so nothing else would be built on the retired word. A chart that plans a rename falsifies its own refs by succeeding, and the ref filters — taught by the 2026-07-24 failure, where 'done' was reported while the files stood unmoved — could not tell the two apart. The berths are records of truth and were not edited to look consistent; re-running /chart against the renamed world would have been writing the chart after the build. So the disposition is a NAMED SUCCESSOR: this ticket now carries a `forwarding` map, and cairn/build_inspector grew a filter (forwarding_order_resolves) that checks it at BOTH ends — the successor must resolve, and the forwarded address must NOT, so a forwarding order can never launder a genuinely missing ref or re-aim a live address. A broken order grants no tolerance at all and reds by its own name, so the failure mode is two loud records rather than one silent pass. The BERTH DOOR deliberately does not inherit the tolerance: judge_survey stays flat, because at berth time every holding resolves by definition and a successor is meaningless there. Proved by six teeth in cairn/build_inspector/proofs/test_inspector.py section 26b (hollowed twice to check they bite: removing the both-ends check turns forwarding-to-nothing into a laundered pass; teaching the pure judge the tolerance breaks the door tooth), green twice bare, sealed twice under TesterDevice(isolation='netns'), validation persisted. SCOPE, NAMED RATHER THAN HIDDEN: build_inspector is not in this ticket's constrain bounds `in`, and it is not in `out` either — the change was taken because the blocked item ('LIVE FIRE at the real doors') is explicitly `in` and a gate red is a finding to dispose, not to route around. It is recorded here so the widening is on the record and can be challenged.\n\n(6) PIECE (b) DID NOT REACH EVERY AT-SEA NODE, AND THE REASON IS STRUCTURAL, NOT AN OVERSIGHT. Measured 2026-07-30 across all cairn/*/state.json: exactly two nodes are at sea. cairn/base migrated (history index 18 carries migrated_from). cairn/build_inspector stands at LEARNME on code-seam@v1 and COULD NOT BE MIGRATED by this voyage. emit_migrated is the one door for a journaled migration and it requires a CROSSING; migrate_to_v2 lands a LEARNME boat on PROVEME, so the only legal target is PROVED — and build_inspector's close is deliberately HELD at that point by ticket a-voyage-names-its-ticket ('This stone HOLDS ITS CLOSE here -- no verdict artifact, no PROVED crossing'). Crossing it forward would close another ticket's voyage from inside this one, and hand-editing the string would be the in-place edit of a record of truth that Law 7 forbids. So a node still stands in LEARNME — the very state this ticket dissolves — and that is the honest state of the migration. It discharges by event, not by poll: when a-voyage-names-its-ticket closes, its PROVED crossing carries the migration if it goes through emit_migrated (which passes an already-v2 string straight through, so it is safe as the universal door). Whether emit_migrated should therefore BE the universal door is a filed question, not built here.",
  "chart_notes": {
    "orient": "The primitive is already built and its charter is emphatic about what it is NOT — read cairn/base/probe.py's header before anything else; the anti-reification (no trigger enum, no carrier enum) is a fix already applied once to this exact area after CC turned an open list of examples into a closed typed set. Do not re-commit that. Also read intentions-other/I-heartbeat-probes-and-bus.md whole, not just the superseded paragraph.",
    "constrain": "OUT unless Akien widens it: building the heartbeat/shim liveness this depends on; changing what a probe IS (the primitive is fixed); the tenure loop; any per-component learning contract (the whole point is that there is one). The rest-state kick-back (piece d) is the piece most likely to want to grow into the escalation ladder — hold it to depositing and poking.",
    "survey": "COMPOSE, do not rebuild: verdict.py's validator and parts renderer, deposit_learning's provenance path, probe.py's trigger/carrier machinery, the exit gate's existing fail-is-a-kick-back routing, transitions.py's back-edge handling ('back-edge out of LEARNME: ungated, and owes nothing' — test_transitions.py:747). Measure what is ABSENT before building it; the last stone found its own answer already sitting in a discarded return value.",
    "hypothesize": "The sharpest kill condition is falsifier clause (2): if an armed probe cannot be shown to FIRE, this node ships a mechanism that looks like learning and is not — which is worse than the prose it replaces. Cast a piece that measures a real fire end-to-end, and be willing to kill the rest of the voyage on it."
  },
  "watchme": {
    "object": "does-optional-mean-never-carried",
    "trigger": "Every ground_loop pulse reaching cairn/base's shim. The probe reads class-space only — it globs CairnCommons/tickets/*.json and counts, over tickets claiming a version whose vocabulary CAN carry a watch (v1 tickets are excluded by their own registered vocabulary, not by a list), how many carry a WATCHME and how many recorded \"none, because X\". No device, no bus, no network, so it is cheap enough to sit on a pulse. THE OWNING TICKET IS EXCLUDED FROM ITS OWN CORPUS and the count must reach 12 eligible before either predicate answers — both added by the live fire, 2026-07-30 (see `enough`).",
    "enough": "CLEARED once the corpus is BIG ENOUGH TO JUDGE (>= 12 eligible tickets) AND some OTHER node has carried a watch. At that moment the question is answered — optional does not mean never — and a standing watch on a settled question is the re-derivation Law 1 refuses. If the answer later goes bad, that is a NEW watch a node carries deliberately, not this one silently resuming. THE SAMPLE-SIZE FLOOR AND THE SELF-EXCLUSION WERE ADDED BY THIS NODE'S OWN LIVE FIRE, 2026-07-30, and this text said neither before. As first written the clear read 'the moment ANY node carries a watch' with no floor, while the trigger next door required 12 — so the watch could CLEAR at n=1 and could not FIRE until n=12, guaranteed to retire before it could ever bite; and the corpus counted THIS ticket, so the n=1 was the author of the mechanism counting their own participation as evidence that authors participate. A watch that clears before it can fire is the v1 LEARNME failure wearing this node's clothes — the exact shape this probe exists to detect.",
    "carrier": "The default: a verdict artifact against THIS ticket's falsifier, written through cairn.chart.verdict.write_verdict against falsifier@watchme-emits-a-probe — the form built in piece (d) precisely so a probe firing months from now has a criteria source that does not depend on a berthed chart chain surviving.",
    "nexus": "hypothesize — the tree that remembers what killed which. This ticket's whole claim is a prediction about how authors behave once a summons is optional; the hypothesize tree is where a prediction that did not hold belongs.",
    "consumer": "AKIEN, through the owner's gate. The probe deposits and pokes; it has NO authority to back-edge this node (Law 6). What it can do is put a measured 'zero of N eligible nodes carried a watch' in front of the owner, whose act the back-edge then is.",
    "probe": "cairn/base/probes/does_optional_mean_never_carried.py"
  },
  "forwarding": {
    "cairn/base/callback.py": {
      "to": "cairn/base/probe.py",
      "why": "piece (f) of this ticket's own decompose, run FIRST by the chart's own triage order so nothing else would be built on the retired word. The chart was berthed BEFORE the rename, so its refs and holdings name the pre-rename addresses; running the rename first is what falsified them. The berths are records of truth and are not edited to look consistent. Charted at orient ref and survey holdings 0 and 1."
    },
    "cairn/base/proofs/test_callback_crossing.py": {
      "to": "cairn/base/proofs/test_probe_crossing.py",
      "why": "piece (f) of this ticket's own decompose, run FIRST by the chart's own triage order so nothing else would be built on the retired word. The chart was berthed BEFORE the rename, so its refs and holdings name the pre-rename addresses; running the rename first is what falsified them. The berths are records of truth and are not edited to look consistent. Charted at survey holding 2."
    },
    "intentions-other/I-heartbeat-callbacks-and-bus.md": {
      "to": "intentions-other/I-heartbeat-probes-and-bus.md",
      "why": "piece (f) of this ticket's own decompose, run FIRST by the chart's own triage order so nothing else would be built on the retired word. The chart was berthed BEFORE the rename, so its refs and holdings name the pre-rename addresses; running the rename first is what falsified them. The berths are records of truth and are not edited to look consistent. Charted at survey holding 18; a model source, so the rename poked the recompile gate in the same act."
    }
  },
  "distinctions_recorded": [
    "OPTIONAL TO CARRY, MANDATORY TO SATISFY ONCE CARRIED — Akien's rule, and the exact inversion of the failure it replaces. v1's LEARNME sat in the backbone, mandatory for every node and gated by NOTHING, so it was crossed by every voyage and satisfied by none. WATCHME is skippable and, once carried, physics: crossing forward out of it requires an EMITTED probe (on disk, armed, spec complete). 'none, because X' is the only other legal answer; silence is refused in every form measured (absent, '', 'none', 'n/a', and the truncated 'none, because').",
    "A PROBE CARRIES NO AUTHORITY, and it is structural rather than avoided. cairn/base/probe.py contains neither the string 'transitions' nor 'emit(' — the module cannot reach the one door that moves state even by accident. Probe is frozen; its only destination field is `to`, a BUS ADDRESS. A probe deposits and pokes; the back-edge that re-opens a node whose intention did not work is the OWNER's act at the register (Law 6).",
    "A PROBE BERTHS WITH WHAT IT WATCHES, NOT WITH ITS INTENT — a deliberate Law 5 exception, recorded so it is not 'fixed' later. Intent, voyage and proofs share an address; a probe is none of the three. It is an INSTRUMENT, and an instrument berths at what it measures. This EXTENDS the three-roots test rather than breaking it: for a compiled probe, 'does the intention have ONE code address?' becomes 'what does it WATCH, and where does THAT live?'. Three destinations placed both real probes with no fourth case and no exception clause — including system_rackmount, which looks like a counter-example and is not: it checks in no probe at all, only a runtime factory.",
    "THE FIRST LIVE FIRE CAUGHT ITS BUILDER TWICE WHERE FOUR GREEN PROOFS HAD NOT, and both bugs were this ticket's own disease. (1) The corpus counted THIS ticket — the node that invented the mechanism, counting its own participation as evidence that authors participate. Home-field advantage as a measurement, at n=1, by the maximally motivated author. (2) `enough` had no sample-size floor while `_trigger` required 12, so the watch could CLEAR at n=1 but could not FIRE until n=12: guaranteed to retire before it could ever bite — a summons crossed by everybody and satisfied by nobody, which is precisely what the probe exists to detect. Either bug alone was fatal. The pattern holds: first live fires keep catching their builders.",
    "A RENAME IS NEVER BEHAVIOUR-FREE IN A SYSTEM THAT CHECKS REFS IN THE WORLD — hypothesis H1, killed by its own text. The rename FALSIFIED THIS TICKET'S OWN CHART REFS BY SUCCEEDING: the WATCHME crossing was refused with five findings naming cairn/base/callback.py and two renamed proofs, the three addresses this ticket's own decompose piece had renamed hours earlier. Disposed by a forwarding order on the record of truth plus a new both-ends filter (build_inspector.forwarding_order_resolves, taking it 17 filters to 18) — never by editing the berths to match. And the enumerated sweep list was itself incomplete: bus-completion.json's LIVE spec named a renamed class, a renamed method and two renamed files that the list never mentioned. An enumerated sweep list is a HYPOTHESIS about completeness, not a statement of it.",
    "A PRECEDENT TRANSFERS ON THE SHAPE OF THE CHANGE, NOT THE SHAPE OF THE ARTIFACT — hypothesis H4, killed by its own falsifier. The skill-class stone had proved 'a version lands in the class file ALONE, zero lines of transitions.py change', and that was read as covering any new version. It did not: WATCHME ROAMS AND NAMES ITS OBJECT, and v1's fixed-position path grammar could not express a free summons, so commit fb03f42 had to teach the parser FREE SUMMONSES. Adding a version is free; adding a GRAMMAR is not.",
    "THE MIGRATION REWRITES NOBODY'S PAST. The workflow string lives in every journaled history entry and history is append-only, so a node migrates when its NEXT journaled crossing names v2 — its past entries go on honestly naming v1, and the migrating entry carries `migrated_from` with the v1 string intact: the old shape PRESERVED in the act of leaving it. Measured in anger on cairn/base at history idx 18, pre-migration prefix hash 9e068d1ca50df84a unchanged. The two nodes standing AT LEARNME were the design driver, not an edge case, and they need no no-op self-loop: PROVED is already a legal target from LEARNME on v1.",
    "USE VS MENTION IS THE LINE A VOCABULARY SWEEP MUST NOT CROSS. A record of truth says what was true when it was written and is never edited to look consistent (Law 7). So the sweep forwarded LIVE POINTERS and left DATED CAPTURES byte-identical — in bus-completion.json, ten live spec lines forwarded and Akien's dated 2026-07-25 verbatim ('the callback is the tool. the poke is the act') untouched, with the forwarding itself recorded in provenance. The cheap alternative — relabelling tickets as 'records of truth' so the grep passes — was available and refused."
  ],
  "filed_edges": [
    "NOTHING PULSES cairn/base's SHIM TODAY. The wall-clock backing that would call `beat` on a cadence is a filed edge (cairn/ground_loop/loop.py header), so `record['overdue']` is never produced in anger and the armed-and-never-fired loudness rides the READ-SIDE door (BaseShim.overdue()) alone. Written into the probe module as tracked debt.",
    "_HORIZON = 1000 IS UNTUNED. The unit is PULSES because the shim counts pulses and a clock is bounded OUT, but with no real beat rate the number is honest as a placeholder and dishonest as a measurement. It MUST be re-tuned when the beat becomes a real number.",
    "stage_needs CARRIES TWO VOCABULARIES AND NEEDS AKIEN'S RULING. cairn/base/proofs/test_needs.py is RED — 'a-node-holds-one-claim.json: live needs block does not conform — stage orient is not in this node's workflow vocabulary' — and it is PRE-EXISTING, proven by stashing cairn/ clean and re-running at HEAD. Measured cause: across 13 tickets, 9 use CHART stages (orient, constrain, survey, decompose, triage, hypothesize, validate) and 4 use WORKFLOW stages (BUILDME); test_needs enforces only the latter. Left unfixed deliberately — it is another owner's space and the question is what the key MEANS, which is a ruling, not a patch.",
    "build_inspector STILL STANDS AT LEARNME under ticket a-voyage-names-its-ticket, whose own cursor note reads 'This stone HOLDS ITS CLOSE here'. Migrating it from inside this voyage would have closed another ticket's deliberately-held voyage (Law 6), so validate criterion 5's named act was SUBSTITUTED — mechanism proven in anger on cairn/base, plus a dry-run at the real door on build_inspector's live string — with the substitution stated in the verdict's evidence rather than hidden. It migrates on its next crossing, by its own owner.",
    "DESTINATIONS (ii) AND (iii) HAVE NO WORKED EXAMPLE. This build populates destination (i) only — a probe checked into this repo beside the code it watches. Instance-space and downstream-project probes remain authored doctrine.",
    "WHETHER ANY SHIM SERVES THE harbor_master ADDRESS IS UNMEASURED. A poke to an address nothing serves is recorded `unwired` by the shim rather than lost — loud enough to find, but not the same as delivered. Wiring it is not this ticket's rung."
  ]
}
