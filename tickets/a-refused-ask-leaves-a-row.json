{
  "id": "a-refused-ask-leaves-a-row",
  "schema_version": "v0",
  "date": "2026-08-18",
  "owner": "CC",
  "node_class": "code-seam",
  "state": "code-seam@v2: THINKME -> [TICKETME] -> BUILDME -> PROVEME -> WATCHME(a_refused_ask_leaves_a_row) -> PROVED",
  "intention": "An ask that RAISES inside resolver() lands a row in inference_domain's ask record, naming the refusal, so the refusal rate of this system's only inference door is readable by the same read that counts answers. Today the store is an ANSWER record wearing an ask record's name.",
  "why": "MEASURED 2026-08-18 at cairn/devices/inference_domain/domain.py, resolve(): the inference_calls row is written only AFTER `result = resolver(request)` returns. A raise means no write. The corpus stands at 2520 rows — 1386 miss, 1134 hit — and zero rows for any ask that failed.\n\nFOUND BY TRYING TO BUILD A WATCH ON IT. Ticket a-non-final-answer-is-refused-by-name specified its WATCHME as 'the probe reads the domain's own ask log' to count refusals; the log cannot count them, so the probe had to be written around the absence — it watches the fault RETURNING (a cached contentless zero-cost row) instead of the guard firing. That is an honest watch and a weaker one, and the reason it is weaker is this defect.\n\nTHE LIVED SYMPTOM, and it is not hypothetical: the voyage before that one explained a failed drive with a cause it had not measured, and the false sentence went into an append-only history, a ticket distinction and a slate. Four real refusals were produced against a cold hex in one sitting the next day; not one of them is countable today. When a failure leaves no row, the only record of it is whatever a human wrote down about it — and a human wrote it down wrong. This is Law 3 at the door: the refusal rate is not measured, so it is not known, and the space where a number should be gets filled with narration.",
  "how": "First cut. Record the row on the way OUT of resolve() whether the resolver returned or raised, so the record is written by the frame rather than by the success path — the same shape harbor_master's clearance door uses for exactly this reason (a writer at the frame cannot miss a refusal nobody has thought of yet). The refused row carries the exception type and its message, cost 0, and a marker distinguishing it from an answer.\n\nTWO THINGS THE BUILD MUST SETTLE FIRST, both named in the cast rather than assumed: WHICH raises earn a row (BadRequest fires before anything is spent and may not deserve one), and whether the row shape passes db_domain's existing write door unchanged. Both are read off the code at build time.\n\nAND THE THIRD FALSIFIER CLAUSE IS THE DESIGN CONSTRAINT, not an afterthought: a refusal must never be served back as a cache HIT. Recording a failure inside a store whose purpose is replaying stored answers is where this fix could end up worse than the defect.",
  "traces_to": "Law 3 (nothing is known until measured — the refusal rate of the only inference door is unmeasured, so it is unknown, and narration fills the gap), Law 7 (errors are permanent in records of truth; an error that leaves no record at all is the limit case), Law 10 (nothing in the system is immeasurable — 'we have not built the measurement yet' is the true and ordinary state this ticket ends).",
  "gates": "code-seam invariant, transcribed: PROVED, and feeds-back-to-origin-on-failure (Laws 3 + 8). PROVE GATE: the tester runs the proofs beside cairn/devices/inference_domain/ under isolation=netns, with at least one tooth that is RED on unmodified HEAD for the right reason — a resolver stubbed to raise, asserted to leave a row, which today leaves none. NON-HOLLOW CLAUSE: a tooth that merely asserts 'resolve raises' is green on HEAD and proves nothing; the tooth must read the STORE back after the raise. LIVE GATE: one real cold-hex ask that raises HostNonFinal, then the row read back out of inference_calls — the fixture world cannot show whether the real write door admits the shape. FEEDBACK EDGE: on failure the finding returns to this ticket and to inference_domain's charter, which is where clause (9) already names the refusal path.",
  "falsifier": "FALSIFIER: DONE when an ask that raises inside resolver() lands a row in the domain's ask record naming the refusal, so the count of refusals is readable from the store by the same read that counts answers. RED on any of: (1) a raise inside resolver() leaves no row — the defect standing; (2) the row for a refused ask is indistinguishable from an answered one, so a reader counting answers silently counts refusals among them (Law 7 — a record of truth may not carry an error quietly); (3) the refusal row is served back as a CACHE HIT to a later asker of the same canonical question — a refusal is not an answer and must never be replayed as one, and getting this wrong converts a loud error into a permanent wrong answer, which is strictly worse than the defect being fixed; (4) writing the row changes what a SUCCESSFUL ask stores or costs — the fix breaking the path it was meant to complete; (5) the refusal row is written anywhere other than through db_domain's existing write door (Law 6 — the holder gates writes to its own state). HORIZON: the next cold-hex drive of the aider_shim apprentice, which is the event that reliably produces refusals, and no later than the next voyage that dials inference at all.",
  "horizon": "the next cold-hex drive of the aider_shim apprentice — the event that reliably produces refusals — and no later than the next voyage that dials inference at all",
  "parent": null,
  "children": "none, because the change is one write path in one function (domain.resolve) plus its proofs and probe. The two things it could split into — recording the refusal, and deciding which refusals qualify — are not separable: the second is a predicate INSIDE the first, and building them apart would mean landing a write path with no rule about what it writes.",
  "dependencies": [],
  "watchme": {
    "object": "a_refused_ask_leaves_a_row",
    "trigger": "each read of inference_calls — the probe counts post-era rows by outcome, so it rides the traffic that already exists rather than a clock, and it needs no live host of its own",
    "enough": "enough when at least one post-era row records a REFUSED ask AND the post-era answered rows are all still carrying real content and non-zero cost — the first half proves the write path exists at all and is not a shape nobody ever reaches, the second proves it did not corrupt the case it was grafted beside. A refusal-recording path that has never recorded a refusal is exactly as unproven as the guard this ticket descends from",
    "carrier": "a verdict artifact against this ticket's falsifier: the count of post-era refused rows by exception type, the count of post-era answered rows, and whether any refused row was ever served as a cache hit",
    "nexus": "hypothesize",
    "consumer": "whoever next debugs a failed drive — and the ticket that just closed, whose own watch had to be written around this absence",
    "probe": "cairn/devices/inference_domain/probes/a_refused_ask_leaves_a_row.py"
  },
  "sorted_berth": "/home/akien/.cairn/devices/skill_block/0/berths/sorted/sorted-20260818T010446-a219999336a3.json",
  "intent_berth": "none, because this entered at the TICKET entry point, not at /intent. CLAUDE.md's three entry points, and /intent's own text: 'Do not route a bug through here: if editing lines of code fixes it, it is a ticket.' This is a measured defect in one function, fixed by moving one write; the measurement is reproducible by reading cairn/devices/inference_domain/domain.py resolve() and counting rows in inference_calls against the raises reachable from it.",
  "from_idea": "none, because it was found at build time inside the voyage for a-non-final-answer-is-refused-by-name (that ticket's WATCHME rank), not captured as an idea beforehand. The finding is recorded verbatim in that ticket's distinctions and in the WATCHME crossing note at cairn/devices/inference_domain/history.json.",
  "owning_intention": "cairn/devices/inference_domain/intention+why.json",
  "provenance": "Born from a spec that could not be built. The watchme spec on a-non-final-answer-is-refused-by-name was written from a plausible model of the store rather than from the store, and passed /sorted's door and the emission gate's spec check on its way through — neither can tell a readable log from an unreadable one. What caught it was writing the probe LAST, after the live fire, which triage had ordered for a completely different reason: that the live fire is what tells a probe what 'enough' looks like. The ordering paid off in a currency it was not chosen for."
}