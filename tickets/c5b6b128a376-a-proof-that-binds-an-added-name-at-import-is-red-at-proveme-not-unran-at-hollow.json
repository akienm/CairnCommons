{
  "id": "c5b6b128a376",
  "title": "a-proof-that-binds-an-added-name-at-import-is-red-at-proveme-not-unran-at-hollow",
  "node_class": "code-seam",
  "task_or_ticket": "ticket",
  "workflow_and_state": "code-seam@v2: THINKME -> TICKETME -> BUILDME -> PROVEME -> WATCHME(a_proof_that_binds_an_added_name_at_import_never_reaches_hollow) -> [PROVED]",
  "date": "2026-09-14",
  "created": "2026-09-14",
  "author": "CC",
  "from_idea": "2026-09-14-yes-ticket-as-to-the-loop-residue",
  "intent_berth": "/home/akien/.cairn/devices/skill_block/0/berths/intent/intent-20260914T145018-28a0fb9c542b.json",
  "sorted_berth": "/home/akien/.cairn/devices/skill_block/0/berths/sorted/sorted-20260914T145210-702804286e21.json",
  "owner": "CC builds it; codemother owns the care of the code (memory codemonkey-owns-builder-processes — inspectors and gates are its things); cairn/tools/proof_coverage is a tool with users (the PROVEME crossing, the tester).",
  "owning_intention": "cairn/tools/proof_coverage/intention+why.json",
  "ruling": "none — a decision is a question bound to its ticket (ticket 9adc6fddf185). Akien's words of 2026-09-14 are the spec and ride from_idea verbatim; the cast opened no question because none of its choices is a spec choice.",
  "decision_verbatim": [
    "yes ticket. as to the loop residue point, if a question is about a ticket, it should have a bidirectional link. and we should explicitly say \"and the answer spawned x, y and z new questions\" in some jasonic way that's not prose"
  ],
  "pre_approval": "none claimed — stands at his review queue; 'yes ticket' orders the cast, not the build.",
  "questions": "none, because his 'yes ticket' (CairnCommons/ideas/2026-09-14-yes-ticket-as-to-the-loop-residue.json) answers the only spec question (does the pattern earn a ticket); the instrument's seat and shape are settled by measurement of the three instances (commits a38204e, e1dcc6e; proof_coverage.py's in-line note of 2026-09-10) and recorded in assumption_check.",
  "intention": "A proof that binds a name its build added at import time is red at PROVEME, by name and line, before the hollow reading spends its budget to say UNRAN.",
  "why": "Law 8 (a proof a hollow build could not pass — a proof that crashes before its first tooth when its subject is removed proves nothing about that subject, and hollow already refuses to count it either way; this moves the refusal to where the proof is written, which is where it is cheap); Law 4 (three measured instances — 2026-09-10 crossings.py, 2026-09-13 a38204e artifact door, 2026-09-14 e1dcc6e test_exemption_set.py — each fixed by hand after a 2–5 minute hollow run and a paragraph in hollow's own output; the rule is prose that has now chafed three times, so it becomes a sieve); Law 1 (the third session re-derived the same fix from the same message; a sieve settles it).",
  "how": "One sieve in cairn/tools/proof_coverage (the PROVEME instrument that already reads a ticket's PROVES-carrying proofs): `proof_binds_its_subject_at_call_time`. For each proof named in the ticket's coverage, walk its MODULE-LEVEL imports with ast, transitively through cairn-rooted modules (bounded depth, module-level statements only), and resolve each imported name to a file and a definition. Compare against what the build ADDED: files absent at the pre-build commit (the BUILDME crossing's commit, via git diff --diff-filter=A) and top-level names absent in the pre-build version of a file that existed. Any module-level binding of an added file or added name is a lack, naming the proof, the line, the import, and the fix in the words hollow already prints ('resolve the names this build ADDED at call time'). Red at PROVEME; the hollow reading is then reached only by proofs that can survive their subject being taken away. Plus the guidance line where a proof is written: /sail's step 3 ('Prove — twice') gains one sentence naming the rule and the sieve; the tester's charter names the sieve as hollow's front door.",
  "falsifier": "DONE when: (1) a fixture proof doing `from <added_module> import x` at module level, beside a fixture ticket whose pre-build commit lacks that module, is red under proof_binds_its_subject_at_call_time naming the proof, line and name; the same import moved inside main() is green; (2) the transitive case (proof imports A at module level; A imports an added name from B at module level) is red naming the chain; (3) the three historical instances rebuilt as fixtures each red at the line the hand fix touched; (4) a fixture ticket at PROVEME with the red proof does not reach the hollow reading (the sieve reds first) — and with the sieve reverted the same fixture reads UNRAN under hollow (the self-reference tooth); (5) /sail's SKILL.md step 3 carries the sentence and skills/sail's proof reads it. Horizon: one build. WRONG INTENT if the walk reads green over a proof hollow then reads UNRAN on a live ticket — then the instrument moves into hollow as a pre-flight import of each proof against the reverted world, and the static walk retires.",
  "traces_to": "Law 8 (a proof a hollow build could not pass — a proof that crashes before its first tooth when its subject is removed proves nothing about that subject, and hollow already refuses to count it either way; this moves the refusal to where the proof is written, which is where it is cheap); Law 4 (three measured instances — 2026-09-10 crossings.py, 2026-09-13 a38204e artifact door, 2026-09-14 e1dcc6e test_exemption_set.py — each fixed by hand after a 2–5 minute hollow run and a paragraph in hollow's own output; the rule is prose that has now chafed three times, so it becomes a sieve); Law 1 (the third session re-derived the same fix from the same message; a sieve settles it).",
  "watchme": {
    "object": "a_proof_that_binds_an_added_name_at_import_never_reaches_hollow",
    "trigger": "every hollow reading (bin/cairn test --hollow) — the tester pokes the probe after the run with the count of UNRAN files; and every proof_coverage run at PROVEME with the count of proof_binds_its_subject_at_call_time lacks. No poller.",
    "enough": "20 hollow readings after the build with ZERO UNRAN files attributed to a module-level import (hollow's `broke` set) — or ONE UNRAN that the sieve read green on the same ticket, which is the finding and the WRONG-INTENT clause (the static walk missed a binding; the instrument moves into hollow).",
    "carrier": "a verdict artifact against THIS ticket's falsifier: hollow readings counted, UNRAN files by cause, sieve lacks counted, and every UNRAN the sieve did not predict with its import chain.",
    "nexus": "the proof-quality tree — the same readings proof_coverage and hollow already feed; this probe is the first that measures the two instruments AGAINST each other.",
    "consumer": "Akien, and codemother (owner of the care of the code) when it reviews proofs.",
    "probe": "cairn/tools/proof_coverage/probes/sieve_predicts_unran.py"
  },
  "gates_bound": "code-seam gate-set: BUILDME rides the intent berth and the sorted berth (buildme_rides_the_intent, buildme_rides_the_sorted) and the_ticket_has_every_answer_it_needs (none opened). PROVEME: proofs sealed green with current fingerprint; hollow reading over every writes_to file — and this ticket's own proofs must bind proof_coverage at call time, or the sieve it builds reds the ticket that built it (the self-reference tooth, same shape as 9adc6fddf185's); proof_coverage naming a tooth per falsifier clause. PROVED: verdict artifact against the falsifier. Feeds back to origin on failure.",
  "children": "none, because the seam is one sieve in one existing tool plus one sentence and one charter edge, all in one build; the fallback (a pre-flight inside hollow) is the falsifier's WRONG-INTENT route, not a child.",
  "depends_on": "d2ecdb867bc9 (crossings record — the pre-build commit the sieve diffs against); the tester's hollow reading (cairn/devices/tester/hollow.py) as the backstop and the WATCHME's other instrument.",
  "follow_ons": [],
  "owed_physics": "hollow.py prints the rule ('resolve the names this build ADDED at call time') as a reason string after a 2–5 minute run; nothing reds the proof where it is written. The sieve at PROVEME is this ticket's build.",
  "cast_checks": {
    "assumption_check": "Load-bearing and checked: (1) 'what the build added' is derivable — measured: proof_coverage already reads the ticket's BUILDME crossing (cairn/tools/base/crossings.py, since d2ecdb867bc9) and hollow reverts against the same pre-build commit, so `git diff --diff-filter=A <pre-build>..HEAD` and a per-file top-level-name diff are the same inputs hollow uses; (2) module-level imports are the class of defect measured three times (crossings.py 2026-09-10, a38204e 2026-09-13, e1dcc6e 2026-09-14) — each was a `from X import name` at module level, none a fixture or constant; the sieve names imports, hollow stays the backstop for the rest, said in the charter; (3) the static walk resolves cairn-rooted modules only (cairn.*, skills.*, bin/cmd) — a stdlib or third-party import cannot be build-added; (4) the transitive depth is bounded by the cairn root, not a number: walk module-level imports of cairn modules until none are new, which import_sieve already does. Not an assumption: the guidance sentence alone is measured as insufficient — hollow has printed it since 2026-09-10 and two instances followed.",
    "missing_check": "Wrapped: the sieve's inputs (ticket's PROVES proofs, pre-build commit, added files, added top-level names), its walk (ast, module-level Import/ImportFrom, transitive through cairn-rooted modules), its lack shape (proof path, line, import chain, the fix), its seat (proof_coverage at PROVEME, so it runs before hollow), the /sail sentence, the tester charter line, the three historical instances as fixtures. Not wrapped and said so: `__init__.py` re-exports (`from .x import y` in a package __init__ makes `import cairn.pkg` bind y) — the walk follows __init__ modules like any other, so it is covered by construction; `importlib`/dynamic imports are invisible to the walk and stay hollow's — recorded as the charter's first edge, and the falsifier's WRONG-INTENT clause routes the instrument into hollow if that edge bites.",
    "falsifier_check": "DONE when: (1) a fixture proof doing `from <added_module> import x` at module level, beside a fixture ticket whose pre-build commit lacks that module, is red under proof_binds_its_subject_at_call_time naming the proof, line and name; the same import moved inside main() is green; (2) the transitive case (proof imports A at module level; A imports an added name from B at module level) is red naming the chain; (3) the three historical instances rebuilt as fixtures each red at the line the hand fix touched; (4) a fixture ticket at PROVEME with the red proof does not reach the hollow reading (the sieve reds first) — and with the sieve reverted the same fixture reads UNRAN under hollow (the self-reference tooth); (5) /sail's SKILL.md step 3 carries the sentence and skills/sail's proof reads it. Horizon: one build. WRONG INTENT if the walk reads green over a proof hollow then reads UNRAN on a live ticket — then the instrument moves into hollow as a pre-flight import of each proof against the reverted world, and the static walk retires.",
    "collision_check": "Whys compared, none exclusive-and-terminal. (1) import_sieve (sole path to inference/db): its why is WHERE an import points; this sieve's why is WHEN it binds. Same walk, different predicate — the walk is shared code, the predicates are not merged (a distinction recorded, not a collision). (2) hollow.py's `unreadable:` reason: its why is that a proof that never ran must not be counted green OR hollow; this ticket does not change that reading, it stops the case from reaching it — hollow's text stays as the backstop's voice. (3) memory broad-tooth-manufactures-coverage: unrelated predicate, same family (coverage instruments crediting the wrong thing); named so nobody merges them.",
    "builder_check": "What a Haiku-level builder needs, carried here: the seat is cairn/tools/proof_coverage/proof_coverage.py, which already resolves a ticket's proofs (declared()) and the BUILDME crossing (cairn.tools.base.crossings.buildme_crossing — bound at call time, per the in-line note there); the pre-build commit is what hollow.py uses (scratch_worktree against that commit); added files: `git diff --name-only --diff-filter=A <pre>..HEAD`; added names: ast-parse both versions of each modified .py in writes_to and diff the sets of top-level def/class/assign names; the walk: for each proof module, ast.parse, take Import/ImportFrom nodes at module level (body of Module only, not inside def/if/try), resolve dotted names to files under ~/dev/src/cairn via importlib.util.find_spec with the repo on sys.path, recurse into resolved cairn-rooted files, stop at already-seen files; a lack when a resolved (file, name) is in the added set — carry the chain [proof:line -> A:line -> B]; the lack rides the same list proof_coverage returns today so PROVEME reds through the existing path; the three fixtures live in cairn/tools/proof_coverage/proofs/fixtures/ as scratch repos built by the proof (two commits each) — self-describing names (memory self-describing-fixture-names); the /sail sentence goes after 'A proof over live data asserts invariants, never snapshots.'; the tester charter (cairn/devices/tester/intention+why.json) gains one filed edge naming the sieve as hollow's front door, written through the charter verb. Left to the builder: the lack's exact wording, whether the walk caches resolved files across proofs of one ticket."
  },
  "challenge_at_birth": {
    "better_approach": "Looked. Smallest: the guidance sentence alone — but that is the paragraph hollow already prints, and it did not stop the second or third instance (memory: a rule that matters is physics). Alternative: make hollow itself do a cheap pre-flight (import each proof module with the subject reverted, before running teeth) and red with the traceback — closer to behaviour than a static walk, and it is the fallback the falsifier names. The static sieve is chosen first because it runs at PROVEME in under a second and names the line; hollow's pre-flight would still cost a worktree per file.",
    "prior_art": "In-house: hollow.py's `unreadable:` reason text (2026-09-10) already states the rule and the fix; proof_coverage.py carries the measured note at its own crossings import ('BOUND AT CALL TIME, NEVER AT IMPORT — a measured requirement'); a38204e and e1dcc6e are the hand fixes; `import_sieve` (sole path to inference/db) is the existing pattern of a static import walk over cairn modules and is the shape to reuse. Field: pytest's collection errors vs. test failures — the same distinction (a module that fails to import is an ERROR, not a FAIL) and the same complaint that an ERROR tells you nothing about the code under test.",
    "hidden_assumption": "(a) That 'what the build added' is derivable from git at the BUILDME crossing commit — it is, for tickets with a crossing record (since d2ecdb867bc9); a ticket without one gets the crossing_record_absent lack proof_coverage already emits. (b) That module-level imports are the whole defect class — a proof can also bind at import through a fixture or a module-level constant computed from the subject; the sieve names imports, and hollow stays the backstop for the rest. (c) That the three historical instances can be rebuilt as fixtures cheaply (a scratch repo with two commits).",
    "real_collision": "None. Distinction recorded: `import_sieve` reds an import for WHERE it points (a forbidden host); this sieve reds an import for WHEN it binds (module level, to a name that did not exist before the build). Same walk, different predicate; the walk is shared, the predicates are not merged.",
    "back_up": "proceed — 'yes ticket' (Akien 2026-09-14) on the slate bullet naming the second instance; the third was 2026-09-10, so it is the third, and the pattern is measured."
  },
  "measured_at_cast": {
    "date": "2026-09-14",
    "by": "CC, session a187d6ab",
    "census": "instances measured: 3 — cairn/tools/proof_coverage/proof_coverage.py (in-line note dated 2026-09-10, crossings.py), commit a38204e 2026-09-13 (test_artifact_door.py, test_artifact_gate.py), commit e1dcc6e 2026-09-14 (test_exemption_set.py); hollow readings that reported UNRAN in this session: 1 (19 files, the exemption proof); guidance text in /sail SKILL.md naming the rule: 0 lines.",
    "filed_through": "the artifact door, verb cast."
  },
  "schema_version": "ticket-v1"
}
