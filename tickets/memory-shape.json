{
  "id": "memory-shape",
  "schema_version": "v0",
  "date": "2026-07-26",
  "owner": "akiendelllinux_cc_0 (CC) gates writes. The DESIGN is Akien's and is being RESTORED, not authored.",
  "intention": "Restore Akien's memory store design as the shape Cairn builds on, and settle the questions it still leaves open — including calving. TWO ERAS, RULED APART BY AKIEN 2026-07-26 (see eras below): the shape to RESTORE is the CURRENT one — a table per tree, tree links as `tree.leaf` addressing, instant node access with no searching, two sets of trees able to index one node set, a tree that grows past its bound CALVES, and the SHEAR renumbers leaf addresses along the split. TheIgors' one-node-list shape is the OLD era: history, read for ideas only, NOT the restore target. SHARPENED 2026-08-05 — see settled_2026_08_05: every sentence above says `node` where it means `leaf`, because CC collapsed the two layers. A NODE is the thing being remembered and never belongs to a tree; a LEAF is the thing indexing it, and `database.tree.leaf` is the leaf's ADDRESS, not anyone's identity. The shear renumbers LEAF addresses and never touches a node.",
  "eras": {
    "the_ruling": "AKIEN, 2026-07-26, correcting this node's first draft: 'so the igor shape is the old shape. and the tree.leaf reference is new as well.' Asked to disambiguate whether `tree.leaf` was his or another misattribution, he ruled: MINE, AND IT SUPERSEDES. So there are two of his own eras, and the first draft of this ticket fused them into one 'his design' — which is the same collapse it was written to fix, one level up.",
    "old__theigors": "ONE node list, the ~2.5M-node word graph built by counting, the 1000-node calving trigger, the serial-calving counter. Superseded. Its status is QUARRY: read for ideas (per the scope ruling — ideas, not code), never the restore target.",
    "current__tree_leaf": "A table per tree; links are `tree.leaf` (the address carries its table, so reaching a node is addressing, not searching); the bound and the SHEAR. This is what 'we have to go back to mine' points at.",
    "cc_read_unruled__the_replacement_reverted_him": "MY READ, FLAGGED, NOT A RULING — and it sharpens the whole node. MAP.md:528's 'nodes and edges each in ONE logical table' plus 'trees never get independent tables' is NOT a novel replacement of the current design: it is the OLD era's shape. So the 2026-07-14 conversation did not just substitute meanings under his vocabulary, it reverted him to a design he had already moved past, while keeping the newer words on top. That is why nothing scanned as missing AND why the result was unexplainable: it is two eras welded together. If this read is right, MAP.md:527-537 conflicts with the current design at its load-bearing joint (per-tree tables), not at a detail."
  },
  "settled_2026_08_05": {
    "the_ruling": "AKIEN, 2026-08-05, correcting CC's reading of the whole shape: 'you are conflating identity and address. and no, a node NEVER belongs to a specific tree. mine aren't organized that way, this system doesn't need that. a leaf has an identity AND an address. database.table.leaf is an address, not an identity. a leaf points to a node, and has edges that point to other leaves, who also point back. And to be clear: I'm making these distinctions: A NODE IS THE THING BEING REMEMBERED, A LEAF IS THE THING INDEXING IT, WITH WEIGHTS AND LINKS TO OTHER LEAVES.'",
    "node_vs_leaf": "TWO LAYERS, and every prior artifact in this ticket collapsed them into one. NODE = the thing being remembered: content, provenance, standing. It has an IDENTITY, it lives in the one node list per database, and it NEVER belongs to a tree. LEAF = the thing indexing it: it lives in a tree's own table at ADDRESS `database.tree.leaf`, points at a node, and carries weights and edges to other leaves. The address is a LOCATOR and changes when the tree reorganizes; the node's identity does not. One node can be indexed by many leaves in many trees — which is what makes 'two tree sets indexing one node set' a mechanism rather than a slogan. MEASURED AGAINST THE RUNNING STORE 2026-08-05: cairn/librarian/trees.py is ONE table (node_id, tree, content, vector, provenance, standing, created) with no link columns of any kind. That row is a node and a leaf collapsed together, which is why its identity function `node_id = sha256(tree + content)` binds the remembered thing to a tree. That is not a hash bug to patch — it is the collapsed row showing through, and it contradicts 'one node list per database' directly. The leaf layer is a PREREQUISITE for calving, not a consequence of it: there is currently nothing to renumber and nowhere for an edge to live.",
    "the_shear__answers_open_question_2": "AKIEN, same turn: 'a leaf points to a node, and has edges that point to other leaves, WHO ALSO POINT BACK.' Edges are TWO-WAY, and that is what makes the shear tractable: a moved leaf's IN-LINK LIST IS the exact set of holders of stale pointers, so the affected set is ADDRESSABLE, not merely small. One-way, that set exists but has no address and you find it by reading every leaf's out-links in the database — which is the searching the whole addressing design exists to eliminate, reappearing in the reverse direction. CC's arithmetic, UNMEASURED: bound B, corpus N, so ~N/B calves over the corpus's life. One-way = an O(N) scan per calve = O(N^2 g / B) total. Two-way = O(B g) per calve = O(N g) total. At B=5,000 and N=2.5M that is ~500 calves: order 10^9 row-touches against order 10^6 edge-entry rewrites. Quadratic versus linear — and the quadratic version gets expensive exactly as the corpus grows large enough to need calving. SECOND PROPERTY, and the one that matters for Law 7: calving is a PURE LEAF OPERATION. Nodes are not read and not written. Index maintenance can never damage a record of truth, because it never touches one. Reorganize the trees as often as you like; provenance cannot drift, because it is not in the layer that moves.",
    "edges__answers_open_question_3": "SETTLED BY THE SHEAR, not by preference. Both kinds exist and the shear is the boundary: an edge that must survive a renumbering is an edge you are STORING; an edge you regenerate is one you are not. ARTICULATED edges — leaf to leaf, directed, stored, two-way — are what a shear fixes. SIMILARITY edges are derived from leaf vectors at walk time, never materialized, so a shear does not touch them and the next walk recomputes them against whatever the tree now holds. That gives the clean rule: EDGES ARE A PROPERTY OF THE INDEX, NEVER OF THE REMEMBERED THING. Nodes have no edges at all. Cost check on why the split is forced: a 5,000-leaf tree calving in half moves 2,500 leaves; at articulated degree ~4 that is ~10,000 entry rewrites, against 2,500 x 5,000 = 12.5M if similarity edges were materialized at full density.",
    "the_vector_is_the_path": "AKIEN, 2026-08-05: 'the vector becomes the path walked.' Precisely: the path is neither stored nor looked up — the query vector RUN AGAINST THE ATTRACTORS generates the route, and the sequence of choices IS the path. Two decisions: which tree (compare against each tree's dominant attractor), then which leaves within it. CC's arithmetic at d=768 float32, N=2.5M leaves, B=5,000 so ~500 trees: flat over all leaves touches 2.5M x 768 x 4 = 7.7 GB per query; two-level touches 500 x 768 x 4 = 1.5 MB of attractors plus 5,000 x 768 x 4 = 15 MB of one tree = ~16.5 MB. The ratio (~470x) is not the point — the point is that 7.7 GB per query fits nowhere and 16.5 MB sits in cache and stays hot. CONSEQUENCE, and it reframes calving: calving along dominant attractors is THE OPERATION THAT CREATES THE PATH. Before the first calve a tree is flat and there is no route to walk, only a scan. Each calve mints an attractor, and the attractor is what a query steers by. The walk-cost bound is a side effect of building structure, not the reason for it. A tree therefore needs a dominant-attractor vector of its own — a tree-level structure trees.py has no place for today. Flat-over-trees stays fine for a long time: 500 attractors is 1.5 MB, and a tree OF trees is not wanted until ~100K trees (~500M leaves).",
    "the_measured_history_that_motivates_all_of_it": "AKIEN, 2026-08-05 — the number this ticket was missing. The conventional build (SQLite, conventional tables, a stored edge table) reached 70,000 words and 2.5M BIGRAM EDGES, and EVERY SINGLE EDGE UPDATE TOOK OVER 30 SECONDS. So the 2.5M in the falsifier below is not a projection: it is the corpus size at which the previous build died. The mechanism: ingesting one memory means many UPDATEs against a 2.5M-row edge table, each an index seek or a scan, each taking a write lock, each syncing — and THE COST GROWS WITH THE CORPUS. The system got slower exactly as it learned more. Postgres buys perhaps an order of magnitude and then meets the same wall, because the wall is storing edges at all. THE TRADE THE DESIGN MAKES: stored edges pay at WRITE time and the price grows with the corpus; derived edges plus bounded trees pay at READ time and the price is capped by the bound rather than by how much has been learned. Adding a memory writes one node row and one leaf row; no similarity edge is updated, because none exists to update.",
    "build_order_for_the_embeddings_generator": "AKIEN'S FRAMING, 2026-08-05: 'we HAD (UU) an embeddings generator that would take the text of a memory into the device, and out would pop embeddings by following the graph trees. So the question becomes: what do we need to make that work?' CC's answer, in build order, each step usable before the next exists: (1) THE LEAF LAYER — `nodes` (one list per database, content-addressed, tree-free) split from `leaf_<tree>` (address, node pointer, vector, weights, edges). Everything below needs it first. (2) TREE ATTRACTORS — each tree carries the centroid of its leaves so a query can pick a tree before touching leaves; without it every walk compares against every leaf in every tree, which is the 2.5M problem in a new hat. (3) A SEED EMBEDDER — text must become some vector to enter the walk; this is the only place a model is required and its job is only to land in roughly the right tree, so it can be small, local, and reached through inference_domain. (4) THE WALK — seed vector to nearest tree attractors (probe the top few, not the top one), then leaves within, ranked. Its output is the path. (5) THE COMPOSER — turn the walked path into the output embedding; simplest version that works is a similarity-weighted mean of the walked leaves' vectors. This is the step that makes it 'embeddings by following the graph trees' rather than embeddings from a model, and it is where the bootstrap resolves: the seed gets you in the door, the composition is the real representation, and the composed vector is what gets stored. (6) THE WRITE PATH — deposit the node (dedupe on content, globally), mint a leaf in the tree the walk landed in carrying the composed vector. (7) CALVING AND THE SHEAR — only needed once a tree exceeds the bound, so it can wait until data demands it, by which time the walk will have shown where the real attractors are instead of us guessing. DELIBERATELY NOT IN THIS LIST, both reversible: articulated edges and leaf weights. A walk composes from geometry alone; edge weights (paths that resolve get stronger) are a real mechanism and the natural correction for a bad router, but they are the second build.",
    "sequencing": "AKIEN, 2026-08-05: 'while i red pencil, you'll return to the extant build order, this change can be number 3.' So this work is slotted at position 3 of the extant build order, not started now."
  },
  "why": "AKIEN'S RULING, 2026-07-26: 'i designed my system because just the reading, nothing else, was overwhelming the usual way of doing it. it wasn't theoretical, it was an experiencial adaptation. if you can't guarantee that it's faster than my solution, we have to go back to mine.' CC cannot guarantee it — there is NO measurement anywhere in the tree comparing the two, no benchmark and no threshold. His design came from load he actually hit; the replacement (MAP.md:527, 'baked in 2026-07-14') came from a design conversation and has never been tested against anything. Law 3 puts the burden on the replacement and the replacement has not carried it. SECOND why, and the sharper one: he can no longer EXPLAIN what the system does, which for him is disqualifying on its own — 'i need to be able to understand and explain to others what we're doing and why at that level.' The cause is measurable: his vocabulary was kept and its meanings changed under it. `calving` survives as a word and now means 'insert a tree row' (MAP.md:529) instead of 'split a tree that grew past its bound'. `the embedding IS the path` survives as a sentence and was read as vectors. Nothing scans as missing, because all the words are still there.",
  "what_was_replaced": {
    "survived": "Two tree-sets indexing one node set — reinforced by the asymmetric-indexing ruling in held-librarian. CORRECTED 2026-07-26 after Akien's era ruling: this line originally also credited 'ONE node list per database (MAP.md:528)' as a piece of his design that SURVIVED. That was wrong, and wrong in the informative direction — one node list is the OLD era's shape, so MAP.md did not preserve a piece of the current design, it restored a piece of the superseded one. See eras.cc_read_unruled.",
    "replaced": "A TABLE PER TREE — MAP.md:528-532 makes trees first-class ROWS and states 'trees never get independent tables'. `table.leaf` LINKS — replaced by an edges table, so you look a node up rather than addressing it. INSTANT ACCESS is weakened to an index lookup: same big-O in practice, different in kind.",
    "dropped_with_no_replacement": "The BOUND and the SHEAR. Physical calving was demoted to 'Postgres partitioning by tree_id when scale demands' — no threshold, no trigger, no shear. That is a deferral in the shape of a plan.",
    "the_stated_reason_and_ccs_read": "MAP's why for collapsing per-tree tables: cross-tree edges must be as cheap as within-tree ones, for cross-domain search and cross-tree invalidation. CC's read, flagged as a READ and not a ruling: that objection is weak against THIS design specifically, because a `table.leaf` link already carries its table, so a cross-tree link is just a leaf address with a different table part. The objection assumes you must SEARCH for edges, which is the exact thing the addressing eliminates."
  },
  "traces_to": [
    "Law 3 — the replacement is unmeasured and the original came from measured experience; the burden sits with the replacement.",
    "Law 1 — TheIgors' memory design is already written down (design docs, tickets, tests). Re-deriving it by conversation is the defect; reading it is the fix. This node's first child is a READ, not a design session.",
    "Law 7 — a record of truth never collapses. The graph is the record; a vector index over it is a derived cache that may be freely calved and rebuilt.",
    "memory build-to-the-tool-naming — a term must be derivable from its native use. Reusing `calving` for a different operation broke exactly that, inside Akien's own vocabulary."
  ],
  "node_class": "code-seam",
  "state": "code-seam@v2: [THINKME] -> TICKETME -> BUILDME -> PROVEME -> PROVED   (CAST 2026-07-26 via /sorted; cursor at THINKME — DELIBERATELY. The completeness gate ran and DID surface unresolved questions, listed in open_questions below. Casting this at BUILDME would be the premature-cast defect the gate exists to stop. THINKME is the honest cursor: the node exists to hold the thread and its gate out of THINKME is answering those questions.)",
  "gates": {
    "prove_gate": "proof gate — and see the falsifier: the proof must run at REAL scale, not charter scale.",
    "durability_gate": "tested AND durably committed."
  },
  "falsifier": "The restored design must be measured AT THE SCALE IT WAS DESIGNED FOR — and as of 2026-08-05 that scale has a MEASURED BASELINE TO BEAT, not just a size: the conventional build reached 70,000 words / 2.5M bigram edges with every edge update taking OVER 30 SECONDS and the cost growing with the corpus (see settled_2026_08_05). So the test is two-sided: access that does not degrade with graph size, AND write cost that does not grow with corpus size. TheIgors' word graph was ~2.5M nodes; Cairn's largest live measurement to date is 66 records across 14 histories. A load test at 2.5M nodes must show node access that does not degrade with graph size — that is the property the whole design exists to deliver, and the property 'the reading alone was overwhelming' was the symptom of losing. If the restored design does not hold at 2.5M, it is not the design that solved his problem and this reopens. A hollow build — proving the shape on a few hundred rows — fails this outright.",
  "horizon": "The design settles when the open questions below are answered by Akien and the load test has run. Not before.",
  "open_questions": [
    "RESOLVER NOTED ON EACH — the resolver for a design DETAIL is the question corpus or a measurement, not Akien; he gets bounds changes, his sequencing, and spec-changing rulings. Marked per item.",
    "[AKIEN] THE BOUND: is 5K the CURRENT bound? NARROWED 2026-07-26 by the era ruling — TheIgors' 'calve if tree exceeds 1000 nodes' is the OLD era's trigger, so it is not a competing claim to adjudicate, it is prior-art evidence that the threshold MOVED (1000 -> 5K). What remains for Akien: confirm 5K is the bound the tree.leaf shape carries, and say whether it is one bound or two (size and depth). NOTE 2026-08-05: this is a bound, so it is his — but it is also a LEARNABLE one (a hand-set constant in a gate is a learned value stranded in a human's head), and the walk-cost arithmetic in settled_2026_08_05 gives the method: baseline plus marginal cost, divide the headroom.",
    "ANSWERED 2026-08-05 — THE SHEAR AND INBOUND LINKS. Akien: edges are two-way, so a moved leaf's in-link list IS the set of stale-pointer holders. See settled_2026_08_05.the_shear__answers_open_question_2. What REMAINS of it is narrower and is listed below as the cross-owner question.",
    "ANSWERED 2026-08-05 — EDGES: STORED, DERIVED, OR BOTH? Both, and the shear is the boundary. See settled_2026_08_05.edges__answers_open_question_3. CC's proposed reconciliation stands, but it is no longer a preference: it is forced by what a renumbering must repair.",
    "[AKIEN] TWO THINGS NAMED CALVING in TheIgors itself — graph calving (tree splitting) and a 'serial-calving counter' in node_id.py for ID uniqueness, flagged in their own docs as 'distinct but related naming'. Which name survives into Cairn. NARROWED 2026-07-26: both are OLD-era artifacts, so this is not a live collision to resolve — it is one name to RATIFY for the current shape. `calving` = a tree past its bound splits. Akien's to confirm; the ID-counter sense does not cross.",
    "ANSWERED 2026-08-05 (SECOND SESSION) — WHERE THE VECTOR LIVES. Neither node nor leaf: its OWN LAYER. An embedding is a record of its own — a vector plus the node it renders — with MANY PER NODE and two-way pointers to that node; a leaf points at an embedding. Akien's UU description is the source ('the embedding record is a blob and points back to it's memory. they point at each other'; 'the nodes can contain links into a list of VECTORS belonging to each node'). This retires the leaf-side derivation recorded here — the derivation's WORRY was right (a node-side vector places a node identically in every tree) and the third layer removes the worry rather than answering it, because a node with several renderings has several placements natively. See notes/2026-08-05-the-index-must-learn-node-embedding-leaf.json; applied to both press_office documents the same day.",
    "[CORPUS] NODE -> LEAF BACK-REFERENCE. Akien's two-way principle bites one layer up too: 'where is this node indexed?' — needed for invalidation when standing changes — is otherwise a lookup against every tree table (~500 of them at 2.5M leaves). Storing the leaf list on the node makes it one read, but then a shear DOES touch nodes, one entry per moved leaf, and the 'calving never writes a node' property above holds only if this back-reference does not exist. Two real options, both cheap, and the choice is the corpus's: stored back-reference, or a per-tree index on leaf.node_id queried across trees.",
    "[CORPUS] CROSS-TREE IN-LINKS ARE CROSS-OWNER WRITES — what remains of the original shear question. If a leaf in tree B points at a leaf in tree T, T's calve must rewrite a row B's owner gates, and under Law 6 the shear cannot reach in. Two-way makes this TRACTABLE (T knows exactly who to notify) but not free: the fixup has to travel through B's gate. Narrowed from 'how do we find them' to 'how is the notification gated'.",
    "[CORPUS] THE MUTUAL INVARIANT NEEDS ONE DOOR. If a link's out-half and in-half can be written separately they can drift, and a drifted back-reference means the shear misses a leaf — whose stale pointer then does not dangle but SILENTLY RETARGETS to whatever now occupies that address. That is a record of truth quietly saying something false (Law 7). So one operation writes both halves or the link does not exist (Law 4 shape), and the falsifier is 'an out-link with no mirror in-link exists'. Cheap belt-and-braces regardless: never reuse leaf numbers after a shear, so a missed pointer dangles loudly instead of retargeting silently.",
    "[MEASUREMENT] MANUFACTURED NOVELTY — a NEW failure mode that falls out of the two-level walk, and the exact twin of the home-field finding. If the router picks the wrong tree the walk misses, and a miss fires the backfill — so A ROUTING ERROR IS INDISTINGUISHABLE FROM GENUINE NOVELTY. The system mints nodes for something it already knows, filed in the wrong neighbourhood, and the mint then wins the similarity race for its own question. Manufactured RESOLUTION is a high score that is not evidence; manufactured NOVELTY is a miss that is not absence, and it is worse because it is self-reinforcing: each routing miss deposits more nodes that make the wrong tree slightly more attractive. Softener: probe the top-k trees rather than the top one (k=5 costs 75 MB instead of 15 MB — nothing). Measurement: router recall@k, by taking queries whose correct leaf is known and checking whether the router lands on its tree.",
    "[MEASUREMENT] DOES THE COMPOSER EARN THE WALK? Steps 3-5 of the build order make a testable claim: THE COMPOSED EMBEDDING IS BETTER THAN THE SEED EMBEDDING. If walking the trees does not produce a better representation than the cheap model you entered with, then for THIS purpose the trees are decoration — they would still earn their keep as a cache, but 'embeddings by following the graph trees' would be false. Check it BEFORE step 6, not after: take text whose correct node is known, compute both vectors, see which retrieves it. Costs one afternoon; if composed wins, it is the strongest measured thing the design has.",
    "SUPERSEDED 2026-08-05 (SECOND SESSION) — see settled_2026_08_05.edges__answers_open_question_3, whose SECOND HALF no longer holds. 'Both kinds exist and the shear is the boundary' STANDS. 'Similarity edges are derived at walk time, never materialized' DOES NOT: a weighted, bounded, STORED similarity structure IS the index, and without it the index cannot learn. The cost check that settled it compared bounded articulated edges against a FULL-DENSITY complete graph nobody proposed. Akien: 'i didn't ask for that. why's that there? i thought that's what the trees were for?' — Law 9, no past artifact outranks him now. Full note: notes/2026-08-05-the-index-must-learn-node-embedding-leaf.json.",
    "[AKIEN] HOW FAST DOES A WEIGHT FALL? New, and it is the only genuinely open question the second session ADDED. A similarity weight that strengthens whenever its path resolves is a positive feedback loop, and §2.7's home-field finding is already a story about one: a wrong route that resolves once gets stronger, is likelier to be chosen next time, and manufactures its own confirmation with a ratchet on it. The correction is the asymmetry the gates already require — COUNTER-EVIDENCE LOWERS A WEIGHT FASTER THAN CONFIRMATION RAISES IT — and how much faster is unmeasured. Same species as THE BOUND above: his to set, learnable in principle, and born red.",
    "[CORPUS] IS THE THOROUGH TIER pgvector, AND WHEN? Settled second session that pgvector is PRECEDENT rather than speculation — Akien: 'pgvector is what we did in UU' — and that it does both exact and approximate search, which is exactly the exhaustive tier's job over the embedding layer. What is NOT settled is whether the tier is built on it or hand-rolled first, and that answer wants the db_domain owner's view, not a preference.",
    "[CORPUS] BUILD ORDER, RATIFIED IN SESSION, AND IT REFINES build_order_for_the_embeddings_generator RATHER THAN REPLACING IT: (1) the embedding layer — cheapest, unblocks the rest; (2) weights in the trees — the compiled inference, and second because it needs the asymmetry guard above; (3) lexical search over node content — nearly free, and the only one of the three that finds what vectors STRUCTURALLY cannot (exact terms, names, identifiers, quoted phrases). LSH is HELD as a tier-1 alternative if the weighted trees do not earn their keep — a known technique, not a build. None of this is cast; casting it is the next act on this ticket.",
    "[CORPUS] THE PROOF THAT MUST BE RETIRED DELIBERATELY — approved by Akien 2026-08-05. cairn/librarian/proofs/test_librarian_trees.py::test_no_edge_table_exists is GREEN and pins the superseded half. Under Law 4 the physics currently enforces the OLD ruling, so replacing the rule means replacing its ENFORCEMENT in the same act — never leaving the corpus with a tooth that bites the new design, and never letting it simply start failing mid-build. The successor must assert the NEW invariant: BOUNDED and WEIGHTED, nothing of unbounded degree registrable. Deleting it bare would hand back the protection it was built to give, which is the 2.5M wall."
  ],
  "parent": null,
  "children": [
    "a — THE READ PASS over TheIgors' memory design for the ideas that carry: the node envelope, calving's thresholds and protected-node list, the split protocol. IDEAS ONLY, per Akien's ruling — no code crosses. RESCOPED 2026-07-26 by the era ruling: this reads the OLD era, so it is prior art and NOT the source of the shape (the addressing in particular comes from the CURRENT era and is Akien's to state, not the quarry's to supply). Its highest-value target is the split protocol, because that is the part TheIgors designed, tested, and never finished. Buildable now; everything else waits on it.",
    "b — SETTLE THE OPEN QUESTIONS above with Akien. Waits on a (the read pass will inform several of them, notably the shear).",
    "c — THE SHAPE: envelope, addressing, per-tree tables, and what gets promoted out of the JSON payload into typed columns. Waits on b. RESCOPED 2026-08-05: its FIRST move is now named — split the collapsed row into `nodes` (one list per database, content-addressed, tree-free) and `leaf_<tree>` (address, node pointer, vector, weights, edges). That split is a prerequisite for child d rather than a detail of it: there is currently nothing for a shear to renumber and nowhere for an edge to live. Steps 1-2 of settled_2026_08_05.build_order_for_the_embeddings_generator are this child.",
    "d — CALVING AND THE SHEAR, including the bound. NOTE: in TheIgors this was designed, TESTED, and NEVER FINISHED — detection was live, the split operation was pending (T-calving-split-op, epic T-graph-calving-execution). So this child FINISHES an unfinished design rather than restoring a working one, and the fact that it was left unfinished is evidence it is harder than it reads. Waits on c."
  ],
  "dependencies": "Child a needs the quarry on disk (present, frozen, measured). Nothing needs the inference host, the bus, or embeddings — building the store is prior to all of them.",
  "provenance": "Opened 2026-07-26 when Akien asked what had happened to his memory design and said he could no longer explain it to others. CC measured the substitution field by field against MAP.md:527-537. Akien then ruled: absent a guarantee the replacement is faster, we go back to his. Later the same session the quarry was located on disk, which changed child a from a design task into a read. CORRECTED THE NEXT SESSION, SAME DAY: Akien read the ticket back and said 'the igor shape is the old shape. and the tree.leaf reference is new as well.' CC could read that three ways and ASKED rather than pick one — the direct lesson of the substitutions this node exists to record. He ruled tree.leaf MINE AND IT SUPERSEDES, which split one 'his design' into two eras and moved MAP.md's one-node-list from 'a piece that survived' to 'a piece of the OLD design that came back'. The node stays at THINKME: three questions remain his. SHARPENED AGAIN 2026-08-05, while drafting two outward-facing press_office pieces about this design: Akien read the drafts and found the collapse this ticket had NOT caught — CC had been writing `node` where the design means `leaf` throughout, in the ticket, in the running store, and in both drafts. He ruled the two layers apart (settled_2026_08_05), answered the shear question with two-way edges, said 'the vector becomes the path walked', and supplied the measured history (70K words / 2.5M bigrams / 30s per edge update) that is the design's actual provenance and had been reduced to the phrase 'the reading was overwhelming'. Two open questions closed, five opened — four of which are the corpus's or a measurement's, not his.",
  "filed_edges": [
    "pgvector IS NOT INSTALLED and is not even an available package on this box (measured: PostgreSQL 16.14, plpgsql only). It is a HOST-SEAM when it is wanted — and it may not be wanted for a while, since if the path is the embedding then similarity is a TRAVERSAL query in plain SQL and the extension is only needed once a vector CACHE is pulled by measured need.",
    "THE TRAINED GRAPH IS GONE — no clan schema or wg_* tables survive in any database on this box. The TRAINER (claudecode/retrain_word_graph.py, Postgres-backed, resumable) and 166 books already extracted to text (training_corpus/, 102MB) both survive. So rebuilding it is a COMPUTE job, not a redesign — but it is downstream of this node, not part of it."
  ],
  "migrated_from": "code-seam@v1 — the dissolved fifth summons stripped by sweep 2026-08-03 (Akien's ruling: the words should have been scrubbed when the version was minted). The dead token is deliberately not reprinted here; tickets/watchme-emits-a-probe is its one record."
}
