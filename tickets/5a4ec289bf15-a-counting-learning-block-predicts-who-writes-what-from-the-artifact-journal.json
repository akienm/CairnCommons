{
  "id": "5a4ec289bf15",
  "title": "a-counting-learning-block-predicts-who-writes-what-from-the-artifact-journal",
  "node_class": "code-seam",
  "task_or_ticket": "ticket",
  "workflow_and_state": "code-seam@v2: THINKME -> [TICKETME:waiting] -> BUILDME -> PROVEME -> WATCHME(raise_rate_falls_as_the_table_grows) -> PROVED",
  "date": "2026-09-13",
  "created": "2026-09-13",
  "author": "CC",
  "from_idea": "none, because it is the second follow-on named verbatim in ticket 30531f6e1c5d's follow_ons field (CairnCommons/tickets/30531f6e1c5d-records-of-truth-change-only-through-the-artifact-door.json), born of the Friston free-energy text Akien brought on 2026-09-13 and captured in CairnCommons/decisions/2026-09-13-records-of-truth-change-only-through-the-artifact-door.json ('an alternative to LLM ... to perform inspections')",
  "intent_berth": "/home/akien/.cairn/devices/skill_block/0/berths/intent/intent-20260913T172352-720d8f4ed0fa.json",
  "sorted_berth": "/home/akien/.cairn/devices/skill_block/0/berths/sorted/sorted-20260913T172545-4e9cdfb2a938.json",
  "owner": "CC builds it; codemother owns the care of the code; the block's table is instance-space state of its holder (Law 6) and no record of truth.",
  "owning_intention": "cairn/machines/counting_block/intention+why.json (to be written beside the code at BUILDME)",
  "ruling": "CairnCommons/decisions/2026-09-13-records-of-truth-change-only-through-the-artifact-door.json",
  "pre_approval": "Akien, 2026-09-13, on the parent ticket: 'approved thru PROVED RULED BLESSED FROM ON HIGH' — the ruling names both follow-ons; the parent's cast recorded them as cast 'when this ticket reaches PROVEME'. Pre-approval of THIS build is not claimed: it stands at his review queue.",
  "intention": "A counting learning block over the artifact journal predicts who writes what (a frequency table over path-class × verb × caller-class) and raises only the entry outside the table, so the inspection of record writes is arithmetic that runs on every append and the LLM is spent only when the table itself must change.",
  "why": "the intention I-the-system-is-a-learning-block.md (spanning; the block anatomy: a door, a prior, an error signal, a local adjustment) and learns-its-gates (the system learns its gates from evidence); Law 1 (the resolver spent on the novel — a triple seen 400 times is settled, only the unseen one costs attention); Law 3 (the table is measured from the journal, never authored)",
  "how": "A tenant of the learning-block anatomy: compile a count table over (path class, verb, caller class) from both artifact journals; on the door's probe poke, a zero-count triple raises one trouble through the trouble device; an akien-class clearance increments the row, a cc-class clearance does not; the first measurement is compression (rows << entries).",
  "how_first_cut": "Under cairn/machines/learning_block's existing anatomy (the charter cairn-machines-learning_block--intention+why.json): (1) the table — counts of (path class from jurisdiction.json, verb, caller class) compiled from both .artifact-journal.jsonl files, kept in instance-space under the holder that assembled it (Law 6 — a tool's state berths under its holder); (2) the prediction — for a new entry, its (class, verb, caller) frequency; (3) the error — an entry whose triple has count zero, or whose caller class is unknown, raised as a trouble through the trouble device with the entry and the table row; (4) the local adjustment — a raised entry that Akien clears increments the table (the world was right, the prior was wrong); (5) the trigger is the door's existing probe poke (callers_are_known already runs after every append) — no poller, no daemon (memory shrinking-footprint-event-not-poll). Friston's loop is cited, not grafted: prediction = frequency, free energy = surprise of the triple, action = raise, learning = the count.",
  "falsifier": "DONE when, measured by cairn/machines/<block>/proofs/: (1) the table compiles from both journals and is reproducible from them alone — delete it, recompile, byte-identical; (2) a seeded journal entry with an unseen triple raises exactly one trouble naming the triple and the row; a seen triple raises nothing; (3) an akien-class clearance increments the row so the same triple no longer raises, and a cc-class clearance does not; (4) the block runs on the door's probe poke with no process of its own between appends (ps shows nothing; the poke is the only entry point); (5) over the live corpus the row count is well under the entry count (compression exists). WRONG INTENT if after 500 appends the table has one row per entry, or if the raise rate does not fall as rows fill — then the triple is the wrong prior and the block routes back to Akien with the table. HORIZON: the compression measurement is the first piece; if it reds, the ticket routes back before any raise is wired.",
  "traces_to": "the intention I-the-system-is-a-learning-block.md (spanning; the block anatomy: a door, a prior, an error signal, a local adjustment) and learns-its-gates (the system learns its gates from evidence); Law 1 (the resolver spent on the novel — a triple seen 400 times is settled, only the unseen one costs attention); Law 3 (the table is measured from the journal, never authored)",
  "watchme": {
    "object": "raise_rate_falls_as_the_table_grows",
    "trigger": "every raise the block makes and every 100 journal appends — both ride the door's probe poke; no poller.",
    "enough": "1000 appends after arming with the raise rate over the last 200 appends below the rate over the first 200, AND at least one akien-class clearance having incremented a row. Does not clear on a timer. If the rate does not fall, that is the finding (the triple is the wrong prior).",
    "carrier": "a verdict artifact against THIS ticket's falsifier naming the row count, the entry count, the raise rate per 200-append window, and every raised triple with its disposition.",
    "nexus": "the who-writes-what tree — this block IS its first learner; the verdict teaches whether the triple is the right prior.",
    "consumer": "Akien.",
    "probe": "cairn/machines/counting_block/probes/raise_rate_falls.py"
  },
  "gates_bound": [
    "proof gate — the block's proofs run green under the tester over scratch journals and a scratch trouble store; the live-corpus compression tooth asserts an invariant (rows << entries), never a snapshot.",
    "commit gate — the block writes no record of truth; its table is instance-space. The troubles it raises go through the trouble device, whose writes ride the artifact door and pass `cairn artifact check`.",
    "build gate — build_inspector reds the block until its validation seals; ticket_inspector reds this ticket on field drift.",
    "feeds-back-to-origin on failure — a red compression measurement routes this ticket back to Akien with the table (WRONG-INTENT clause); a red at PROVEME kicks it to BUILDME through the emit chokepoint."
  ],
  "children": "none, because the block is one tenant of the learning-block anatomy with ordered pieces (compile, compression measurement, raise, clear-with-class, probe) that prove together; the rename ticket (cast beside this one) is a sibling under the same ruling, not a child.",
  "depends_on": "30531f6e1c5d — the artifact door must stand at PROVED: the rename rides its rename verb and the block reads its journal and rides its probe poke.",
  "follow_ons": [],
  "owed_physics": "none beyond the parent's (git commit --no-verify; a CC launched outside superclaude; a double-forked scope) — this ticket adds no new escape.",
  "cast_checks": {
    "assumption_check": "THREE, with standing. (1) That the triple (path class from jurisdiction.json, verb, caller class) is the right granularity — coarse enough to compress into few rows with high counts, fine enough that a gate writing a hand-edit or cc writing a seal is a zero-count row. UNMEASURED; measured at the first piece by compiling the table from the 1,326 genesis entries plus today's writes and reading the row count against the entry count (the falsifier's WRONG-INTENT clause: one row per entry means no compression). (2) That the door's probe poke is a usable trigger for a second consumer. MEASURED 2026-09-13: callers_are_known runs after every append (tooth test_the_probe_is_armed_with_carry_and_enough), so the poke exists; whether the door can carry two probes without a registry is decided at BUILDME's second piece — the cheapest shape is one probe module that calls the block, not a registry. (3) That cairn/machines/learning_block's anatomy fits a table-and-count tenant. Its charter (cairn-machines-learning_block--intention+why.json) is the prior art; read at BUILDME, and if its door demands an LLM-shaped prior the block is a NEW machine beside it and this cast's node_class stays code-seam either way.",
    "missing_check": "TWO points asserted wrapped that are not. (a) WHO CLEARS a raised triple: the trouble device owns raising and clearing, and a cleared trouble increments the row — but 'cleared by whom' matters: cc clearing its own novel write teaches the table that cc's novelty is normal. The cast records the answer: only a clearance whose caller class (read by the same door) is akien increments the table; a cc clearance retires the trouble and leaves the count at zero. Recorded on the ticket; the proof carries a tooth for it. (b) Laplace smoothing vs zero-count: a hard zero raises on the first write of every new component's state.json (a new class × append × gate row). That is CORRECT on day one (a new component is news) and noise by day thirty. The cast records the first shape as hard-zero with the smoothing parameter as the one knob the LLM adjusts (memory sieves-measure-not-judge), and the WATCHME measures whether the raise rate falls.",
    "falsifier_check": "DONE when, measured by cairn/machines/<block>/proofs/: (1) the table compiles from both journals and is reproducible from them alone — delete it, recompile, byte-identical; (2) a seeded journal entry with an unseen triple raises exactly one trouble naming the triple and the row; a seen triple raises nothing; (3) an akien-class clearance increments the row so the same triple no longer raises, and a cc-class clearance does not; (4) the block runs on the door's probe poke with no process of its own between appends (ps shows nothing; the poke is the only entry point); (5) over the live corpus the row count is well under the entry count (compression exists). WRONG INTENT if after 500 appends the table has one row per entry, or if the raise rate does not fall as rows fill — then the triple is the wrong prior and the block routes back to Akien with the table. HORIZON: the compression measurement is the first piece; if it reds, the ticket routes back before any raise is wired.",
    "collision_check": "Judged over the whys. Law 6: the table is DERIVED from the journal and reproducible, so it is instance-space state of the block's holder, not a record of truth — it does not go through the artifact door (jurisdiction.json's not_records already says derived surfaces are not records); a distinction recorded. The trouble device already owns raising/clearing; the block only feeds it — no second raiser. The corrosion sieve (constraint_enforcement_holds) is the same expected-vs-seen shape over a HAND-AUTHORED set; this block's set is measured, which is the difference Law 3 asks for — kin, not collision. memory ground-loop-is-just-the-heartbeat: the block adds nothing to the ground loop; it rides the door's poke. No exclusive-and-terminal collision.",
    "builder_check": "A Haiku-level builder needs, and the ticket's how carries: (1) The table's shape: {(path_class, verb, caller_class): count}, path_class = the jurisdiction rule that matched (e.g. 'CairnCommons/tickets', 'cairn/state.json'), persisted as JSON under ~/.cairn/devices/<holder>/0/machines/<block>/table.json with the journal line counts it was compiled to. (2) The compile: read both .artifact-journal.jsonl from line 0, count triples; incremental on poke (append the new entry's triple). (3) The raise: trouble device's existing raise verb with evidence = the entry (path, verb, caller cgroup, at) and the row (count 0); one trouble per triple, deduped by triple. (4) The clear: on the trouble device's clear, read the clearer's class through artifact.caller(); akien increments, cc does not. (5) The trigger: the door's probe poke — build ONE probe module that calls the block, at cairn/machines/<block>/probes/raise_rate_falls.py, with trigger/carry/enough as probe.py demands. (6) The proof: a scratch pair of journals under set_diagnostic_roots, seeded triples, the trouble device pointed at a scratch store — the pattern test_artifact_door already uses."
  },
  "challenge_at_birth": {
    "better_approach": "Looked. The probe callers_are_known already answers the ONE question that matters most (caller=unknown); the counting block generalises it to 'this caller does not normally write this class with this verb'. The cheaper alternative is adding two more probes by hand; the block replaces hand-written probes with a table that grows itself, which is what the learning-block intention asks for. If /sorted finds the triple space too small to need a table, the answer is the two probes and this ticket routes out.",
    "prior_art": "In-house: cairn/machines/learning_block (the anatomy is built; this is a tenant), probes/callers_are_known.py (the first consumer of the journal, cited in the WATCHME nexus of 30531f6e1c5d as 'defines the frequency table it will predict from'), the corrosion sieve (a constraint that stopped constraining — the same shape: expected vs seen). Field: Friston free-energy minimisation as Akien quoted it; in practice a Laplace-smoothed count table, which is the degenerate case of the same loop.",
    "hidden_assumption": "That the triple (path class, verb, caller class) is the right granularity: coarse enough to compress (few rows, high counts) and fine enough that a hand edit by a gate, or a seal written by cc, is a zero-count row. Also that the journal volume (1,326 genesis entries + a few hundred per day) is enough to settle a table within a session, not a month.",
    "real_collision": "None. The trouble device already owns raising and clearing; the block only feeds it. Law 6: the table is the block's holder's state in instance-space, not a record of truth — it is derived from the journal and reproducible, so it does not go through the artifact door (a distinction to record, and jurisdiction.json's not_records already says derived surfaces are not records).",
    "back_up": "proceed — depends_on 30531f6e1c5d reaching PROVED (the journal must be standing and the probe poke wired); ruled by the same 2026-09-13 ruling"
  },
  "measured_at_cast": {
    "date": "2026-09-13",
    "by": "CC, session a187d6ab",
    "census": "893 CairnCommons records + 433 cairn records journaled at genesis (1,326 entries); 127 non-proof files / 322 sites in cairn, skills, bin spell .json for a record path; ZERO unthreaded writers remain after the door (7 files / 10 hits: comments, prints, door calls, one disabled proof).",
    "filed_through": "the artifact door, verb cast — this ticket's first journal entry is its filing."
  },
  "schema_version": "ticket-v1"
}
