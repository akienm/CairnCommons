{
  "id": "chart-orient",
  "schema_version": "ticket-v1",
  "date": "2026-07-28",
  "owner": "CC (code+proofs); Akien (the go: '/sorted approved ratified make it so very so number one!')",
  "intention": "STONE 1 of the chart device: the ORIENT NEXUS v0 + the /chart skill's first stage (merged at cast — the ceiling needs a runner). One narrow question: WHAT IS ACTUALLY BEING ASKED, AND WHERE DOES IT LIVE? Two strata ship: the DETERMINISTIC FLOOR (cairn/chart/orient.py — request parsing + existence verification, with TERRITORY measurement COMPOSED from the orient instrument's device_census, never a parallel scan) and the CLAUDE CEILING (the /chart skill's mini agentic loop, curtain held as stated discipline: read only the request + floor facts, never the target folders). The exit artifact is the orient packet — intent/domain/scope/refs/unknowns/confidence/provenance — refused at the gate (validate_orient, the append-door pattern) on missing fields, malformed confidence, provenance not covering every authored field, or ANY ref the floor cannot verify exists. Packets berth in instance-space (~/.cairn/devices/chart/0/packets/); the tree stratum is the next stone.",
  "why": "The parent's why, made concrete first here because premature convergence was MEASURED at home (the web-server carrier miss) and orientation is where it strikes. Akien's skill-door facts make the delivery mechanism: the preamble lives behind /chart, and the floor runs BEFORE the ceiling reasons — the request grounded against what exists, not what the nearest pattern suggests.",
  "traces_to": [
    "Law 1 — the floor composes the SETTLED measurer (cairn/orient) instead of re-deriving scans; a recurring orientation compiles into the tree stratum next stone",
    "Law 3 — floor output is measurement (exists/missing, never merged); confidence and per-field provenance travel as data",
    "Law 4 — the schema gate at the handoff; an invented ref physically cannot reach downstream",
    "Law 6 — one door into the house (the import-allowlist tooth pins cairn.orient.orient as the ONLY cairn import)",
    "Law 7 — every refusal is loud and names its cause; the packet berth validates at the door and a refused packet leaves nothing behind it"
  ],
  "node_class": "code-seam",
  "state": "code-seam@v2: THINKME -> TICKETME -> BUILDME -> PROVEME -> [PROVED]   (cursor at PROVED 2026-07-28, same day as the cast; component history/state beside the code at cairn/chart/)",
  "gates": "proof gate: cairn/chart/proofs/test_chart_orient.py — 12 teeth under the tester's netns seal, validation persisted beside-code. Live gate at LEARNME: /chart's stage-1 protocol fired on a REAL request (the next stone's own work), floor + ceiling + gate + berth, reported honestly.",
  "falsifier": "RED on any of: the floor scanning territory in parallel with the orient instrument (the composition tooth); a floor answer crossing from what-exists into what-applies; an invented ref passing the gate; a packet field without provenance; found and missing merged; a refused packet leaving residue behind the write door; a second cairn import in orient.py.",
  "horizon": "re-read when the tree stratum lands (the walk between floor and ceiling), when /chart runs on enough real tasks to read the dial (fields by floor vs tree vs claude), and when the curtain gets its physics (parent edge b).",
  "distinctions_recorded": [
    "THE NEXUS'S FIRST LIVE FIRE CAUGHT ITS OWN BUILDER (2026-07-28): the floor reported `orient` as a component on the roster — and CC had never surveyed for one. cairn/orient/ existed, PROVEN, one day old (born 2026-07-27 from the three-conclusions morning): 'the prebuild orientation instrument', Akien-ranked how-we-work AND foundational, whose filed edge (b) is literally the skill-mouth this stone was building. CC's first-cast floor carried its own charter-glob roster — a Law 1 re-derivation of the settled measurer, committed WHILE BUILDING THE MACHINERY THAT EXISTS TO STOP EXACTLY THIS. Premature convergence n=3, and the first one caught by the system instead of by Akien.",
    "THE FIX IS COMPOSITION AS PHYSICS: component_roster now derives from device_census rows (charter_on_disk filter), and the import-allowlist tooth pins cairn.orient.orient as the ONLY door into the house — a parallel scan cannot return without going red. A new tooth (gamma: code without a charter — census sees it, roster must not) pins the composition behaviorally.",
    "STONE MERGE AT CAST: the planned orient-v0 and /chart-skill-v0 stones merged, named in the /sorted completeness check — a nexus without its skill has no ceiling runner.",
    "THE FLOOR'S TOKENIZER LEARNED THE HOUSE'S OWN NAMES: '+' joined the path class because intention+why.json carries it — caught by the proof's first red, the only red of the stone.",
    "FIRST LIVE PACKET BERTHED: orient-20260728T110828-63dcfc770585.json — the tree-stratum request grounded (4 components, 5 paths/records, all floor-verified), 4 honest unknowns (table grain, resolution floor, embed-seam cost, home-field fencing), confidence 0.72 as data."
  ],
  "parent": "chart",
  "children": [],
  "dependencies": [
    "orient — the prebuild orientation instrument (device_census IS the floor's territory measurement) — PROVED 2026-07-27",
    "tester (netns seal) — standing"
  ],
  "provenance": "Cast 2026-07-28 inside the /sorted Akien fired ('approved ratified make it so very so number one!'), the same day the parent intention was ratified and cast. Completeness check ran green with two named assumptions surfaced (the stone merge; the exit-schema fields) and one IOU carried (the curtain as discipline, parent edge b).",
  "filed_edges": [
    "(a) the TREE STRATUM — the very request the first live packet grounds; next stone",
    "(b) /moreabout v0 — the learning door over berthed packets (scoped lookup + the signal log)",
    "(c) the floor's staleness story once anything caches (event-driven, parent edge d)",
    "(d) skill_roster is a plain directory listing, not an instrument scan — if a skills census lands in cairn/orient someday, the floor composes it the same way"
  ],
  "migrated_from": "code-seam@v1 — the dissolved fifth summons stripped by sweep 2026-08-03 (Akien's ruling: the words should have been scrubbed when the version was minted). The dead token is deliberately not reprinted here; tickets/watchme-emits-a-probe is its one record."
}
