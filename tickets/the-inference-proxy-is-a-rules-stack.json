{
  "id": "the-inference-proxy-is-a-rules-stack",
  "schema_version": "ticket-v1",
  "date": "2026-08-08",
  "owner": "CC (the build: three stacks + nest routing + routed resolver + conforms); Akien (the ruling: decision 2026-08-08-inference-proxy-is-a-rules-stack, confirmed in-session 'confirmed, ruled, etc. for at least the second time. :) Good luck doctor! make it look like that.')",
  "intention": "inference_domain becomes the ruled rules stack: provider, model, and provider-model-combo tables (shareable git-JSON beside the code; machine facts in ~/.cairn/inference/) plus a nest of sieves — cairn.base.nest, this component as its second carrier — that lets out the lowest-cost route for every inference request: Hex first, cloud failovers, loopback never routed by default; the sole_path mesh joins build_inspector's roster (ruling item 1: in the build inspection).",
  "why": "The lived symptom, 2026-08-08: Akien typed 'hello' into the librarian's chat and nothing came back — the turn died at a 120s timeout because the proxy had exactly one road, a hard-wired 127.0.0.1 literal, and this box's CPU takes ~100s per 7B generate while Hex sits on the LAN 8x faster on its SMALLEST model. host.py filed this edge itself 2026-07-26 ('when a second appears this reads from ~/.cairn/'); the second appeared. The routing rules are also the Hex payback meter's supply side: every call routed to Hex instead of cloud is a measured saving against its purchase price.",
  "traces_to": "Ruling 2026-08-08-inference-proxy-is-a-rules-stack (confirmed); Law 6 (one owner: the proxy alone reaches inference hosts, and the inspection makes the sole path a build-time red); Law 1 (the routing decision compiled as rules, not re-derived per call); Telos 1 (the meter in the door makes every route's cost a measured fact).",
  "node_class": "code-seam",
  "state": "code-seam@v2: THINKME -> [TICKETME] -> BUILDME -> PROVEME -> WATCHME(does-the-route-leave-loopback) -> PROVED — cast 2026-08-08, first-pass through the sorted door.",
  "intent_berth": "/home/akien/.cairn/devices/skill_block/0/berths/intent/intent-20260808T164404-28c610f6f07b.json",
  "sorted_berth": "/home/akien/.cairn/devices/skill_block/0/berths/sorted/sorted-20260808T164512-e18a88949aa6.json",
  "from_idea": "none, because the intention was born as a confirmed ruling with his words verbatim: CairnCommons/decisions/2026-08-08-inference-proxy-is-a-rules-stack.json",
  "gates": "code-seam@v2's registered set: entry gate at the BUILDME forward crossing (cast ticket + intent_berth + sorted_berth + berthed chart chain claiming this ticket); build gate at the PROVEME forward crossing (build_inspector's nest over cairn/inference_domain); emission gate at the WATCHME forward crossing (probe berthed at cairn/inference_domain/probes/does_the_route_leave_loopback.py, importable, module-level PROBE with carry and enough, armed before the crossing); exit gate at the PROVED entry (verdict artifact answering the claiming validate berth's criteria by their instruments); proof gate: plain-python teeth beside the code under the tester (netns seal), the standing test_host.py + test_inference_domain.py suites green through the change.",
  "watchme": {
    "object": "does-the-route-leave-loopback",
    "probe": "cairn/inference_domain/probes/does_the_route_leave_loopback.py",
    "trigger": "every generate MISS row the proxy's own cache (inference_calls, read through db_domain) records after the era floor 2026-08-08T17:00:00-06:00: the probe counts post-era generate misses whose provenance.host names 127.0.0.1/localhost. Fires when that count >= 1 — the ruled rule is NEVER, so a single routed loopback generate is the watch's finding, carrying which sieve let it out.",
    "enough": "cleared when loopback_generate_misses == 0 across >= 12 post-era generate misses — the same one variable pair (loopback count zero vs nonzero, with a floor that makes zero meaningful) — the route demonstrably leaves loopback and holds under real traffic.",
    "carrier": "a verdict artifact against THIS ticket's falsifier — the finding names the row count, the offending canonical(s), which provider the nest chose and why (the sieve trace), and the suggestion",
    "nexus": "hypothesize — the finding rides to harbor_master as an efficacy question about the routing rules",
    "consumer": "Akien at the register, and the Hex payback meter thread (CairnCommons/notes/held-hex-payback-meter.json) this routing exists to feed"
  },
  "falsifier": "RED on any of: a generate miss lands with provenance.host = 127.0.0.1 while hex.local answers /api/version; any module outside cairn/inference_domain/ can dial an inference host while build_inspector's roster stays green (ruling item 1 unenforced); Hex unreachable does not fail over to the next-cheapest dialable provider (proven with a fake provider, never the unkeyed rungs); the stacks exist but route() reads a literal anyway (rules decorative); the boat stands short of PROVED when this ticket claims done.",
  "horizon": "Re-read at: the first cloud key landing in ~/.cairn (the failover rungs go live — do they dial?); the first measured divergence between authored costs and metered reality (assumption 2's expiry — the stack should start learning its numbers); the tenure loop reaching route learning; and the gradation watch's clear (this build makes inference_domain the second carrier of cairn.base.nest).",
  "parent": "none — new track, ruled directly",
  "children": "none, because the stacks, the nest assembly, and the routed resolver land at one address (cairn/inference_domain/) as one seam — each unprovable alone; the build_inspector roster line and the librarian seam swap are the same seam's conforms, precedent the banding-berths ticket's three-charter conform (2026-08-07).",
  "provenance": "Born 2026-08-08 from live failure: the first real chat turn after the port-80 move timed out on the loopback literal; Akien ruled the four stacks the same hour ('that's been the intention all along. we had some intentions and built many things. but clearly not to that level'). Hex verified serving both verbs before the cast (nomic pulled onto it under the same ruling's bound: nomic only trains graph trees learning words and bigrams, toward tree-generated embeddings). /intent routed_forward (berth intent-20260808T164404-28c610f6f07b; challenge pass records why config-only and standalone-router both lose to the ruled shape), /sorted cast first-pass (berth sorted-20260808T164512-e18a88949aa6)."
}
