{
  "id": "intentions-model-compiler",
  "schema_version": "v0 — migrates when Q6 (intention envelope) lands",
  "date": "2026-07-23",
  "owner": "akiendelllinux_cc_0 (CC) — CC gates writes; Akien-ratified (signature gate, 2026-07-23 design conversation: name is `intentions-model-compiler` (not `intentions-compiler`) — 'right now that folder is the model'; `intentions-compiler` deliberately RESERVED for a future front-end that ingests a page of prose into intentions; 'sorted.').",
  "intention": "The single owner-gated door that compiles the `intentions-congruency-lab/` MODEL — regenerating `intentions-congruency-lab/` as a pure projection from its sources (`intentions-not-beside-code/` plus the charters homed beside the code), so that one folder can be carried to a bare machine and regrow the system (CLAUDE.md: `intentions-congruency-lab/` is COMPILED from `intentions-not-beside-code/` plus the charters). It is reached exactly ONE way (its compile door) but POKED from three: `cairn compile` from the CLI (via cairn-command), the `/sorted` resolution pivot, and a WRITE GATE that fires when a source changes (the live recompile-on-change path — the file-watching that was moved to GATES, event-driven, not a poller). The name says MODEL on purpose: right now the folder IS the model, so this compiles the model. `intentions-compiler` is reserved (unbuilt, no intention yet) for a possible future front-end that ingests a page of authored prose INTO intentions — a distinct concern the reserved name keeps free.",
  "why": "Law 4 + the roots table: a compiled view is never hand-edited, and `intentions-congruency-lab/` is the carry-to-a-bare-machine model — so it must be REGENERABLE from its sources by physics, not kept current by whoever remembers to. That regeneration needs one write-door (Law 6 — exactly one owner gates writes to `intentions-congruency-lab/`) and a proof a hollow build could not pass (Law 8 — a compile that silently drops or mangles a source is the defect). Poked-from-many-but-one-door is the shape: the CLI hand-crank, the /sorted pivot, and the write gate all POKE the same door, none of them writes `intentions-congruency-lab/` themselves. Kin to the PROJECTOR (charter-state-history-split — state compiled from a component's history): same pattern (pure projection, single append/write door, never hand-edited), different corpus (per-component state vs the system-wide intentions model) and different owner — cross-referenced, NOT merged (asked the why of each: local-component-truth vs carry-to-bare-machine-model — not exclusive-and-terminal).",
  "traces_to": [
    "Law 4 (a rule that matters is physics — `intentions-congruency-lab/` is regenerable from sources, not hand-maintained; the 'compiled view is never hand-edited' IOU in CLAUDE.md 'Rules awaiting physics')",
    "Law 6 (exactly one owner gates writes to `intentions-congruency-lab/` — the compile door; every trigger pokes it, none writes directly)",
    "Law 1 (the model is compiled once from structure, not re-derived by each reader — the same move as the projector, one corpus out)",
    "Law 8 (the compile carries a proof a hollow build could not pass — a dropped/mangled source goes RED)",
    "the roots table (CLAUDE.md: `intentions-congruency-lab/` is COMPILED from `intentions-not-beside-code/` + the beside-code charters — the folder carried to a bare machine to regrow the system)",
    "kinship: charter-state-history-split (the projector — same projection pattern, different corpus/owner; cross-referenced not merged)"
  ],
  "node_class": "code-seam",
  "state": "code-seam@v2: THINKME -> TICKETME -> BUILDME -> PROVEME -> [PROVED]   (crossed 2026-08-01 via emit_migrated — v1's LEARNME dissolved on the crossing, migrated_from on the record. The close was the RECORDS, not the code: the copies rebuild + whole-repo walk landed 2026-07-31 by Akien's ratification; today his 'fix the recompile … approved, go go go' closed edges (d)-(f), both proofs re-sealed netns green, 44 intentions in the lab, every poke counted at the trace wire.)",
  "gates": "proof gate (tester) — a proof a hollow build could not pass: give the compiler a known set of sources (`intentions-not-beside-code/` entries + beside-code charters) and assert the projected `intentions-congruency-lab/` contains exactly what the sources imply — every source represented, nothing invented, a changed source changes its output, a REMOVED source removes its projection (no stale carry-over); assert `intentions-congruency-lab/` is written ONLY through the compile door (a hand-edit is overwritten on next compile — the view is never authoritative); assert the compile is DETERMINISTIC (same sources -> same output, so drift is detectable). VALIDATION persisted beside-code via cairn/tester/validation_store.persist_validation and grep back. Durability gate: proven, persisted, committed (git).",
  "falsifier": "the compiler regenerates `intentions-congruency-lab/` as a faithful, deterministic projection of its sources — every source represented, nothing invented, removals propagated, a hand-edit to `intentions-congruency-lab/` reverted on next compile (the view is never a record of truth). Wrong-shape signals: (1) `intentions-congruency-lab/` is written by any door OTHER than the compiler (a second writer — Law 6 breach); (2) the compile is non-deterministic or lossy (a source silently dropped — Law 8 defect, and drift becomes invisible); (3) the compiler starts AUTHORING content not in its sources (it has become an editor, not a projector — the compiled view has become a rival record of truth, Law 7); (4) it grows to also own the projector's job (per-component state) — the two corpora collapsed when they were meant to stay distinct vantages; (5) it grows its OWN nightly scanner (that work belongs to the inspector regime — a compiler that inspects has taken on a second job it does not own).",
  "horizon": "first real recompile-on-write — when a charter or `intentions-not-beside-code/` source is written, a GATE fires, pokes this door, and `intentions-congruency-lab/` regenerates without a human running anything; re-check then: is beside-code source-walking fast enough on every write, or does the gate need to hand the compiler a CHANGED-SET rather than trigger a full reprojection? And revisit the projector kinship — if the two compilers end up sharing real projection machinery, factor the shared core out (not before the second member proves the seam).",
  "parent": null,
  "children": [
    "(buildable now, cairn) a — THE COMPILE DOOR: the pure projection `intentions-not-beside-code/` + beside-code charters -> `intentions-congruency-lab/`, as the single owner-gated write-door for `intentions-congruency-lab/`. Deterministic, lossless, removal-propagating; a hand-edit to the compiled folder is reverted on next run. This is the engine every trigger pokes (and /sorted, and `cairn compile`). Berths in cairn/ beside the folder it owns; this ticket migrates there to become its history when it proves. NO blocking deps — build this first.",
    "(the CLI face, waits on cairn-command) b — the `cairn compile` verb: a `bin/cmd/compile` that pokes the compile door from the command line — the manual hand-crank, one of the three triggers. A thin registration against the cairn-command dispatcher.",
    "(BUILT + PROVEN + WIRED 2026-07-23, host-seam) c — the WRITE GATE: 'a source was written' fires a poke of the compile door — the LIVE recompile-on-change (shrinking-footprint — the write IS the event, no polling). Built as a FileChanged HOST-SEAM: recompile_gate.sh (git, proven — 3 teeth) + a hook in .claude/settings.local.json (instance-space). Host-NOTICING, an IOU standing in for the owner-gate emit (the emit-chokepoint physics state-machine-physics.json tracks) that is the resting state — kin to cairn-command's PATH seam (a host dependency named as debt, not faked into physics), retires when that emit lands. Covers the beside-code charters; the intentions-not-beside-code/ half awaits a matcher/scope resolution (charter host_seam edges)."
  ],
  "waits_on": "child a waits on NOTHING (buildable now — build first). Child b (the `cairn compile` CLI face) — DONE via cairn-command. Child c (the write gate) — DONE as a FileChanged host-seam (host-noticing, an IOU standing in for the owner-gate emit state-machine-physics.json tracks; the emit lands later and retires the IOU, it no longer BLOCKS child c). SEPARATELY OWNED, not a wait here: the nightly INSPECTOR regime (held-inspectors-janitors-filters) may surface model-staleness as a compliance FINDING; its JANITOR — never this compiler — would poke the door to resolve it. Cross-referenced, owned there.",
  "provenance": "cast 2026-07-23 via /sorted, alongside cairn-command, from a single /intent that bundled a repo-scanner + an intentions-folder updater + a user-facing command runner. The name sharpened in the design conversation from `intentions-compiler` to `intentions-model-compiler` (Akien: 'right now that folder is the model', reserving `intentions-compiler` for a future prose-ingesting front-end his inner UI designer sees coming but has no intention toward yet). CORRECTION, same session: the completeness gate first flagged the scanner as a poller and I re-cast it as commit-fired — both wrong. Akien clarified the /intent's 'repo-scanner' IS the nightly INSPECTOR (held-inspectors-janitors-filters — runs once at midnight, each callback's condition is the thing inspected: code smells, misplaced files, post-build checks), a separate already-held node whose inspectors never act; the compiler's LIVE trigger is a GATE (the file-watching 'moved to gates'), not a scanner. So the bespoke scanner child was removed and the triggers corrected to CLI + /sorted + write-gate. Akien: 'sorted.' AMENDED 2026-07-30 (measured during ticket watchme-emits-a-probe, not built here): filed_edges (d)-(f) record that this ticket's own falsifier is falsified by measurement — 14 of 33 beside-code charters are outside the gather scope. This ticket stands at PROVEME; the finding is filed, not fixed, and names the design question the fix must answer first. TRACE-WIRED 2026-08-01 while this ticket stood at PROVEME (deploy pass, Akien-approved: 'start the trace wire on the four PROVEME components'): the component's door now counts its firings — green and red — through learning_block's trace wire; proof teeth ride the component's own proofs. RECONCILED 2026-08-01 (Akien: 'fix the recompile. this all looks good! approved, go go go!'): the record-vs-code drift is the thing fixed today — the code was rebuilt 2026-07-31 (copies, whole-repo walk, his ratification in the docstring) while this ticket still read 'filed, not fixed / wants Akien's signature gate'. Edges (d)-(f) carry their closes; both proofs re-sealed under netns 2026-08-01 (green, validations beside code — the 2026-07-23 seals described the dead _model.json shape); the voyage crosses PROVEME -> PROVED through the chokepoint, migrating the v1 string in the same act (emit_migrated journals migrated_from).",
  "filed_edges": [
    "(d) THE GATHER SCOPE IS THE FALSIFIER, AND IT IS FALSIFIED — measured 2026-07-30 (during ticket watchme-emits-a-probe, whose skill-charter edits provoked the check). This ticket's falsifier says 'every source represented, nothing invented'. gather_sources (cairn/intentions_model_compiler/compiler.py:170) scans exactly os.listdir(code_root)/<component>/intention+why.json with code_root=<repo>/cairn — ONE level, under ONE directory. Measured: 33 beside-code charters exist in the repo; the model carries 19 (+8 homeless = count 27). FOURTEEN ARE INVISIBLE: all ten skills/*/ (challenge, chart, commit, intent, loadslate, moreabout, note, sail, saveslate, sorted) plus bin/, launchers/, learning/, press_office/. Reproduce: `find . -name intention+why.json -not -path './.git/*' | wc -l` (33) against `python3 -c \"import json;print(json.load(open('CairnCommons/intentions-congruency-lab/_model.json'))['count'])\"` (27). · CLOSED 2026-08-01: the fix landed 2026-07-31 ahead of this edge's disposition — gather_sources walks the WHOLE repo (<repo>/**/intention+why.json), _model.json is DELETED, the lab holds byte-identical copies. Measured at close: 44 sources copied (35 beside-code + 9 homeless), proof tooth test_the_real_repo_reaches_both_trees_and_every_charter pins the reach.",
    "(e) THE bin/ EDGE GREW TEETH AND NOBODY WAS TOLD. The state cursor above already filed 'a repo-root bin/ charter is not yet gathered (gather scans cairn/*/)' on 2026-07-23 — a one-charter edge on a directory with no state. On 2026-07-29 ticket skills-ride-the-chokepoint gave the ten skills charters AND state.json/history.json — they became beside-code components in every sense CLAUDE.md names — and the edge silently became a 14-charter hole. Nothing reported the change because the compiler REPORTS SUCCESS AND A COUNT AND NEVER SAYS WHERE IT DID NOT LOOK: same silent-accept class as troubles/append-door-has-no-schema-gate. Consequence, not theory: /intent's dup/conflict gate (skills/intent/SKILL.md) reads _model.json, so 14 intentions cannot be found by the cheapest gate in the system (Law 1's own door), and CLAUDE.md's 'carried to a bare machine and regrow the system' is short by 14 charters. /sorted's write-through rule is the one contradicted by measurement: a skill charter write pokes recompile_gate.sh and the door dutifully recompiles WITHOUT it. · CLOSED 2026-08-01 by the same fix, with the honest residue named: the walk skips _SKIP_DIRS (.git, __pycache__, proofs, validations, …) — a charter berthed under one of those would be invisible, and none legitimately berths there. The silent-count failure class shrank with the envelope: the lab IS the corpus now, so a missing charter is a missing FILE (diffable), not a number nobody can check.",
    "(f) THE FIX IS NOT TAKEN HERE, DELIBERATELY. Widening the glob is a two-line change and exactly the kind of silent widening /sail forbids: which directories are model sources is a DESIGN question (is bin/ an intention? is launchers/? does the scan walk the repo or read a declared roster?), and a wrong answer quietly changes what the dup-check sees. Cheapest honest shape, unratified: gather takes a declared list of scan roots and REFUSES ON A CHARTER IT FOUND OUTSIDE THEM, so the next gap is loud at the door instead of measured six days late. Wants Akien's signature gate. · CLOSED 2026-08-01 — the design question this edge refused to answer silently was answered by AKIEN, 2026-07-31, unambiguous (recorded in the door's own docstring): the scan walks the repo; every intention+why is a source; the one-artifact _model.json dies. The signature this edge waited on arrived twice — that ratification, and today's 'fix the recompile … approved, go go go' (gate-record lb-20260801-f4f21cbb). The declared-roster alternative died with the envelope."
  ],
  "watchme": "none, because this node's efficacy is already instrumented WITHOUT a probe, and a probe would be a second, weaker instrument for the same question. The compile is event-fired on READ (recompile_gate.sh), and every poke is counted at the trace wire — so 'is the model actually consulted, and is it fresh when consulted?' is answered continuously by the counter, not by a watch that would have to accumulate the same evidence a second time. RECORDED 2026-08-03, late and honestly: this node crossed to v2 on 2026-08-01 via emit_migrated with an author present, and the author dropped the watch without recording the reason — so watchme_spec's sweep has been red on this one ticket since that crossing. It is not fallout of the 2026-08-03 scrub sweep (measured: red on the pre-session tree too); it is the debt that sweep surfaced."
}
