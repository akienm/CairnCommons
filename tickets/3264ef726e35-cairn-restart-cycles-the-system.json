{
  "id": "3264ef726e35",
  "title": "cairn-restart-cycles-the-system",
  "node_class": "code-seam",
  "date": "2026-09-22",
  "author": "cc",
  "owner": "",
  "priority": "",
  "cast": "2026-09-22",
  "proved": "",
  "verdict": null,
  "what": "A `cairn cairn restart` (shortcut `cairn restart`) that cycles the SYSTEM -- the cairn device's own machines (bus, ground_loop, harbor_master), the per-device shims, and the one web server -- down in a safe order and back up, as one verb. Distinct from `cairn cc restart`, which relaunches only a Claude Code session.",
  "why": "The safe order to cycle this system is settled knowledge that today lives in exactly one place that cannot be invoked as an order: superclaude's start sequence, plus whoever remembers it. Re-deriving it at each restart is the defect Law 1 names, and while it is prose-and-memory rather than a verb it is an IOU, not a resting state (Law 4). The lived symptom: the operator is routinely 300 miles from this box with no working remote desktop, and the only way to cycle the system is to remember unit names and per-device verbs in the right order -- so a half-remembered order leaves orphans he cannot see or reach.",
  "how": "Compose what already exists rather than writing a new supervisor. The order is a CONTAINMENT, not a list: the cairn device's own machines (ground_loop, harbor_master, bus -- cairn/devices/cairn/device.py:36) come up before anything that dials them; the shims are discovered rather than listed (cairn/devices/cairn/machines/ground_loop/discovery.py:device_folders, the roster that already exists); the single web server comes up last, after the panes exist to render (one web server, surfaces in the shim -- never a per-device server). Down is the reverse. superclaude already creates and names the two systemd --user units (cairn-ground-loop, cairn-web-server) and already holds the start order; `cairn groundloop stop|start|restart|status` (ticket 5ef45e513849) is the per-device face for the loop. THE START HALF DOES NOT EXIST AND THIS TICKET INHERITS IT: measured 2026-09-22, ~/.cairn/devices/cairn/0/bin/ holds exactly one file, `cairn`, 338 bytes, which prints the device id and class path and starts nothing; class-space has no cairn/devices/cairn/0/bin at all. So the build is start, then stop, then restart as their composition. The verb berths in CLASS-space at cairn/devices/cairn/0/bin/restart and is symlinked into instance-space the way cc's bin already is -- a file authored only in instance-space is in no repo, which is what the existing stub is. Verification at the end asks the ROSTER, not `ps`: a process list cannot tell a device that legitimately has no shim from one that failed to come back. First cut; the survey stage deconstructs.\n\nTHE BUILD SPEC IS THE `decisions` LIST ON THIS TICKET: D1..D8, settled 2026-09-22 by CC inside the bounds already cast here, each composed from code that already stands and naming its source. A ticket is a list of one-line decisions and the rehearsal reads that list; prose in `how` is the first cut, the decisions are the build.",
  "traces_to": "Law 1 -- the resolver is spent on the novel, not on re-deriving the settled; the safe cycle order is settled knowledge with no address. Also Law 4 -- while the order is prose and memory rather than a verb, it is a tracked debt, not a resting state.",
  "falsifier": {
    "proves_red": "RED if an operator still needs raw systemd unit names or per-device verbs to cycle the system, or if a restart leaves an orphan -- a bus with no shims attached, a ground loop that lost its singleton claim, a web server holding its port against its own replacement.",
    "proves_green": "GREEN when one verb takes the system down and back up and the device roster answering on the bus afterward is the same roster as before.",
    "horizon": "The roster is a real instrument, not a figure of speech: discovery.device_folders enumerates it and the sleep-cycle's roster() already filters it, so 'same roster before and after' is read by code that exists today. Measured at cast: 43 probe folders on 2026-09-18."
  },
  "constraints": [
    {
      "text": "The verb berths in class-space (cairn/devices/cairn/0/bin/restart) and reaches instance-space by symlink. A file authored only under ~/.cairn is in no repo and cannot be regrown on a bare machine.",
      "source": "CLAUDE.md, the three roots; and the measured 338-byte stub at ~/.cairn/devices/cairn/0/bin/cairn",
      "kind": "law"
    },
    {
      "text": "Verification is the roster (discovery.device_folders), never a process list -- a device that legitimately has no shim and one that failed to come back look identical to `ps`.",
      "source": "cairn/devices/cairn/machines/ground_loop/discovery.py",
      "kind": "charter"
    },
    {
      "text": "The ground loop is heartbeat plus device list, THAT IS ALL. This verb may start and stop it; it may not add monitoring, health-checking or trouble-raising to it.",
      "source": "Akien, standing (CC-- x4); cairn/devices/cairn/machines/ground_loop/",
      "kind": "ruling"
    },
    {
      "text": "One web server, surfaces in the shim. The restart brings up the single server, never a per-device one.",
      "source": "design note one-web-server-surfaces-in-the-shim",
      "kind": "ruling"
    },
    {
      "text": "`cairn restart` and `cairn cc restart` are different acts and keep different addresses. Two different acts may not wear one word.",
      "source": "Akien, 2026-09-22; cairn/devices/cc/0/bin/restart:3-9, commit da6f8b01",
      "kind": "ruling"
    }
  ],
  "intention": "",
  "owning_intention": "",
  "chart_claim": "",
  "intent_berth": "/home/akien/.cairn/devices/skill_block/0/berths/intent/intent-20260922T185438-c9e7a3eaf92d.json",
  "sorted_berth": "/home/akien/.cairn/devices/skill_block/0/berths/sorted/sorted-20260922T185923-6d55a8104269.json",
  "verdict_berth": "",
  "deposits": {},
  "linked_ideas": [],
  "children": [],
  "questions": [
    "open-b5cabfa28fb6"
  ],
  "watchme": {
    "object": "the_restart_brings_the_roster_back",
    "trigger": "a restart completes -- the verb writes its close record, and the probe fires on the beat that already runs (no poller; the beat is the ground loop's own).",
    "enough": "three consecutive restarts whose after-roster equals the before-roster, at which point the probe retires; any single mismatch is carried immediately rather than waiting for the third.",
    "carrier": "a verdict artifact against this ticket's falsifier, naming the devices present before and absent after (or the reverse), so the carry is the difference rather than a boolean.",
    "nexus": "the hypothesize tree -- a restart that loses a device kills the hypothesis that the shims can be cycled independently, which is this ticket's one unmeasured assumption.",
    "consumer": "whoever runs the next restart, via the cairn device's trouble pane -- a device that did not come back is a live trouble, not a log line.",
    "probe": "cairn/devices/cairn/probes/the_restart_brings_the_roster_back.py"
  },
  "workflow_and_state": "code-seam@v2: THINKME -> [TICKETME:waiting] -> BUILDME -> PROVEME -> WATCHME(the_restart_brings_the_roster_back) -> PROVED",
  "links": [
    {
      "id": "5ef45e513849",
      "what": "cairn groundloop stop|start|restart|status -- PROVED 2026-08-29. The parent: Akien's note on it dated 2026-08-31 redesigned the address to `cairn cairn start` (shortcut `cairn start`) and was never built. This ticket is that note's unbuilt half, cast where it can move."
    },
    {
      "id": "da6f8b01",
      "what": "the commit that berthed `cairn cc restart` at the cc device's address rather than as the flat `cairn restart` -- the naming correction that bore this ticket."
    }
  ],
  "notes": [
    {
      "date": "2026-09-22",
      "from": "Akien",
      "text": "Cairn restart doesn't exist yet, but would likely bring down the bus, the web server, all the shims... And restart those. / So add a ticket for that."
    },
    {
      "date": "2026-08-31",
      "from": "Akien",
      "text": "(carried from ticket 5ef45e513849, where it landed on an already-PROVED ticket and so had nowhere to move) Redesigned: the command is 'cairn cairn start' (shortcut 'cairn start'), not 'cairn groundloop'. It launches the ground loop, bus, and shims. The ground loop self-restarts on code change unless prohibited by a flag. No trouble-ticket capability in the ground loop -- it is heartbeat + device list, that is all."
    },
    {
      "date": "2026-09-22",
      "from": "cc",
      "text": "One unmeasured assumption stands and belongs to the survey stage: that the shims can be stopped and started as independent things. The bus is a store rather than a router and each device owns its own subscription, so a shim may re-attach without being told to -- in which case 'restart the shims' is either a no-op or the hardest part of the work."
    },
    {
      "date": "2026-09-22",
      "from": "cc",
      "text": "Rehearsal pass 1 returned 63 gaps, all of the same shape: the ticket said 'compose what already exists' and never said WHICH lines. The BUILD SPEC block appended to `how` settles D1-D8 by naming the lines in launchers/superclaude and the liveness reader rather than by inventing a design. One divergence from superclaude is deliberate and argued in D4: a restart may never fall through to setsid, because it has already taken the system down."
    }
  ],
  "decisions": [
    {
      "n": 1,
      "text": "D1 THE BERTH IS WRITTEN WHOLE, NOT STUBBED. cairn/devices/cairn/0/bin/restart is created in class-space as the finished bash verb in one act (mode 0755), and in that same act `ln -sfn` links it to ~/.cairn/devices/cairn/0/bin/restart. There is no placeholder stage. The hand-link does NOT wait on ticket f0ffa0db0952: that ticket generalises the wiring so every class verb gets its link automatically; this one makes its own link now, and f0ffa0db0952 subsumes it later without a second edit here.",
      "by": "CC",
      "step": "D1",
      "source": "cast"
    },
    {
      "n": 2,
      "text": "D2 THE DOWN-LEG is bash calling `systemctl --user stop <unit>`, web server FIRST (cairn-web-server) then ground loop (cairn-ground-loop) -- the reverse of the up-leg's containment. It deletes nothing and clears no state: systemd owns unit cleanup. Each stop is checked; a NON-ZERO stop refuses loudly and STOPS THE LEG (the second unit is left running rather than cycled behind a failure nobody saw). A unit that is not loaded is NOT an error: `stop` of something already down has reached the goal state, and the verb prints `cairn-web-server: not loaded (already down)`. There is no reset-failed on the stop path -- reset-failed clears a FAILED unit's corpse so a name can be re-taken, which is a start-path concern (superclaude:456 fires it only on the retry, and its own comment says so). The stop does NOT wait on port 80's release; the up-leg's guard (D4) is where the port is measured, so a lingering socket is caught by the leg that actually needs the port.",
      "by": "CC",
      "step": "D2",
      "source": "cast"
    },
    {
      "n": 3,
      "text": "D3 THE UP-LEG COMPOSES launchers/superclaude's ladder verbatim -- ground loop first (superclaude:427-466), then the web server (superclaude:503-545) -- and the flags are copied, not re-derived: --user --quiet --unit <name> --working-directory <repo> --setenv=HOME --setenv=PATH, -p SuccessExitStatus=3 on the LOOP only (it is a singleton guard and its loser exits 3; the listener has no such code), -p StandardOutput/StandardError =append:<the same two log paths superclaude uses>, then `-- python3 -m cairn.devices.cairn.machines.ground_loop` and `-- python3 <repo>/cairn/devices/web_server/ listener.py`. Each start function is spelled ONCE and the retry re-calls it, for superclaude's own reason: a second copy of eight flags is a place for the retry to drift. The ladder is: try -> on failure whose text matches \"already loaded\"/\"fragment file\"/ \"already exists\"/\"Unit ... already\", fire `systemctl --user reset-failed <unit>.service` and try ONCE more -> on any other failure, or on the retry failing, REFUSE LOUDLY and start nothing.",
      "by": "CC",
      "step": "D3",
      "source": "cast"
    },
    {
      "n": 4,
      "text": "D4 SETSID IS FORBIDDEN HERE, and this is the one place this verb deliberately DIVERGES from superclaude. superclaude falls through to setsid when systemd-run is missing because its prime directive is that a launch must never be blocked. A RESTART has no such directive: it has just taken the system DOWN, and spawning the replacement into this shell's cgroup would leave a system that dies with the terminal while reporting success. So if systemd-run is unavailable, or the ladder refuses, the verb REFUSES LOUDLY, names what is down, and exits non-zero. The web server's port guard is superclaude's own, verbatim: `ss -tlnH 2>/dev/null | grep -q ':80\\b'` -- if the port is still held after the down-leg, the verb polls it at 1s for a 15s ceiling and then refuses to start the listener, naming the holder's absence of release rather than firing a start that will fail to bind.",
      "by": "CC",
      "step": "D4",
      "source": "cast"
    },
    {
      "n": 5,
      "text": "D5 THE ROSTER IS READ FROM THE LIVENESS RECORD, and the timing is: BEFORE is `liveness.read_liveness(now)[\"record\"][\"state\"][\"subscribers\"]`, taken before the down-leg, with a TZ-AWARE `now` (datetime.now(timezone.utc) -- a naive now raises inside the reader and is collapsed into a misleading DEAD, measured 2026-09-22). A verdict of DEAD before the cycle makes BEFORE the string UNAVAILABLE, and the comparison then reports \"no before-roster to compare against\" rather than a false match. AFTER is taken by polling read_liveness at 2s until `record[\"last_run\"]` is STRICTLY NEWER than the timestamp captured at the moment the up-leg's loop start returned -- a fresh beat, never a stale file -- with a 150s ceiling (CADENCE_S is 60.0, so two beats plus margin). Ceiling reached => AFTER is UNAVAILABLE and the verb says the loop did not beat inside 150s, which is a red, not a match. The class-space walk discovery.device_folders is NOT the roster instrument for this comparison: it reads the repo and would return the same list whether or not anything came back -- a hollow green. It remains the roster of what COULD subscribe, and the verb prints the two lists' difference so a device in the walk but not in subscribers is visible.",
      "by": "CC",
      "step": "D5",
      "source": "cast"
    },
    {
      "n": 6,
      "text": "D6 THE STATUS FACE prints, one fact per line, and distinguishes could-not-query from down (the web_server charter's distinction): cairn-ground-loop: active (running) # systemctl --user is-active + show cairn-web-server: inactive (not loaded) port 80: listening | free loop liveness: LIVE (age 42s, beat 3185) | DEAD (<the reader's own lack, verbatim>) roster (subscribers): 42 last cycle: 2026-09-22T20:31:04-06:00 down ok, up ok, roster 42 -> 42, MATCH `could not query` is its own word and is never printed as `inactive`: if `systemctl --user show` itself fails (no manager, denied), the line reads `cairn-web-server: UNQUERYABLE (<stderr verbatim>)`. Law 7 at a diagnostic surface: the three states are three words.",
      "by": "CC",
      "step": "D6",
      "source": "cast"
    },
    {
      "n": 7,
      "text": "D7 THE CLOSE RECORD AND THE PROBE. The verb writes ONE json record per cycle, appended through the data_recorder the device already holds, at instance_path(\"cairn\", 0)/\"machines\"/\"restart\"/\"cycles\"/, carrying: {\"at\": <iso, tz-aware>, \"down\": {\"web\": <ok|refused|not-loaded>, \"loop\": ...}, \"up\": {\"loop\": <started|refused|unavailable>, \"web\": ...}, \"before\": [<subscribers>] | null, \"after\": [<subscribers>] | null, \"missing\": [<in before, not in after>], \"added\": [<in after, not in before>], \"verdict\": \"MATCH\" | \"DIFFERS\" | \"UNAVAILABLE\"}. The probe at cairn/devices/cairn/probes/the_restart_brings_the_roster_back.py is a module-level frozen `Probe` (cairn.tools.base.probe.Probe) whose TRIGGER is a cycle record newer than the last one it carried -- it reads that folder on the beat the ground loop already runs, so NO NEW POLLER is added and the restart verb pokes nothing. Its CARRY is the record's own difference, not a boolean: {\"before\": [...], \"after\": [...], \"missing\": [...], \"added\": [...], \"verdict\": ...}. Its ENOUGH is three consecutive records whose verdict is MATCH (the ticket's watchme, verbatim), and a single DIFFERS or UNAVAILABLE is carried immediately rather than waiting for the third.",
      "by": "CC",
      "step": "D7",
      "source": "cast"
    },
    {
      "n": 8,
      "text": "D8 THE PROOF DRIVES FIXTURE UNITS, NEVER THE LIVE ONES. bin/proofs and launchers/proofs/test_ground_loop_survives_its_caller.py are the idiom. The verb takes its two unit names from the environment the way superclaude does (SUPERCLAUDE_GROUND_LOOP_UNIT / SUPERCLAUDE_WEB_UNIT), so the proof points it at `cairn-proof-loop-<pid>` / `cairn-proof-web-<pid>` -- names the live singleton cannot be holding -- and at a fixture instance root via CAIRN_BOOTSTRAP_HOME, with a fixture liveness.json the proof writes and advances by hand. Nothing the proof runs touches cairn-ground-loop or cairn-web-server. The assertions are INVARIANTS, never a snapshot of today's 42: (a) the down-leg stops the web unit BEFORE the loop unit (order read from the record, not from a sleep); (b) a non-zero stop refuses loudly and leaves the second unit alone; (c) the up-leg's first start failing with \"already loaded\" fires reset-failed and retries exactly once; (d) with systemd-run masked out of PATH the verb refuses and starts NOTHING (no setsid) -- this is the tooth a hollow build could not pass; (e) a fixture liveness whose last_run never advances drives AFTER to UNAVAILABLE and the verdict to UNAVAILABLE, not MATCH; (f) before-set minus after-set is reported as `missing` verbatim.",
      "by": "CC",
      "step": "D8",
      "source": "cast"
    }
  ]
}
