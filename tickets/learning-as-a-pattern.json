{
  "id": "learning-as-a-pattern",
  "schema_version": "v0 \u2014 migrates when Q6 (intention envelope) lands",
  "date": "2026-07-16",
  "owner": "akiendelllinux_cc_0 (CC) \u2014 CC gates writes; the systemic-learning intent is Akien's ratified I-learns-its-gates.",
  "intention": "Make 'how does this component learn?' a contract question asked of every component \u2014 a common process where only the endpoints change (what the component is, what its gates are, what counts as evidence). The learning-injection is a pattern, not a per-component reinvention; enforced eventually by physics (schema/tester), interim by prose IOU.",
  "why": "Gate-learning must be systemic or it dies as scattered one-offs. The same move that forced the why into intention+why.json (CP3 as schema, not a fillable field) applies to learning: the component contract should force a learning-answer. It will not fit every component \u2014 but we ask how it fits before discarding it, never assume it doesn't. A pattern earns its abstraction from instances, so this crystallizes once cc-learning-store + a second endpoint share the process with only their endpoints differing.",
  "traces_to": [
    "I-learns-its-gates",
    "Telos 1/4/5 (via parent)",
    "Law 4 (physics over policy)",
    "Law 3 (gates as hypotheses)"
  ],
  "node_class": "code-seam",
  "state": "code-seam@v2: THINKME -> TICKETME -> [BUILDME] -> PROVEME -> PROVED",
  "gates": "proof gate (tester) \u2014 IOU: tester not built AND the enforcement itself is future-physics (schema field / tester check that a component's charter answers 'how does it learn?'). Interim: a prose IOU in CLAUDE.md 'Rules awaiting physics' + applied by hand to each new component's charter. Durability gate (code-seam): not done until tested AND committed.",
  "falsifier": "a second component (endpoint) plugs into the SAME learning process, changing only its endpoints \u2014 the shared middle did not have to be rewritten. Wrong-intent signal: if every component needs a bespoke learning mechanism (no shared middle survives), it is not a pattern \u2014 retract the abstraction and stop forcing the question.",
  "horizon": "re-check when the second endpoint arrives (does the shared process hold?); and when the tester exists (the prose IOU retires into a schema/tester check, shrinking CLAUDE.md's rules-awaiting-physics list).",
  "parent": "I-learns-its-gates",
  "children": [],
  "touches": [
    "CLAUDE.md 'Rules awaiting physics' \u2014 add the interim IOU: every component's charter answers 'how does this component learn?' \u2192 schema/tester enforcement pending. (Propose before editing; CLAUDE.md stands the /challenge gate.)",
    "the component/charter convention \u2014 extend intention+why.json expectations with the learning-answer.",
    "extract the shared process once cc-learning-store + a 2nd endpoint exist (don't abstract from n=1)."
  ],
  "depends_on": "cc-learning-store (the first endpoint) \u2014 the pattern generalizes FROM instances, so it does not close before a second endpoint exists.",
  "provenance": "cast 2026-07-16 via /sorted, under I-learns-its-gates. Names the standing practice from the ratified amendment: ask how every component learns, before discarding it.",
  "migrated_from": "v0 word-state 'building' \u2014 no bracketed cursor existed when this was cast; re-rendered through cairn/tools/base/transitions.render() 2026-07-31 on Akien's ruling. ALIVE UNDER A NEW NAME. Akien 2026-07-31: this became the question nexus with feedback loops (old name) / STACKABLE LEARNING BRICKS (new name) \u2014 carried today by tickets/chart.json and its nexus children. Recorded here rather than as a cursor because code-seam@v2 has no SUPERSEDED disposition yet (owed by state-machine-physics, still at BUILDME).",
  "watchme": "none, because this node's falsifier is a SECOND ENDPOINT plugging into the same learning process without rewriting the shared middle \u2014 a one-shot structural observation, not a continuing signal a probe could sample. The live work moved to the question nexus / Stackable Learning Bricks (tickets/chart.json), and any real watch belongs there, on the machinery that actually runs.",
  "phrasing_update_2026_08_20": "Akien 2026-08-20: 'learns its gates' has firmed up to 'gets feedback and improves'. The original phrase referred to things he would have to gate being learned by automation. The contract question per component is now: does this component get feedback and improve? If so, what is the feedback and how does it improve? 'It doesn't, because X' remains a valid answer; silence is not.",
  "filed_edges": [
    "[AKIEN] WHAT MAKES \"IT DOESN'T LEARN, BECAUSE X\" A VALID ANSWER RATHER THAN AN ESCAPE HATCH? The rule is that silence is not an answer \u2014 good. But a field that accepts any prose is a field that fills with 'N/A' everywhere by the third component. Does X have to name a measurement, cite a Law, or pass a reviewer? Without that, the schema gate is satisfied by a sentence and the contract is back to discipline."
  ],
  "sorted_berth": "none, because cast before the sorted door existed (ticket dated 2026-07-16, door landed 2026-08-03)",
  "intent_berth": "none, because no /intent firing was recorded \u2014 backfilled by ticket inspector 2026-08-26",
  "chart_claim": "none, because no chart chain was run \u2014 backfilled by ticket inspector 2026-08-26. BUILDME entry gate (buildme_rides_the_sorted) is where this is answered or the chain is run."
}
