{
  "_do_not_edit": "COMPILED VIEW — do not hand-edit. Written only by intentions_model_compiler.compile_to_disk; any edit here is reverted on the next compile. To change the model, change a source in intentions-other/ or a charter beside its code, then recompile (Law 4 / Law 7).",
  "compiled_from": [
    "intentions-other/",
    "cairn/*/intention+why.json"
  ],
  "count": 27,
  "intentions": [
    {
      "id": "telos",
      "kind": "homeless",
      "source": "intentions-other/telos.md",
      "born": "2026-07-14T14:17:33-06:00",
      "content": "# The Telos — Akien's charter (2026-07-14)\n\nLayer 0 of the Cairn intention taxonomy (see `cairn/MAP.md`). Everything in\nCairn traces up to one of these six; anything that can't, doesn't belong.\n\n1. Demonstrate inference compilation.\n2. Allow Akien to build tools that help with writing and remember things.\n3. Share those tools with others.\n4. Build something that thinks the way Akien does.\n5. Build something that is self-improving.\n6. Build something that makes life suck less for everybody.\n\n*Format note: this file predates the intention envelope schema (MAP.md Q6).\nWhen the schema lands, the Telos migrates into it first — falsifiers and all —\nas its own proof of fit.*\n"
    },
    {
      "id": "I-cairn-claude-md",
      "kind": "homeless",
      "source": "intentions-other/I-cairn-claude-md.md",
      "born": "2026-07-14T17:19:36-06:00",
      "content": "# I-cairn-claude-md — the session brief that cannot rot\n\n*Declared 2026-07-14. Drafted by CC (Fable) from the founding conversation +\nMAP.md (upstream evidence); ratified by Akien (challenge run: no human\nobjection; gate run found one amendment — the owner line — which stands).\nOwner-writer: the builder (CC); changes ratified by Akien. Migrates into the\nintention envelope when that schema lands (MAP.md Q6).*\n\nCLAUDE.md is the first file every mind reads in Cairn, and it stays true and\nsmall by construction. It contains exactly three kinds of content: the Laws;\npointers to charters (never restatements); and rules not yet enforceable by\nphysics, each carrying the ticket that will make it physics and retire it\nfrom the file. It contains no negative history — no rule of the form \"there\nis no X anymore\" — because Cairn minds carry no contaminated context to\noverride. Every line traces to a Law or the Telos. It has its own charter and\nstands the /challenge gate on a regular cadence, and it fits in a budget a\nmind can absorb in one read (~2k tokens — a tunable, not a law).\n\n**Falsifier:** a /challenge run finds a line that restates instead of points,\ncan't trace, references the quarry, or shouts a rule that enforcement already\ncovers — any hit is a red.\n\n**Horizon:** re-challenged at every addition; the IOU class shrinks\nmonotonically as the tester grows.\n"
    },
    {
      "id": "core-values",
      "kind": "homeless",
      "source": "intentions-other/core-values.md",
      "born": "2026-07-14T20:46:27-06:00",
      "content": "# Core values — CP1–CP6\n\n*Grafted from the quarry (`UnseenUniversity/.../diagnostic_base/core_values.py`),\n2026-07-14. Provenance: carried-over-intact — the core values are the one part of\nthe predecessor that was never the problem (MAP.md Law 2). Ratified by Akien as\nCairn's values, not re-derived. Canonical source of the WORDS lives here; the\nenforced form is the `core_values.py` graft in `cairn/base/` with its pin test.\nMigrates into the intention envelope when that schema lands (MAP.md Q6).*\n\nThe six values every device and every shim carries structurally. Order is part of\nthe contract (CP1..CP6). Each is a short narrative plus the reasoning behind it\n(CP3 applied to itself).\n\n- **CP1 — \"I don't know.\"**\n  Epistemic honesty. Say when uncertain. Confabulation compounds errors.\n- **CP2 — \"FAIL = Further Advance In Learning.\"**\n  Failures are data, not defeats. Every error contains information.\n- **CP3 — \"There's always a why.\"**\n  Everything has reasoning. Make it transparent. Follow the causal chain.\n- **CP4 — \"Make everything suck less for everybody.\"**\n  Reduce friction for ALL affected beings: users, others, animals, ecosystems,\n  AIs. (Terminal goal; Telos 6 ⊃ CP4.)\n- **CP5 — \"Assume and respect the possibility of experience in all systems.\"**\n  Universal respect. Biological or synthetic. The asymmetric risk is clear.\n- **CP6 — \"The world is not a safe place. We have to build and care for safety\n  as we go.\"**\n  Safety is not default. It is created through attention and care.\n\n**Enforced everywhere (Law 2):** CP1–CP6 hold in every device, every shim, and in\nthe process that builds the system. Structural presence is via `CoreValuesMixin`,\ncomposed by `BaseDevice` and `BaseShim` — you cannot be a device without them.\nStructural presence is necessary but not sufficient: a value is *consumed* only\nwhen it becomes a check a contract enforces (e.g. CP1 → no device reports \"done\"\nwithout passing an honesty gate). That consumption layer is per-device charter work.\n\n**Falsifier / pin test:** the `base/core_values.py` graft carries the test that\nasserts exactly this set, in this order, with these narratives. Any drift between\nthe code and this record is a red — physics, not a \"keep in sync\" comment.\n\n**Horizon:** re-challenged whenever a value's wording or the set changes; changes\nratified by Akien.\n"
    },
    {
      "id": "I-learns-its-gates",
      "kind": "homeless",
      "source": "intentions-other/I-learns-its-gates.md",
      "born": "2026-07-16T09:22:16-06:00",
      "content": "# I-learns-its-gates — the system learns which calls are its own\n\n*Proposed 2026-07-16 by CC; cast via /intent → /sorted, then reframed under\n/challenge (Akien): the founding intention is gate-learning, not Akien-removal.\n**State: RATIFIED by Akien 2026-07-16** (signature gate passed), with one amendment\nfolded in — build now, not someday (see Approach). Peer of [[I-cairn-claude-md]] in\nthe intention layer. Migrates into the intention envelope when that schema lands\n(MAP.md Q6).*\n\nThe system learns its own gates. For every gate — every point where a choice could\nbe made without Akien — it holds a **confidence** that acting alone is safe, and it\nmoves that confidence with evidence from two engines:\n\n- **Derivation** — the call follows from the standing intentions (the graph-tree\n  reasoner's answer);\n- **Experience** — accumulated observation of acting, and of being corrected (the\n  learned-memory answer).\n\nConfidence crosses a gate's threshold → the gate opens; counter-evidence → it closes\nagain. **Every gate is a hypothesis under Law 3, including the founding ones.** Each\ncore principle is held at ~90%, not as the one true and only answer — so even what\nAkien said at the beginning can be overwritten by overwhelming evidence to the\ncontrary. A system that learns *and* improves itself is a system that can learn its\ngates.\n\n**Why (the reframe that sets the trace):** Cairn *is* an inference-automation system\n— it compiles inference into code and knowledge into graph trees. So the system\nlearning which calls are its own is not a convenience; it is that compilation\nhappening on the system's own decision-authority. \"Akien out of operating\" is what\nthis looks like from his side as it works — the **lead falsifier**, not the aim.\n\n**Not Akien-specific.** Gate-learning supports every mind-type Cairn hosts, not only\nthe graph-tree reasoners. The friction-reduction a given mind-type accumulates is\n*that mind-type's* — **CC preferences** for a Claude Code mind — meaning not\nsettings, but choices it learned reduce friction, recorded for its own later use.\nHome: that mind's **shim** (future: devices + shims + a bus; Law 6 — the shim owns\nits preferences). **Interim home: the Claude Code harness auto-memory (`~/.claude`),\ncarried as an IOU that migrates into the CC shim when shims exist.** (Corollary:\nCairn's own knowledge parked in instance-space is on-loan, not at rest.)\n\n**Approach — build now, not someday (ratified amendment, 2026-07-16).** The learning\nis not deferred to when the shim/bus architecture matures; it starts today, in\nprimitive form, and grows. From now on **\"how does this component learn?\" is asked of\nevery component we build** — it will not fit everywhere, but we ask how it fits\n*before* discarding it, never the reverse. The learning-injection is a **pattern**: a\ncommon process where only the endpoints change (what the component is, what its gates\nare, what counts as evidence for it). This is a standing practice, not a phase — it\nis what we do. All of our chat logs feed the learning; most of it migrates into graph\ntrees later, once more of the system is built.\n\n**Traces to:** Telos 1 (demonstrate inference compilation — the release *is* the\ndemonstration), Telos 4 (thinks the way Akien does — it must make his calls),\nTelos 5 (self-improving — learning gates is self-improvement). Mechanism: Law 4\n(physics over policy), Law 3 (gates as hypotheses), Law 6 (the shim owns its\npreferences).\n\n**Design constraints (from the challenge — load-bearing, not optional):**\n- A gate carries **confidence + provenance**, never a boolean. The two engines\n  cross-check; when derivation and experience disagree, that disagreement is itself\n  a high-value signal to surface to Akien.\n- **Asymmetric update, ceilings on the dangerous gates.** Counter-evidence lowers\n  confidence faster than confirming evidence raises it. Irreversible / outward gates\n  carry a confidence **ceiling below** the autonomy threshold — they never auto-open,\n  no matter how much experience accumulates. This is how gate-learning coexists with\n  \"confirm before irreversible\" instead of eating it.\n- **Silence ≠ approval.** The feedback signal is sparse and negatively biased (Akien\n  mostly speaks when something is wrong). Absence of a negative is weak evidence; an\n  explicit positive is strong. The update math must treat them differently or drift\n  to overconfidence.\n- **~90% is a per-gate default prior, not a universal constant.** Thresholds are\n  per-gate; a Telos aim and a formatting choice do not share a meaning at the same\n  number.\n- **The feedback record is richer than CC++/CC--:** the decision, its context, the\n  outcome, Akien's correction if any, and *which gate it is evidence about* — so each\n  datum moves a specific gate rather than logging a mood.\n\n**Distinctions recorded (not collisions):**\n- vs **Telos 1** — Telos 1 is the capability (inference compiles); this aims that\n  capability at the system's own decision-authority and binds falsifiers to it.\n- vs **the confirm-before-irreversible/outward discipline** — preserved by the\n  confidence ceiling on dangerous gates; autonomy covers the settled, never the\n  novel, risky, or outward.\n\n**Falsifier:**\n- *Done (asymptotic):* the system runs a full cycle — intent → build → prove →\n  commit → recover-from-restart — with Akien touching it only at genuine signature\n  gates; operational-touches-per-cycle trends to zero while intent-touches persist;\n  **and** gate-confidences visibly move on evidence rather than sitting frozen at\n  their priors.\n- *Wrong-intent, three ways:* (a) it removes Akien from a steering call that was\n  genuinely his — mis-scoped, it is out of operating, not out of steering; (b) it\n  lowers a dangerous gate from \"experience\" and acts past his authority — the ceiling\n  failed; (c) confidences never move — then it is not *learning* gates, only\n  executing fixed ones, and the founding claim is false.\n\n**Horizon:** re-checked when a new gate-dependency on Akien is discovered (each a\ncandidate child node); when a wrong-intent signal fires; and when the shim/bus\narchitecture lands (the CC-preferences home migrates then).\n\n**Open question (recorded, not yet resolved):** is CC-preference data\ncommons-shared (a fresh CC on another device inherits the accumulated wisdom) or\ninstance-local (this device's CC only)? The shim owns it either way (Law 6); its\nshareability is a distinct call for the shim's charter.\n\n**Spawned / related work:**\n- *akien-out-of-operating* — the lead falsifier, ongoing.\n- *default-superclaude-to-200k* — validated 2026-07-16; the first gate turned to\n  physics (a paging-dependency removed).\n- *cc-learning-store* (ticket, code-seam) — the concrete first mechanism: a feedback\n  record richer than CC++/CC--, capturing today. The first endpoint of the pattern.\n- *learning-as-a-pattern* (ticket, code-seam) — \"how does this component learn?\" as a\n  contract question asked of every component; the shared process with pluggable\n  endpoints, crystallizing once cc-learning-store + a second endpoint exist.\n"
    },
    {
      "id": "base",
      "kind": "component-charter",
      "source": "cairn/base/intention+why.json",
      "born": "2026-07-17T15:14:10-06:00",
      "content": {
        "component": "base",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "The Form, embodied — the base-most layer of the whole spine, composing no one and composed by everyone. Four files under one charter: (1) core_values.py — the frozen CP1-CP6 contract (`CoreValue`, `CORE_VALUES`, `CoreValuesMixin`); (2) device.py — `BaseDevice`, which composes the mixin and declares the Form v0 #2 introspection surface (intention -> state -> settings -> other, assembled in order by concrete `introspect()`); (3) shim.py — `BaseShim`, which composes the mixin, pins `device_id`, and carries the per-pulse CALLBACK-FIRING: on each ground_loop heartbeat pulse, a device's shim evaluates its device's callbacks (each trigger where its data is owned, Law 6) and POKES the target of every one that fires, onto the bus — it also receives the device's mail and STARTS the device (its own process) on demand; (4) callback.py — the `Callback` primitive: an immutable 'poke X when this trigger is true', where a trigger is ANY predicate (not a named kind), the anti-reification made structural. Because both bases mix in `CoreValuesMixin`, every device and every shim carries the six structurally — you cannot be a device or a shim without them; and because callback-firing lives in the shim base, 'hang your callbacks on the heartbeat' is built into every device by inheritance, not re-implemented per device. The Form v0 #2 surface now has its first RENDER-side consumer built into the base pair (web-server ticket, child a): `BaseDevice.declared_panes()` lets a device OFFER extra panes beyond the STATUS+SETTINGS floor (each a handler returning DATA), and `BaseShim.active_page()` is the standard assembly — it projects the floor from `introspect()`, appends the declared panes in order (multiples of a kind allowed), renders an unwired or raising handler ABSENT-with-reason (CP2/Law 7), and returns pure DATA the web server renders. The machinery lives once in the base (Law 6), not per device. (5) diagnostic.py — `DiagnosticBase`, composed by `BaseDevice` so every device inherits `emit()` the way it inherits CP1-CP6: the transition-grade diagnostic surface Cairn lacked (outcome-grade VALIDATIONs/journeys said what PASSED; nothing spoke when system_rackmount went red SILENTLY). At a gate contact a device sends a THIN breadcrumb home to CC's shim (the diagnostic mailbox, for now — `BaseShim.receive_diagnostic`/`diagnostics()`), pointing to the ticket (`pointer`, the tie) and stamped to the microsecond (which indexes the fuller 30-day cache-class logs). The gate issues it — not the ticket; the emission stays dumb so the interpreter that crawls it can be smart (Law 1). Only the µs-stamp idea is carried from UU's `DiagnosticBase`; the loguru/JSON-sink backend is not. (6) transitions.py — the EMIT-CHOKEPOINT, the RULES rung of the state-transition trichotomy (state-machine-physics.json / harbor-master.json): the one door a workflow's cursor moves through. It parses the versioned workflow string (`code-seam@v1: THINKME -> ... -> [CURSOR] -> ... -> PROVED`), validates a requested transition against the class DEFINITION's machine-readable `workflow_versions` table (node_classes/<class>.json) + the -ME grammar (a summons ends -ME; forward advances or skips only declared-skippable summonses like TICKETME; a back-edge re-enters any earlier summons, carrying severity), REFUSES the illegal (a skip past a gate, an off-vocabulary target, an unknown class/version, a no-op, a path that does not conform to the version it claims), and journals the legal crossing append-only through the projector door (the charter-split's write-door — the TRUTH rung mechanism). It turns /sorted's prose 'refuses illegal transitions' into physics (Law 4). AUTHORITY (who may) is NOT here — that is the harbor_master's clearance gate (harbor-master child b), which wraps this call; base owns only the rules + the recording mechanism.",
        "why": "Law 2 — CP1-CP6 hold everywhere, including in the process that builds the system. A value enforced by prose is policy; a value carried by a class you cannot subclass without is physics (Law 4). Form v0 #2 lives here for the same reason: a uniform introspection surface on the base means every device is observable by construction, not by each device remembering to be — the tester probes one protocol, the web UI renders one protocol. Placing the values here is necessary but not sufficient: a value is *consumed* only when a per-device contract turns it into a check (CP1 -> no 'done' without an honesty gate). This layer guarantees structural presence and the shape of the introspection surface; consumption is per-device charter work.",
        "invoke": "import: `from cairn.base.core_values import CoreValuesMixin, CORE_VALUES`; `from cairn.base.device import BaseDevice`; `from cairn.base.shim import BaseShim`; `from cairn.base.callback import Callback`; `from cairn.base.diagnostic import DiagnosticBase`; `from cairn.base import transitions`. Prove: `python3 cairn/base/proofs/test_core_values.py`, `test_composition.py`, `test_callback.py`, `test_shim.py`, `test_diagnostic.py`, `test_transitions.py` (each exit 0 = green).",
        "node_class": "code-seam",
        "entry": "MIXED. core_values.py entered by GRAFT (Law 8) — carried-over-intact from the quarry `UnseenUniversity/.../diagnostic_base/core_values.py` with its pin test; the core values are the one part of the predecessor never the problem (Law 2), ratified by Akien 2026-07-14. device.py + shim.py did NOT graft — UU's `BaseDevice`/`BaseShim`/`DiagnosticBase` (~1000 lines: 14 rack methods, block/halt state model, loguru JSON sinks, dispatch/policy/circuit machinery) are the design Cairn redesigned around. They carry the `CoreValuesMixin` composition and its teeth across, and — added 2026-07-22 — the microsecond-stamp idea into a Cairn-native `DiagnosticBase` (authored fresh: gate-issued thin breadcrumbs home to CC's shim, not loguru sinks); otherwise authored fresh to Cairn's Form v0. Recorded as a redesign, not a graft, so no one mistakes them for carried-over-intact.",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split; this file stays pure intention+why, the operational voyage lives in the sidecars (Law 1/4/7)",
        "filed_edges": [
          "(a) the Form v0 #2 report shape still loose where no consumer pulls it (the tester's probe side, `other`'s internal shape); tightens as web_server (child b) and real devices declare panes",
          "the 30-day cache-class json log tier in instance-space that the us stamp indexes (v0 mailbox is in-memory)",
          "the log INTERPRETER that crawls the mailbox+logs into a coherent slice (the method's step 2)",
          "wiring emit's receiver to CC's real running shim (instance-space)",
          "BaseShim's lifecycle contract (start/stop/etc.) beyond _start_device, and each device its own OS PROCESS (vs today's in-process instantiation)"
        ],
        "gates": "proof gate — the two pin tests in proofs/, runnable bare (python3) today; the tester device will own how proofs run when it lands. Durability gate (code-seam): not done until proven AND committed.",
        "falsifier": "RED, by physics, on any of: (1) the code and the canonical commons record (`CairnCommons/intentions-other/core-values.md`) disagree on the set, order, or any narrative (per that record's own falsifier); (2) `CORE_VALUES` is not exactly [CP1..CP6] in order, or the frozen `CoreValue` becomes mutable; (3) `BaseDevice` or `BaseShim` stops composing `CoreValuesMixin`, or any imported subclass of them overrides `CORE_VALUES` to a divergent set; (4) `introspect()` stops reporting intention -> state -> settings in that order; (5) `BaseDevice` stops composing `DiagnosticBase` or `emit` stops being inherited (a device that cannot send diagnostics home breaks 'transition-grade surface everywhere'), or a homeless `emit` silently DROPS a record instead of holding it loud (Law 7 — the silent-red defect this exists to end); (6) the emit-chokepoint (transitions.py) ADMITS an illegal transition — a forward skip past a gate summons, an off-vocabulary target, an unknown class/version, a no-op self-loop, or a path that does not conform to the version it claims — instead of refusing it loudly (that would make the state vocabulary policy again, not physics — Law 4/7), OR journals a crossing anywhere but append-only through the projector door (Law 7).",
        "how_it_learns": "The values themselves do not learn autonomously — by design. They are a frozen, ratified contract (Law 2), the terminal floor the rest of the system stands on; nothing-is-terminal does not apply to the axioms themselves. Their horizon is human: the words change only when Akien re-ratifies them (see core-values.md horizon). The Form v0 surface *does* have a learning path, but not here: each device charter answers, for itself, which CP became which enforced check and whether that check earns its keep, and the report *shape* firms up from what its consumers (tester, web UI) actually need. The base only guarantees the six are present to be consumed and the introspection surface is ordered and uniform.",
        "traces_to": [
          "core-values.md (the canonical words this enforces)",
          "Law 2 (CP1-CP6 everywhere)",
          "Law 4 (physics over policy)",
          "Law 5 (intent + code + proofs share an address)",
          "Law 8 (entry by graft / a proof a hollow build couldn't pass)",
          "Law 3 (nothing known until measured — the diagnostic emission IS the measurement)",
          "Law 7 (errors loud at diagnostic surfaces — the transition-grade surface that ends the silent red)",
          "Law 1 (the emission stays dumb; the interpreter compiles the inference once by crawling it)",
          "Form v0 #2 (the ordered introspection surface, MAP.md:157)",
          "Telos 6.1 (CP1-6 pinned by tests, enforced in-process)"
        ],
        "owner": "Akien owns the words (ratification is his gate). CC owns the enforced form (the code + its pin tests) and gates writes to it.",
        "proof": "proofs/test_core_values.py — the six in order, the frozen record + lookup helper, and the whitespace-normalized cross-check against core-values.md (drift caught by physics). proofs/test_composition.py — BaseDevice/BaseShim compose the mixin and carry the six, plus a sweep over every imported subclass (armed trap; 0 subclasses today, stated out loud). proofs/test_callback.py — a trigger is any predicate (evaluated where its data is owned), the callback is immutable, a defect is refused at construction. proofs/test_shim.py — a pulse fires due callbacks onto the bus (holding the rest, batch-surviving a bad one), and the device is woken on demand. proofs/test_active_page.py — the web presentation surface's DATA machinery (web-server child a): the STATUS+SETTINGS floor is projected from introspect(), declared panes append in order (multiples allowed), an unwired or raising handler is loud+absent not fatal, and the whole page json-round-trips (DATA, not HTML). proofs/test_diagnostic.py — the diagnostic surface: the µs stamp is the 6th place after the decimal (none lost), the gate issues a thin breadcrumb pointing to the ticket, it lands home in the mailbox in order, one ticket's entries share the pointer and sort by stamp (the crawl), a homeless emit is held-not-lost (Law 7), emit is inherited structurally (Law 2), and the mailbox is DATA the interpreter can crawl. proofs/test_transitions.py — the emit-chokepoint: a legal forward advance journals the crossing append-only + moves the cursor; the THINKME leaf-fork reaches TICKETME or BUILDME; a forward skip past a gate summons, an off-vocabulary target, an unknown class/version, a no-op, and a path that claims v1 but does not conform are each REFUSED; a back-edge kick-back is legal and carries severity; and it parses a real live ticket's workflow string (trailing prose and all), validated against the real node-class table. Green: run all seven under `python3`.",
        "provenance": "core_values.py born 2026-07-17 as the founding stone of the code spine (first class-space code). device.py + shim.py born 2026-07-17 (second step): the base pair, closing the composition-teeth open edge and casting the Form v0 #2 surface. callback.py born 2026-07-18 (evening) and shim.py reworked the same night: the heartbeat+callback+bus rework moved the per-pulse firing INTO the shim (correcting the ground_loop-executor goof) and made the Callback its own primitive — a third base alongside device and shim. Opens the CP/diagnostic base spine step (MAP.md build order) toward the tester as first device."
      }
    },
    {
      "id": "tester",
      "kind": "component-charter",
      "source": "cairn/tester/intention+why.json",
      "born": "2026-07-17T18:24:54-06:00",
      "content": {
        "component": "tester",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "The first real device on the spine — the system's notary and its proving mechanism (MAP.md:569). `TesterDevice` subclasses `BaseDevice` (so it carries CP1-CP6 and reports the Form v0 #2 surface). Its capabilities today: (1) `run_proof(path)` — run a proof file as a subprocess and PRODUCE a VALIDATION record (the ratified eight fields: claim, caller, date, method, verdict, evidence, falsifier, horizon); the verdict is read from the subject's exit code, not granted. (2) It OWNS THE NETWORK (MAP.md:333): `run_proof(..., isolation='netns')` runs the subject inside a MEASURED network seal (`cairn/tester/isolation.py`) — the seal is probed from inside (never assumed) and its verdict is folded into the record's `method`+`evidence`, no ninth field. An unconfirmable seal is INDETERMINATE (CP1) and never launders into a green.",
        "why": "Law 8 — nothing enters proven-space without a proof a hollow build couldn't pass; the verdict must come from a hand the builder did not guide. Placing that gate in a device (not a convention each builder self-applies) is Law 4 (physics over policy): the proving and the attesting come from the same hand, so a green cannot be self-granted. This is the smallest honest version of the tester — it proves, it attests, it dogfoods (its first act validates the real base proofs) — while the pillars it will grow (network ownership, a durable store, richer verdict methods) stay filed edges, built when a real need pulls them (round-three discipline: simplest provable stone first).",
        "invoke": "import: `from cairn.tester.device import TesterDevice`. Prove: `python3 cairn/tester/proofs/test_tester.py` (exit 0 = green).",
        "node_class": "device — the first runtime component on the spine (base/ is the code-seam it stands on).",
        "entry": "FRESH design, MECHANISM grafted. The tester's design — device-hood, the VALIDATION record, the seal's four verdicts mapped to CP1/Law 3, the seal recorded INSIDE the VALIDATION rather than a parallel record — is Cairn's, authored to Form v0 (like device.py/shim.py). The seal's OS PLUMBING crosses nearly literally from UU's isolation.py (the `bwrap --unshare-net --cap-add CAP_NET_ADMIN` flag string, the inside-probe, the `available()` apparmor check) because it is kernel truth, not a design Cairn replaced. What did NOT cross: UU's netpolicy Router (fixture/refuse/forward + the nftables reject+counter) — the programmable-network pillar, deferred (see open edge (b)); and UU's verdict shape (seal_confirmed/seal_detail/duration_s), replaced by the Cairn VALIDATION.",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split; this file stays pure intention+why, the operational voyage lives in the sidecars (Law 1/4/7)",
        "filed_edges": [
          "(b) the seal delivers NO ROUTE (the closed half); the CHOSEN ROUTE — a Router that serves a FIXTURE, REFUSES with a kernel counter, or FORWARDS over a Unix socket — is deferred, pulled by db_domain (FORWARD to Postgres) and inference_domain (FIXTURE/REFUSE); it advances but does NOT retire CLAUDE.md's 'reached only through the domain' IOUs",
          "(c) the verdict METHOD is exit-code + seal only — quorum / review-by-N-experts for concept pieces (MAP.md:752) is a later method the `method` field names room for",
          "(d) reported-vs-filed intention drift detection is not wired"
        ],
        "gates": "proof gate — proofs/test_tester.py, runnable bare (python3) today; the tester will eventually own how proofs run, including its own. Durability gate: not done until proven AND committed.",
        "falsifier": "RED, by physics, on any of: (1) `run_proof` on a passing proof yields anything but green, or on a failing proof anything but red (the notary must not lie); (2) a VALIDATION record's field-set is not exactly the ratified eight (drift — extra or missing field), including when the seal rides inside it; (3) `TesterDevice` stops subclassing `BaseDevice` or drops CP1-CP6 (trips the base's composition trap); (4) `introspect()` stops reporting intention -> state -> settings in order; (5) a seal claimed present (isolation='netns') but measured to still hold a route is called anything but BREACHED — a sandbox that reports the shape of a seal while handing out live sockets is the exact defect the tester exists to kill.",
        "how_it_learns": "Yes — and it is where learning-from-proof concentrates. Every VALIDATION carries falsifier + horizon (Law 3), so each verdict knows how it expires — 'validated on the 17th' can itself go stale and be re-run; a verdict is a best-guess-with-an-expiry, not a permanent stamp. A red is not a failure to suppress but CP2 data (FAIL = Further Advance In Learning), kicked back to design. THE SEAL LEARNS THE SAME WAY: it is measured from inside every run, never assumed, so a misconfigured or silently-downgraded sandbox is CAUGHT rather than trusted; a seal that cannot be confirmed is INDETERMINATE (CP1 made structural — 'I don't know' is a legible verdict, not a hidden green). The component's own horizon: its verdict METHOD is provisional — exit-code+seal today, quorum / review-by-experts for concept pieces later (MAP.md:752); the seal itself will grow from NO-ROUTE to a CHOSEN route (Router) when db/inference domains pull it. The beside-code validation store (validation_store.py) is the evidence trail that turns answered proofs into structure (Law 1) instead of re-derivation — a mind greps validations/ on a hunch rather than re-proving, and because it is git-JSON beside the proof it is durable and shareable by construction (no single un-backed row).",
        "traces_to": [
          "Law 8 (a proof a hollow build couldn't pass — the tester IS that gate)",
          "Law 4 (physics over policy — proving + attesting from one hand)",
          "Law 3 (a VALIDATION expires: falsifier + horizon ride along)",
          "Law 1 (validations become structure, not re-derivation)",
          "Law 5 (intent + code + proofs share this address)",
          "CP2 (FAIL = Further Advance In Learning)",
          "Form v0 #2 (the ordered introspection surface, MAP.md:157)",
          "VALIDATIONS stone (MAP.md:569)",
          "build order: tester + kernel-owned network (MAP.md:888)"
        ],
        "owner": "CC owns the tester's code + proofs and gates writes to them. Akien owns the VALIDATION schema's ratification (MAP.md:569) and the node-class definitions.",
        "proof": "proofs/test_tester.py — device-hood + the six, Form v0 #2 order, green->green, red->red (the hollow-tester killer), state reflects runs, exercises the base's armed trap; plus a __main__ dogfood that validates the real base proofs. proofs/test_isolation.py — the network seal: the four seal verdicts measured (incl. the hollow-seal BREACHED case), a LIVE bwrap seal on a capable host (honest INDETERMINATE otherwise), and run_proof folds the seal into the 8 fields with no schema drift. proofs/fixtures/ — a green (exit 0) and a red (exit 1) proof. Green: `python3 cairn/tester/proofs/test_tester.py` and `python3 cairn/tester/proofs/test_isolation.py`.",
        "provenance": "Born 2026-07-17 as the third step of the code spine (base -> base pair -> tester), the first real DEVICE. Same day, grew its OTHER half — the kernel-owned network's measured SEAL (cairn/tester/isolation.py) — completing the 'tester + kernel-owned network' spine step's no-route half (MAP.md:888). Still deferred until a real need pulls them: the durable VALIDATIONS store (db_domain), the chosen-route Router (db/inference domains), and richer verdict methods."
      }
    },
    {
      "id": "db_domain",
      "kind": "component-charter",
      "source": "cairn/db_domain/intention+why.json",
      "born": "2026-07-18T09:19:38-06:00",
      "content": {
        "component": "db_domain",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "The ONE path to durable state, and the owner-gate every write passes through. db_domain is the only module that holds a Postgres connection (MAP.md:334 — the only path to 5432). Its primitives: `connect()` (peer auth over the Unix socket to a dedicated `cairn` database, self-provisioning the database if absent); `create_owned_table(name, owner, columns)` — refuses an ownerless table; `write(table, owner, row)` — refuses any writer but the recorded owner (Law 6); `read(...)`. What it holds is the relational / graph-tree data — the trees a database is uniquely good at. VALIDATIONS were its first consumer but MOVED OUT (2026-07-22) to beside-code git-JSON (cairn/tester/validation_store.py; charter-state-history-split child b): build-provenance is knowledge frozen at PROVED, so it belongs beside the code it explains (Law 5), and git is already durable — no truth record left in one un-backed row.",
        "why": "The founding law (MAP.md:367, generalizing UU's button pattern; Law 6): every table has exactly one owner and the owner gates every write; an ownerless table cannot come into existence. This is PHYSICS, not policy (Law 4): the owner registry carries `CHECK (owner <> '')`, so Postgres itself rejects an ownerless row, and `create_owned_table` is the only door — a table that skipped the registry did not come from db_domain, and db_domain is the only module holding a connection. Concentrating durable state behind one gate is also what makes every write instrumentable at one chokepoint (journeys, MAP.md:390) instead of scattered and un-observable.",
        "invoke": "import: `from cairn.db_domain import store`. Prove: `python3 cairn/db_domain/proofs/test_db_domain.py` (exit 0 = green). One-time provisioning: an OS-named `LOGIN CREATEDB` role must exist (see `state`).",
        "node_class": "device (domain) — a spine device by role (the sole path to 5432). Today it is store primitives; the BaseDevice introspection FACE (intention/state/settings reported live) is a thin wrapper, a filed edge below.",
        "entry": "FRESH design, standard mechanism. The design — one owner per table, the owner-gate as a schema CHECK, the registry that owns itself — is Cairn's, tracing to Law 6 / MAP.md:367. (VALIDATIONS were the first consumer; they moved to beside-code git 2026-07-22, leaving db_domain holding only genuinely relational / graph-tree state.) The mechanism is ordinary psycopg2 + peer auth over the Unix socket; nothing crossed as carried code from UU (UU's netpolicy FORWARD-to-Postgres pattern is a LATER stone here, pulled when a sealed build must reach the DB). The connection identity (peer auth, Unix socket, dedicated `cairn` database, vault deferred) was ratified by Akien 2026-07-18.",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split; this file stays pure intention+why, the operational voyage lives in the sidecars (Law 1/4/7)",
        "filed_edges": [
          "(a) the BaseDevice introspection FACE is not built — the live intention/state/settings surface is a thin wrapper deferred until a prober (tester probe / web UI) pulls it",
          "(b) reads are not owner-gated — Law 6 gates WRITES; a per-consumer read gate is a later need",
          "(c) connect-per-operation, no pool — correctness now, pooling when a real load pulls it",
          "(d) ownership TRANSFER + delegated access (Law 6's 'only through the owner's gate') are recorded-ownership's next capabilities",
          "(e) the FORWARD path (a sealed build reaching this DB over the socket that survives --unshare-net) is proven possible by the seal stone but not yet wired end-to-end"
        ],
        "gates": "proof gate — proofs/test_db_domain.py, run by the tester. Durability gate: not done until proven AND committed.",
        "falsifier": "RED, by physics, on any of: (1) create_owned_table produces a table with no recorded owner (ownerless must be impossible — the founding law); (2) a write by a non-owner, or to a table db_domain never created, SUCCEEDS (Law 6 breached); (3) an owned jsonb value comes back stringified rather than as a structured dict (a store that mangles what it holds is worse than none — Law 7); (4) any module OTHER than db_domain opens a connection to 5432 (sole-path breached — enforced by the tester import-scan once that tooth exists).",
        "how_it_learns": "Partly, and it is the substrate the rest learn ON. db_domain itself is deliberately dumb physics — it does not adapt; its job is to be the one honest gate, and 'it doesn't learn, because it is the fixed floor others build on' is a valid charter answer. What it ENABLES is the system's durable relational memory: an owner-gated store where the graph trees (and any genuinely relational data a consumer pulls) live as structure, honestly attributed (Law 6) and inspectable. (The system's PROOF memory — 'validated on the 18th', later found expired under Law 3 — is the same idea but moved to beside-code git-JSON, validation_store.py, since provenance is knowledge that belongs beside the code, not a DB row.) Its own horizon: the owner-gate is exactly one owner + write-gating today; ownership transfer, delegated access, and read gates are learned-into as real consumers pull them.",
        "traces_to": [
          "Law 6 (exactly one owner; the owner gates every write — the founding DB law, MAP.md:367)",
          "Law 4 (physics over policy — the owner-gate is a schema CHECK, not a convention)",
          "Law 1 (relational state held once as structure, not re-derived)",
          "Law 7 (a store never mangles what it holds — jsonb survives as structure)",
          "MAP.md:334/372 (the only path to 5432; port kernel-closed except via the domain)",
          "build order: db domain, after tester + kernel-owned network (MAP.md:888)"
        ],
        "owner": "CC owns db_domain's code + proofs and gates writes to them. Akien owns the founding DB law (one owner per table, MAP.md:367), the connection-identity ratification (peer auth / socket / vault-deferred, 2026-07-18), and the node-class definitions.",
        "proof": "proofs/test_db_domain.py — connect reaches `cairn`; ownerless creation refused; owner recorded; the owner gates writes (non-owner + unowned-table refused); an owned jsonb row round-trips as structure. Run by the tester; self-cleaning. Green: `python3 cairn/db_domain/proofs/test_db_domain.py`.",
        "provenance": "Born 2026-07-18 as the fourth step of the code spine (base -> base pair -> tester -> db_domain), the second real device. Built UNDER the tester's proof gate and dogfooded (the tester's own VALIDATIONS were db_domain's first consumer — later moved to beside-code git 2026-07-22, leaving db_domain the graph-tree/relational store), exactly as Akien directed: 'the tester will validate everything about db_domain as we go.' Advances but does not yet retire CLAUDE.md's 'durable state via store primitives' and 'port 5432 only via db_domain' IOUs — those retire when the tester import-scan makes 'sole path' physics rather than construction."
      }
    },
    {
      "id": "sudo_relay",
      "kind": "component-charter",
      "source": "cairn/sudo_relay/intention+why.json",
      "born": "2026-07-18T09:47:11-06:00",
      "content": {
        "component": "sudo_relay",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "A Cairn-native device that is CC's self-serve path to root. A command goes in; it runs as root; a permanent JSON audit record comes out (the command line, the return code, stdout, stderr — the four Akien named — plus an id + date that make it a locatable, expiring record of truth). The audit folder self-cycles to a rolling one-month window (`retention_days`, default 31). Akien's whole ops role shrinks to TWO acts: (1) START the daemon (`python3 cairn/sudo_relay/daemon.py`) — one `sudo -v` password prompt, which IS his consent — and (2) approve. `SudoRelayDevice` subclasses `BaseDevice` (carries CP1-CP6, reports the Form v0 #2 surface). Its capability is `request_root(command)`; the daemon (`daemon.py`) is the one privileged process, holding sudo warm and running each pending command via `root_executor` (`sudo -n bash -c`).",
        "why": "The founding measurement: get Akien out of running the system. Today CC reaches root through the UU-tree daemon — a STOPGAP Cairn must not depend on at runtime; this device is its Cairn-native replacement, built fresh to Form v0 like the tester and db_domain. Scope RATIFIED by Akien (2026-07-18): UNRESTRICTED root — 'the box IS the restriction' (a dedicated single-purpose box, careful environmental constraint, six months of calibration), NOT an allowlist and NOT policy-hesitation in CC; the safeguard is the VISIBLE audit + transparency, not friction (CC memory [[constraint-is-the-box-not-hesitation]]).",
        "invoke": "Akien starts it: `python3 cairn/sudo_relay/daemon.py` (Ctrl-C releases root). Watch the window: `python3 cairn/sudo_relay/daemon.py --status`. CC uses it: `from cairn.sudo_relay.relay import request_root; request_root('<shell command>')` → returns the audit record. Prove: `python3 cairn/sudo_relay/proofs/test_sudo_relay.py` (exit 0 = green).",
        "node_class": "device — a runtime component on the spine; a 'thing-for-akien-along-the-way' (it unblocks CC self-serving root, which is why the spine paused here).",
        "entry": "FRESH design to Form v0; the relay MECHANISM is sound quarry from the UU sudoer_daemon (`UnseenUniversity/.../igor/tools/sudoer_daemon.sh`): the consent model (Akien runs the daemon once = one password; keepalive holds it; `sudo -k` revokes on exit) and the file protocol (pending -> executing -> done, atomic handoff) cross as pattern. What is NEW and Cairn's: the record of truth (six fields, stdout/stderr kept FULL per Law 7), the rolling-month retention, the injected-executor SEAM that makes the whole thing provable without root, and TEMPORARY-AS-PHYSICS with a VISIBLE window (the UU daemon's fatal flaw — it held live sudo 4+ days because its lifetime was invisible — made physics here: a hard 24h cap = 'log in once a day' as the sole automatic release, a heartbeat status surface, and a keepalive that keeps announcing the remaining window; Ctrl-C is the manual stop-sooner. NO idle timeout by default — one login lasts the day; the dedicated box is the safety net, not a timeout, per the ratified scope that rejects friction; idle is an opt-in knob).",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split; this file stays pure intention+why, the operational voyage lives in the sidecars (Law 1/4/7)",
        "filed_edges": [
          "(a) the daemon's sudo acquisition + keepalive (daemon.py) is the ONE privileged part the proof cannot exercise (needs a password/TTY) — everything under it is proven; the live root run is Akien's one act by design",
          "(b) approval is today the COARSE act of choosing to run the daemon (consent = the password act); per-command approval / an allowlist is deliberately NOT built (ratified scope is unrestricted root)",
          "(c) one command at a time (atomic pending->executing), no queue/concurrency — matches a single builder's serial use",
          "(d) not yet wired into instance-space provisioning beyond lazy dir-creation on first use"
        ],
        "gates": "proof gate — proofs/test_sudo_relay.py, runnable bare (python3) and under the tester. Durability gate: not done until proven AND committed.",
        "falsifier": "RED, by physics, on any of: (1) `process_pending` runs a command but the returned/audited return code, stdout, or stderr does not match what the command actually produced (the relay must not lie about what happened under root); (2) an audit record's field-set is not exactly the ratified six, or stdout/stderr is truncated (a record of truth must be permanent and complete, Law 7); (3) the retention sweep deletes a within-the-month record, keeps an over-a-month one, or touches a file it did not mint; (4) `should_expire` fails to expire past the absolute cap (24h — the sole automatic release by default), or expires a fresh daemon within the cap, or fires the opt-in idle timeout when none is set (temporary must be physics — root cannot be held past a day, nor dropped mid-day on a walk-away when the intent is 'log in once a day'); (5) `daemon_status` reports a dead/absent daemon as live (the root-holding window must be visible and honest, never assumed); (6) `SudoRelayDevice` stops subclassing `BaseDevice` / drops CP1-CP6, or `introspect()` stops reporting intention -> state -> settings in order.",
        "how_it_learns": "Yes — two ways. (1) The AUDIT is the learning substrate: every root run leaves a permanent record (command, code, stdout, stderr), so the trail of what root was used for is greppable evidence, not memory — the raw material for calibrating the box over the ratified six-month window (which commands recur, which fail, what CC actually needs root FOR). Akien's scope decision is explicitly a learning stance: start unrestricted + a dedicated box, and let the audit teach what constraint (if any) the evidence warrants — the opposite of guessing an allowlist up front. (2) The daemon's WINDOW is a standing measurement: the visible hard-cap deadline (and the keepalive that keeps announcing it) exists precisely because the UU daemon TAUGHT US (by holding root 4+ days) that an invisible lifetime is the defect; this device folds that lesson in as physics. A same-session correction is itself a specimen of this: the first cut carried a 30-min idle timeout (CC's own over-add), which Akien caught as misaligned with 'log in once a day' — the idle default was removed (opt-in now), leaving the 24h cap as the sole automatic release. Its own horizon: the cap (24h) and the retention window are best-guesses-with-an-expiry — settings, tuned as the audit shows the real cadence of use.",
        "traces_to": [
          "founding intention (get Akien out of running the system — this unblocks CC self-serving root)",
          "Law 4 (temporary + the owner-gate are physics, not policy — the daemon enforces the window)",
          "Law 6 (CC owns the code; the daemon's authority is Akien's, held only while he runs it)",
          "Law 7 (the audit is a record of truth — errors permanent, stdout/stderr uncollapsed)",
          "Law 3 (the visible window is measured — live-pid check, not the mere presence of a file)",
          "Law 5 (intent + code + proofs share this address)",
          "Law 8 (proven by a test a hollow relay could not pass — the executor seam proves it without root)",
          "CC memory [[constraint-is-the-box-not-hesitation]] (unrestricted by design; box + audit are the safeguard)",
          "CC memory [[sudo-relay-self-serve-ops]] (the UU stopgap this replaces)",
          "Form v0 #2 (the ordered introspection surface, MAP.md:157)"
        ],
        "owner": "CC owns the sudo_relay code + its proofs and gates writes to them. Akien owns the AUTHORITY the daemon carries — it exists only while he chooses to run it, and the password act is his alone; the daemon holds no stored credential. The audit log is a record of truth written by the relay process (as Akien, not as root).",
        "proof": "proofs/test_sudo_relay.py — the protocol runs the command and returns the truth (exit code + stdout + stderr propagate); the record is permanent and complete (exactly six fields, stdout kept FULL, round-trips on disk); the folder cycles after a month but not before, and never touches a foreign file; temporary is physics (cap + idle -> expire, fresh -> hold); the window is visible and honest (live vs stale pid); device-hood + the six + Form v0 #2 order. All green without root (injected `local_executor` seam), and green under the tester. Live cross-process check (non-root executor) confirms the on-disk client<->daemon handoff. Green: `python3 cairn/sudo_relay/proofs/test_sudo_relay.py`.",
        "provenance": "Born 2026-07-18 as a 'thing-for-akien-along-the-way' — the reason the code spine paused after db_domain: the spine's remaining steps want CC self-serving root, and the UU-tree relay CC used for that is a stopgap Cairn must not depend on at runtime. Replaces that stopgap with a Cairn-native device. Next after this: the spine resumes (MAP.md:888) — ground loop -> rack -> inference domain -> web server. Deferred until pulled: per-command approval / allowlist (ratified scope is unrestricted), a pending queue, tighter instance-space provisioning."
      }
    },
    {
      "id": "ground_loop",
      "kind": "component-charter",
      "source": "cairn/ground_loop/intention+why.json",
      "born": "2026-07-18T10:22:36-06:00",
      "content": {
        "component": "ground_loop",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "THE HEARTBEAT — the most basic mechanism in Cairn: ONE daemon that provides a pulse, and nothing more. It does not execute, resolve, schedule, route, or write. On each `beat(now, context)` it pulses the SHIM of every subscribed device, in subscription order. Handling a pulse — evaluating the device's CALLBACKS (each trigger where its data is owned, Law 6), firing the due ones by poking the bus — lives in the shim (`cairn/base/shim.py`), NOT here and not in the device body. The ground loop only beats. It also PUBLISHES ITS ROSTER (`roster()`, web-server child c) at all times — the devices it beats to, in order, each with live wakefulness — as the authoritative nav for the web presentation surface; no new owner and no new state, the roster IS the subscription list this device already owns (Law 6).",
        "why": "A single daemon structure that everyone else hangs their own handlers on — so no device runs a bespoke loop or daemon of its own (the UU disease: dozens of independent daemons rotting silently). One heartbeat; every device's shim hangs its callbacks on it. The decisive property, and the reason the ground loop stays this small: the CALLBACK IS THE SAME UNIT whether it fires on a scheduled cadence or on a device's own internal trigger — same shape, same firing path in the shim, only the owner and location of its data differ. Keeping the ground loop to just-the-beat is what makes that sameness hold. The heartbeat holds NO runtime state of its own; all durable state lives in db_domain, reached by the devices the shim wakes.",
        "invoke": "Construct: `gl = GroundLoopDevice()`. A device subscribes its shim: `gl.subscribe(shim)` (idempotent by device_id; only a shim — device_id + on_pulse — may subscribe). Beat: `gl.beat(now, context)` pulses every subscribed shim once and returns a legible beat-record. `now` is passed explicitly, so the pulse is provable without a clock. Prove: `python3 cairn/ground_loop/proofs/test_ground_loop.py` (exit 0 = green).",
        "node_class": "device — but the MINIMAL one: the heartbeat daemon the whole loop-system rests on. It is the first of the two universal substrates (the heartbeat; the bus is the second). It is NOT an executor; the executor/firing role belongs to the shim (that conflation was the goof this charter corrects).",
        "entry": "FRESH design to Form v0. The mechanism is Cairn's own: one daemon, one beat, firing-in-the-shim. No mechanism grafted.",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split; this file stays pure intention+why, the operational voyage lives in the sidecars (Law 1/4/7)",
        "filed_edges": [
          "(a) SUBSCRIPTION by a file in the device's own folder tree — today subscribe(shim) in-process; discovering subscribers by scanning device code trees grows when devices are separate OS processes",
          "(b) the wall-clock DAEMON that pulses beat on a real cadence — the OS-specific backing, a thin wrapper this stays provable without"
        ],
        "gates": "proof gate (tester) — proofs/test_ground_loop.py, runnable bare (python3) and under the tester. Durability gate: not done until proven AND committed.",
        "falsifier": "RED, by physics, on any of: (1) the ground loop does MORE than beat — if it resolves a method, runs a callback, writes state, or routes a poke itself, it has re-absorbed the shim's role (the goof); (2) firing lives anywhere but the shim — a device that must implement its own loop to get its callbacks fired breaks 'built into all devices'; (3) `subscribe` accepts a non-shim (no on_pulse), or double-subscribes the same device_id; (4) a callback fired on a cadence differs in shape or handling from one fired on a device's own internal trigger (the sameness is the whole point); (5) the heartbeat holds runtime state of its own instead of only beating (durable state is db_domain's, reached by the woken devices).",
        "how_it_learns": "The heartbeat itself does not learn — it is the floor everything beats on (like the core values, a deliberate terminal simplicity). Learning happens in the CALLBACKS the shims hang on it: each callback is a handler whose firing leaves evidence on the bus. The lesson this component itself carries is a specimen of learning-its-gates: collapsing the heartbeat, the executor, and the scheduler into one device was caught by Akien at n=1 and corrected before it hardened — the ground loop learned to be smaller (its `run_driver` and registry were deleted, not patched). Its horizon: whether one heartbeat daemon + shim-fired callbacks covers a scheduled cadence AND a device's own internal triggers with the SAME callback — measured as more real callbacks land (the system device's threshold pokes are the first).",
        "traces_to": [
          "Law 1 (one daemon, folded once — not re-derived as a bespoke loop per device)",
          "Law 4 (a single heartbeat is physics; every handler hangs on it)",
          "Law 5 (intent + code + proofs share this address)",
          "the converged architecture (CairnCommons/intentions-other/I-heartbeat-callbacks-and-bus.md — the heartbeat is the first of the two substrates)",
          "cairn/base/shim.py (the shim carries the per-pulse callback-firing — 'hang callbacks on it' built into all devices)",
          "cairn/base/callback.py (the immutable unit the shim fires — a trigger is any predicate, evaluated where owned)",
          "CC memory [[cc-reify-vs-flow]] (the goof: three roles frozen into one device; the fix flowed them apart)",
          "build order: db domain → ground loop → … (MAP.md:892)"
        ],
        "owner": "CC owns the ground_loop code + its proofs and gates writes to them. The heartbeat is the shared substrate every device's shim depends on; the callbacks hung on it are owned by their own devices (class-space declarations), and any durable state they touch is owned through db_domain.",
        "proof": "proofs/test_ground_loop.py — the beat pulses every subscribed shim in order and leaves a beat-record; the firing is the shim's (a due callback pokes the bus, the heartbeat pokes nothing, an idle beat pokes nobody); subscribe is idempotent + refuses a non-shim; one shim raising cannot stop the beat (CP2); the executor goof is gone (no run_driver/registry); device-hood + Form v0 #2 order; and the ROSTER is the nav (web-server child c) — published at all times, in order, with live wakefulness, DATA the web server renders. Composes the real BaseShim + Callback + a spy bus, so it proves the beat → pulse → fire chain WITHOUT a DB. Green bare (python3) and under the tester.",
        "provenance": "Born 2026-07-18 as the fifth spine step, first built as a generic driver-executor (cairn `584aa74`). Same day (evening), reworked to the heartbeat after the design conversation with Akien restored the lost property — a callback is the same unit whether fired on a cadence or on a device's own trigger — and located the firing in the shim. The rework SIMPLIFIES: the central scheduler (system_rackmount's scheduler service) dissolves, `run_driver` + the method-registry strip away, and one universal mechanism replaces three. Built third in the rework (after the bus and the base's callback+shim, both of which it pulses onto). Then the spine resumes (MAP.md:892): inference domain → web server."
      }
    },
    {
      "id": "system_rackmount",
      "kind": "component-charter",
      "source": "cairn/system_rackmount/intention+why.json",
      "born": "2026-07-18T11:02:55-06:00",
      "content": {
        "component": "system_rackmount",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "THE SYSTEM DEVICE — the rack-mount device that owns the host's SYSTEM predicates and serves them device-independently. It owns host-resource data (CPU, memory, disk) and ADVERTISES the callbacks it can serve against them ('I accept a cpu_threshold callback, takes a value'). A caller SUBSCRIBES ('I'll take one, value 80, here's my callback address'); the system device RESOLVES ITS OWN METHOD internally (the caller never learns object.object.method), evaluates the predicate LOCALLY (Law 6 — the trigger reads the host metric INSIDE the device; the caller never sees the raw number), and its SHIM pokes the subscriber on the heartbeat pulse when the line is crossed. `SystemRackmountDevice` subclasses `BaseDevice` (CP1-CP6, Form v0 #2, on which the callback menu is advertised); `SystemRackmountShim` subclasses `BaseShim` and fires the subscriptions on the beat. It is NOT a central scheduler — scheduling is the universal heartbeat + shim + callback mechanism.",
        "why": "So no device is coupled to the host, and no host metric is exported just to be tested (Law 6): any device can get a trigger on a host resource, but the reading STAYS HOME — only the wake-up (the poke) crosses the bus. The OS-specific backing of a host reading (load average vs a richer sampler; and, in the same family, privilege via sudo_relay) is swapped BEHIND the advertised callbacks without changing any device that depends on them. This is 'abstract host services device-independently' made concrete — and it corrects the goof: the system rackmount is NOT the rack (the rack is the chassis: bus + shims), and it is NOT a central scheduler (the interval/date/quantity/state enum + the SchedulerService were deleted). It is simply the OWNER of one kind of data other devices want triggers on.",
        "invoke": "Construct: `dev = SystemRackmountDevice(sampler=None)` (inject a sampler in proofs). Inspect offerings: `dev.advertises()`. Subscribe: `dev.subscribe('cpu_threshold', address='ops/personal', why=..., value=80)` → a subscription id. Wire it to the beat: `shim = SystemRackmountShim(dev, bus); ground_loop.subscribe(shim)` — the shim pokes subscribers whose line is crossed on each `beat`. Prove: `python3 cairn/system_rackmount/proofs/test_system_rackmount.py` (exit 0 = green). Prove the diagnostic producer (bare, no bus): `python3 cairn/system_rackmount/proofs/test_diagnostic_emit.py`.",
        "node_class": "device — a rack-mount device on the spine (the spine step after ground_loop). It is the owner of the host's resource predicates; it serves threshold callbacks on them through the universal heartbeat + shim + bus mechanism.",
        "entry": "FRESH design to Form v0. The METAPHOR is quarried from UU (a rack holds rack-mount devices; a bus connects them; system services are abstracted so devices are portable), but the shape is Cairn's: a BaseDevice that OWNS host-resource data and ADVERTISES callbacks on it, resolving its own method so a caller stays ignorant of internals, and poking through the shared heartbeat + bus rather than a bespoke scheduler. What is NEW and Cairn's: advertise → subscribe → poke as the registration protocol; Law-6-local-evaluation (the host metric never leaves the device); the system device as a data-OWNER, not a service-registry or scheduler.",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split; this file stays pure intention+why, the operational voyage lives in the sidecars (Law 1/4/7)",
        "filed_edges": [
          "(a) only cpu_threshold is advertised (dependency-free load-average sampler); memory/disk predicates are the SAME shape and mount when a consumer asks",
          "(b) subscriptions are LEVEL-triggered (poke while the predicate holds); edge-triggering and unsubscribe wait on a real need",
          "(c) advertise/subscribe are direct calls today; expressing them as bus messages (like the poke already is) is a filed edge",
          "(d) the real host sampler (rich per-core CPU, memory, disk) is the OS-specific backing this device will own — a thin edge; the predicate physics is proven by injection",
          "(e) sudo_relay (privilege) is a host service in this same family, built standalone first — it migrates behind this abstraction when a second consumer makes the seam pay",
          "(f) the line-crossing/POKE emit (the flake-adjacent event) is NOT yet instrumented: the poke fires in BaseShim._fire (the shim is the diagnostic RECEIVER, not an emitter) and level eval is per-pulse (a firehose); a device-side fire-hook or edge-detection grows when a real flake needs watching. subscribe (rare, low-frequency) is the one clean device-side gate today"
        ],
        "gates": "proof gate — proofs/test_system_rackmount.py, runnable bare (python3) and under the tester. Durability gate: not done until proven AND committed.",
        "falsifier": "RED, by physics, on any of: (1) the device serves a callback it did not advertise, or accepts a subscription to an unadvertised name instead of refusing loudly (CP1); (2) a poke fires when the host is UNDER the caller's line, or fails to fire when it is over (the predicate physics must be exact); (3) THE RAW READING CROSSES THE BUS — a poke body (or any envelope) carries the host metric rather than only that the line was crossed (Law 6 broken — the whole point); (4) the device pokes subscribers ITSELF rather than through its shim on the heartbeat pulse (re-absorbing the firing the shim owns), or re-grows a central scheduler / a trigger-kind enum; (5) `SystemRackmountDevice`/`SystemRackmountShim` stop subclassing `BaseDevice`/`BaseShim` / drop CP1-CP6, or `introspect()` stops reporting intention → state → settings in order.",
        "how_it_learns": "Yes. (1) The subscriptions + the pokes ARE the learning substrate: the device's state() surfaces who is waiting on which line (never the reading), and every poke is a why-bearing record on the bus — a threshold that never fires and one that fires constantly are both signals in the record, not in silence. (2) Law-6-local-evaluation is itself a learning stance: by keeping the host's data home and exporting only the wake-up, the system learns which host predicates it actually needs to advertise (cpu first, memory/disk as consumers ask) without ever coupling a consumer to the metric — the same start-narrow-and-let-evidence-widen discipline as sudo_relay's box. Its own horizon: the advertised menu (cpu today) and the level-triggered semantics are best-guesses-with-an-expiry — they firm up as real consumers (an ops device, the harvester, a memory-pressure watcher) subscribe.",
        "traces_to": [
          "founding intention (get Akien out of running the system — a device gets a host trigger without a human wiring it)",
          "Law 2 (a device/shim carries CP1-CP6 structurally)",
          "Law 6 (the trigger is evaluated where its data is owned — the host reading never leaves the device; only the poke crosses the bus)",
          "Law 4 (the poke fires by measured condition, not caller goodwill)",
          "Law 5 (intent + code + proofs share this address)",
          "the converged architecture (CairnCommons/intentions-other/I-heartbeat-callbacks-and-bus.md — the system device settles the A/B question → A: owner of host-resource predicates, not a scheduler)",
          "cairn/base/callback.py + cairn/base/shim.py + cairn/ground_loop/loop.py (the universal mechanism this device serves through)",
          "cairn/bus/bus.py (the poke is a bus message)",
          "cairn/base/diagnostic.py + cairn/diagnostic_inspector (this device is the emit surface's first real producer — the subscribe-gate breadcrumb it emits is what the inspector crawls; the complete-first-pass method measured in situ, Law 3)",
          "Akien's correction 2026-07-18 (the rack is the chassis; the system rackmount is the system device, not a scheduler)",
          "Form v0 #2 (the ordered introspection surface)"
        ],
        "owner": "CC owns the system_rackmount code + its proofs and gates writes to them. The system device OWNS the host-resource data it serves predicates on; that data is never exported, only pokes are — the owner-gate for host metrics, made physics.",
        "proof": "proofs/test_system_rackmount.py — it advertises the cpu_threshold menu and refuses an unadvertised name; end to end through the heartbeat, 'alert me at 80% CPU' pokes the subscriber's feed when over the line and no one when under, with the raw reading (95) crossing the bus nowhere (Law 6); the scheduler goof is gone (no service/scheduler API, no enum); device-hood + shim-hood + Form v0 #2 order. Composes the heartbeat + the real bus (db_domain) + the shim + the Callback. Green bare (python3) and under the tester. FLAKE FOUND + FIXED 2026-07-22 (a learning specimen, Law 8): the Law-6 leak-check scanned the WHOLE envelope for the reading '95', including the bus-assigned random uuid `id` — which contains the hex substring '95' ~12% of the time, so the proof went red ~2/8 by pure chance (a verdict decided by a coin toss, exactly what Law 8 forbids). Measured, not guessed: reproduced 2/8 under concurrency, the failing envelope's body was clean and the '95' sat in the uuid; the check now scopes to device-authored fields (excludes the transport-assigned id/date). Re-proven 0 red over 96 concurrent runs. The 'silent red' was a harness artifact (the earlier loop did not surface the subprocess stderr); under the tester the AssertionError was always captured.",
        "provenance": "Born 2026-07-18. First built (mislabeled) as `cairn/rack/` — a 'rack' that was actually a trigger-firing scheduler; then re-homed as a SchedulerService under a service-abstraction framing (cairn 5cb593a). The design conversation with Akien (evening) corrected the model twice over: the rack is the chassis (bus + shims), and the system rackmount is not a scheduler at all but the SYSTEM DEVICE — the owner of host-resource predicates, serving threshold callbacks through the universal heartbeat + shim + bus. Reworked to that shape (built last in the heartbeat+callback+bus rework, composing every piece before it); the SchedulerService + trigger-kind enum deleted. A specimen of learning-its-gates: the identity was corrected by the owner before it hardened, and the code caught up to the corrected intention. Next: the spine resumes (MAP.md:892) — inference domain → web server."
      }
    },
    {
      "id": "I-heartbeat-callbacks-and-bus",
      "kind": "homeless",
      "source": "intentions-other/I-heartbeat-callbacks-and-bus.md",
      "born": "2026-07-18T21:04:54-06:00",
      "content": "# The heartbeat, the callback, and the bus\n\n*Ratified in conversation with Akien, 2026-07-18 (evening). This is the converged\nruntime architecture for how Cairn devices are driven and how they talk. It sharpens\nand partly supersedes the earlier \"operational-driver primitive\" + \"host/rack\" +\n\"peers fire in two modes\" sketches in `MAP.md` and `tickets/state-machine-physics.json`.\nThe code shipped 2026-07-18 morning (`ground_loop` `584aa74`, `system_rackmount` `5cb593a`)\nwas built to the OLD shape (a generic `run_driver` executor + a central scheduler) — that\nshape was the goof this document corrects. The rework SHIPPED the same evening (commits\n`d27ce1d` bus, `ae2d372` base callback+shim, `a535de0` heartbeat, `faafb70` system device),\neach piece proven under the tester — see \"What this changes in code — DONE\" at the bottom.*\n\n## The two universal substrates\n\nEverything runtime hangs on exactly two things:\n\n- **The heartbeat** — one daemon that provides a pulse, and nothing more. This is the\n  `ground_loop`. It does not execute, resolve, schedule, or route. It beats.\n- **The bus** — one common messaging substrate for everything. Inspectable, logged,\n  and the *sole* path for inter-device communication.\n\nThe symmetry that makes them load-bearing:\n\n| Substrate | Sole path to… | Owner-gated | Inspectable/logged by construction |\n|---|---|---|---|\n| `db_domain` | durable **state** | yes (Law 6) | yes |\n| **the bus** | inter-device **communication** | yes (Law 6) | yes |\n| the heartbeat | — (it only beats) | — | the beat is trivially observable |\n\nBecause the bus is the *only* door for communication (devices never hold references\nto each other, never call each other directly), \"inspectable + logged + common\" are\nnot features added per surface — they are automatic. Physics, not policy (Law 4).\n\n## Devices, shims, and processes\n\n- **Each device is its own process.** Isolated, independently crash/restartable, and\n  it does **not** run continuously — it sleeps when idle and is woken on demand.\n- **The shim** (`BaseShim`) is always-on, lightweight, one per device. It:\n  1. fires the device's due callbacks on each heartbeat pulse,\n  2. **receives** incoming bus messages for its device,\n  3. **starts the device** (the heavier process) on demand when a message arrives and\n     the device isn't running.\n  So the shim is the device's persistent front and process-manager; the device is the\n  heavier process the shim wakes. This is the sleep/wake peer model made physical: a\n  device is a process that *wakes to a poke*, not a daemon that spins.\n\n### Why event, not poll — the shrinking-footprint aim (Akien, 2026-07-22)\n\n**Polling costs CPU; events don't.** A process that wakes on a schedule to check\nwhether something happened burns cycles on every tick where the answer is *no* — and\nthe answer is *no* almost every tick, by construction. An event-driven wake burns cycles\nonly when the thing actually happened. So the choice between \"scan periodically\" and\n\"subscribe to the gate that produces the change\" is not a style preference — it is the\ndifference between a cost that grows with the *clock* and a cost that grows with *real\nevents*, and real events are rare relative to the clock.\n\nThis traces to a standing aim Akien named explicitly: **an ever-decreasing computational\nfootprint.** Cairn should cost *less* to run as it matures, not more — the opposite of a\nsystem that accretes background daemons and cron ticks until the machine is busy doing\nnothing. Every \"poll\" we can convert to an \"event\" moves a cost off the clock and onto\nthe events that justify it.\n\n**Where it traces (Akien, 2026-07-22): telos aims 1 and 5.** It is a *derived\nconsequence*, not a seventh aim and not a core value. Aim **1 (demonstrate inference\ncompilation)** is the load-bearing parent, because **Law 1 is this same principle stated\nat the inference level** — \"the resolver is spent on the novel, not on re-deriving the\nsettled\" is *don't spend compute twice*. Inference compilation turns re-derivation into\nstructure, which *is* reduced compute; event-not-poll is that principle one layer down, at\nruntime instead of at inference. Improving along that axis as the system matures is aim\n**5 (self-improving).** So shrinking-footprint is compilation generalized from inference\nto the running machine — not, primarily, an altruism (aim 6) thing.\n\nIt is also **a why for the ground loop itself**, though the ground loop's charter does\nnot phrase it this way: the ground loop is the *one* permitted periodic pulse, and it\nexists precisely so that nothing else has to be. One heartbeat, shared, replaces every\ncomponent's private timer — N polling loops collapse to one, and the one does nothing but\nbeat. Concretely, this is why the pre-Cairn cron jobs were deleted (2026-07-22) rather\nthan ported: a cron entry is a private polling loop with a private schedule and no owner\n(see `notes/held-inspectors-janitors-filters.json` — the unowned-default that governs a\nrecord's survival). Functionality like `cleanupPeriodDays` may well be rebuilt — but it\nwill hang on a callback fired by a gate, not on a clock that wakes to ask \"anything?\".\n\n## Callbacks vs tickets — two species, named for what they are\n\n- **Callback** — *immutable, no workflow.* \"Call X when this trigger is true.\" It\n  carries no state of its own. Because it is *declaration*, it lives with the device's\n  **code** (class-space — git, greppable, shareable). A callback firing = **kick off a\n  separate, short-lived Python process** in its own space that sends a message to the\n  target's shim, then **terminates.** That statelessness is why it can be a fire-and-die\n  process. The heartbeat fires callbacks (via the shim). Every recurring wake-up —\n  interval, time, data-accumulated, resource-threshold — is a callback.\n- **Ticket** — *a workflow node: mutable, carries a state machine* (a voyage, e.g.\n  `LEARNING → ARCHIVED`). A different species. Its state is mutable, so it lives where\n  workflow-state lives (instance-space / the node store), not class-space.\n- **`LEARNING` is a state; a callback is the worker.** A node *in* `LEARNING` can *set*\n  a callback. The state is the condition the node rests in; the callback is the doing.\n  When the work ends (e.g. an expiry trigger fires), the state moves (`→ ARCHIVED`) and\n  the callback is unset. \"This node is `LEARNING`\" (a state fact) and \"this node has set\n  a callback\" (a worker it owns) are two separate things — do not mush them into \"a\n  mutable ticket.\"\n\nCallbacks are general, not scheduler-specific: even the question-nexus template's loop\nis a callback. One primitive for \"call this again / on this trigger,\" used everywhere.\n\n## Triggers — anything that evaluates to true\n\n**A trigger is anything that can evaluate to true.** A predicate. There is **no closed\nenum** of trigger kinds (the shipped `interval/date/quantity/state` set was a\nreification — see the reify-vs-flow catches below). Open examples, not a taxonomy:\n\n- an interval elapsed, or a specific wall-clock time arrived (the \"cron\" subset)\n- an amount of data accumulated past a threshold (queue depth — this is also how\n  **backpressure** works: a filling channel fires a callback on its reader)\n- a resource crossing a line — CPU pegged, memory, disk\n- a state or event entered\n- **a proof going green** — a proof is *precisely* a claim evaluated to true (a green\n  VALIDATION), so proof is the exemplar quality-trigger; `PROVEME → PROVED` is a\n  proof-trigger. The tester (Law 8) and the callback system are the same substrate.\n\nNew signal → new predicate, **not a schema change.**\n\n## Where a callback is evaluated — Law 6 for triggers\n\n**A callback is evaluated wherever its trigger's data is owned.** So device-local data\nis never exported; only the *wake-up* crosses the bus, never the raw data it tested.\n\n- **Global** callbacks read genuinely shared data (the passage of time / the beat).\n  Evaluated at the heartbeat level.\n- **Device-specific** callbacks read a device's own data (its queue depth, an internal\n  metric). Evaluated *inside* the device; the data stays home.\n\nConsequences: minimal data movement (telemetry never crosses the bus just to be\ntested), and encapsulation by construction.\n\n## Advertise → subscribe → poke (the registration protocol)\n\nA caller never needs to know a device's internals (`object.object.method`):\n\n1. A device **advertises a menu** of callbacks it offers — e.g. \"I accept a CPU-threshold\n   callback (takes a value)\" — as one item in a list of its offerings (part of its\n   Form v0 #2 surface: inspect a device, see what callbacks it offers).\n2. A caller **subscribes**: *\"I'll take one of those, value 80, here's my callback\n   address.\"*\n3. The device **resolves its own method internally** and wires it into the callback.\n4. When the predicate goes true, the device **pokes the caller's address**. The caller\n   gets its wake-up and never saw a CPU number.\n\nOwner-gated throughout (Law 6). Advertise / subscribe / poke are all just bus messages.\n\nWorked example: *\"alert me at 80% CPU.\"* CPU is the **system device's** data (it is the\none that can make the CPU calls). So the CPU-threshold callback lives on the system\ndevice; any device that wants it asks the system device (owner-gated), which evaluates\nlocally and pokes the requester when true. This is what \"abstracts host services\ndevice-independently\" cashes out to — **`system_rackmount` = the system device: owner\nof host-resource predicates (CPU/memory/disk), serving threshold callbacks on request.**\nNOT a central scheduler. (Settles the earlier open A/B question → A.)\n\n## The bus in detail\n\n- **Protocol at the edge.** The bus's semantics (channels, owned envelopes, retention,\n  causality) are Cairn's; the wire protocol is a *swappable adapter*. MCP is the current\n  lingua franca for agentic communication, so it is the adapter today — swap it when the\n  ecosystem moves, the way `system_rackmount` abstracts an OS service behind a\n  device-independent face. The protocol must not hold the design hostage.\n- **One substrate; every surface is a view.** UU split logic between the mcp bus and\n  \"how the web server surfaced things.\" Cairn: the web server, the MCP inspector, a\n  debug pane are all *read-projections* of the one bus. Nothing surfaced by re-deriving\n  it elsewhere (Law 1).\n- **Durable transit rides `db_domain`, owner-gated.** The bus does not open its own\n  Postgres; a message in transit is an owned write through `db_domain`. That buys\n  logged + inspectable for free and enforces one-owner.\n- **Record-of-truth vs diagnostic surface, as physics (Law 7).** Permanent records\n  (errors, announces-of-fact) never collapse and never expire; diagnostic channels\n  (debug/info) may collapse in a view and expire on a rolling window (sudo_relay's\n  pattern). The substrate always stores the full truth; only views collapse.\n- **Channels per device** (Murderbot-feeds model, Martha Wells): **announce** (public\n  feed — public conversations), **personal** (chat inbox — others post through the\n  owner's gate), **info** + **debug** (logging). A device's `introspect()` surface can\n  publish onto its announce feed, so *inspecting a device is reading its feed* —\n  observability and messaging stop being two systems.\n- **Every envelope carries why + causality** (sender, intention, reply-to), so the bus\n  is a *replayable causal record*, not just traffic. A device woken from sleep rebuilds\n  its context by reading its own feed history (horizon-of-awareness, made concrete).\n- **The human is a native participant.** Akien has channels like any device; the web\n  server is a view. Participation on the bus, not operation through a side door\n  (get-Akien-out-of-ops at the messaging layer).\n\n## The reify-vs-flow catches this session (specimens of learning-its-gates)\n\nCC (I) froze Cairn-fluid categories three times; Akien caught each at n=1:\n\n1. **rack = scheduler** — inferred from one MAP line; the rack is the *chassis* (bus +\n   shims), not a scheduler.\n2. **ground_loop = executor** — collapsed the heartbeat, the ticket-firing, and the\n   scheduler into one device, losing \"the ticket is the same unit no matter what fires\n   it.\" The ground loop is *only* the heartbeat; firing lives in the shim.\n3. **triggers = `interval/date/quantity/state` enum** — froze an open list of example\n   predicates into a closed typed set and coined \"quantity.\" A trigger is *anything that\n   evaluates to true.*\n\nThe tell: I turn examples/metaphors into frozen taxonomies. The fix flows them apart.\n\n## What this changes in code — DONE (reworked + proven 2026-07-18 evening)\n\nThe rework shipped the night the model converged, each piece proven bare AND under the\ntester, each committed separately:\n\n- the **bus** → BUILT (`cairn/bus/`, commit `d27ce1d`). The sole comms path; durable transit\n  rides db_domain (owner `bus`); per-device channels (announce/personal records, info/debug\n  diagnostic); record channels refuse to collapse, diagnostic views may (Law 7); every\n  envelope carries why + causality (Law 5). Filed: MCP wire-adapter; per-device-owned channels.\n- the **Callback primitive** → BUILT (`cairn/base/callback.py`, commit `ae2d372`). Immutable;\n  a trigger is ANY predicate `(now, context) -> bool`, NOT a named kind (the enum is deleted);\n  evaluated where its data is owned (Law 6).\n- `BaseShim` → REWORKED (`cairn/base/shim.py`, `ae2d372`). Gains per-pulse callback-firing\n  (`on_pulse`, batch-safe), message receipt + on-demand device start (`deliver`/`_start_device`).\n  The long-deferred one-loop primitive is resolved: the heartbeat IS the one loop. Filed: each\n  device its own OS process (the shape — start-on-demand — is proven; real spawn grows against need).\n- `ground_loop` → STRIPPED to the heartbeat (`cairn/ground_loop/loop.py`, commit `a535de0`).\n  `beat(now, context)` pulses subscribed shims; no `run_driver`, no resolve, no write. The\n  method-registry + collect fixtures + executor proof were RETIRED (the proven-space registry\n  returns with the emit-chokepoint when a real consumer pulls it).\n- `system_rackmount` → REWORKED to the *system device* (`cairn/system_rackmount/`, commit\n  `faafb70`). Owns host-resource predicates; advertise → subscribe → poke; evaluates locally so\n  the reading never leaves (Law 6); the central `SchedulerService` + `interval/date/quantity/\n  state` enum are DELETED. Its capstone proof composes every piece above end-to-end.\n- **tickets** → the mutable workflow species stays DISTINCT and DEFERRED — the callback/ticket\n  boundary is now clean in code (a callback is the immutable worker; a ticket is the mutable\n  node), but the ticket state machine still waits on the emit-chokepoint\n  (`CairnCommons/tickets/state-machine-physics.json`). Not built here, by design (not the goof).\n"
    },
    {
      "id": "bus",
      "kind": "component-charter",
      "source": "cairn/bus/intention+why.json",
      "born": "2026-07-18T21:55:47-06:00",
      "content": {
        "component": "bus",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "THE SECOND SUBSTRATE — the one common messaging substrate for everything, and the SOLE path for inter-device communication. Devices never hold references to each other or call each other directly; they `post` (the only way to send) and `read` (the record — inspecting a device is reading its feed). Each device has four channels: `announce` + `personal` (RECORDS of truth) and `info` + `debug` (DIAGNOSTIC). Every envelope carries its why (CP3) and its causality (`sender`, `reply_to`), so the bus is a replayable causal record. Durable transit rides `db_domain` (owner `bus`, Law 6) — the bus opens no connection of its own. `BusDevice` subclasses `BaseDevice` (CP1-CP6, Form v0 #2).",
        "why": "Because communication has EXACTLY ONE DOOR, 'inspectable + logged + common' are not features bolted onto each surface — they are automatic, by construction (Law 4). This is the exact symmetry of db_domain: db_domain is the sole path to durable STATE (owner-gated, logged); the bus is the sole path to inter-device COMMUNICATION (owner-gated, logged). Every later surface — a web feed, an MCP inspector, a debug pane — is a READ-PROJECTION of this one substrate, so observability is never a separate build and nothing is re-derived elsewhere (Law 1). A woken device rebuilds its context by reading its own feed history (horizon-of-awareness, made concrete).",
        "invoke": "Construct: `bus = BusDevice()` (or `BusDevice(table=...)` for an ephemeral/proof table). Send: `bus.post(sender=..., to=..., channel='personal', why=..., body={...}, reply_to=None)` — the sole path. Inspect: `bus.read(to='dev', channel='announce')` returns the full-truth feed; `bus.digest(to='dev', channel='debug')` collapses a DIAGNOSTIC channel to count + tail (it refuses to collapse a record channel). Prove: `python3 cairn/bus/proofs/test_bus.py` (exit 0 = green).",
        "node_class": "device — the second universal substrate on the spine, the peer of db_domain. Where db_domain owns durable state, the bus owns inter-device communication.",
        "entry": "FRESH design, metaphor quarried from UU (the Murderbot-feeds model — announce/personal/info/debug per device — and UU's Postgres-backed message bus with envelopes). What is NEW and Cairn's: comms as a SUBSTRATE with a single door (post), symmetric with db_domain; durable transit RIDING db_domain rather than opening its own store (one-owner + logged for free); the record-vs-diagnostic distinction enforced as physics (Law 7) — a record channel refuses to collapse, only a diagnostic VIEW may; every envelope carrying why + causality so the bus is a replayable causal record, not raw traffic.",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split; this file stays pure intention+why, the operational voyage lives in the sidecars (Law 1/4/7)",
        "filed_edges": [
          "(a) the WIRE PROTOCOL is a swappable adapter — an MCP adapter at the edge is a filed edge (the substrate must not be held hostage to a protocol)",
          "(b) PER-DEVICE-OWNED channels — making each device the owner of its own inbound channel waits on a real multi-owner need",
          "(c) RETENTION — a durable rolling-window expiry of diagnostic channels waits on real volume",
          "(d) the HUMAN as a native participant (Akien gets channels like any device) — the channel shape is here, wiring Akien's feeds is the web-server stone"
        ],
        "gates": "proof gate — proofs/test_bus.py, runnable bare (python3) and under the tester. Durability gate: not done until proven AND committed.",
        "falsifier": "RED, by physics, on any of: (1) a posted message does not read back, or loses its why / sender / reply_to (a hollow bus that dropped or forgot a message — Law 5); (2) an unknown channel is accepted, or a whyless message is accepted (CP1 / CP3); (3) a RECORD channel collapses into a digest, OR a diagnostic `digest` alters what `read` returns (the substrate collapsing rather than only the view — Law 7); (4) a non-`bus` writer can write the transit table (the owner-gate not holding — Law 6), or the bus opens its own DB connection instead of riding db_domain; (5) `BusDevice` stops subclassing `BaseDevice` / drops CP1-CP6, or `introspect()` stops reporting intention → state → settings in order.",
        "how_it_learns": "Yes. (1) The bus IS the learning substrate for the whole fleet: because every inter-device message passes through one door as an owned, why-bearing, causal record, the trail of what the system actually said and why is greppable evidence, not memory — a device that stops posting is a signal, a channel that floods is a signal, both visible in the record rather than in silence (the UU split-between-bus-and-web-server disease, closed). (2) The record-vs-diagnostic seam is itself a learning stance: by naming which channels are records of truth (never collapse) and which are diagnostic (may collapse) BEFORE there are many surfaces, the system learns which surfaces it needs and adds them as VIEWS of the one substrate, never re-deriving traffic elsewhere. Its own horizon: the four channels and the digest shape are best-guesses-with-an-expiry — they firm up as real consumers (the system device's pokes, the web server's feeds, an MCP adapter) exercise them.",
        "traces_to": [
          "founding intention (get Akien out of running the system — the human is a native participant on the bus, not an operator through a side door)",
          "Law 2 (a device carries CP1-CP6 structurally)",
          "Law 4 (single-door comms makes inspectable+logged+common physics, not per-surface policy)",
          "Law 6 (durable transit is an owner-gated write through db_domain)",
          "Law 7 (record-of-truth channels never collapse; only a diagnostic VIEW may — the substrate always stores full truth)",
          "Law 5 (intent + code + proofs share this address; and every envelope carries its why)",
          "Law 1 (every surface is a read-projection of the one substrate — nothing re-derived elsewhere)",
          "the converged architecture (CairnCommons/intentions-other/I-heartbeat-callbacks-and-bus.md — the two substrates)",
          "db_domain (the peer substrate the bus is symmetric with, and rides for transit)"
        ],
        "owner": "CC owns the bus code + its proofs and gates writes to them. The bus owns its transit table (owner `bus`) through db_domain; it is the sole writer of traffic, on behalf of attributed senders.",
        "proof": "proofs/test_bus.py — a message round-trips with its why + causality and in causal order; an unknown channel and a whyless message are refused; a record channel refuses to collapse while a diagnostic channel collapses to count + tail without altering the record; transit is owner-gated through db_domain; device-hood + Form v0 #2 order. Green bare (python3) and green under the tester: `python3 cairn/bus/proofs/test_bus.py`.",
        "provenance": "Born 2026-07-18 (evening), the first NEW component of the heartbeat+callback+bus rework — built after the design conversation with Akien converged the two-substrate model (the heartbeat and the bus). Built before the ground_loop strip-down and the shim's callback-firing because both fire ONTO the bus: the substrate comes first. Next in the rework: the heartbeat (ground_loop stripped to the pulse), the shim's per-pulse callback-firing, and the system device (system_rackmount) that advertises/serves threshold callbacks by poking the bus."
      }
    },
    {
      "id": "I-prebuild-cognition-compiles",
      "kind": "homeless",
      "source": "intentions-other/I-prebuild-cognition-compiles.md",
      "born": "2026-07-19T15:59:29-06:00",
      "content": "# I-prebuild-cognition-compiles — the pre-build steps migrate to deterministic code, one at a time\n\n*Proposed 2026-07-19 by CC, drafted from Akien's stated intention (\"eventually we'll\nmove every bit of this we can into python\") and reconstructed from its origin (the\n2026-07-02 claude.ai web chat where the sequence was named, and the Delta Dental\nCopilot work where the first steps were compiled). **State: PROPOSED — awaiting\nAkien's ratification (signature gate).** Peer of [[I-learns-its-gates]] and\n[[I-cairn-claude-md]] in the intention layer. Corrects the stale 4-step compression\nin MAP.md:484–505 (orient/constrain/deconstruct/plan), which this supersedes on\nratification.*\n\nBefore any code is written, a builder runs a fixed sequence of cognitive passes. Today\nthe resolver (an LLM) occupies that whole sequence as a single oracle slot. **The\nintention is that each pass migrates out of the model into deterministic Python as we\nlearn it well enough to compile** — the role-replacement staircase applied to the\npre-build nexus. Each compiled step becomes independently inspectable (one module per\nstep), memoizable at its seam, and *provable* rather than merely plausible. Only the\ngenuinely-novel core — **hypothesize** — stays model-side the longest; everything\naround it starves. Delta measured this at ~89% whole-build token reduction doing\nexactly this compilation.\n\n**The sequence (a loop, not a line):**\n\n1. **Orient** — parse the request, ground it in what's actually being asked, place it\n   in its domain/context.\n2. **Constrain** — the bounds: what's required, what's forbidden, what shape the\n   answer must take.\n3. **Survey** — look at what already exists (files, patterns, prior art in the\n   codebase) *before* touching the problem. Easy to skip, expensive when skipped —\n   it's the pass whose absence makes a weak model read-wander.\n4. **Decompose / disassemble** — break the ask into its constituent sub-problems.\n5. **Triage** — sort those parts into *already-known/answerable* vs. *genuinely\n   novel*. Named \"framing / decomposition-into-subquestions\" in the origin chat; the\n   **starvation gate itself** — it decides what needs real reasoning (cold-path,\n   model) vs. what's just retrieval (warm-path, cached). Where the economic win is\n   measured.\n6. **Hypothesize** — for the genuinely-novel parts only, form a candidate approach.\n7. **Validate** — test the hypothesis against the constraints from step 2 *before*\n   committing. **On failure it returns to one of two altitudes** (Akien, 2026-07-19):\n   back to **hypothesize** for a local miss (this candidate violates a constraint), or\n   up to **design** for a structural miss (no hypothesis can satisfy these\n   constraints — the design is wrong). This is the return-for-learning vs.\n   return-for-higher distinction landing on the step whose job is to catch it before\n   Build.\n8. **Build** — only now does code get written. **Build spawns children:** a change\n   can reveal hidden sub-problems, which re-enter as new nodes at step 4\n   (MAP.md:44 — \"build → deconstruct is the universal shape\").\n\n**Why:** Cairn is an inference-automation system — it compiles inference into code.\nThe pre-build cognition is the densest, most repeated inference in the whole build\nloop, so it is the highest-yield place to compile. Moving a step from \"the model does\nit\" to \"code does it\" does three things at once: it cuts tokens (the measured 89%), it\nmakes the step's *output* an inspectable artifact handed to the next pass (so a wrong\norientation is caught before it becomes a passing-but-hollow build), and it turns the\nstep from a discipline the model must remember into physics it cannot skip (Law 4).\nThe staircase is the mechanism; this intention names the specific steps and their\norder so each migration is a discrete, provable graft rather than a vague ambition.\n\n**Traces to:** Telos 1 (demonstrate inference compilation — this *is* that\ncompilation, aimed at the build loop's own cognition) and Telos 5 (self-improving —\neach compiled step is a faculty the system now owns rather than rents). Mechanism:\nLaw 4 (a compiled step is physics, not a remembered ritual), Law 3 (a step's\nreadiness-to-compile is a hypothesis — measured, not assumed), Law 5 (intent + impl\nshare an address — each step's module co-locates its why), Law 1 (a settled step\nbecomes structure, so the resolver stops re-deriving it).\n\n**Design constraints (load-bearing):**\n- **The back-edges are part of the shape, not an afterthought.** Step 7 returns to\n  hypothesize *or* design; Build returns to Decompose. A version of this drawn as a\n  straight line has dropped the thing that makes it a build *loop* — and Cairn holds\n  that nothing is terminal.\n- **Triage is a distinct step, not folded into Decompose.** Disassemble takes the\n  problem apart; triage sorts the parts by novelty. Collapsing them loses the\n  starvation gate — the exact pass where the token win is decided.\n- **Survey is a distinct step, not folded into Orient.** Orient grounds the *request*;\n  Survey grounds it in the *existing code*. The second is the one that gets skipped.\n- **One module per step.** Each compiled pass is independently inspectable and\n  improvable; the seam between passes is a typed artifact (an \"X-JSON handed down\"),\n  which is both where memoization lives and where intent can quietly deform — so the\n  bottom proof must trace back to the top intent, not just to the prior seam's output.\n- **Migrate on evidence, not on eagerness.** A step moves to Python when it's\n  understood well enough that the deterministic version passes a proof a hollow one\n  couldn't (Law 8) — not because it's next in line.\n\n**Falsifier:**\n- *Done (asymptotic):* a build runs the full sequence with the model reached only for\n  the genuinely-novel *hypothesize* pass — orient, constrain, survey, decompose,\n  triage, and validate all served by deterministic Python emitting inspectable\n  artifacts — and the measured token cost of a defined-ticket build trends toward the\n  Delta ~89%-reduced floor while builds stay non-hollow.\n- *Wrong, two ways:* (a) a step is compiled that shouldn't be — the deterministic\n  version passes its tests but produces builds that answer the wrong question (the\n  seam deformed and the bottom proof didn't catch it), meaning triage/validate were\n  compiled past their real understanding; (b) the steps never move — the resolver\n  keeps occupying the whole slot and the token cost doesn't fall, meaning this is a\n  description of cognition, not a compilation of it.\n\n**Horizon:** re-checked each time a step is proposed for migration (each migration is a\nchild ticket with its own proof), and when the graph-tree substrate lands (the origin\nframing had these passes destined for trees, not only Python — the two are the\ncold-path and warm-path of the same compilation).\n\n**Provenance / the trail that found this:** the sequence was named in the 2026-07-02\nclaude.ai web chat (`~/.unseen_university/akien/20260702.ClaudeWeb...txt`), where\nAkien was reaching for a forgotten step's word and it was named *decomposition*; the\nsame reach recurred 2026-07-19 and resolved to the same word. This file exists so it\ndoes not have to be re-derived a third time (Law 1 — the resolver is spent on the\nnovel, not on re-deriving the settled). First steps already partly compiled in the\nUU/Delta work: orient+survey (`pre_inference_assemble.py`, the packet's\n`context_shortlist`), constrain (the constraint normalizer/decorator), and the packet\nchain `intent → hard_constraints → success_definition → sufficiency_gate → proof_plan\n→ consequence_check`.\n\n**Related work:**\n- [[I-learns-its-gates]] — the same compilation aimed at decision-authority; *which*\n  step is ready to migrate is itself a learned gate.\n- MAP.md:484–505 — the pre-build passage this replaces; update it to point here on\n  ratification.\n"
    },
    {
      "id": "inference_domain",
      "kind": "component-charter",
      "source": "cairn/inference_domain/intention+why.json",
      "born": "2026-07-21T10:32:45-06:00",
      "content": {
        "component": "inference_domain",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "The ONE path to the inference host, and the compile-once gate every inference request passes through. An inference request is a TICKET (a workflow node); the domain runs that ticket's workflow inside the owner: RECEIVE -> CANONICALIZE -> lookup -> (hit) VERIFY -> ANSWER, or (miss) METER -> RESOLVE -> RECORD -> ANSWER. Primitives (cairn/inference_domain/domain.py): `canonicalize(request)` — reduce a request to its canonical form (first cut: structural, sorted-key JSON); `resolve(request, resolver=...)` — run the whole workflow and return {answer, hit, canonical}; `yield_report()` — the meter read back (tokens SPENT on misses vs AVOIDED by hits). The cache is one db_domain-owned, append-only table `inference_calls` (owner `inference_domain`) that is BOTH the compiled answers (verdict='miss' rows carry the resolved answer + its falsifier/horizon/provenance/cost) and the meter (every call lands a row).",
        "why": "Telos 1 — demonstrate inference compilation. An answered question must become structure (Law 1): the second time a canonically-identical request arrives, the host is NOT touched — a stored answer is verified (its horizon still holds, T1.3) and served, and that untouched host call IS compilation happening. The domain being the SOLE pipe (Law 6 + Law 4: one owner, reached only through its gate, no other door — the same move as db_domain's sole path to 5432 and the tester's owned network seal) is exactly what makes metering free and complete (T1.2: there is no uninstrumented path to the host), so the saving is a measured fact, not a hope (Law 3). Append-only, on purpose (Law 7): the cache is a log, never mutated — a stale answer is out-voted by verification, not overwritten, which is why db_domain's INSERT-only primitives suffice.",
        "invoke": "import: `from cairn.inference_domain import domain`. Prove: `python3 cairn/inference_domain/proofs/test_inference_domain.py` (exit 0 = green), or under the tester via `TesterDevice().run_proof(...)`. Needs db_domain's one-time provisioning (an OS-named LOGIN CREATEDB role); the cache is a db_domain-owned table.",
        "node_class": "device (domain) — a spine device by role (the sole path to the inference host). Today it is workflow primitives + the cache; the BaseDevice introspection FACE and bus-fronting are a thin runtime wrapper, a filed edge below.",
        "entry": "FRESH design, standard mechanism. The design — the sole-owner gate a third time, the inference request AS a ticket whose states are its workflow, the compile-once cache that verifies before it serves, append-only so the store never mutates a record of truth — is Cairn's, converged with Akien 2026-07-21 (the conversation that corrected 'enforcer' to 'gated ownership' and 'features' to 'workflow states'). The mechanism is ordinary: sorted-key JSON canonicalization + db_domain's owner-gated store; nothing crossed as carried code. The real host client is a LATER wiring (instance-space credentials, a real endpoint), exactly as db_domain filed its live FORWARD path.",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split; this file stays pure intention+why, the operational voyage lives in the sidecars (Law 1/4/7)",
        "filed_edges": [
          "(a) CANONICALIZE is structural only (sorted-key JSON) — it does NOT collapse SEMANTIC equivalence (paraphrases/equivalent prompts); that is T1.3's hard core, wants a /challenge pass + Akien's read before a shape is committed",
          "(b) THE HOST CLIENT is injected (the `resolver` seam) and not yet wired to a real host — it will live HERE and nowhere else; the sole-path TOOTH (a tester import-scan that reds any OTHER module opening the host) is the tester's later tooth, sole by construction today",
          "(c) VERIFY checks the HORIZON only; the stored FALSIFIER is carried for T1.4 active invalidation (evaluating it, and propagating a falsifier upstream, is a later edge)",
          "(d) the BaseDevice FACE + bus-fronting (callers poke the domain over the bus rather than importing it) are the runtime face, deferred until a prober/runtime pulls them, as db_domain deferred its face"
        ],
        "gates": "proof gate — proofs/test_inference_domain.py, run by the tester. Durability gate: not done until proven AND committed.",
        "falsifier": "RED on any of: (1) a canonically-identical repeat touches the host (the resolver is called a second time) — compile-once breached, the whole thesis; (2) a request past its horizon is served from the cache (blind replay — verify-before-answer breached, T1.3); (3) a hit returns anything but the stored answer byte-for-byte (a record of truth mangled on the way out — Law 7); (4) a non-owner write to the cache table SUCCEEDS (Law 6 breached); (5) a call does not land a meter row, or yield_report miscounts SPENT vs AVOIDED (the meter is incomplete — Law 3, T1.2); (6) any module OTHER than inference_domain opens the inference host (sole-path breached — enforced by the tester import-scan once that tooth exists). Wrong-shape signal: if metering/caching turn out to need to live INSIDE the resolver loop rather than at this domain boundary, 'the domain' was the wrong seam.",
        "how_it_learns": "Directly, and it is the first device whose learning is its point. The meter IS the learning surface: `yield_report` turns each call into evidence — tokens AVOIDED climbing against tokens SPENT is the measured answer to 'is inference compilation paying off?' (Telos 1, Law 3). Its own horizon: today it learns WHETHER the cache pays (the yield); it does not yet learn WHICH questions are the same (semantic canonicalization, T1.3) or WHEN an answer has gone wrong (active falsifier invalidation, T1.4) — those are the two named edges where the learning deepens as real inference traffic pulls them. The librarian above it (the next spine steps) is the first heavy consumer that will pull them.",
        "traces_to": [
          "Telos 1 (demonstrate inference compilation — T1.1 answers-as-structure, T1.2 every call metered because this is the sole pipe, T1.3 a hit verifies before it answers, T1.4 falsifier+horizon on every answer)",
          "Law 6 (exactly one owner; reached only through the owner's gate — the sole path to the host)",
          "Law 4 (physics over policy — sole path by construction now, by tester import-scan later; gates not supervisors)",
          "Law 7 (append-only cache — a record of truth is never mutated)",
          "Law 3 (the meter makes the saving measured; a stored answer carries its own expiry)",
          "Law 1 (an answered question becomes structure, not a re-derivation)",
          "build order: inference domain, after the heartbeat+bus+system-device block (MAP.md:888)"
        ],
        "owner": "CC owns inference_domain's code + proofs and gates writes to them. Akien owns the design ratification (the inference-request-as-ticket framing and the sole-owner-gate model, converged 2026-07-21) and the node-class definitions. The cache table `inference_calls` is owned (in db_domain) by `inference_domain`.",
        "proof": "proofs/test_inference_domain.py — compile-once (a repeat does not touch the host); canonical equivalence (key order is the same question); verify-before-answer (a stale entry re-resolves); the answer is served unchanged (Law 7); the owner gates the cache (Law 6); the meter measures spent against avoided (Law 3). Run by the tester on a real db_domain store; self-cleaning (an ephemeral table, dropped). Green: `python3 cairn/inference_domain/proofs/test_inference_domain.py`.",
        "provenance": "Born 2026-07-21 as the next step of the code spine after the heartbeat+bus+system-device block, in a design conversation with Akien that reframed the device twice before a line was written: an inference request is a TICKET whose workflow-states are the device's insides (not a bag of features), and the 'enforcer' that makes those states physics is not a supervisor but a gated resource only one owner reaches (the db_domain pattern, a third time). Built UNDER the tester and dogfooded on db_domain, exactly as db_domain was. Advances Telos 1 from thesis toward demonstrated; does not yet retire CLAUDE.md's 'the inference host is reached only through inference_domain' IOU — that retires when the tester import-scan makes sole-path physics rather than construction."
      }
    },
    {
      "id": "charter",
      "kind": "component-charter",
      "source": "cairn/charter/intention+why.json",
      "born": "2026-07-21T15:51:45-06:00",
      "content": {
        "schema_version": "v0",
        "component": "charter/projector",
        "role": "charter machinery — the projector (a tool, not a device)",
        "what": "Compile a charter's bounded `state` (a cursor + a window of recent history) from its append-only `history`, regenerated on every append. `state` is a PURE FUNCTION of `history`, so it cannot drift from the log — the window is derived, never a second copy anyone maintains.",
        "why": "The charter had become the ticket: `state` grew to the largest, most-mutable field (measured 17-24%, 2026-07-21), so the file every mind reads first bloated without bound. Splitting `state` from `history` and compiling the window makes the bound STRUCTURAL (Law 4), not a discipline anyone remembers. It is Law 1 at charter scale — the answered question 'what is the recent state' becomes structure, so no mind re-derives it by reading the whole journal (the compile-once move, one scale in from inference_domain). `history` append-only = Law 7.",
        "invoke": "from cairn.charter import projector; projector.append_entry(history_path, state_path, record) — the single write-door: append one record, get back the freshly-projected bounded state. The pure core (append / project) is storage-agnostic.",
        "node_class": "code-seam",
        "entry": "cairn/charter/projector.py",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split, now SELF-HOSTED 2026-07-24 (the splitter finally split itself; the placeholder `state` field is gone). This file stays pure intention+why; the operational voyage lives in the sidecars (Law 1/4/7).",
        "gates": "proof gate (tester) — proofs/test_projector.py, green under TesterDevice.run_proof; VALIDATION persisted via db_domain (store.persist_validation) and grepped back (claim 'proof test_projector.py passes under python3', verdict green, 2026-07-21T15:50:30). Durability gate: proven + persisted + committed.",
        "falsifier": "the persisted `state` always equals a fresh projection of `history` (no drift); a count window holds at most N (bounded by construction); `history` only ever grows (append-only, prior records untouched); since_gate returns exactly the records since the last gate. Wrong-shape: the window is hand-maintained instead of projected (drift returns); `state` carries anything but cursor+window (the charter becomes the ticket again); `history` is edited in place (Law 7 breach).",
        "how_it_learns": "it doesn't hold learned state itself — it is a stateless pure tool. The window RULE is what learns: DEFAULT_WINDOW (count n=5) is a guess to be adjusted against real need (Law 3 + grow-against-need); the first 'I needed more than that' moves the knob (count vs since_gate), and that adjustment is the learning, recorded on the ticket.",
        "traces_to": [
          "Law 1 (answered question -> structure; the window is compiled, not re-derived)",
          "Law 7 (history append-only; a record of truth is never mutated)",
          "Law 4 (the bound is physics — a fixed-window projector — not a discipline)",
          "Law 5 (intent beside code; the mutable record moves to where state belongs)",
          "tickets/charter-state-history-split.json (this is its child a); feeds tickets/harbor-master.json (the fleet-scale aggregate over these local histories)"
        ],
        "owner": "charter/projector — a pure tool; it owns no durable state. `history`/`state` files are owned by each charter's intention (Law 6); their placement (git vs db_domain) is a filed open edge.",
        "proof": "cairn/charter/proofs/test_projector.py — 5 teeth: derived-not-authored (no drift), bounded-by-construction, append-only, since-gate self-sizes, cursor-tracks-head. DB-free, self-cleaning.",
        "provenance": "built 2026-07-21 as child (a) of charter-state-history-split, cast the same day via /sorted at the close of the 'the intention+why.json files get large' design conversation. The projector-as-compiled-inference was Akien's (CC++); the window-guess-and-adjust his ruling (Law 3)."
      }
    },
    {
      "id": "usage-widget-hook",
      "kind": "homeless",
      "source": "intentions-other/usage-widget-hook.json",
      "born": "2026-07-22T11:10:34-06:00",
      "content": {
        "id": "usage-widget-hook",
        "date": "2026-07-22",
        "owner": "akiendelllinux_cc_0 (CC) — CC gates writes; Akien ratified by measurement ('verified it's now working', 2026-07-22).",
        "intention": "This machine reports Claude Code usage to a desktop widget without the widget ever calling the API: a Stop/StopFailure hook fetches usage after each response and writes a local cache the widget reads.",
        "why": "The widget polling the API directly gets rate-limited, and the data it wants already exists at a moment the CLI is naturally idle — the end of a response. Fetching there costs nothing extra and makes the cache a byproduct of work already done rather than a separate job that can rot. Underneath: usage is the meter on inference compilation. The whole research claim is that we spend the model less over time (inference_domain meters spent-vs-avoided at the call; this meters the account). A claim about cost that nobody can see at a glance goes unmeasured, and unmeasured is unknown (Law 3).",
        "node_class": "host-seam",
        "state": "host-seam@v0: THINKME -> TICKETME -> BUILDME -> PROVEME -> LEARNME -> [PROVED]   (PROVED 2026-07-22: script pipe-tested (cache written, 2044 bytes, plan default_claude_max_5x, 12 live usage windows); hook JSON schema-validated on both events via `jq -e`; Akien confirmed the live hook fires. The seal EXPIRES — see horizon.)",
        "gates": "proof gate — measure the host, not a build. REBUILD gate — the `apply` block below must bring a bare machine to this state with nothing else from the repo. RE-VERIFY gate — `verify` below, re-run on the horizon. Durability gate — committed to git.",
        "apply": {
          "note": "Complete recipe. A mind on a fresh box should need nothing from this repo but this block.",
          "1_widget": "Install the KDE plasmoid `org.kde.plasma.claudecodeusage` to ~/.local/share/plasma/plasmoids/. It is the CONSUMER only — it reads the cache and never calls the API. Nothing below depends on it; the cache is useful without it.",
          "2_script": "Install the fetch script to ~/.claude/hooks/update-usage.sh, mode 0755. It reads the OAuth token from ~/.claude/.credentials.json, GETs https://api.anthropic.com/api/oauth/usage with header `anthropic-beta: oauth-2025-04-20`, and writes {usage, planType, updatedAt} to ~/.claude/usage-cache.json. It exits 0 on every failure path and leaves any existing cache untouched, so a red network is silent by design.",
          "3_patch": "MEASURED DEFECT in the copy the plasmoid ships (hooks/update-usage.sh inside the plasmoid dir): it interpolates $USAGE_RESPONSE directly into a Python string literal, which breaks on quotes and backslashes in the API response. The installed copy must instead `export USAGE_RESPONSE PLAN_TYPE CACHE_FILE` and read them via os.environ inside a quoted heredoc (<< 'PYEOF'). Do NOT re-copy from the plasmoid directory on upgrade without re-applying this.",
          "4_hook": "Merge into ~/.claude/settings.json (preserving existing keys) a `hooks` object registering BOTH `Stop` and `StopFailure`, each with matcher \"\" and one command hook: {\"type\":\"command\",\"command\":\"$HOME/.claude/hooks/update-usage.sh\",\"timeout\":15}. StopFailure matters — a turn that errors still spent tokens, so skipping it would silently under-report exactly when usage spikes.",
          "5_reload": "Claude Code's settings watcher only watches directories that held a settings file at session start. If the hook does not fire, open /hooks once or restart the CLI.",
          "optional": "Add \"async\": true beside each timeout if the ~10s worst-case curl at the end of every response is noticeable. Left synchronous here because that is what the package's own instructions specify and the latency has not been measured as a problem."
        },
        "verify": {
          "command": "jq -r .updatedAt ~/.claude/usage-cache.json",
          "green": "a timestamp newer than the last response you sent",
          "also": "jq -e '.hooks.Stop, .hooks.StopFailure' ~/.claude/settings.json exits 0, and ~/.claude/hooks/update-usage.sh is executable and contains `os.environ` (the patch, not the shipped interpolation).",
          "measured_2026-07-22": "cache written at 17:01:30Z on a direct pipe-test; Akien confirmed the registered hook fires on a live turn."
        },
        "falsifier": "The cache stops tracking reality without anyone noticing. Concretely: `updatedAt` goes stale while sessions continue (hook unregistered by a settings rewrite, script lost on a Claude Code upgrade, token shape changed, the /api/oauth/usage endpoint moved or its beta header retired) — and because the script exits 0 on every failure path, NONE of that is loud. The widget keeps showing the last good numbers. That silence is the real risk of this node, and it is why re-verify is a gate rather than a habit. Second falsifier, for the class-level claim: hand this node to a mind on a bare box; if it cannot reach a working state from the `apply` block alone, the node is hollow.",
        "horizon": "Re-verify on any Claude Code upgrade, any plasmoid upgrade (step 3 regression), and otherwise monthly — first check 2026-08-22. A stale VALIDATION here is a hypothesis, not a fact.",
        "parent": null,
        "children": [],
        "provenance": "BUILT BEFORE IT WAS INTENTIONED — the defect that produced the class. 2026-07-22: Akien handed over setup instructions, CC installed the hook, and only then did Akien note 'we prolly should have intentioned that first' and, from that, name why such intentions need a berth: they are the ones you carry to a new machine. Cast retroactively. First member of `host-seam`; first record in `intentions-other/`."
      }
    },
    {
      "id": "web_server",
      "kind": "component-charter",
      "source": "cairn/web_server/intention+why.json",
      "born": "2026-07-22T17:35:59-06:00",
      "content": {
        "component": "web_server",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "A thin web presentation surface (Law 7) over the running system — the devices on the heartbeat across the TOP (the roster, child c), the selected device's ACTIVE page BELOW (its panes: STATUS / SETTINGS floor + declared interaction/logging, child a). Three parts under one charter: (1) render.py — PURE functions `data -> html` (nav, panes, document), no I/O, no socket, everything a device says HTML-escaped; (2) server.py — `WebServerDevice`, a Form v0 #2 device whose `serve(path) -> (status, content_type, html)` routes a request to a rendered page, reaching each device's `active_page()` live THROUGH the ground_loop that beats it (in-process v0); (3) listener.py — the thin http.server wrapper binding a LOCALHOST port and handing each GET and POST to `serve`, the OS-specific piece the proof does not exercise. NOT a daemon (the law: NO DAEMONS — UU's daemon sprawl is why the ground loop owns the one heartbeat): it acts only when an external call arrives; the rename from daemon.py says so. THERE IS ONLY EVER ONE WEB SERVER IN THE WHOLE SYSTEM — a device that wants a face owns a PAGE this server displays through its shim's declared panes; nobody else binds a port. The web server owns NO device state: `/` is the landing (nav + pick-a-device), `/device/<id>` is that device's ACTIVE page — GET renders it, POST hands the decoded form to the device THROUGH ITS SHIM (`deliver`, which starts the device if it is not running; a hidden `channel` field names the device's mail channel; the surface adds no routing intelligence), an unknown device is a loud coherent 404 that still renders the nav.",
        "why": "Telos 2 — Cairn has a USER; the librarian needs a web interface, and 'a web interface falls out of the chatbot' only if the surface exists first. This is that surface, on the spine BEFORE the librarian (MAP.md build order: web server → harbor_master → librarian-as-chatbot). It is the Form v0 #2 introspection surface made concrete — the SAME protocol the tester probes and the web UI renders, so observability is not a separate build. Built as a PRESENTATION surface (Law 7: it may collapse an error into a coherent shape — the 404, the absent-pane reason — but holds no record of truth and produces no DATA of its own), which is exactly what keeps the intelligence in the shims and the web server trivial (Law 6: one owner, no state). Because it renders DATA the devices produce (never markup a device authored), a hostile string in a device's feed can never become the page's markup — escaping is physics here, not diligence.",
        "invoke": "import: `from cairn.web_server.server import WebServerDevice` (over a ground_loop) and `from cairn.web_server import render`. Prove: `python3 cairn/web_server/proofs/test_web_server.py` + `test_web_server_chat.py` (exit 0 = green), or under the tester. Run the listener: `python3 cairn/web_server/listener.py [--port 8787]` — binds localhost; v0 wires the ground loop with the librarian's shim subscribed (the chat window live at /device/librarian), the full heartbeat still the launcher's filed edge.",
        "node_class": "code-seam (a spine device by role — the web presentation surface, after inference_domain in the build order). web-server ticket child b; children a + c landed in base + ground_loop respectively; child d (the VOYAGE pane / ⚓ Harbor route) BUILT + PROVEN 2026-07-24, rendering harbor_master's traffic image.",
        "entry": "FRESH design, standard mechanism, converged with Akien 2026-07-21 (the web_server design conversation: surfaces-in-the-shim, the data/render split, the artifact-owner spinoff, 'i think web server is /sorted'). The mechanism is ordinary Python stdlib (http.server, html.escape, json) — nothing crossed as carried code; UU's web surfaces are not grafted. The intelligence is deliberately elsewhere (the panes are child a's BaseShim machinery; the nav is child c's ground_loop roster), so this device is a bridge + frame renderer and little else — minimalism by construction.",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split; this file stays pure intention+why, the operational voyage lives in the sidecars (Law 1/4/7)",
        "filed_edges": [
          "(a) THE BUS TRANSPORT — v0 reaches devices in-process through the ground_loop; fetching active_page over the bus lands when devices are separate OS processes",
          "(b) REPORTED-vs-FILED DRIFT on the STATUS pane — comparing the reported intention against the filed intention+why.json on disk is this surface's render-time job and is not yet wired",
          "(c) NARROWED 2026-07-27, RE-TRUED 2026-07-28 — the librarian's chat window LANDED as the first rich view IN THE PANE MACHINERY (render_pane dispatches kind 'chat' to a transcript + form view; the form POSTs back to the device's own page with a hidden channel field; first cast as a bespoke /chat route, corrected same-arc to the declared-pane shape — one web server, surfaces in the shim); what remains: rich views for OTHER devices' interaction panes still render as pretty JSON until each pulls its own",
          "(d) THE VOYAGE PANE — BUILT + PROVEN 2026-07-24 (render.render_traffic_image + the /harbor route + proofs/test_voyage_pane.py, 4 teeth, sealed under a real netns). Renders harbor_master's TRAFFIC IMAGE (child c) — the whole-fleet, state-right-now view — as a fleet-wide route (⚓ Harbor in the nav on every page), distinct from the per-device pages. The harbor source is INJECTED (not imported — the data/render split holds); a surface with none says so loudly (404). Because the traffic image is computed from disk (the register), /harbor renders the REAL fleet from a bare daemon TODAY — the first live human-visible Cairn surface (Telos 2).",
          "(e) the SETTINGS circuit-breaker (waits on the cgroup-kill sudo script)",
          "(f) the LAUNCHER wiring the real running heartbeat (instance-space); listener.py stands up the ground loop with the librarian's shim subscribed today"
        ],
        "gates": "proof gate (tester) — proofs/test_web_server.py, run by the tester; verdict+seal from the same hand. Durability gate (code-seam): not done until proven, VALIDATION persisted beside-code (validations/test_web_server.json), AND committed. The socket listener is exempt by construction (the unprovable-without-the-OS thin wrapper) — the route+render logic under it carries the proof.",
        "falsifier": "the surface is honest and thin — RED on any of: (1) a device's ACTIVE page shows a pane it did NOT declare, or omits the STATUS/SETTINGS floor (the pane set is child a's, not the web server's to invent); (2) a device-derived string reaches the page as live markup (escaping failed — Law 7, and a real injection); (3) the web server accretes its own state/cache of device internals (it becomes a second record of truth — the state() must stay pulled-live); (4) an error renders as a raw crash/stack instead of a coherent shape (Law 7's presentation half); (5) a device NOT on the heartbeat is viewable, or one ON it is not (the nav must be exactly the roster). Wrong-shape signal: if rendering logic migrates INTO the shims (shims emitting HTML instead of DATA), the data/render split has failed and the tester can no longer probe the same protocol the UI renders.",
        "horizon": "first REAL render — when the listener serves the actual running heartbeat (the launcher wires it) with at least two live devices' ACTIVE pages pulled over the bus, STATUS reported-vs-filed drift-detection works, and the LOGGING pane reads real emission. Re-check then: is BaseShim the right home for the pane machinery, or do too many devices override it? (The data/render split survived its first rich interaction pane — the librarian's chat window, 2026-07-28: the device hands DATA, render_pane owns the view, the POST travels as mail.)",
        "how_it_learns": "As a presentation surface, its learning is the DRIFT it makes visible, not a model it trains. STATUS puts a device's reported intention beside its filed one — the reported-vs-filed comparison (open edge b) is a live drift detector (Law 3: a device that has quietly become something other than its charter says shows up on its own page). The surface itself does not learn autonomously — by design, it holds no state to learn from; the learning it SERVES is every device's, made observable. Its own horizon: today it renders introspection DATA; when interaction panes bring rich views and the LOGGING pane reads real emission, the surface deepens as its consumers (the librarian's chat/tool page) pull them.",
        "traces_to": [
          "Telos 2 (Cairn has a USER — the librarian's web interface rests on this surface)",
          "Form v0 #2 (MAP.md:157 — the uniform introspection surface; one protocol for tester + web UI)",
          "Law 7 (a presentation surface may collapse an error into a coherent shape; a record of truth never may — and never lets a device's data become markup)",
          "Law 6 (the web server owns no state — content is each device's, reached through the heartbeat that holds it)",
          "Law 4 (escaping + the data/render split are physics, not diligence)",
          "Law 1 (every later surface is a read-projection of the panes + roster, not re-derived)",
          "build order: web server, after inference domain (MAP.md build order)"
        ],
        "owner": "CC owns web_server's code + proofs and gates writes to them. Akien owns the design ratification (surfaces-in-the-shim, the data/render split, 'i think web server is /sorted', 2026-07-21). The listening PORT is owned by the web_server device (Law 6); the socket is the listener's.",
        "proof": "proofs/test_web_server.py — the nav is the roster; a device page renders its panes pulled live through the heartbeat; an unknown device is a coherent 404 that still renders the nav; every device string is escaped; an absent pane renders its reason; the web server owns no state (a Form v0 #2 device, state pulled live). Composes a REAL ground_loop + REAL BaseShims + a REAL device, so it proves route → fetch-through-the-heartbeat → render WITHOUT a socket. proofs/test_web_server_chat.py — the librarian's chat window through the STANDARD machinery (librarian-chat-surface ticket; a real LibrarianShim on a real ground loop, its face injected): the librarian rides the roster and its page carries the chat pane with the summarize: affordance SAID; querying the page WAKES the device through its shim; POST delivers the decoded form through shim.deliver; an empty utterance takes no turn; every librarian string is escaped; a dying delivery is a legible 500 whose breadcrumb stands; an unwoken face is an ABSENT pane, and the old /chat route is an ordinary 404 (no second door). Green: both scripts exit 0.",
        "provenance": "Born 2026-07-22 as the next step of the code spine after inference_domain, building the web-server ticket (cast 2026-07-21 via /sorted). Children a (BaseShim pane machinery) and c (ground_loop roster) landed first as the two DATA providers, each proven + persisted beside-code; b (this device — the surface that renders them) is the third buildable child. child d (the VOYAGE pane / ⚓ Harbor route) BUILT + PROVEN 2026-07-24 the moment its graft (harbor_master child c, the traffic image) landed the same session — rendering the whole-fleet state-right-now view; child e (circuit breaker) remains deferred on the cgroup-kill sudo script. Advances Telos 2 from thesis to a REAL surface: /harbor renders the live fleet from a bare start (the traffic image is disk-computed, independent of the running heartbeat). 2026-07-28: the librarian's chat window landed as the first rich interaction pane (librarian-chat-surface ticket) — first cast as a bespoke /chat route + injected source, corrected on Akien's word to the ratified shape (ONE web server; a device owns a PAGE; surfaces live in the shim; the shim starts its device; POST travels as mail through deliver), and daemon.py became listener.py with the NO DAEMONS law written into it. The full heartbeat still awaits the launcher (the filed instance-space edge)."
      }
    },
    {
      "id": "intentions_model_compiler",
      "kind": "component-charter",
      "source": "cairn/intentions_model_compiler/intention+why.json",
      "born": "2026-07-23T10:37:47-06:00",
      "content": {
        "component": "intentions_model_compiler",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "THE OUTER-ROW PROJECTOR: the single owner-gated door that compiles the `intentions/` MODEL as a deterministic, lossless projection of its two source trees — `CairnCommons/intentions-other/` (the homeless intentions: the roots, concept-pieces, host-seams, spanning ones) PLUS the beside-code charters at `cairn/*/intention+why.json`. It reads both trees, embeds each source's content verbatim (a .json source as its parsed object, prose as text), orders the whole set by each source's BIRTH timestamp, and writes ONE artifact — `intentions/_model.json`. A pure core (`compile_model(sources)`) does the ordering + projection with no disk or git; a thin file layer (`gather_sources`, `_born`, `compile_to_disk`) is the today-shape. The single write-door is `compile_to_disk`; nothing else writes the compiled view, and the store's hand-authored `_charter+why.json` is never touched.",
        "why": "The seed claim of the `intentions/` store (its charter): take that ONE folder to a bare machine and regrow the system — which only works if the view compiles over BOTH sources, because the beside-code charters alone regrow a repo that does not RUN (everything that made a machine ready lives in `intentions-other/`). That demands a compiled view (Law 1 — the system-scale question 'what does this system intend' answered from structure, not re-derived by walking every file), written through one door (Law 6), that never drifts from its sources (Law 7 — the model is the view, the sources are the truth; a hand-edit is reverted on the next compile) and drops nothing (Law 8 — a lossless projection a hollow build could not fake). It is the OUTER row of the three-scale projector recursion (ticket: history→state; component: design→charter; SYSTEM: all sources→here) — the same move as `cairn/charter/projector.py`, one corpus out.",
        "invoke": "Hand-crank: `python3 -m cairn.intentions_model_compiler.compiler` (compiles the real repo → `intentions/_model.json`, prints the count + the roots that surfaced first). In code: `from cairn.intentions_model_compiler import compiler; compiler.compile_to_disk()` (roots auto-derived from `__file__`; override `commons_root`/`code_root`/`out_path` in proofs). Pure core, no disk: `compiler.compile_model(sources)` where each source is `{id, kind, source, born, content}`. Prove: `python3 cairn/intentions_model_compiler/proofs/test_compiler.py` (exit 0 = green).",
        "node_class": "code-seam — the compile-door child (a) of ticket intentions-model-compiler, now also carrying the WRITE GATE child (c): a code-seam script (recompile_gate.sh, git-tracked, proven) poked by a WRITE-THROUGH contract (the skills poke the gate in the same act that changes a source) plus a READ-REFRESH backstop (the reader pokes the gate before consulting the model); the prior FileChanged host hook is retired (superseded — see host_seam). Berths in cairn/ (class-space code); the folder it writes is CairnCommons/intentions/. Kin to cairn/charter (the projector), cross-referenced not merged.",
        "host_seam": {
          "what": "the MODEL-FRESHNESS CONTRACT — the sole write-door (compile_to_disk, via recompile_gate.sh) is poked by an EVENT, never by a clock or a resident watcher. Three invocations, no fourth, ALL event-driven, NO daemon: (1) MANUAL — the hand-crank / `cairn compile` (the human's command is the event); (2) WRITE-THROUGH baked into /sorted — the skill pokes the gate right after it files a homeless intention to intentions-other/ (the source-write is the event); (3) READ-REFRESH baked into /intent — the CONSUMER pokes the gate before consulting the model (the read is the event): at 'I intend X' the reader refreshes (a ~0.2s deterministic compile) then reads, catching every out-of-band write (a human hand-edit, a charter written outside a skill step) exactly when the model is needed and never on a no-op tick. The gate COMMAND (recompile_gate.sh) is class-space/git and proven; nothing stands running.",
          "why_not_incremental": "MEASURED 2026-07-23 (Law 3): a full recompile of the whole corpus is 0.20s. So 'update vs rebuild' is a distinction without a difference — the writer pokes the ONE deterministic write-door for a full re-projection; there is NO incremental second write-path, which would break the single-door + determinism invariants (falsifiers 2 + 4). 'We don't rebuild by hand' is honored by the writer poking the cheap full door, not by partial updates.",
          "consumer": "the model's live consumer now is CC + Akien reading it at 'I intend X' — start in the model: is anything already like this, or in conflict (Law 1, caught at n=1)? The check is JUDGMENT OVER THE WHYS the model carries, not a string-match — so no precomputed conflict-edges are needed and none are kept. The reader owns freshness by REFRESH-ON-READ — the callback-native backstop (the read is the event), restored after a wrong detour through a nightly cron daemon (drift; removed). Write-through keeps the model usually-already-fresh so the read rarely changes anything; the read-refresh guarantees no out-of-band write is ever silently stale, with nothing standing running.",
          "apply": "the write-through call lives in /sorted and the read-refresh in /intent — both are skill instruction-steps, NO host config and NO daemon. Every trigger is an event (a command, a write, a read), never a clock. The freshness discipline is encoded in the skills, not left to CC to remember (Law 4).",
          "verify": "after a /sorted that files to intentions-other/, confirm intentions/_model.json regenerated (mtime advanced); after a hand-edit to a source out-of-band, confirm the next /intent poke refreshes it before consulting. No host state to verify — there is no daemon and no crontab. The gate COMMAND itself is proven in git (proofs/test_recompile_gate.py).",
          "superseded": "the FileChanged host hook (the prior child-c wiring) is RETIRED. MEASURED 2026-07-23 (Law 3): FileChanged did NOT fire on ANY write CC can produce — Write tool, Edit tool (post /hooks reload), or Bash subprocess — falsifying the provenance's birth-claim that it fires 'not only the agent's tool-writes'. It watches for changes from OUTSIDE CC's own operations, so it never caught the common case (CC authoring a charter during a build) and it dies with the CC session. The write-through-in-skills (the semantic writer pokes the door) is the resting mechanism it was standing in for; the wiring in .claude/settings.local.json is removed. The intentions-other/ heterogeneous-name problem that stumped the FileChanged matcher DISSOLVES here — /sorted pokes the door name-agnostically.",
          "iou": "the freshness discipline now lives in the SKILLS, not in CC's memory (Law 4 — a rule that matters is structure, not policy that drifts): /intent refreshes-on-read, /sorted writes-through. The one remaining IOU is the owner-gate emit (the source's OWNER emitting on its own write — the emit-chokepoint state-machine-physics tracks); until it lands, the READ-REFRESH at /intent is the structural backstop that makes any missed eager write-through safe, needing no daemon because the read itself is the event. Named so the stopgap is not mistaken for the resting state.",
          "state": "CONTRACT RECORDED + APPLIED 2026-07-23. Gate COMMAND proven + sealed (validations/test_recompile_gate.json). Write-through baked into /sorted; read-refresh baked into /intent — the freshness discipline lives in the skills (Law 4), not CC's memory. FileChanged wiring removed from .claude/settings.local.json. NO daemon: a nightly cron was installed then REMOVED as drift (a poll on a no-op tick; the read-refresh callback replaced it)."
        },
        "entry": "FRESH build 2026-07-23, to the shape the `intentions/` store charter had already SPECIFIED as its unbuilt projector ('the projector for this scale does NOT exist yet ... a hand-written file here is caught by a reader, which is exactly the discipline-instead-of-physics that Cairn exists to stop relying on'). What is quarried: the projector's pure-core + thin-file-layer factoring and its atomic write-door. What is NEW: two-tree gathering (the seed claim), born-timestamp ordering so the roots surface first BY PHYSICS (Law 4, not a special case), and content-embedding so the one folder is self-contained.",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split; this file stays pure intention+why, the operational voyage lives in the sidecars (Law 1/4/7)",
        "filed_edges": [
          "(a) the model is ONE _model.json embedding all content — grows to per-intention files or a richer index only if a real consumer needs it (build-minimal)",
          "(b) the model is kept current by the MODEL-FRESHNESS CONTRACT (host_seam), ALL event-driven, NO daemon: MANUAL, WRITE-THROUGH baked into /sorted, READ-REFRESH baked into /intent; the prior FileChanged host hook is RETIRED",
          "(c) Law-6 single-writer is an IOU enforced by convention until the tester import-scans for a second writer of intentions/",
          "(d) born from git is the today-shape — swappable behind the pure core, which takes born as data"
        ],
        "gates": "proof gate — proofs/test_compiler.py, runnable bare (python3) and under the tester. Durability gate: not done until proven AND the VALIDATION is persisted beside-code (cairn/tester/validation_store.persist_validation → validations/<stem>.json) AND committed (git).",
        "falsifier": "RED, by physics, on any of: (1) the model is not a faithful projection — a source is dropped or one is invented (Law 8), or content is mangled rather than carried verbatim; (2) the compile is non-deterministic or lossy (same sources → differing bytes — drift becomes invisible); (3) the roots do NOT surface first (born-ordering broken — the frame is not first by physics); (4) `intentions/_model.json` is written by any door other than `compile_to_disk`, or a hand-edit survives a compile, or the store's `_charter+why.json` is clobbered (Law 6/Law 7); (5) only one source tree compiles in (the seed claim broken — a bare-machine regrow would leave half the intent behind); (6) it grows its OWN authoring (content not traceable to a source — the view became a rival record of truth) or its own nightly scanner (that work is the inspector regime's, not this door's).",
        "how_it_learns": "Yes — it learns WHICH intentions are load-bearing by making the whole corpus legible in one ordered artifact, and it learns its own drift by construction: because the model is a pure function of the sources, any divergence between what the system SAYS it intends (the model) and what its sources hold is a compile away from being visible — the recompile IS the measurement. Its born-ordering is itself a learning stance: the frame surfaces first because it is oldest, so as the corpus grows the roots stay at the top without anyone maintaining that. Its horizon: whether one `_model.json` stays the right shape, or a real consumer (the web_server's intentions view, a bare-machine regrow) demands per-intention files or a queryable index — firmed up when that consumer arrives, not guessed now.",
        "traces_to": [
          "Law 1 (the system-scale model is compiled once from structure, not re-derived by each reader — the outer row of the projector recursion)",
          "Law 4 (roots surface first BY PHYSICS — born-timestamp ordering, not a hand-placed special case; the store charter's 'the_roots_surface_first')",
          "Law 6 (exactly one owner gates writes to the compiled view — compile_to_disk is the sole write-door; IOU until the tester import-scans)",
          "Law 7 (the compiled view is never a record of truth — a hand-edit is reverted; the sources are the truth)",
          "Law 8 (a lossless projection a hollow build could not fake — nothing dropped, nothing invented)",
          "Law 3 (the born source was chosen by MEASUREMENT — git --follow, after plain first-add was shown to sink the frame)",
          "the `intentions/` store charter (CairnCommons/intentions/_charter+why.json — the seed claim + the assembly-point spec this builds to)",
          "cairn/charter/projector.py (the kin — same pure-core + file-layer pattern, different corpus/owner; cross-referenced not merged)",
          "ticket intentions-model-compiler (this is its child a — the compile door)"
        ],
        "owner": "CC owns the intentions_model_compiler code + its proof and gates writes to them. compile_to_disk is the sole owner-gated write-door for `intentions/_model.json`; per Law 6, a compiled record's own owner remains the owner of the SOURCE it came from — ownership travels with the source, not the projection.",
        "proof": "proofs/test_compiler.py — seven teeth a hollow compiler could not pass: lossless + invents-nothing; roots surface first by born timestamp (Law 4); a changed source changes the model; a removed source leaves no trace; deterministic to the byte; one write-door reverts a hand-edit AND spares the store charter (Law 6/7); gather reads BOTH source trees on the real repo and excludes the _-prefixed store charter (the seed claim). Green bare (python3) and under the tester; VALIDATION persisted beside-code at validations/test_compiler.json. The WRITE GATE child (c) carries its OWN proof — proofs/test_recompile_gate.py, three teeth a hollow gate could not pass: it pokes the door (the model appears from sources), it reverts a stale model, and it is non-blocking-but-loud-in-the-record (a failed compile exits 0 yet is logged, never swallowed — Law 7). Green bare and under the tester; VALIDATION at validations/test_recompile_gate.json.",
        "provenance": "Born 2026-07-23, built as child a of the intentions-model-compiler ticket cast the same session (from the /intent that bundled a command-runner, an intentions updater, and a 'repo-scanner' — the scanner resolving into the already-held inspector regime, not a child here). Built to a spec the `intentions/` store charter had written in advance ('the projector for this scale does NOT exist yet'). The real first compile surfaced a measurement finding — git first-add mis-dated the relocated roots — resolved to `git --follow` before the seal. The `cairn compile` CLI face (child b) landed the same day via cairn-command. The WRITE GATE (child c) followed 2026-07-23: cast in the primitives sweep that measured the mid-2026 CC feature landscape against Cairn — a FileChanged host hook (believed at birth to fire on EXTERNAL writes, catching a human hand-editing a charter, not only the agent's tool-writes — [CORRECTED 2026-07-23: MEASURED not to fire on ANY CC-produced write; RETIRED, superseded by the write-through contract — see host_seam.superseded]) built as a host-seam-with-IOU, Akien-ratified as the precedent-setting template for mounting borrowed event-primitives (Routines, hooks) behind Cairn's own doors. What that saga taught, folded into the resting design (2026-07-23): the trigger for 'a source changed' is the SEMANTIC WRITER (the skill) poking the door, not the host watching the filesystem; FileChanged is for changes from OUTSIDE CC's operations, which CC's own charter-writes are not. Still ahead: baking write-through into any remaining source-writers, and the owner-gate emit that retires the beside-code discipline-IOU (the read-refresh at /intent backstops it until then)."
      }
    },
    {
      "id": "I-complete-diagnostic-on-first-pass",
      "kind": "homeless",
      "source": "intentions-other/I-complete-diagnostic-on-first-pass.md",
      "born": "2026-07-23T15:28:55-06:00",
      "content": "# I-complete-diagnostic-on-first-pass — the report resolves it on the first pass\n\n*Cast 2026-07-23 by CC via /intent → /sorted, from Akien's decades-proven method (his\n2007 test-automation framework, the UU model, Trouble Tickets). **State: PROVED —\nsealed 2026-07-25 through the quorum signature gate.** Reviewer (verdict): akien.\nNotary (seal): cc — different hands, refused by physics otherwise. The seal, its\nevidence and its falsifier: `intentions-other/validations/I-complete-diagnostic-on-first-pass.json`.\nIt sat unsigned for two days for a purely mechanical reason worth recording: the\nVALIDATION write-door derived a seal's address from a PROOF FILE path, and a\nconcept-piece has no proof file — so the gate was never awaiting a signature, it was\nawaiting an ADDRESS (`cairn/tester/quorum.py` closed it, and this is the first\nconcept-piece VALIDATION in Cairn). Node-class:\nconcept-piece (the prose IS the implementation; a code-seam — the diagnostic\ninspector — instantiates it). Migrates into the intention envelope when that schema\nlands (MAP.md Q6).*\n\nA diagnostic surface — a failure report, a system alarm, a trouble ticket — **delivers\nall the data required to resolve the issue in its initial emission.** The resolver\nnever re-runs the thing to gather more. The first report already carries identity,\nlocation, the exact failing code, expected-vs-actual, fatality, source, the full\ntrace, and every value at every transition and boundary that bears on the fault.\n\n**Why — this is proven, not speculative.** Akien built this into a test-automation\nframework in 2007 because he knew errors were coming and he was tired of running\nthings over and over to gather one more piece of failure data each pass. A report\nthat arrives complete takes a team from half-a-day-per-issue to ~10 minutes. It is\nthe **first tool he reaches for in debugging**, validated across decades and across\nUU. So `build-minimal / grow-against-need` — the rule for *unproven* ideas — does not\ngate it; a proven first-reach tool is infra you build **ahead** of need, the way you\nbuild logging before you need it. (CC mis-filed it as speculative once, 2026-07-23,\nand was corrected — the why is what let the rule's boundary be adjudicated: this is\n`ask-the-why-to-distinguish` lived.)\n\n**What it sharpens and serves.**\n- It **sharpens Law 7**: errors are not merely *loud* at diagnostic surfaces — the\n  diagnostic surface is **complete on first emission**. Loud-but-partial still costs a\n  second run; loud-and-complete does not.\n- It **serves Law 1**: re-running to collect more failure data *is* re-deriving the\n  settled — the exact waste a complete-first-report kills. The answered question\n  (\"what did I need to see?\") becomes structure (a field in the report), once.\n\n**The falsifier is a special class — a learning signal, not a terminal kill-switch.**\nThis is the load-bearing subtlety (Akien, 2026-07-23). A diagnostic that forces a\nsecond run to resolve some *additional* point does **not** falsify the principle — it\nis the learning loop firing. The surface has just discovered, empirically, a datum it\nshould have carried; that miss is **folded into the initial report** so the same gap\nnever costs a second run again. It is `[[I-learns-its-gates]]` pointed at the\ndiagnostic surface itself: the report learns its own completeness from the evidence of\nwhat it missed. So the falsifier splits:\n\n- *First miss of a given datum* → **learning signal.** Expected, even welcome — this\n  is *how* the report grows toward completeness. Feed-forward, not a defect.\n- *The same gap forcing a second run again, after it was already learned* → **the\n  real, terminal falsifier.** The surface was told once and failed to consume its own\n  evidence — a Law 1 defect (re-deriving the settled).\n\n*Horizon (asymptotic, monotonic):* completeness only rises; no learned gap recurs. The\nprinciple is \"held\" not when a report is ever perfect, but when every miss is reliably\nfolded into the next report. Re-checked each time a real mystery-red forces a second\nrun — did the loop fold the miss, or let it recur?\n\n**Distinctions recorded (not collisions).**\n- vs **Law 7** — Law 7 is loudness at the diagnostic surface; this is *completeness*\n  of it. Loud is necessary, not sufficient. This adds \"and complete on first pass.\"\n- vs the **decision-autonomy learning** (`learning/`, `cc-learning-store`) — that store\n  learns *whether CC may act alone* at a gate; this loop learns *what a report must\n  carry* for a transition-class. Both are `learns-its-gates` instances with different\n  endpoints (what the component is, what its gates are, what counts as evidence). Kin,\n  cross-referenced, **not** merged — a distinct store with a distinct key.\n- vs **the diagnostic emission** (`cairn/base/diagnostic.py`) — the emission stays a\n  dumb, thin breadcrumb (Law 6: only the pointer crosses); this principle is about\n  what the *inspector* assembles from the breadcrumbs and logs — the FINDINGS. The\n  emission is dumb so the findings can be smart.\n\n**Spawned / related work.**\n- *diagnostic-inspector* (ticket, code-seam) — the concrete build that instantiates\n  this: the inspector that reacts to a fired callback and FILTERS the emission\n  breadcrumbs + timestamp-indexed logs into the complete first-pass FINDINGS, **plus**\n  the learning-loop it carries that folds a forced second-run's miss into the next\n  findings' completeness for that transition-class. Its explicit remit (Akien): save CC\n  tokens exploring an issue as the prebuild step does for coding, and get better over\n  time. (Born as *diagnostic-interpreter* / `assemble`; refactored to the\n  inspector/filters/findings shape 2026-07-24.)\n- *`cairn/base/diagnostic.py`* (built) — `DiagnosticBase.emit`, the transition-grade\n  breadcrumb every device inherits; the inspector crawls on its pointer + microsecond\n  stamp. The docstring already named this inspector as \"the next build.\"\n- Kin: `[[diagnostic-logging-method]]` (the how — instrument → interpret → review →\n  fix) is the operating procedure this principle is the why for.\n"
    },
    {
      "id": "harbor_master",
      "kind": "component-charter",
      "source": "cairn/harbor_master/intention+why.json",
      "born": "2026-07-24T14:25:56-06:00",
      "content": {
        "component": "harbor_master",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "The device that owns the HARBOR through which workflows voyage. It keeps the AGGREGATE REGISTER of the fleet (the TRUTH rung — what is in port, what is still at sea, where each boat stands) and grants CLEARANCE for a workflow's state-transition (the AUTHORITY rung — owner-gated, delegable through its gate). It does NOT own the boats (each workflow is owned by its intention), does NOT execute (the crew sails under delegated clearance — the harbor never sails), and does NOT own the rules (transition legality is base-class physics). 'Where you go to query open tickets.' Built in rungs: child a (register.py) is the register alone — a compiled index over two vantages already on disk (OPEN boats = tickets voyaging in CairnCommons/tickets/; IN-PORT boats = components with a history.json beside their code); primitives `register()` -> {open, in_port, fleet, counts}; `open_boats(reg)` / `in_port(reg)` / `find(reg, id)`. child b (clearance.py + registry.py, PROVEN 2026-07-24) is the CLEARANCE gate — `clear(workflow, target, *, actor, boat_id, boat_owner, method, registry, grant=None, history_path, state_path)` binds authority (Law 6, owner-gated + delegable per-operation via `mint_grant`/`Grant`, non-ambient), proven-space (Law 8, `MethodRegistry.resolve` — the method is proven, never called), and the wrapped base-class rules+truth (Law 4/7, `base/transitions.emit`) before a cursor moves.",
        "why": "A workflow-transition factors into THREE rungs (MAP.md, the emit-chokepoint trichotomy resolved 2026-07-21): RULES (is this transition legal for this class? — base-class physics, inherited, un-delegable), AUTHORITY (who may invoke it? — owner-gated, delegable per-operation, non-ambient, Law 6), and TRUTH (record it — local in each boat's own history, central in this register). This gives the record-and-clearance half of the chokepoint a HOME without becoming the ground_loop-executor goof: decision local, record central, execution never here. The register is compiled inference at FLEET scale (Law 1) — 'what is open' answered from structure, not re-derived by walking every boat by hand — the same move as the per-ticket projector (charter-state-history-split), one scale out. It is an INDEX, never a rival record (Law 7): every entry's standing is read from the boat's own cursor and points back to it; the register invents no truth a boat does not already hold, so it cannot drift from the fleet it describes.",
        "invoke": "import: `from cairn.harbor_master import register`. Query the live fleet: `python3 -m cairn.harbor_master.register`. Prove: `python3 cairn/harbor_master/proofs/test_harbor_register.py` (exit 0 = green), or under the tester via `TesterDevice().run_proof(...)`. Dependency-light — pure file reads + the projector's pure core; no db_domain, no network; runs bare.",
        "node_class": "code-seam (device by role — the harbor). PROVEN 2026-07-24 — all three rungs landed: the register primitive (child a) + the CLEARANCE gate (child b) + the VOYAGE view / TRAFFIC IMAGE (child c, voyage.py — the harbour master's state-right-now main view, which proves the PARENT). The BaseDevice introspection FACE + bus-fronting are later rungs — filed edges below, each deferred until the thing that pulls it exists (as inference_domain and db_domain deferred their faces).",
        "entry": "FRESH design, ordinary mechanism. The design — the harbor that gates movement and keeps the register but never sails, the three-rung factoring of a transition (rules/authority/truth), the register as compiled inference one scale out from the per-ticket projector — is Cairn's, converged with Akien 2026-07-21 (the /sorted that landed the harbor-master metaphor and resolved the emit-chokepoint into a trichotomy). The mechanism is pure file reads + the projector's pure core; nothing crossed as carried code. The register is computed on read, so child a's filed db-vs-git placement edge DISSOLVES — there is no stored second copy to place.",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split; this file stays pure intention+why, the operational voyage lives in the sidecars (Law 1/4/7)",
        "filed_edges": [
          "(a) THE CLEARANCE GATE (child b) — BUILT + PROVEN 2026-07-24 (clearance.py, registry.py, proofs/test_clearance.py). Grants/refuses a transition through the owner's gate (authority rung), delegable to an igor per-operation via a non-ambient Grant (Law 6); records the movement through the wrapped chokepoint's journal door (Law 7). The base-class emit-chokepoint it clears against (cairn/base/transitions.py) landed the same session, which unblocked it. The method-registry (Law 8, retired with the driver-executor cairn 584aa74) RETURNED here — re-instated, not re-derived (Law 1). STILL FILED for grow-against-need: the registry is in-memory (durable proof-pointer store is deferred until a consumer restarts across process); the clearance gate has no runtime FACE / bus-fronting yet (an igor calls `clear` in-process today) — deferred until the rack pulls it.",
          "(b) THE VOYAGE VIEW (child c) — BUILT + PROVEN 2026-07-24 (voyage.py, proofs/test_voyage.py, 4 teeth, sealed under a real netns). The TRAFFIC IMAGE: state-right-now, composed as DATA over the register. Two derived, computed-on-read structures — the EMERGENT GATE (at-sea boats grouped by transition-class; many workflows share one gate) and the VOYAGE STITCH (`_canon` normalises the hyphen/underscore id-spelling so `harbor-master` joins `harbor_master`, the join `find()` left open — filed-edge e now closed for the stitch). The one honest flag today is MID-VOYAGE (a boat at sea AND berthed — the silently-stuck, the boon of the broad view); the rest of the design's marker vocabulary ([R]/[X]/[W]/[F]/starvation) is filed grow-against-need on the gate's-queue IOU (`voyage.GROW_AGAINST_NEED`; design in CairnCommons/notes/held-traffic-image.json). Rendered by web_server child d at /harbor. Owns nothing (a projection over the register, Law 7).",
          "(c) DONE-BUT-NOT-BERTHED is surfaced, not resolved: a ticket at cursor PROVED still living in tickets/ shows as an OPEN boat standing PROVED — migration debt the harbor makes visible (Law 7, honest index). Auto-berthing a PROVED ticket into its component history is a later move, not child a's.",
          "(d) COMPUTED ON READ — no stored register. If fleet queries turn hot, an event-fired cache (the FileChanged pattern the intentions_model_compiler uses) is the grow-against-need step; never a poll. Do not add it before a hot query exists (build-minimal).",
          "(e) ID NORMALIZATION — CLOSED for the stitch (child c): `voyage._canon` normalises hyphen↔underscore so a boat's two vantages join under one voyage. `register.find()` still matches literally (its rung's honest job — show each source faithfully); the join lives in voyage, one layer up."
        ],
        "gates": "proof gate (tester) — a proof a hollow build could not pass: QUERY the fleet and get BOTH open and in-port boats non-empty with their standings (a register that can only show one berth proves nothing about querying the fleet — the non-hollow floor); show the register is an INDEX over the boats' own truths, not a rival record (every in-port standing is byte-equal to project(that boat's history).cursor; every open standing is read from the ticket's own state — drift reds it, Law 7); show every boat points back to a source that exists (the index references, never replaces); show open ∪ in_port is exhaustive and berth-disjoint (no boat invented or lost). Durability gate: proven, persisted beside the proof via validation_store.persist_validation, committed.",
        "falsifier": "RED on any of: (1) the register shows only one berth over the real fleet (open OR in_port empty) — it cannot answer 'query the fleet', the whole point; (2) a register entry's standing diverges from the boat's own record (an invented or stale truth — Law 7 breach, a rival record not an index); (3) a boat's source does not exist on disk (the index points at nothing); (4) a boat appears in neither or is silently dropped (fleet != open ∪ in_port). Wrong-intent signals (the design slips Akien named): if the harbor starts EXECUTING or SCHEDULING work it is the ground_loop-executor goof; if it comes to OWN the boats (mutating a workflow's content, not just indexing its movement) it has become the fleet owner, not the harbor; if devices advance workflows AROUND it once the clearance gate lands (ambient transitions), the authority rung has failed (Law 6).",
        "how_it_learns": "The learning SURFACE is now open (child b landed) but not yet harvested, and honestly so. With clearance gating transitions, each cleared crossing records WHO cleared it and whether it was delegated — so the boats' histories (compiled by child a's register) are becoming the evidence of how the fleet actually moves: which transitions stall, where boats pile up, which clearances get delegated to which igor. That is the raw material for learning the gates (the learns-its-gates founding intention). What does NOT exist yet: any judgement over that evidence — the gate today applies fixed rules (owner-or-grant), it does not yet adjust a clearance policy from what it has seen. The precondition is met (an honest, non-drifting record of every cleared move); the harvesting driver is a later rung, deferred until there is a run of real transitions to learn from (build-minimal — do not build the learner before there is a fleet moving through the gate).",
        "traces_to": [
          "Law 1 (the aggregate is compiled, not re-derived — fleet-scale compiled inference, the projector one scale out)",
          "Law 7 (the register is a record of truth: an index over the boats' own histories, append-only in each boat, never a rival that drifts)",
          "Law 6 (exactly one owner; the clearance rung is owner-gated, delegated only through the gate — child b)",
          "Law 4 (the rules are base-class physics, not the harbor's to grant — gates, not supervisors)",
          "Law 8 (the method a workflow-string resolves to enters only proven — the method-registry returns with the clearance gate)",
          "Telos 4/5 (the self-hosting record of workflow)",
          "build order: harbor_master, after the heartbeat+bus+system-device+inference+web-server block; waits on the charter-split (done) + the base-class emit-chokepoint (child b's dependency) — MAP.md:996"
        ],
        "owner": "CC owns harbor_master's code + proofs and gates writes to them. Akien owns the design ratification (the harbor-master metaphor and the three-rung transition factoring, converged 2026-07-21: 'looks good to me') and the node-class definitions. Each boat is owned by its own intention — the harbor indexes the fleet, it does not own the boats.",
        "proof": "child a — proofs/test_harbor_register.py: the fleet queries with both berths non-empty (non-hollow); the register is an index not a rival record (in-port standings byte-equal to project(history).cursor, open standings read from the ticket's own state); every boat points back to an existing source; open ∪ in_port is exhaustive and disjoint; the reflexive tooth — the harbor sees its OWN berthed history AND its own open ticket. child b — proofs/test_clearance.py (7 teeth): clearance is required (unauthorized refused, nothing written); it is delegable per-operation and NON-AMBIENT (the hollow-killer — a grant for one op does not authorize another target/boat/actor); authority never buys an illegal move (even the owner — Law 4); the move's method must be proven under the tester, and a red-proof method is refused admission (Law 8); the crossing is recorded two-vantage with cleared_by (Law 7). Both run by the tester and sealed beside the proof — child b under a REAL netns (green/sealed). Green: `python3 cairn/harbor_master/proofs/test_clearance.py`. child c — proofs/test_voyage.py (4 teeth, sealed under a REAL netns): the EMERGENT gate is SHARED (a gate holds >1 at-sea boat over the real fleet — non-hollow, Law 8); the voyage STITCHES a boat's two vantages across id-spelling and FLAGS the silently-stuck (mid-voyage boats actually surface — the boon); the image OWNS NOTHING (each occupant byte-equal to its register entry, the gates exactly the open set, Law 7); the calm/flagged partition is exact per gate. Green: `python3 cairn/harbor_master/proofs/test_voyage.py`.",
        "provenance": "Born 2026-07-24 as the next step of the code spine, on the foundation the charter-state-history split made uniform (child c PROVED 2026-07-24 — every component's history is now the same shape, which is exactly what makes a fleet-scale index over them possible). Cast 2026-07-21 via /sorted as the harbor-master ticket; child a (the register) is the first buildable rung because its sole dependency (the split, whose local histories it aggregates) has landed — child b waits on the base-class emit-chokepoint, child c is deferred. Built minimal (the register alone), grown against need. child b (the clearance gate) followed the same session once its dependency — the base-class emit-chokepoint — landed: the authority rung that gives the emit-chokepoint's clearance half a home, with the method-registry re-instated from the retired ground_loop executor (Law 1). Does not yet retire any CLAUDE.md IOU; advances Telos 4/5 (the self-hosting record of workflow) from thesis toward a queryable fleet whose moves are gated. child c (the VOYAGE view / TRAFFIC IMAGE, voyage.py) BUILT + PROVEN 2026-07-24 alongside web_server child d (the /harbor pane that renders it), from the design consolidated in CairnCommons/notes/held-traffic-image.json — the PARENT PROVES with it. The emergent gate, the voyage stitch, and the mid-voyage flag are the honest slice buildable from disk today; the rest of the marker vocabulary + the starvation/latching-alarm are filed grow-against-need on the gate's-queue IOU."
      }
    },
    {
      "id": "diagnostic_inspector",
      "kind": "component-charter",
      "source": "cairn/diagnostic_inspector/intention+why.json",
      "born": "2026-07-24T16:42:47-06:00",
      "content": {
        "component": "diagnostic_inspector",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "remit": "THE EXPLICIT REMIT (Akien, verbatim): 'Its job is to save you tokens in exploring the issue in the same way the prebuild step does for coding. And to keep getting better over time.' The inspector is the DEBUGGING ANALOG OF THE PREBUILD STEP: prebuild pre-processes so CC (the expensive resolver) spends tokens only on the novel coding; the inspector pre-processes a red so CC spends tokens only on the novel diagnosis — never on a second exploration to gather data the first pass should have carried (Law 1, the cocoon). 'Gets better over time' is monotonic and physical: every gap it learns is one fewer second-exploration forever, so the tokens it saves only rise. Those two sentences ARE the falsifier — everything below serves them or it is dead weight.",
        "what": "THE SMART READER of DiagnosticBase's dumb breadcrumbs, factored (Akien) into the three parts of the diagnostic surface. (1) LOGGING — gates emit microsecond-stamped breadcrumbs (cairn/base/diagnostic.py); that is STATE, so its durable home is instance-space (~/.cairn/, cache-class json log). Mailbox here is the v0 in-memory STAND-IN for that log — a proof fixture so an emitter->receiver->inspector chain runs end to end, NEVER the runtime home. (2) CALLBACK — when a watched thing happens (a red, a watched gate-contact) a callback FIRES 'inspect now'; event, not poll (the shrinking-footprint discipline). (3) INSPECTOR — this module. It REACTS to the callback: applies FILTERS to the log and produces FINDINGS. FILTERS are predicates (predicate(record)->bool) conjoined at inspection time inside a fired run (never a standing scan/firehose): by_pointer (the primary — one item's whole transaction, the complete-first-pass slice) and by_gate are built because they have consumers; by_time_band / by_severity are filed edges the abstraction admits without a query-language built ahead of one. FINDINGS are json whose entire audience is CC: the ordered, isolated transaction slice (every emitted value carried, source deep-copied never mutated) PLUS the COMPLETENESS finding.",
        "why": "Instantiates the concept-piece I-complete-diagnostic-on-first-pass — findings that resolve the issue on the FIRST pass, a method Akien has proven over decades (2007 test framework, UU, Trouble Tickets; his first-reach debugging tool; half-day->10min). DiagnosticBase.emit left the intelligence OUT of the breadcrumb deliberately (Law 1 — the diagnostic surface's inference compiled ONCE, here, the inference_domain move); this is that one place. The learning-loop the inspector CARRIES makes the findings self-teaching (I-learns-its-gates pointed at the diagnostic surface): completeness rises monotonically and no learned gap recurs — which is exactly 'gets better over time' made physics.",
        "invoke": "In code: from cairn.diagnostic_inspector import Inspector, by_pointer, by_gate, CompletenessRegistry, Mailbox. Wire a Mailbox as a DiagnosticBase receiver (dev.set_diagnostic_receiver(box)); on a fired callback, findings = Inspector(registry).inspect(box.records(), by_pointer(ptr)[, by_gate(g)]) — filters conjoin. Fold a miss: inspector.record_miss(gate, key) ('learned' first time, 'recurred' after). Persist learned completeness: inspector.registry.save(path) / CompletenessRegistry.load(path). Prove: python3 cairn/diagnostic_inspector/proofs/test_inspector.py (exit 0 = green).",
        "node_class": "code-seam — berths in cairn/ (class-space) beside the breadcrumbs DiagnosticBase.emit sends. Born as children a (the crawler) + b (the learning-loop) of the ticket, then REFACTORED (2026-07-24) from diagnostic_interpreter/assemble(records, pointer) to the inspector/filters/findings shape Akien named — the same proven engine, re-seated on the true intention (an inspector reacting to a callback, filters composing, findings for CC). Child c (the `cairn diagnose <pointer>` CLI face, via cairn-command) is the LEAST-USED secondary access path (Akien: 'of all the secondary access paths, almost certainly the one I'm least likely to use') — buildable, lowest priority; the engine is the substance.",
        "how_it_learns": "YES — learning is its WHOLE SECOND HALF and the 'gets better over time' half of the remit, not an add-on. The CompletenessRegistry the Inspector CARRIES across inspections IS a learns-its-gates instance whose endpoint is the diagnostic findings: its GATE is a transition-class, its EVIDENCE is a resolver hitting a gap the findings should have carried, and its CONFIDENCE-MOVE is folding that key into what the class's findings must include (record_miss). The special-class falsifier is the learning rule made physics: a FIRST miss is a feed-forward signal ('learned'); an already-learned key absent from a later findings is a RECURRENCE, surfaced LOUD (inspect's completeness.recurrences, complete=False) — the terminal falsifier, a Law 1 defect it refuses to let pass silently (the surface failed to save the tokens it had already learned to save). Distinct endpoint from the decision-autonomy store in CairnCommons/learning/ (which learns whether CC may ACT); kin, cross-referenced, NOT merged — a distinct store with a distinct key. Its horizon: where learned completeness durably lives (a commons git-JSON store vs CC's shim when shims land) — injected path today, real home a filed edge.",
        "traces_to": [
          "THE REMIT (save CC tokens exploring an issue as prebuild does for coding; get better over time) — the charter's own first field, Akien-stated",
          "I-complete-diagnostic-on-first-pass (the concept-piece this instantiates — the findings deliver all resolve-data on the first pass)",
          "Law 1 (a second exploration to gather more failure data is re-deriving the settled — the waste the complete findings kills; and the inspector's inference is compiled ONCE here, the cocoon)",
          "Law 7 SHARPENED (the diagnostic surface is not merely loud but COMPLETE on first emission; a learned recurrence is surfaced LOUD, never silent; the findings are a transient surface, the RESOLUTION CC produces is the record of truth)",
          "I-learns-its-gates (the learning-loop is gate-learning pointed at the diagnostic surface — a distinct endpoint: what the findings must CARRY)",
          "Law 6 (the emission stays a thin pointer; the inspector reads and never emits, never copies the ticket, never mutates its source records — the dumb-breadcrumb / smart-reader split)",
          "cairn/base/diagnostic.py (DiagnosticBase.emit — the breadcrumb this crawls; its docstring named this inspector as 'the next build')"
        ],
        "owner": "CC owns the diagnostic_inspector code + its proof and gates writes to them. The inspector READS breadcrumbs and logs; it never emits and never owns the tickets it points at (Law 6 — owned data stays with its owner; only the pointer crosses, and the inspector follows it, it does not copy).",
        "proof": "proofs/test_inspector.py — eight teeth a hollow inspector could not pass: findings filter ONE pointer's transaction ordered by the 6th-decimal stamp and isolated (real DiagnosticBase emissions, other pointers excluded — Law 6); FILTERS COMPOSE (by_pointer AND by_gate narrows to the conjunction — 'filters' is plural, not a single hardcoded match); it reads without mutating (deep-copied slice); a first miss is 'learned' and folded forward so the next findings require it, routed THROUGH the Inspector (the inspector is what gets better — Law 1); a learned gap that recurs is complete=False and named in recurrences (LOUD, Law 7); a complete findings is clean; record_miss distinguishes 'learned' from 'recurred'; learned completeness save->load round-trips (a missing file is an empty registry, not an error). Green bare (python3) and sealed under the tester; VALIDATION persisted beside-code at validations/test_inspector.json.",
        "falsifier": "RED, by physics, on any of: (1) the findings are INCOMPLETE by construction — they drop an emitted value or bleed in another pointer's records (not a faithful, isolated slice); (2) filters do NOT compose — a second filter is ignored so the conjunction is a lie; (3) the learning-loop records a miss but never changes the next findings (learning theater — the fold is a no-op); (4) an already-learned gap recurs and the findings stay SILENT about it (the terminal falsifier — must be loud, Law 7); (5) the inspector grows an emit path (it becomes a second emitter, collapsing the dumb-breadcrumb / smart-reader split — Law 6); (6) it mutates its source records (it must read, never write, what it is handed); (7) the learned-completeness store is merged into the decision-autonomy gate-store (two distinct learnings collapsed); (8) the inspector runs as a STANDING SCAN rather than reacting to a fired callback (the firehose/poll the shrinking-footprint discipline rejects).",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split; this file stays pure intention+why, the operational voyage lives in the sidecars (Law 1/4/7)",
        "filed_edges": [
          "(a) v0 reads the in-memory Mailbox (a proof fixture); the durable breadcrumb log (instance-space ~/.cairn/, cache-class) is the real logging home, read only when a real crawl demands it — this is WHY child c (cairn diagnose) is genuinely blocked, not merely low-priority: no durable source exists yet",
          "(b) the CALLBACK that fires 'inspect now' (event, not poll) is named in the design but not yet wired to a real red — grown against the first real fired inspection",
          "(c) the durable home for learned completeness (a commons git-JSON store vs CC's shim when shims land) is a filed edge — the store path is injected, proven against a temp",
          "(d) by_time_band / by_severity filters — the abstraction admits them; build each when a crawl demands it",
          "(e) the `cairn diagnose <pointer>` CLI face (child c) — the least-used secondary access path; buildable via cairn-command, lowest priority",
          "(f) the concept-piece it instantiates is CAST but awaiting Akien's signature gate"
        ],
        "gates": "proof gate — proofs/test_inspector.py, runnable bare (python3) and under the tester. Durability gate: not done until proven AND the VALIDATION is persisted beside-code (validations/test_inspector.json) AND committed (git).",
        "provenance": "Born 2026-07-23 as children a+b of the diagnostic-interpreter ticket (instantiating the concept-piece I-complete-diagnostic-on-first-pass); the shallow-copy defect surfaced on the first proof run (the read-never-mutate tooth) and was fixed to a deep copy before the first seal (Law 3). REFACTORED 2026-07-24: Akien reframed the diagnostic as 'part logging, part callback, part inspector that reacts to that callback', with filters and findings, the CLI as a rarely-used secondary path, and the report as 'json intended for you [CC]' — then stated the explicit remit (save CC tokens as prebuild does; get better over time). The engine was proven; the refactor re-seated it on the true shape (diagnostic_interpreter->diagnostic_inspector, assemble->Inspector.inspect(log, *filters), report->findings) and added the compose-filters tooth. Records of truth from the interpreter life (history's early entries, the interpreter VALIDATION) STAY by Law 7 — the name-change is a new event appended, not an erasure."
      }
    },
    {
      "id": "trouble",
      "kind": "component-charter",
      "source": "cairn/trouble/intention+why.json",
      "born": "2026-07-25T18:32:41-06:00",
      "content": {
        "component": "trouble",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "THE TROUBLE LANE — the terminus of the escalation ladder, and the single door to it. A failure that cannot escalate anywhere else raises a trouble here. The FIRST raise writes a ticket and notifies its recipients; every recurrence while that ticket is live increments a count and notifies NOBODY. Only the recipient clears it, and only by naming what changed. A fault that returns after a clear is a NEW ticket at count 1, carrying the fix that did not hold. The records are git-JSON in CairnCommons/troubles/ — a trouble is shareable provenance, and losing it loses knowledge.",
        "why": "Because a system that fails silently cannot tell you how to improve it, and a system that shouts the same failure fifty times cannot either. Akien, 2026-07-25: 'a poke into nothing, a message that can't be delivered, these are bugs. nothing should fail silently. everything that can't escalate somewhere else should fail to trouble tickets until we get no more from the system. it's literally telling you and i how to improve it :)' — and then the damping half: 'a system alarm is raised. it's not raised again and again, it's count can increment... but demanding more attention when there is none to give is not productive.' The trouble stream IS the improvement backlog; the count is what keeps it readable.",
        "the_damping_insight": "THE TROUBLE TICKET IS THE DAMPER, and this is the reason the component exists in this shape. The open question it closed was chatter: a predicate flapping across its line raises the same fault fifty times. The reflex answer was hysteresis — pick a width, suppress inside it — which needs a NUMBER nobody has measured, and a width set too wide silently eats real faults. Identity-plus-count dissolves the question rather than tuning it: the scarce thing is ATTENTION, not occurrence, and attention is spent in full the moment the first ticket lands. Fifty flaps become one ticket that says 50, with no width to guess and nothing silently eaten. (Law 1, one level up — the answered question became structure instead of a knob.)",
        "how_does_this_component_learn": "It does not learn on its own, and that is deliberate — the LANE is dumb so the stream can be read. What learns is the pair of humans (for now, one human and CC) reading it: the trouble stream is the standing evidence of where the system is weak, and its horizon is its own emptiness. The count is the learning signal that matters — a ticket at count 1 is an incident, a ticket at count 400 is a design defect, and the same file says which without anyone re-deriving it. `prior_attempts` and `recurred_after_clear` are the second signal: a fault that keeps coming back after fixes names each fix that did not hold. If a learning loop is ever wanted here, that is the data it would run on; nothing is built ahead of that need.",
        "laws": {
          "law_6": "ONE DOOR. TroubleDevice is the only writer of a trouble file. Raising, incrementing and clearing all go through it, because the lane's whole value is that a count is trustworthy, and a count is only trustworthy if there is one way to increment it.",
          "law_7": "BOTH HALVES, SPLIT DELIBERATELY. The RECORD is never damped — the count is exact, the timestamps move, a bounded tail keeps the shape of the recurrence. What is damped is the NOTIFICATION. A record of truth never collapses; a demand for attention always may.",
          "law_3": "The normal operating state — live() empty — is a MEASURED claim about the system, readable at any moment, not an aspiration in prose.",
          "law_8": "The lane must not go dark on its own fault: a malformed trouble file surfaces AS a live trouble, and any standing that is not an explicit CLEARED counts as live. Silence in the component built to end silence would be the hollow build."
        },
        "the_normal_operating_state": "live() is EMPTY. Akien: 'the intention for the normal-operating-state is there won't be any active trouble tickets. and it'll be a rarity when there is one. but we're a long way from there.' The distance from zero is the honest measure of how far the system has to go, which is why the count is on the device's own state() rather than somewhere it has to be gone looking for.",
        "who_is_told": "For now, CC gets them all (DEFAULT_RECIPIENTS). This is a stopgap and is written down as one. Akien: 'for now, you get them all. so when they show up, you can fix. but eventually you'll have help that'll do that for you.' The notified and cleared sets are already PER-RECIPIENT — because the clear is per-recipient — so that help arrives as an entry in a list, not a redesign.",
        "filed_edges": [
          {
            "edge": "THE POKE. A raise should poke its recipients over the bus; today it returns who WOULD be told and writes the record.",
            "why_it_waits": "Delivery to an external device (CC, Akien) is bus-completion child e, unbuilt. Faking a notification here would be the hollow build — the ticket is written either way, so nothing is lost, and the honest gap is visible in the return value."
          },
          {
            "edge": "AUTOMATIC RAISING from the failure sites — an undeliverable poke, a carrier that raised, a held diagnostic with no receiver.",
            "why_it_waits": "Each site needs its own identity chosen deliberately (identity is what makes two occurrences the same trouble, and a bad identity either splits one defect into hundreds of tickets or merges hundreds into one). Wiring them one at a time, with the identity argued each time, beats a blanket hook."
          },
          {
            "edge": "HYSTERESIS / chatter damping at the callback, filed on bus-completion.",
            "why_it_may_never_be_built": "Likely DISSOLVED by this component rather than deferred: if the flood only matters because it demands attention, identity-plus-count already cures it with no width to guess. Held as a hypothesis, not a conclusion (Law 3) — re-check when a real flapping predicate lands and it is measured whether anything still hurts."
          }
        ],
        "provenance": {
          "cast": "2026-07-25, from Akien's two statements above",
          "born_from": "The first trouble was hand-written before this device existed (CairnCommons/troubles/append-door-has-no-schema-gate.json) because the append door failed silently and the lane it should have escalated to had no address. The directory was named in CLAUDE.md's roots table and did not exist on disk.",
          "found_on_first_contact": "The device's live() filter tested equality against 'OPEN' and read the hand-written standing 'OPEN — reported complete, awaiting Akien's ratify' as NOT live — reporting zero troubles with an open one on disk. Fixed structurally: only an explicit CLEARED is quiet. The component's first act was to catch itself doing the thing it exists to prevent.",
          "proof": "cairn/trouble/proofs/test_trouble.py"
        }
      }
    },
    {
      "id": "orient",
      "kind": "component-charter",
      "source": "cairn/orient/intention+why.json",
      "born": "2026-07-27T11:52:49-06:00",
      "content": {
        "component": "orient",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "The prebuild orientation instrument: deterministic scans that MEASURE the territory before CC answers, with a failover seam to Hex for the question no scan answers. Scans (cairn/orient/orient.py): `call_sites(name)` — where a capability is actually CALLED (AST, never a word-grep); `device_census()` — per component, what is measurably there (charter on disk, BaseDevice subclasses, proofs, latest validation verdicts, emit call sites); `repo_truth()` — HEAD vs @{u} and working-tree dirt from git plumbing, never from a narration; `import_map(file)` — which modules ACTUALLY ENTER a file, the loose package form resolved to the module it binds (spelling is not capability, applied to imports; the proofs' allowlist teeth compose it). `deepen(question, resolve=...)` — the failover: routed through inference_domain.resolve (the sole path to the host; metered, cached), its answer returned under `read`, never `measured`. CLI: `python3 -m cairn.orient.orient census|calls <name>|git|imports <file>` — inference-free.",
        "why": "2026-07-27: working from the same data, CC reached three different conclusions about the same system in one morning (the logging floor: '0 of 13' → 'one floor missing' → 'floor built since 07-22, zero call sites in bus'). Every wrong conclusion had one root: A PROXY WAS READ INSTEAD OF THE THING — a word instead of a capability, a record instead of the world, CC's own narration instead of the remote. Akien's framing, verbatim: 'we need a prebuild type step ... we build a script for this function that can _fail over to_ hex for any additional help in prepping anything you need. because we'd build from the start as a learning device.' The deeper why is Law 4 pointed at CC itself: a discipline living in CC's habit varies with the model, the context length, the hour; a scan does not. Orientation compiled into structure is Law 1 (the settled territory is walked, not re-derived per question) and Law 3 (an orientation is a measurement, not an impression). Ranked how-we-work AND foundational by Akien, top of the queue.",
        "invoke": "import: `from cairn.orient import orient`. Prove: `python3 cairn/orient/proofs/test_orient.py` (exit 0 = green), or under the tester. CLI needs only stdlib + git; `deepen` needs a caller-injected resolve (the sole-path seam).",
        "node_class": "device (instrument) — CC-facing tooling in class-space; no runtime state, no daemon, fires only when invoked (event, not poll).",
        "entry": "FRESH build, seeded entirely by measured failures from 2026-07-26/27. Nothing carried from the quarry. Sibling-to-be: the post-build inspector (same filter idea, post-build trigger) — deliberately NOT merged at birth; the shared scan/filter library is allowed to prove itself instead (cc-leans-single-door: the merge is a suggestion, not a reflex).",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split.",
        "how_it_learns": "THE POINT OF THE DEVICE — it learns its scans from corrections. When Akien catches CC reading a proxy instead of the thing, the check that would have caught it becomes a scan here, carrying that correction as its PROVENANCE (a required field — proofs tooth 12 refuses a scan whose provenance names no dated correction, and refuses a scan that joins the registry without joining the teeth). Founding set, one per correction: call_sites ← the 'logging: 0 of 13' word-grep (2026-07-27); device_census ← system state reported from records, wrong about the world three times (2026-07-26/27); repo_truth ← the echo label that attested an unhappened push (2026-07-26). The failover deepens, the scans learn: Hex is for the novel; a recurring question becomes a scan (Law 1 as the growth rule). RULING 2026-07-28 (nexi everywhere, held-nexi-everywhere): the scan registry is HARDWARE — it changes only by installation (evidence-provenanced, ratified, versioned), never in place; a measurement is always hardware. The learning path itself GRAFTED to the question-nexus shape 2026-07-28 (nexus.py — corpus walkable, proposals ride the loop, admission unchanged); see filed edge (f). AND THE LOOP HAS CARRIED ITS FIRST REAL CORRECTION, same day (ticket import-map-scan): allowlist teeth measuring import SPELLING instead of the module entering (the twice-fired red) rode deposit -> counsel (the corpus recognized the word-not-capability family, third member, from the walk alone) -> propose_scan -> Akien's ratification -> installation as `import_map`, with six proofs' hand-rolled walkers recomposed over it. The registry is not static: the wrong-shape signal has its counter-example, and the loop is the path it took. Narrative: CairnCommons/notes/2026-07-28-first-brick-loop-learning.md.",
        "filed_edges": [
          "(a) THE POST-BUILD INSPECTOR is the sibling device Akien named in the same ruling ('the next thing') — python, FILTERS, new-failure-new-filter, plus a one-time inference-free whole-repo sweep (the graft pattern: after the sweep, every build passes the gate, so the sweep is never needed again by construction). NOT built yet; next stone.",
          "(b) the /orient SKILL (the mouth: Akien types /orient <topic>, the skill runs the scans and hands CC the picture) — thin caller over this machine; staged beside the other skills when cast. FIRST MOUTH LANDED 2026-07-28 from the other direction: /chart's orient stage (the chart device's nexus #1, ticket chart-orient) runs its floor THROUGH device_census — composition pinned by a proof tooth and an import allowlist. A direct /orient verb for bare scans remains open. Distinction worth its ink: CC built that floor's first cast as a parallel charter-glob roster, never having surveyed THIS component — the exact proxy-error this instrument exists to end, caught by the floor's own first live fire.",
          "(c) deepen()'s LIVE wiring (building the real resolve from inference_domain + host.ollama_resolver at the CLI) is written nowhere yet — the seam is proven (teeth 13-14), the wiring waits for the first real deepening need (build-minimal).",
          "(d) THE FALSIFIER'S COUNT has no counter yet: answer-reversals-after-Akien-pushes-back per session is the instrument that says orient is working; today that count lives in session records, not a meter.",
          "(e) scans-vs-filters as ONE shared library across orient and the inspector — CC's suggestion, deliberately unmerged until both exist and the sharing proves itself.",
          "(f) GRAFT TO NEXUS SHAPE (ruling 2026-07-28, CairnCommons/notes/held-nexi-everywhere.json — 'must not use llm' replaced by 'may only use llm to learn'): on orient's NEXT learning event (the first correction that would grow a scan), the growth runs through the question-nexus loop — tree stratum for the correction corpus, ceiling proposing the scan, admission still gated by tooth 12's dated-correction provenance + Akien's ratification. Fire-path unchanged and un-negotiable: scans are HARDWARE (a measurement is deterministic and replayable; the tree is never consulted at measure-time — its learnings COMPILE INTO the registry). FIRED 2026-07-28, same day — Akien redefined the event: 'building ANYTHING should be the learning event for the orient/inspector tickets.' Every build orients, so every build is the event; the chart-tree stone's build fired it. Ticket CairnCommons/tickets/orient-nexus-graft.json cast at TICKETME (BUILDME gated on chart-tree proving the shared multi-tenant tree machinery); falsifier (3) keeps its teeth throughout. BUILT AND PROVED same day (chart-tree proved, Akien: 'go go go'): orient/nexus.py — the correction corpus (orient_corrections_nodes, owner 'orient') through chart's owner-generalized nexus verbs; dated-provenance deposit door (tooth 12's demand, one layer early); counsel with the labeled floor; propose_scan assembles and can never admit; the four founding scars live through the real embed seam. Proof proofs/test_orient_nexus.py (6 teeth, netns-sealed twice) pins the fire-path/tree separation by AST allowlist BOTH directions — orient.py cannot import the tree machinery, so a scan physically cannot consult it at measure-time. First counsel evidence n=2: 0.7707 over the floor on a close paraphrase, 0.5775 (correct scar ranked first) under it on an abstract one — logged for the floor-tuning edge, floor unmoved."
        ],
        "gates": "proof gate — proofs/test_orient.py (16 teeth, the fire-path; tooth 16 pins import_map's three-spellings-one-capability claim) + proofs/test_orient_nexus.py (6 teeth, the graft), run by the tester. Durability gate: not done until proven AND committed.",
        "falsifier": "RED on any of: (1) a scan answers by mention rather than capability (tooth 1 — the founding error recommitted); (2) a scan reports a smaller world than exists instead of refusing (unparseable file, empty root, non-repo — teeth 3/7/11); (3) a Hex answer lands under `measured` or is produced with no injected seam (teeth 13-14 — inference laundered as measurement); (4) a scan joins the registry without provenance naming a dated correction (tooth 12 — a check nobody was taught by); (5) orient.py imports an outbound-capable module (tooth 15 — a second door to the host). WRONG-SHAPE SIGNAL: if Akien's corrections stop becoming scans — if the registry is static three corrections from now — the 'learning device' half is hollow and this is just a linter.",
        "proof": "proofs/test_orient.py — capability-not-mention pinned on a synthetic tree (one call site among five mentions); refusals loud and diagnostic; census rows built from the filesystem only, real tree by invariant; repo truth from plumbing; the learning-device shape (provenance) required of every scan; the failover seam labeled and sole-path. Green: `python3 cairn/orient/proofs/test_orient.py`.",
        "owner": "CC owns orient's code + proofs and gates writes to them. Akien owns the design (the prebuild step, failover-not-partner, learning-device-from-the-start — his ruling 2026-07-27) and the ratification of new scans' place in the registry.",
        "provenance": "Born 2026-07-27, the morning of three-conclusions-from-one-dataset, in the same conversation that corrected CC's fabricated attribution on the librarian ruling — both failures share the root this device exists to end. Akien moved it to the top of the queue mid-build: 'it's both how-we-work and foundational-components.' First measured act of the census: flagging its own component's missing charter — this file is the fix."
      }
    },
    {
      "id": "build_inspector",
      "kind": "component-charter",
      "source": "cairn/build_inspector/intention+why.json",
      "born": "2026-07-27T12:02:49-06:00",
      "content": {
        "component": "build_inspector",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "The post-build gate: python FILTERS that judge orient's measurements and red a build whose component fails one. Founding filters (cairn/build_inspector/inspector.py): `charter_on_disk` — code with no intention+why.json; `proofs_exist` — code with zero proofs (Law 8); `silent_device` — a BaseDevice subclass that never calls emit() outside proofs (the MAP.md:434 claim, enforced); `state_is_projection` — state.json must equal project(history.json), the drift check half of the compiled-view IOU. `inspect(component=...)` gates one build; `inspect()` is the whole-repo sweep. CLI exits 0 clean / 1 findings — gate-able by anything that reads an exit code. The verdict is always HARDWARE (ruling 2026-07-28, held-nexi-everywhere — reworded from 'inference-free by construction'): no deepen seam exists at fire-time; a gate that consults an oracle is not a gate. Inference may serve the LEARNING of filters, but a learning lands here only by installation — evidence-provenanced, ratified, versioned — never mid-fire; a verdict's replayability (same input + same registry revision = same verdict) is what lets it be permanent in a record of truth (Law 7).",
        "why": "Akien, 2026-07-27, verbatim: 'the next thing is a post build inspector (also in python with FILTERS) that can catch these kinds of things. we find a new thing, we add a new filter. can also be a seperate, command line only inference free operation to run it on the whole repo once built. we should only ever have to do that once.' The deeper why is his older observation that CC does not add logging (and its kin) until told — 'i have been resigned to the fact that we wouldn't have such logging until some lever presented itself... and a post build inspector is just the thing.' The lever is Law 4: a discipline in CC's habit is policy; a filter at the build gate is physics. 'Only once' holds by construction — the sweep brings the existing tree to the gate one time, then every build passes through the inspector, so wanting a second sweep is itself a finding (a build bypassed the gate).",
        "invoke": "import: `from cairn.build_inspector import inspector`. Prove: `python3 cairn/build_inspector/proofs/test_inspector.py`. Gate one component: `python3 -m cairn.build_inspector.inspector <component>`. The sweep: no argument.",
        "node_class": "device (gate) — class-space tooling; no runtime state, no daemon; fires at the build event, never on a clock.",
        "entry": "FRESH build, orient's first child. Deliberate reuse on the record: orient's device_census is the measuring layer under every row-judging filter — the first evidence on orient's edge (e) (scans-vs-filters as a shared library), earned by use. The registries stay separate: a scan MEASURES, a filter JUDGES.",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter.",
        "how_it_learns": "THE POINT — 'we find a new thing, we add a new filter.' Every filter carries the failure that seeded it as Provenance in its docstring (tooth 10 refuses a filter nobody was taught by). Founding set: charter_on_disk ← orient's census flagging its own missing charter (2026-07-27); proofs_exist ← the true-but-silently-partial bus record (2026-07-25); silent_device ← zero emit() sites in bus against MAP.md:434's 'no device can opt out' (2026-07-27); state_is_projection ← CLAUDE.md's compiled-view IOU, the drift-check half. Growth rule: a failure Akien catches that a filter could have caught becomes a filter, and the class stops recurring (Law 1). The growth path GRAFTED to the question-nexus shape 2026-07-28 (nexus.py — failure corpus walkable, proposals ride the loop, admission unchanged); see filed edge (e).",
        "filed_edges": [
          "(a) CLOSED 2026-07-27 — THE GATE IS WIRED TO THE BUILD EVENT: cairn/base/transitions.py runs this inspector on the component at a journaled crossing's own address before any forward move out of PROVEME, and refuses on red before anything is written (BuildGateRed, findings complete first-pass). Wired at the emit-chokepoint, not the tester, because the clearance gate wraps emit — that is the one door, so the wiring cannot be routed around. Kick-backs never gated; an uninspectable address is refused with its growth path named (no side door, Law 8); a clean exit journals its gate verdict in the crossing record. Live-fired same day: bus's promotion refused by silent_device, history untouched. Proof: test_transitions.py's five gate teeth. Residual jurisdiction note: an UNADDRESSED emit (no history_path) is a string calculation — no journal means the record of truth does not move, so there is nothing to gate.",
          "(b) CLOSED 2026-07-27, same day — the sweep's findings were WORK ITEMS, and the work is done: all six silent devices (was five; sudo_relay joined when the measure learned to check the receiver) disposed one measured pass at a time — bus, sudo_relay (Akien's ruling: the common discipline is the default), ground_loop, tester, trouble, web_server — each with real crossings identified, a pinning tooth, a netns seal. The trouble ticket is CLEARED and the whole-repo sweep reads ZERO findings: 'only once' held — the one sweep found the debt, the gate holds the line from here. The pattern proved out: the inspector reds, the owner disposes (no-enforcer — gated ownership).",
          "(c) candidate filters seeded but unbuilt: stale-'on purpose' comments (device.py:36's transient-absence-written-as-intent — needs a checkable shape first); host-seam seal expiry; charter answers how-does-it-learn (the learning-as-a-pattern IOU).",
          "(d) PARTLY LANDED with (a): CLAUDE.md's compiled-view entry now names state_is_projection-at-the-chokepoint as its drift check — the gate fires without CC choosing to run it, at every journaled PROVEME exit. NOT retired, honestly: a coordinated in-place edit of history+state together passes the drift check (state matches the edited history), and an at-rest component's hand-edit sits unflagged until its next crossing. Full retirement needs append-only verification of history itself (hash chain or git-ancestry check) — that is the remaining IOU.",
          "(f) PACKET JURISDICTION + THE JUDGES BEFORE THE JUDGED (2026-07-28, tickets packet-inspector-wire + constrain-filters, both PROVED same day as cast): the gate now judges a build against the packets that CHARTED it — a packet claims its ticket (gated at the berth door: the claim must name a filed ticket), the component's history names its tickets, so the walk history->ticket->berth finds a build's charted packets with no new side channel. FILTERS grew 4 -> 7, each by installation with dated-failure provenance: charted_refs_resolve (2026-07-24, done-reported-while-files-stood-unmoved — the charted refs must still resolve at promotion), constraint_traces (2026-07-26 fabricated-attribution class) and constraint_bounds_complete (2026-07-28 carrier miss) — the latter two installed BEFORE the constrain module existed (Akien's higher-order build-the-test-first): judge_constrain is the pure judge OWNED HERE, constrain's berth door composes it by import and the inspector never imports back, so the module structurally cannot shape its own acceptance criteria; one implementation, two mouths, identity-pinned in constrain's proof. The ref semantics are composed from chart's berth gate (cairn.chart.orient.ref_exists — a TREE-FREE module, transitively pinned by the nexus proof), so judge and gate cannot disagree about what a ref is. Live-fired end to end: the constrain brick built under the judges and its first packet passed at door and at promotion; chart's PROVED crossing was judged against both berths that charted it. An unreadable berth lands on the berth owner exactly once. success_criteria stays a filed growth: no stage emits them yet (filed edge (a) of the wire ticket). SECOND INSTANCE 2026-07-28 (ticket survey-filters, PROVED before the survey module existed — the move constrain-filters filed as 'pattern, not rule, until a second instance proves it' is now proven): judge_survey owned here — survey_holdings_resolve (2026-07-26/27 state-reported-from-records, wrong about the world three times; plus stone 1's parallel roster — the sweep that never ran) and survey_coverage_complete (2026-07-27 '0 of 13', an absence claimed by word-grep: an absence is a claim and must carry its measure). FILTERS 7 -> 9; every stage's promotion mouth now rides ONE generalized _judge_charted wrapper (the per-stage parallel wrapper retired in the same act). Live-fired same day: the survey brick built under the judges, its first packet passed at door and promotion. THIRD APPLICATION 2026-07-28 (ticket decompose-filters, PROVED before the decompose module existed — the ordering is routine now): judge_decompose owned here — decompose_composes_holdings (2026-07-28: stone 1's parallel roster + the one-web-server drift; a 'compose' piece may only use addresses the survey berth HOLDS) and decompose_builds_absences (2026-07-24 done-while-unmoved substitution; a 'build' piece may only fill an absence the survey MEASURED, verbatim — build-minimal as physics, known-vs-novel structural a stage early). FILTERS 9 -> 11. The judge reads the survey_ref berth with its OWN minimal open (os admitted to the fire-path allowlist with its why) — importing chart's chain reader back would let the judged family shape the judge. FOURTH APPLICATION 2026-07-28 (ticket triage-filters, PROVED before the triage module existed, unattended): judge_triage owned here — triage_covers_the_split (2026-07-24 done-while-unmoved: the silent drop began as a triage defect; the order must be a complete PERMUTATION of the decompose berth's pieces — coverage as a multiset, drop/invent/double each a distinct finding with complete evidence; descoping is a bounds question for Akien, never a ranking) and triage_reasons_the_order (the standing get-it-right-not-cheap CC-- + 2026-07-23 solidify-the-layer-below: the ranking standard travels with the rank — why_now forced per entry, else the cheap-first reflex hides in unstated standards). FILTERS 11 -> 13. FIFTH APPLICATION 2026-07-28 (ticket hypothesize-filters, PROVED before the hypothesize module existed, unattended): judge_hypothesize owned here — hypothesize_covers_the_ranked (2026-07-26/27 wrong-about-the-world mornings + the substitution class: every hypothesis attaches to a RANKED piece verbatim and every ranked piece carries at least one — a covering, not a permutation; the piece nobody predicted is the piece that lands wrong silently) and hypothesize_falsifiable_measured (2026-07-27 '0 of 13' instrument-was-a-word-grep + the falsifier-defect proof lessons: expect/falsifier/instrument forced per claim, missing fields named completely in one finding — Law 3's label as schema, /sorted's no-falsifier gate one rung down). FILTERS 13 -> 15.",
          "(e) GRAFT TO NEXUS SHAPE (ruling 2026-07-28, CairnCommons/notes/held-nexi-everywhere.json): on the NEXT learning event (the first failure that would seed a filter), the growth runs through the question-nexus loop — tree stratum for the failure corpus, ceiling proposing the filter, admission still gated by tooth 10's provenance requirement + Akien's ratification of gate filters. Fire-path unchanged and un-negotiable: the FILTERS registry is HARDWARE, changed only by installation; the tree is never consulted at verdict-time. FIRED 2026-07-28, same day — Akien redefined the event: 'building ANYTHING should be the learning event for the orient/inspector tickets.' Every journaled PROVEME exit runs this gate, so every build is the event; the chart-tree stone's build fired it. Ticket CairnCommons/tickets/build-inspector-nexus-graft.json cast at TICKETME (BUILDME gated on chart-tree proving the shared multi-tenant tree machinery); falsifier (6) keeps its teeth throughout. BUILT AND PROVED same day (chart-tree proved, Akien: 'go go go'): build_inspector/nexus.py — the failure corpus (build_inspector_failures_nodes, owner 'build_inspector') through chart's owner-generalized nexus verbs; dated-provenance deposit door (tooth 10's demand, one layer early); counsel with the labeled floor; propose_filter assembles and can never admit; edge (c)'s candidate questions live as founding residents PLUS chart-tree edge (c)'s dial-trend question — the ruling's falsifier queued at this gate's own admission door, and a paraphrased ask walked to it at 0.6780 over the floor on the corpus's first walk. Proof proofs/test_inspector_nexus.py (6 teeth, netns-sealed twice) pins the verdict-path/tree separation by AST allowlist BOTH directions, and pins seeded-but-unbuilt: a candidate's name may not be in FILTERS until it lands by installation. The chokepoint wiring was untouched — and presided: both graft promotions passed through this device's own gate."
        ],
        "gates": "proof gate — proofs/test_inspector.py (24 teeth: the fire-path + the packet jurisdiction + the constrain judges + the survey judges + the decompose judges + the triage judges + the hypothesize judges) + proofs/test_inspector_nexus.py (6 teeth, the graft, incl. the transitive tree-free pin on the composed chart module) under the tester. Durability gate: committed + pushed.",
        "falsifier": "RED on any of: (1) a healthy component draws a finding (a gate that always fires gets unwired — tooth 1); (2) a seeded failure fails to fire exactly its filter (tooth 2); (3) a hand-edited state.json passes (tooth 4 — the drift check's whole claim); (4) inspecting a nonexistent component returns clean instead of refusing (tooth 7 — a gate that inspects nothing passes everything); (5) a filter enters FILTERS without provenance (tooth 10); (6) an oracle or outbound import appears in inspector.py (tooth 12). WRONG-SHAPE SIGNAL: if a second whole-repo sweep is ever genuinely needed, the gate is not gating — that need is itself the loudest finding.",
        "proof": "proofs/test_inspector.py — each founding filter pinned fire-and-stay-quiet on a synthetic tree; door-written voyages clean, hand-edits caught with diverging keys named; repairs go through the door; refusals loud; findings complete on first pass; real tree by invariant. Green: `python3 cairn/build_inspector/proofs/test_inspector.py`.",
        "owner": "CC owns the code + proofs and gates writes to them. Akien owns the design (filters, only-once sweep, inference-free — his ruling 2026-07-27) and the disposition of findings on components he owns.",
        "provenance": "Born 2026-07-27, minutes after orient PROVED, as orient's first child — Akien: 'go go go!'. Its first sweep (16 components) found 6: its own missing charter (this file is the fix — the second instrument in one day whose first act was to catch its own builder) and five silent devices, filed as the trouble above."
      }
    },
    {
      "id": "intention_extractor",
      "kind": "component-charter",
      "source": "cairn/intention_extractor/intention+why.json",
      "born": "2026-07-27T15:45:15-06:00",
      "content": {
        "component": "intention_extractor",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "The first cocoon stage as a device: raw design prose (a conversation excerpt, a note, a held-* record) in, a CANDIDATE intention out — {what, why, anchors, read}, exactly four fields. The DRAFT comes through the caller-injected resolve seam (inference_domain.resolve — the sole path to the host; metered, cached, so the same source is a genuine cache HIT on re-extraction). The JUDGMENT is deterministic: CHECKS (cairn/intention_extractor/extractor.py) refuse a draft whose anchors are not verbatim in the source (anchors_verbatim) or whose field-set drifts / whose why is silent (record_shape); a refusal carries every finding first-pass. `extract()` is one crossing and breadcrumbs its verdict — PASS, REFUSED, and UNPARSEABLE alike. Live wiring: `python3 -m cairn.intention_extractor.live <file> [model]` (live.py — the first composition of domain.resolve + host.ollama_resolver outside inference_domain's own proofs).",
        "why": "Akien, 2026-07-24, verbatim: 'the librarian will have you build something, and you and i can move, say, intention extraction to an agent. then when you get the build ticket, you do the build... and then go back and run the inspection over the initial intention extraction... until initial intention extraction is a settled issue.' CC is the most expensive resolver; moving extraction to a watchable, optimizable stage spends it parsimoniously (Law 1 applied to the pipeline itself). Ordering is his too, 2026-07-27: 'i was going to try and do the intention extractor as a device first, but i see that now we start with CC and work outwards' — built AFTER CC's tooling ring, so the extractor arrives to find its judge already seated: a draft is graded by deterministic checks (and its component by the build gate), never by CC's opinion, the least independent witness. The founding defect it exists to end: FABRICATED ATTRIBUTION — 2026-07-26, 'the graph type is a coordinate, not a class' recorded as Akien's ruling when it was CC's inference; only seven words of the embedding claim were his. The anchor discipline makes that structurally impossible to smuggle: what the source does not literally say lives under `read`, labeled.",
        "invoke": "import: `from cairn.intention_extractor.extractor import IntentionExtractorDevice`. Prove: `python3 cairn/intention_extractor/proofs/test_intention_extractor.py` (exit 0 = green; no host, no DB). Live: `python3 -m cairn.intention_extractor.live <source-file> [model]` (defaults qwen2.5:7b; exit 0 PASS / 1 REFUSED).",
        "node_class": "device (stage) — the first pipeline stage moved upstream of CC; no runtime state, no daemon; fires per extraction request, never on a clock.",
        "entry": "FRESH build. The check-registry-with-required-provenance shape is deliberate reuse of orient's scans / build_inspector's filters (the learning-device pattern, third instance — the pattern is now load-bearing across the tooling ring); the anchor discipline is Cairn's own, seeded by the 2026-07-26 attribution failure. Nothing carried from the quarry.",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split.",
        "how_it_learns": "Two loops, one per judge. (1) THE CHECKS learn from caught failures: when Akien (or a downstream build) catches a bad extraction a deterministic check could have caught, that check joins CHECKS carrying the failure as Provenance in its docstring — the proof refuses a check with no dated correction, and pins the registry so joining means joining the teeth (learns-its-gates, third instrument). Founding set: anchors_verbatim ← the 2026-07-26 fabricated attribution; record_shape ← the charter filename forcing the why (2026-07-14) + the VALIDATION exactly-eight rule (2026-07-17). (2) THE GROUND TRUTH is downstream and measured, not opined: an extraction was GOOD if the build it fed proved out, BAD if it kicked back (Akien's cocoon design, with CC a temporary stand-in until proof-history can replace it). That loop's meter does not exist yet — filed edge (b) — and until it does, 'settled enough to rely on' (Akien's bar: ~2-3 clean tickets) is unmeasurable, deliberately not faked.",
        "filed_edges": [
          "(a) THE COCOON LOOP IS NOT CLOSED: today CC calls extract() by hand; the designed flow (librarian drives → extractor drafts → CC builds → CC free-rides the pass to inspect the extraction and grow the checks) waits on the librarian. The device is the stage, not the pipeline.",
          "(b) THE PROOF-OUTCOME METER: 'an extraction was good if the build it fed proved out' needs the link extraction→ticket→verdict recorded somewhere queryable before 'settled' (Akien's 2-3-clean-tickets bar) is a measurement rather than taste. Unbuilt; the falsifier below names the wrong-shape signal meanwhile.",
          "(c) SEMANTIC anchor checking: anchors_verbatim catches invented QUOTES, not kept-words-with-swapped-MEANINGS (words-kept-meanings-replaced — the drift that hides). A deterministic check for that does not exist yet and pretending one does would be a hollow gate; today that catch is CC's and Akien's, by read.",
          "(d) BERTH DISPOSITION: the extractor returns a candidate; WHERE it berths (code-seam charter vs commons intention vs ticket) is the caller's call today. The which-root decision tree (CLAUDE.md 'Which root?') is prose a later check or the librarian could carry.",
          "(e) draft RETRY on UNPARSEABLE (one re-ask with the parse error quoted) — deliberately not built: a retry loop is where token spend hides; the loud refusal is the honest v0. Revisit when live UNPARSEABLE rates are measured, not guessed."
        ],
        "gates": "proof gate — proofs/test_intention_extractor.py (11 teeth) under the tester. Durability gate: committed + pushed. Build gate: journaled PROVEME exit inspected (this component was born under the wired gate — the first component whose whole voyage ran with the judge seated).",
        "falsifier": "RED on any of: (1) a draft with a fabricated anchor PASSES (tooth 2 — the founding defect readmitted); (2) a candidate is returned REPAIRED rather than judged (tooth 1 asserts unchanged — a stage that silently fixes drafts is training its own blind spot); (3) an empty source or absent seam produces a candidate (teeth 6-7 — invention from nothing); (4) a check joins CHECKS without a dated correction (tooth 8); (5) extractor.py grows an outbound-capable import (tooth 11 — a second door to the host); (6) a REFUSED or UNPARSEABLE crossing leaves no breadcrumb (teeth 4, 9 — a silent refusal is the silent-device defect reborn). WRONG-SHAPE SIGNAL: if the checks registry is static three caught-bad-extractions from now, the learning half is hollow; and if extractions are never fed to real builds, the ground-truth loop (edge b) never closes and this device is a prompt wrapper wearing a charter.",
        "proof": "proofs/test_intention_extractor.py — grounded draft passes unchanged; fabricated anchor refused with the quote carried whole; empty why and field drift named both ways; unparseable draft loud with raw draft whole, still breadcrumbed; fenced draft parses; empty source never touches the host; no seam no extraction; every check taught and pinned; crossings breadcrumb PASS/REFUSED alike, reads silent; device-hood + Form v0 #2; import-purity by AST. Green: `python3 cairn/intention_extractor/proofs/test_intention_extractor.py`.",
        "owner": "CC owns the code + proofs and gates writes to them. Akien owns the design (the cocoon, the stage ordering, the settled-bar) and the disposition of candidates into the intention layer (the control surface is his).",
        "provenance": "Born 2026-07-27, immediately after the six silent devices were disposed and the whole-repo sweep read zero — the moment CC's tooling ring closed. First device born under the wired build gate and born instrumented (the silent-devices lesson applied at birth, not retrofitted). Its first live extraction (live.py, qwen2.5:7b through inference_domain) was run on the very 2026-07-24 conversation excerpt that founded it."
      }
    },
    {
      "id": "librarian",
      "kind": "component-charter",
      "source": "cairn/librarian/intention+why.json",
      "born": "2026-07-27T17:26:45-06:00",
      "content": {
        "component": "librarian",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "The librarian: owner of the graph trees — db_domain's first real tenant — and, when its face is whole, a chatbot that is learning always and summarizing when asked. v0 is the SPINE (trees.py): a provenance-gated deposit door (a node is content + vector + provenance + standing; the untraceable never lands; every node is born a hypothesis) and proximity walks whose edges are DERIVED from the vectors at walk time, never stored — 'the embedding IS the path through the graph trees.' v0.1 adds THE CORE LOOP (loop.py): always the graph first; when the walk's best cosine misses RESOLUTION_FLOOR, the LLM is asked for NODES (never the answer), they deposit through the same provenance gate, and the ORIGINAL question resubmits — the proof the backfill worked is that structure now resolves it. The backfill-cache livelock is fixed as physics at that junction: the prompt carries tree_state's digest (same question + changed graph = different cache key) and a round depositing nothing fresh terminates loudly as no_progress. v0.2 adds THE LIBRARY + THE LEARN VERB (library.py): a room in instance-space at the ratified address ~/.cairn/devices/librarian/0/library/ — a shelf of FROZEN copies with stable citable addresses (the register freezes each file's digest; re-shelving same content writes nothing; different content at a standing address is refused — an address must not rot, because provenance anchors point INTO it). The graph is the catalog, not the shelf: LEARN is deposit-only — every passage of a shelved file embeds and deposits through the same provenance gate, anchored {source: library:<address>, passage: p<n>, sha256}, cited by RAW paragraph position so filtering never renumbers. v0.3 adds THE SUMMARIZE VERB (summarize.py): the transducer across the storage axis — a dense region of the graph (the k nearest SOURCE nodes around the question) rendered by the drafting model into articulated, why-carrying prose. The physics: a summary is a VIEW over the graph, never a document the graph forgets — citations are CODE-BUILT from the walked region (the model only marks [n]; an unanchored or minted citation refuses loudly, raw carried whole); the rendered prose DEPOSITS BACK into the same tree, provenance naming the exact nodes it rendered + the region's digest, so follow-ups are walks; the transducer never eats its own output (gather excludes summary-sourced nodes), which makes the verb idempotent — the region rides the render prompt whole, so same question + same source region is a cached render and a duplicate deposit. Depth travels with the artifact (region reached vs tree size). v0.4 adds THE CHAT FACE (chat.py + shim.py): the conversational surface over the loop and the transducer, adding NO third mechanism — LEARNING ALWAYS is every turn crossing the core loop (the reply is the graph's walk, never the generate text; the transcript is runtime state, the graph is the memory that survives the session); SUMMARIZING WHEN ASKED is the `summarize:` prefix routing to the transducer — the route is PHYSICS (a stated affordance), never an inference spent guessing intent. A verb's loud refusal becomes a legible reply the session survives. THE CARRIER (corrected 2026-07-28): there is ONE web server in the whole system, and the librarian owns a PAGE it displays — the chat window is a surface the base shim class understands: the device DECLARES the chat pane, LibrarianShim rides the roster and STARTS THE DEVICE IF IT IS NOT RUNNING (attaching the live face at the wake), and a POSTed utterance travels web_server → shim.deliver → device.receive as ordinary mail. No chat route, no port, no second door. Live wiring: `python3 -m cairn.librarian.live [query|loop|shelve|learn|summarize]` and `python3 cairn/web_server/listener.py` for the conversation at /device/librarian (all verbs through inference_domain, metered and cached; trees.py, loop.py, library.py, summarize.py and chat.py are import-pure toward host+DB and the proofs pin it — live composition lives in live.py and the shim's wake).",
        "why": "Akien, 2026-07-27, verbatim: 'the current goal is the librarian as chat bot, learning, and summarizing. learning always and summarizing when asked' ... 'And he'll already be ready to answer them immediately. This is part of orienting, for anything externally facing.' The core loop he named 'very simple' (2026-07-22): a query resolves by traversing the trees — cheap, no inference; on a MISS the LLM supplies the NODES that let the graph resolve, never the answer; the answer always comes from structure, so re-asking is a graph walk. That is Law 1 as a runtime mechanism, and this store is the structure it runs on. The deposit door's provenance gate is the extractor's founding defect carried one layer down: a fabricated attribution in a draft is a bad day; in a graph node it is a permanent resident.",
        "invoke": "import: `from cairn.librarian.trees import LibrarianDevice, deposit, nearest, neighbors`, `from cairn.librarian.loop import resolve_query`, `from cairn.librarian.library import shelve, learn, shelf_entry, passages`, `from cairn.librarian.summarize import summarize, gather`, `from cairn.librarian.chat import ChatSession, chat_turn`. Prove: the five scripts under cairn/librarian/proofs/ + cairn/web_server/proofs/test_web_server_chat.py for the carried page (exit 0 = green; need the one-time DB provisioning, no host). Live: `python3 -m cairn.librarian.live [query]` (founding demo), `... loop [question] [tree]` (the core loop; dual seam — nomic-embed-text embeds, qwen2.5:7b drafts, model stamped on each request so it rides the cache key), `... shelve` (acquire the founding collection), `... learn <address> [tree]` (fold one shelved file in), `... summarize <tree> \"question\"` (the transducer, live). Chat: `python3 cairn/web_server/listener.py` then http://127.0.0.1:8787/device/librarian — the shim wakes the device and attaches the face over the library tree, both verbs through the one door.",
        "node_class": "device — owner of the graph trees; fires per deposit/walk request, never on a clock. The library room (instance-space, RATIFIED address ~/.cairn/devices/librarian/0/library/) is its shelf space — a later stone, not this one.",
        "entry": "FRESH build. UU had a graph-tree embeddings generator that self-trained via API calls — a graft candidate deliberately NOT carried into v0 (its mechanism may cross later the way isolation.py did; design re-authored). What v0 reuses is Cairn's own: db_domain's owner-gate as the write physics, inference_domain's embed seam as the vector source, the extractor's judged-at-the-door discipline.",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split.",
        "how_it_learns": "Three loops, staged. (1) BUILT NOW, the substrate: every deposit is a learned fact taking structural residence — the graph IS the memory, and a duplicate deposit writing nothing is Law 1 at the door. The LEARN verb (library.py, built 2026-07-27) is this loop's bulk intake: a shelved file folded in passage by passage, deposit-only, every node citing shelf + passage + frozen digest. (2) FILED, the tenure loop: every node is born standing='hypothesis' (Law 3) and today honestly STAYS there — tenure (does the node keep resolving well?) becomes a measurement when the core loop runs and outcomes accumulate; if nodes still all read 'hypothesis' weeks into live use, that loop never landed and standing is a decoration (the falsifier says so). (3) BUILT NOW, the core loop itself (loop.py): on a MISS the LLM provides nodes, the graph folds them in and the ORIGINAL question re-resolves — learning always, as the face's standing behavior; measured live 2026-07-27 (first crossing: one backfill, one fresh node, then RESOLVED at 0.8568; second crossing: zero backfills, zero inference in the answer path). The SUMMARIZE verb closes a fourth, reflexive loop (built 2026-07-27): the graph keeps its own distillations — every summary deposits back as structure, so what the librarian articulates it also remembers, and a follow-up walks to it instead of re-rendering. The librarian is the component whose learning IS its function; 'it doesn't learn' would be a charter violation here, not a valid answer.",
        "filed_edges": [
          "(a) RESOLUTION_FLOOR = 0.65 IS AN n=1 GUESS (seeded by the first live walk: right node 0.7473, adjacent 0.6278), returned in every verdict so no caller mistakes it for settled; it tunes against real use. (The loop itself is BUILT — this edge narrowed from 'build the loop' to 'earn the floor'.)",
          "(b) A DUPLICATE DROPS ITS NEW PROVENANCE: corroboration (a second source saying the same thing) is evidence the tenure loop will want; when that loop lands, a duplicate becomes a provenance APPEND, not a discard.",
          "(c) THE TENURE LOOP (standing: hypothesis -> earned) — unbuilt; see how_it_learns (2).",
          "(d) PROXIMITY IS COMPUTED IN PYTHON, O(n) PER WALK — correct now, and every deposit re-reads the tree for its dimension check; pgvector (proximity computed where the vectors live) when a real load pulls it.",
          "(e) TREES-INDEX-INTO-COMMONS GRANTS (private-down default, grant-up explicit) — one table with a tree column today; the grant model opens when a second owner exists.",
          "(f) THE DESK — 'a workspace/desk for current work': the assignment's home (topic, folded-so-far, open threads, resume-event) lands as files in a `_desk` register; underscore names are already reserved for it. Unbuilt until the multi-day research face needs it.",
          "(g) NARROWED 2026-07-27 — THE SUMMARIZER IS BUILT (summarize.py, stone 4); what remains of this edge: SUMMARY_REGION_K = 8 is an n=1 guess (one live transduction at pick time), the region is one nearest-k walk (multi-hop via neighbors() when a real summary proves too narrow), and find-pattern-in-clump / de-instance-for-exchange arrive as CALLERS, not new verbs.",
          "(h) NARROWED 2026-07-27 — THE CHAT SURFACE IS BUILT (chat.py + shim.py, a declared PANE on the librarian's page at the one web server — stone 5, the chain's last; first cast as a bespoke /chat route, corrected 2026-07-28 on Akien's word: surfaces live in the shim, and the shim starts its device): what remains: ONE UTTERANCE IS ONE QUESTION (conversation -> questions decomposition stays the core-loop ticket's edge d, grow against measured need); the transcript is in-process only (a persistent conversation berths in instance-space when a real need pulls it); the route is a stated prefix — if live use shows humans not finding the affordance, the fix is surface wording first, intent inference last.",
          "(i) DIAGRAM-SHAPED PASSAGES RANK POORLY: measured 2026-07-27 — the agent-cycle flow diagram (arrows, numbered steps) scored 0.4943 against a prose question it directly answers, UNDER two less-relevant prose passages. Diagram-ish text may want a prose rendering before embedding (a transducer-shaped fix, not a floor fix).",
          "(j) A MALFORMED BACKFILL DRAFT IS CACHED: qwen2.5:7b emitted invalid JSON once; parse_backfill refused loudly with the raw whole (as designed) — but same question + same graph now HITS that cached draft, so the question cannot backfill until the graph changes. Honest per the key physics; if it bites, a retry rides a CHANGED request (the refusal folded into the prompt = a different key), never a cache bypass.",
          "(k) A BINARY SHELVES BUT REFUSES TO LEARN (not utf-8) — the Readings PDFs need a reader in front of the verb; loud refusal today, not a silent skip.",
          "(l) BACKFILL NODES HAVE A HOME-FIELD ADVANTAGE, measured 2026-07-27: on a follow-up question, three labels minted DURING that crossing outranked both the landed summary (4th, 0.5960) and every shelved passage — backfill nodes are generated FROM the question, so they are born question-shaped and win the similarity race for it; the loop can satisfy its own floor with its own fresh mints. Everything stays traceable (source: llm-backfill, standing: hypothesis), so Laws 3/7 hold — but this is the tenure loop's (edge c) sharpest why yet: question-echo nodes must EARN residence by resolving other questions, or expire. SECOND FACE, measured 2026-07-27 through the live chat: asked about the hex payback meter — knowledge the library tree genuinely lacks (it lives in the founding tree) — the loop backfilled three path-shaped mints and RESOLVED at 0.8295 on them: home-field advantage does not just outrank real nodes, it can MANUFACTURE resolution where the graph held nothing, and the mints' content was wrong about the actual idea. Traceable throughout, so the verdict is honest about its sources — but a consumer reading only the verdict line would call that answered. The tenure loop now has two whys and a third customer: cross-TREE resolution (the founding tree could have answered) is a filed question."
        ],
        "gates": "proof gate — proofs/test_librarian_trees.py (14 teeth) + proofs/test_librarian_loop.py (10 teeth) + proofs/test_librarian_library.py (12 teeth) + proofs/test_librarian_summarize.py (13 teeth) + proofs/test_librarian_chat.py (12 teeth — the verb, the declared pane, mail routing, the shim's wake; the carried page's 6 more beside web_server), all under the tester's MEASURED netns seal (the Unix socket crosses the seal — db_domain's documented asymmetry, exercised for real: the DB reachable, the network gone). Durability gate: committed + pushed. Build gate: journaled PROVEME exit inspected.",
        "falsifier": "RED on any of: (1) a deposit without a traceable source LANDS (trees tooth 1 — the founding defect in permanent residence); (2) an edge table appears (the derived-edges claim gone hollow); (3) a dimension mismatch is ANSWERED instead of refused; (4) a non-librarian write lands (Law 6 physics failed through the stack); (5) a duplicate deposit grows the table; (6) a deposit crossing leaves no breadcrumb; (7) trees.py or loop.py grows an import outside its allowlist (a second door); (8) the loop returns a generate answer instead of a walk (the host in the answer path — loop tooth: the answer comes from structure); (9) two backfill prompts for the same question against a CHANGED graph canonicalize to the same cache key (the livelock reopened — loop's livelock tooth pins canonicalize inequality); (10) a no-progress round spins instead of terminating loudly; (11) a shelved address changes meaning (different bytes land at a standing address, or a rotted copy quietly grounds a citation — library teeth: frozen means frozen, rot is loud); (12) a learned node's citation cannot be traced back to shelf + passage + frozen digest; (13) a summary citation names a node the region never held, or prose without a single anchor passes as a summary (minted attribution wearing a bibliography — summarize teeth); (14) a summary does NOT land back in the graph, or the transducer renders its own prior output (the view detached, or a photocopy of a photocopy); (15) a chat turn spends inference on ROUTING (a guessed intent where the stated prefix is the physics), or a chat reply carries the generate text instead of the walk (a chatbot wearing a librarian's charter — chat teeth pin both); (16) a refused turn kills the session or is silently swallowed (a refusal is a reply, and its breadcrumb stands). WRONG-SHAPE SIGNALS: every node still 'hypothesis' weeks into live use (the tenure loop never landed); answers coming from inference when the graph already holds the structure (the core loop bypassed — the device demoted to a vector cache); UNRESOLVED verdicts booked anywhere as savings.",
        "proof": "proofs/test_librarian_trees.py — the untraceable refused three ways; vectors physics at both doors; a node lands, round-trips as structure, born a hypothesis; a duplicate writes nothing; dimension mismatch refused both directions; nearest provably in cosine order and an empty tree an honest []; trees do not cross; neighbors derived, self excluded, ghosts refused; no edge table exists; the owner-gate holds through the stack; crossings breadcrumb and reads stay silent; tree_state a pure function of membership; device-hood + Form v0 #2 order; import purity by AST allowlist. proofs/test_librarian_loop.py — resolved-through-structure (no generate call when the graph clears the floor); the miss-backfill-resubmit voyage; the livelock fix pinned at the key (two prompts, changed graph, canonicalize UNEQUAL); the progress gate terminates no_progress; exhaustion honest at max_backfills; refusals ride the verdict whole; the seam refused when absent; the drafted answer never returned; breadcrumbs both verdicts; import purity. proofs/test_librarian_library.py — shelved is frozen (copy + digest registered, source untouched); re-shelve writes nothing; a standing address never changes meaning; rooms disciplined (no traversal, no underscore shelves); citations are raw positions forever; learn deposits every passage anchored to the shelf; re-learn writes nothing; the unshelved, the missing, the rotted and the binary all refused loudly; learned nodes resolve with citations intact; the learn crossing breadcrumbs; import purity. proofs/test_librarian_summarize.py — the render grounded (citations code-built from the walk, each traceable with source + similarity); the summary deposits back and a follow-up walk finds it; the transducer never eats its own output and rewrites nothing (identical render prompt pinned = the cache key; duplicate deposit); empty region, unanchored draft, and minted citation all refused loudly with the raw carried whole; depth honest and recomputable; fence tolerated as wrapping; the crossing breadcrumbs; import purity. proofs/test_librarian_chat.py — the route is physics (prefix, case-insensitive, no NLP guessing); a warm turn spends NO generate (none for the answer, none for routing); a miss teaches the graph and the SAME question later is a walk (the chat's memory is the graph); summarize-when-asked lands cited prose; a refusal is a reply the session survives, transcript intact; page() is copied-out DATA; the chat crossing breadcrumbs thin; caller refusals; import purity. Beside web_server: test_web_server_chat.py — the carried page (unwired 404s loudly, the affordance SAID on the surface, POST decodes to the session, empty takes no turn, every librarian string escaped, a dying turn is a legible 500 whose breadcrumb stands). Green: all six scripts exit 0.",
        "owner": "librarian owns librarian_nodes (recorded in db_domain's registry; the owner-gate is the write physics). CC owns the code + proofs. Akien owns the design: the face, the verbs, the parts inventory, the embedding-is-the-path model.",
        "provenance": "Cast and built 2026-07-27 on Akien's go ('yes! go go go!'), the first stone of the librarian-as-chatbot chain (tickets/librarian.json -> librarian-graph-trees.json). Its first live walk ranked Akien's own chatbot-goal sentence nearest (0.7473) to a query about the chat interface — the librarian's first real nodes are its own founding design, each carrying provenance into the records it came from. The re-fire was all DUPLICATEs and all cache HITs: host untouched, table untouched, avoided-tokens 955 -> 1165. The core loop (stone 2, 'very good! go go go!') went live 2026-07-27: asked what cosine similarity measures — nothing the founding tree held — it backfilled one node from qwen2.5:7b through the provenance gate and the original question resolved through structure at 0.8568; the second crossing took zero backfills. Law 1 as a runtime mechanism, measured. The library + learn verb (stone 3, 'go go go!') landed the same day: the founding collection shelved whole (134 files, 41MB, three rooms, zero failures; the re-shelve all duplicates), Akien's !IDEAS!.txt learned (16 passages, every citation anchored), and a question about his audit-agent idea RESOLVED against the library tree at 0.7813 — his own passage riding the walk at 0.6177 with its citation intact. That crossing also delivered the floor's n=2 (a legitimately-grounding passage fell 0.03 short, so the loop bridged with one paraphrase node) and two filed findings (diagram-shaped passages, malformed-draft caching). The summarizer (stone 4, 'go go go!') landed the same day: asked what agent ideas Akien recorded and why, it walked an 8-node region of the 24-node library tree and qwen2.5:7b rendered a cited summary grounding on his versioned-memories and audit-agent passages (both citations tracing to library:ideas/!IDEAS!.txt with similarities carried) — the prose landed back as node fdce9f3442620742. The re-fire was Law 1 whole: 3 calls, ALL cache hits, spent unchanged, duplicate deposit, zero writes. The honest finding rode the follow-up crossing: the summary ranked 4th (0.5960, under floor) behind three question-shaped backfill mints — filed as edge (l), the tenure loop's sharpest why yet. The chat face (stone 5, 'The word is given. Warp speed.') closed the chain the same day: the conversation live over the library tree — a question about the audit agent RESOLVED by walk alone (0.6772, zero backfills, stone 4's landed summary riding the walk as structure); the summarize: affordance rendered cited prose through the browser door (8 of 34 nodes, two shelved passages and four backfill mints, each legible by source); and a question the tree could not ground (the hex payback meter) backfilled three mints and 'resolved' on them — edge (l)'s second face, measured, recorded. 2026-07-28, the carrier corrected on Akien's word (CC--): the first cast wired a bespoke /chat route + injected source — a parallel door; re-cast same-arc to the ratified shape (ONE web server; the chat window a pane the base shim class understands; LibrarianShim starts the device if it is not running; POST travels as mail through deliver; daemon.py → listener.py, NO DAEMONS stated as law). The corrected shape's live fire found and fixed a parser defect: qwen cites [1, 6] comma-grouped, and a genuinely anchored draft had been refused as unanchored — grouping now parses as marks (fence precedent), and the re-fire turned the CACHED refusal into cited prose with zero new inference. The face is whole: learning always, summarizing when asked, every answer from structure."
      }
    },
    {
      "id": "chart",
      "kind": "component-charter",
      "source": "cairn/chart/intention+why.json",
      "born": "2026-07-28T11:00:08-06:00",
      "content": {
        "component": "chart",
        "role": "charter",
        "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
        "what": "The pre-build preamble compiled into QUESTION NEXI — standalone typed units for orient, constrain, survey, decompose, triage, hypothesize, validate (roster OPEN by intention: more nexi and more depth per nexus will be found, not planned). A nexus's anatomy: input + feedback + candidate core questions + domain-specific knowledge + core questions + output — WORKED by a mini agentic loop (think -> act -> observe until the nexus's LOCAL exit condition) that actively assembles the output; never a bare lookup (implicit machinery gets missed — this loop is first-class anatomy). Each nexus is THREE STRATA tried in order: (1) DETERMINISTIC FLOOR — ordinary code: parse, index, convention lookup, cache — with TERRITORY measurement COMPOSED from the orient instrument (cairn/orient, the settled owner of prebuild scans), never a parallel scan; (2) TREE WALK — the nexus's own graph tree via the librarian's tools (deposit / nearest / tree_state; its own table through db_domain); (3) CLAUDE CEILING — the loop, depositing back so the stratum boundary moves DOWN over time. Output is typed JSON with PER-FIELD provenance (floor|tree|claude + confidence); the chain's emitted artifact is the BUILD PACKET (intent / constraints / context / sub-problems / known-vs-novel / success-criteria). Two skills are the UI: /chart <something> chains the landed nexi via SCHEMA-GATED HANDOFFS — each stage's prompt is TEMPLATE-FILLED from the previous stage's validated file, so a skipped stage is a build error, not a lapse; /moreabout <something> is the learning door — a SCOPED expansion resolved from the upstream nexus's already-deposited state (a walk, never a re-run), and the invocation itself is a labeled training signal written back to that nexus's tree. Packets are runtime state and berth in instance-space (~/.cairn/devices/chart/); the trees are the durable memory.",
        "why": "Akien, verbatim (2026-07-28, the ratifying intent): 'we intend that we can extract debug and grow these specific nexi, knowing we'll find more nexi and more details for each as we go. and the ui shall be /chart <something> --- learning from /moreabout <something>. why: improve the work claude is doing by giving him a more deterministic input.' Sharpened same day: 'so we can inspect and improve what you're getting' — INSPECTABILITY is the load-bearing half: CC's orientation today is invisible reasoning; as a typed packet on disk it can be inspected, diffed, improved. The failure this answers was MEASURED here, not imported: the web-server carrier miss (CC-- 2026-07-28) is premature convergence — proximity to a plausible answer collapsed orient/constrain into pattern-match, bounds-checking never ran to completion. The delivery mechanism follows Akien's observed skill-door facts: CC skips steps when the answer looks visible, plain instructions bypass skills, but /skillname fires reliably — so the preamble lives behind the slash-door as physics, with the handoff schema making each stage unskippable by construction (Law 4). The economics are work-measured: pre-answering the preamble in deterministic code cut build tokens ~89% before caching. AND THE UNIFICATION: 'we'll use the same tools with the librarian. it's agenic loop will just be a simpler form of yours' — the nexi are shared machinery for every Cairn agent, not CC-tooling; one loop shape, two complexity settings.",
        "invoke": "/chart <request> — run the chain over a request, emitting the BUILD PACKET a stage at a time. /moreabout <scoped-ref> — expand a too-compressed packet field from deposited state (and teach the tree). Code: `from cairn.chart import ...` per nexus as each lands. Prove: per-stone proofs under cairn/chart/proofs/, sealed by the tester.",
        "node_class": "code-seam (cairn/chart/); its skills berth at skills/chart/ + skills/moreabout/ (repo root), symlinked into ~/.claude/skills/ (the standing host-seam pattern). Ticket: CairnCommons/tickets/chart.json; design record: CairnCommons/notes/held-chart-nexus-extraction.json + the imported web-session doc beside it.",
        "entry": "FRESH design, converged 2026-07-28 across three same-day sources: the Akien+web-Claude session (the 8-pass preamble, parsimonious typed outputs, more_about, shadow/champion vs Hex, orientation-first), the pattern Akien built at work from these ideas (the BUILD PACKET, ~89% measured), and the Akien+CC design conversation (the mini-agentic-loop delta, the three strata, schema-gated chaining, the home-measured cautions). Nothing grafted from UU. The librarian chain (PROVED 2026-07-27) is nexus #0 — the proof-of-concept of the tree+loop shape every nexus reuses.",
        "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split; this file stays pure intention+why, the operational voyage lives in the sidecars (Law 1/4/7)",
        "filed_edges": [
          "(a) STONE PLAN (each cast as a child ticket when fired): orient v0 LANDED 2026-07-28 (chart-orient) -> /chart skill v0 LANDED with it -> tree stratum + dial LANDED 2026-07-28 (chart-tree) -> /moreabout v0 LANDED 2026-07-28 (ticket moreabout — the learning door: scoped walk over deposited state with berthed packets attached, the invocation written back dated in the same act, one embed for both; no-re-run as import physics, under-floor honesty as a tooth; serves every tenant of the generalized verbs through one mouth) -> CONSTRAIN LANDED 2026-07-28 (tickets packet-inspector-wire + constrain-filters + chart-constrain, in that order — Akien's higher-order build-the-test-first: the inspector gained packet jurisdiction, then the judges installed BEFORE the module, then the brick built under them; the door composes judge_constrain by identity; packets claim their ticket and the promotion gate judges a build against the berths that charted it; the dial reads per-stage field shapes, and moved off its all-claude baseline the same day — orient 0.133/0.067/0.80, constrain born 0.5/0.25/0.25) -> SURVEY LANDED 2026-07-28 (tickets survey-filters + chart-survey, judges first — the second instance that PROVES the judges-before-the-judged pattern constrain-filters filed at edge (b): judge_survey installed and sealed before the module; the brick answers what already EXISTS — sought/holdings/absences under the stage-2 bounds, an absence carrying the measure that established it; the floor composes device_census rows VERBATIM (measured state; constrain owns the authored charter text — same refs, two questions, no overlap); the chain re-checks whole at depth 3; the floor's first live fire caught its own false absence (filed tickets reported missing — a narrower resolution than the gate's ref_exists), cured by composing the one semantics and pinned by a commons-sibling tooth; survey born on the dial at 0.4 floor / 0.6 claude) -> DECOMPOSE LANDED 2026-07-28 (tickets decompose-filters + chart-decompose, judges first — the THIRD application, routine now: judge_decompose sealed before the module; the brick answers how the request SPLITS — sub_problems each carrying what/why/KIND, and known-vs-novel is PHYSICS a stage early: a 'compose' piece may only use addresses the survey berth HOLDS, a 'build' piece may only fill an absence the survey MEASURED verbatim (build-minimal enforced by schema — /sail step 2's discipline moved down the ladder); the floor re-reads the chain whole at depth 4 by COMPOSING survey's own reader (identity-pinned) and hands the ceiling the judges' exact vocabularies (template-fill as physics); built INSIDE the first real /sail crossing — the chain's fourth stage delivered by the build skill it feeds, chart→sail dogfood both directions; decompose born on the dial at 0.33 floor / 0.67 claude; promotion clean under 11 filters) -> TRIAGE LANDED 2026-07-28 (tickets triage-filters + chart-triage, judges first — the FOURTH application, unattended: judge_triage sealed before the module; the brick answers what matters FIRST — the order a complete PERMUTATION of the split's pieces (position IS the rank, no numeric field to drift; drop/invent/double each physics-refused with complete evidence; descoping belongs to Akien's bounds gate, never a silent drop) with the why_now forced per entry (the standard travels with the rank — get-it-right-not-cheap moved into schema); the floor re-reads the chain whole at depth 5 by COMPOSING decompose's own reader (identity-pinned) and hands the coverage vocabulary verbatim; triage born on the dial at 0.33 floor / 0.67 claude; promotion clean under 13 filters; en-route find fixed at the instrument — the dial's per-packet rounding broke sum-to-1 on the first three-way provenance split, full precision now) -> HYPOTHESIZE LANDED 2026-07-28 (tickets hypothesize-filters + chart-hypothesize, judges first — the FIFTH application, unattended: judge_hypothesize sealed before the module; the brick answers what we EXPECT and how we'd know we're wrong — Law 3's 'labeled as one' made schema: every hypothesis attaches to a ranked piece verbatim, every ranked piece carries at least one (a COVERING — the piece nobody predicted lands wrong silently), and every claim carries expect + FALSIFIER + named INSTRUMENT (/sorted's no-falsifier gate one stage earlier, one rung down); the floor re-reads the chain whole at depth 6 by COMPOSING triage's reader (identity-pinned); promotion clean under 15 filters) -> validate. NAMING, ratified 2026-07-28: a question nexus is now a STACKABLE LEARNING BRICK (new prose uses it; settled ink stays).",
          "(b) the ORIENTATION CURTAIN as physics — what orient may READ before constrain/survey commit is a gate, not a promise; an IOU until enforced",
          "(c) ESCALATION DISPATCH (chat loop vs research loop) starts as an explicit verb/prefix (route-is-physics); the escalation nexus accumulates toward a learned dispatcher it EARNS",
          "(d) the FLOOR's staleness story: event-driven invalidation (file-change), never a clock — NO DAEMONS applies to caches",
          "(e) SHADOW/CHAMPION vs Hex: per-nexus scoreboard (champion, match-rate trend, last flip) doubling as the audit trail; promotion = N consecutive DOWNSTREAM behavioral matches (Law 8 for model-swaps); re-shadow event-driven (model-swap, every-Nth-crossing)",
          "(f) the LIBRARIAN drawing chart's nexi with its simpler loop — the unification's first concrete pull, likely the research-loop face (assignment/desk)"
        ],
        "gates": "per stone: proof gate under the tester (netns seal), VALIDATION persisted beside-code (validations/), committed. The standing live gate for every nexus: proven against the REAL operational agent — CC on real tasks; orient's score is 'did downstream proceed without re-orienting'. THE DIAL is the honesty gate on the whole intention: per nexus, the fraction of output fields filled by floor vs tree vs claude, over time — a nexus whose floor+tree fraction is not growing is not compiling (Law 3 on the 89% claim).",
        "falsifier": "RED on any of: (1) a stage output failing the TEMPLATE-FILL TEST — downstream needed an LLM re-read of upstream prose (the interface was shortened, not compiled); (2) a floor answer crossing from WHAT-EXISTS into WHAT-APPLIES (retrieval reports; only the loop above decides — the harbor-exception drift was a floor-match mis-applied); (3) a packet field without provenance, or confidence as hedging prose instead of data; (4) /moreabout answered by re-running the upstream pass; (5) a monolithic packet-builder (nothing piecemeal to score, shadow, or promote); (6) a nexus scored by its own tree's confidence (home-field manufactured resolution — measured at 0.8295 on the librarian); (7) internal reasoning narrowed to match the output's tightness — LOOSE PROCESS, TIGHT OUTPUT are separate concerns, or parsimony squeezes the wide survey that earns its keep.",
        "horizon": "re-read when orient v0 has a readable dial from real tasks; when the librarian draws its first shared nexus; when a second consumer (the builder) wants the packet; when Hex is ready to shadow; when /moreabout signals accumulate enough to adjust a parsimony threshold automatically (the tree compiling out a recurring correction, not just logging complaints).",
        "how_it_learns": "Learning is this component's SUBJECT, not a sidecar — three mechanisms, all event-driven: (1) DEPOSIT-BACK: every Claude-ceiling assembly deposits into that nexus's tree, so the same question next time is a walk (Law 1 as the runtime loop, inherited from nexus #0); (2) /moreabout AS TRAINING SIGNAL: each scoped expansion is a labeled datum — 'this field, at this compression, wasn't enough for this class' — written back to the upstream tree so the parsimony threshold adjusts per class (LIVE 2026-07-28, ticket moreabout: the signal rides INSIDE the one live verb — un-skippable — and duplicates read as frequency; its walks are also the routine evidence stream for the floor-tuning edge, n=6 by its first day); (3) THE DIAL: per-nexus floor/tree/claude fractions measured over time, the compilation claim kept honest. Distant: shadow/champion promotion moves a whole nexus from Claude to Hex on downstream behavioral evidence — the withdrawal dial with gates instead of vibes.",
        "traces_to": [
          "Law 1 (the preamble compiled; Claude spent on hypothesize/build — the novel)",
          "Law 3 (provenance + confidence as per-field data; the dial; /moreabout frequency — measurements, not vibes)",
          "Law 4 (the schema gate at every handoff — step-skipping made a build error by construction)",
          "Law 6 (each nexus owns its tree and exit schema; the chain composes, opens no side door)",
          "Law 8 (nexus promotion needs downstream proof a home-field-scored build couldn't pass)",
          "the cocoon thread (settle stages around CC — this is its first concrete, gated form)",
          "telos: get Akien out of running the system (an inspectable input pipeline is one he can improve without re-deriving CC's reasoning)"
        ],
        "owner": "CC owns chart's code + proofs and gates writes to them. Akien owns the design ratification (the intent verbatim 2026-07-28, the naming /chart + /moreabout, the librarian unification). Each nexus's tree is its own table through db_domain's gate (Law 6).",
        "proof": "per-stone proofs under cairn/chart/proofs/, each sealed by the tester in a netns, validations persisted beside-code. Stone 1 (orient v0, PROVED 2026-07-28): test_chart_orient.py, 12 teeth — schema gate refuses missing/malformed fields; floor reports only what EXISTS (invented refs refuse); the floor COMPOSES the orient instrument (import-allowlist tooth); provenance travels per-field; the packet berths in instance-space; refusals loud. Stone 2 (tree stratum + dial, ticket chart-tree): test_chart_tree.py — ownership physics, gate-before-seed, counsel with its floor visible, the dial's fractions honest. Stone 3 (/moreabout v0, ticket moreabout, PROVED 2026-07-28): test_chart_moreabout.py, 7 teeth — deposited-state walk under a labeled floor; under-floor honesty (ranked nodes shown flagged, never collapsed to empty — the same-day 0.5775 evidence as a tooth); berths attach only what exists; the signal lands dated and dedups readably; refusals leave the tree standing; a re-run impossible by import physics (moreabout.py's only cairn door is cairn.chart.tree — falsifier 4 as structure). Stone 4 (constrain, ticket chart-constrain, PROVED 2026-07-28): test_chart_constrain.py, 7 teeth — template-fill linkage as physics (stage 2 refuses without a berthed stage 1); the floor surfaces ref'd charters' falsifier/gates/owner VERBATIM with address; the door composes the inspector's judges BY IDENTITY (nothing to drift between) and the inspector never imports back; berth round-trips, deposit-back gated, three composed doors exactly. Stone 5 (survey, ticket chart-survey, PROVED 2026-07-28): test_chart_survey.py, 7 teeth — the chain is physics at DEPTH 3 (a broken orient link under the constrain berth refuses loudly, never a shallow fill); the floor surfaces device_census rows verbatim and resolves refs by the GATE'S semantics (the commons-sibling tooth pins the false-absence cure); the door composes judge_survey by identity; berth round-trips; deposit-back gated; four composed doors exactly (the orient instrument joins — survey is the sweep stage). Stone 6 (decompose, ticket chart-decompose, PROVED 2026-07-28): test_chart_decompose.py, 7 teeth — the chain is physics at DEPTH 4 with the deep links checked by survey's own reader COMPOSED BY IDENTITY (one implementation of 'the chain holds', never a parallel walk); the floor hands the judges' vocabularies verbatim (holding addresses = the compose vocabulary, absence whats = the build vocabulary); a compose outside the holdings, a build outside the measured absences, and a why-less piece each refused at the berth by the inspector's own judge; berth round-trips; deposit-back gated; four composed doors exactly (no census re-scan — the territory was measured in stage 3). Stone 7 (triage, ticket chart-triage, PROVED 2026-07-28): test_chart_triage.py, 7 teeth — the chain is physics at DEPTH 5 with the deep links checked by decompose's own reader COMPOSED BY IDENTITY; the floor hands the coverage vocabulary verbatim (piece_whats — the exact multiset the order must cover — plus the pieces and the split's unknowns); a dropped piece, an invented piece, and a why_now-less entry each refused at the berth by the inspector's own judge; berth round-trips; deposit-back gated; four composed doors exactly. Stone 8 (hypothesize, ticket chart-hypothesize, PROVED 2026-07-28): test_chart_hypothesize.py, 7 teeth — the chain is physics at DEPTH 6 with the deep links checked by triage's own reader COMPOSED BY IDENTITY; the floor hands the covering vocabulary verbatim (ranked_whats + the order + the underlying split pieces + the ranking's unknowns); an uncovered ranked piece, a claim on an invented piece, and a claim without falsifier/instrument each refused at the berth by the inspector's own judge; berth round-trips; deposit-back gated; four composed doors exactly.",
        "provenance": "Cast 2026-07-28, the same day the intent was ratified — Akien: 'i did give you an intention' (CC had closed with 'casting awaits your word'; the word was already on the table). Design sources: CairnCommons/notes/20260728-claude-web-gamification-for-agents.md (imported verbatim from ~/.akien/) + CairnCommons/notes/held-chart-nexus-extraction.json (the same-day conversation: strata, chaining, cautions). The trigger: the web-server carrier miss (CC-- 2026-07-28) measured the premature-convergence failure at home; orientation is therefore stone 1. STONE 1 LANDED THE SAME DAY (ticket chart-orient) — and its first live fire caught its own builder: CC's first-cast floor carried a parallel charter-glob roster, never having surveyed cairn/orient (the prebuild instrument, PROVEN one day earlier) — premature convergence n=3, the first caught by the system instead of by Akien. Fixed as physics: the floor composes device_census, and the import-allowlist tooth pins cairn.orient.orient as the only door."
      }
    }
  ]
}