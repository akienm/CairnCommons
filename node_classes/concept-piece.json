{
  "class": "concept-piece",
  "role": "node-class-definition",
  "schema_version": "v0",

  "what": "A written artifact — an explanation, a design essay, a ruling — that yields no code and is proved by human judgment.",
  "why": "Not all work compiles to code; some of it is understanding made shareable (Telos 2–3). A concept-piece still gets proved and still feeds back — but its proof is review, not a tester run. This exists so the workflow rides on node-class, needing zero new artifact types: the same ticket + VALIDATION schema records a human-proved node.",
  "invariant": "proved + feeds-back-to-origin-on-failure (Laws 3 + 8)",
  "prove_gate": "quorum signature gate — N human reviewers read and sign. Recorded as a VALIDATION: method = 'review by N readers', caller = the reviewers, evidence = their restated-back understanding.",
  "extra_gates": "none beyond the quorum gate in v0.",
  "kicks_back_on": "a reviewer rejects → red → routes back to the point of creation exactly like a red build (CP2).",
  "workflow_versions": {
    "v1": {
      "path": ["THINKME", "TICKETME", "BUILDME", "PROVEME", "PROVED"],
      "skippable_summons": ["TICKETME"],
      "free_summons": ["WATCHME"],
      "why": "The machine-readable transition table the emit-chokepoint validates a concept-piece string against — the same physics code-seam rides, on the class whose BUILDME is writing the piece and whose PROVEME is the quorum's review. `path` is the canonical sequence; TICKETME forks exactly as it does for code (a leaf piece goes THINKME->BUILDME). WATCHME is a free summons: a written rule can carry a live watch on whether the corpus obeys it, and a watch must name its object. Registered to match the strings the corpus already carries (tickets/ticket-and-task.json claims concept-piece@v1 with this backbone); the demand that forced the registration is ticket an-artifact-is-present-tense-or-past-tense-never-both."
    }
  },
  "members_so_far": ["press_office/IntentionBasedDesignForHumans.md"],
  "provenance": "first concept-piece: IntentionBasedDesignForHumans.md, born + reviewed 2026-07-15. Note: verdict (reviewers) and seal (notary) are different hands here — not a code-proof."
}
