{
  "class": "skill",
  "role": "node-class-definition",
  "schema_version": "v0",
  "what": "A repo-linked command that shapes how the work loop runs — a question nexus (fixed question set fired at a variable artifact) plus its charter and proofs.",
  "why": "The work loop and its utilities are themselves precipitate of the loop (process artifacts); they need the same intent+proof discipline as any component. Skills are links into the repo, not copies — one source of truth, no sync.",
  "invariant": "proved + feeds-back-to-origin-on-failure (Laws 3 + 8)",
  "workflow_versions": {
    "v1": {
      "path": [
        "THINKME",
        "TICKETME",
        "BUILDME",
        "PROVEME",
        "LEARNME",
        "PROVED"
      ],
      "skippable_summons": [
        "TICKETME"
      ],
      "why": "Registered 2026-07-29 (ticket skills-ride-the-chokepoint), retiring the chokepoint's honest refusal found at /sail's first real crossing ('no registered workflow for class skill', filed sail edge (d)). The path mirrors code-seam@v1 verbatim — a skill is built, proved by its first real crossing, and learns, exactly like a code-seam — because the class's own invariant already names the shape: 'proved + feeds-back-to-origin-on-failure' needs a PROVE summons to hang on (PROVEME), and 'learns' needs a stage after it (LEARNME) before the rest (PROVED). `skippable_summons: [TICKETME]` is the same one fork the grammar cannot derive on its own (a leaf skill goes THINKME->BUILDME; a parent goes THINKME->TICKETME) — no different for a skill than for a code-seam. PROVE_GATE MAPPING: this class's `prove_gate` (below) is the derivation gate — cairnmap recompiles green — and it maps onto the PROVEME summons: crossing PROVEME is where that gate would fire, exactly as the tester's proof gate fires there for a code-seam. cairnmap ITSELF STAYS THE LABELED IOU IT ALREADY WAS (`prove_gate` text below is unchanged) — this stone registers the WORKFLOW VOCABULARY only, never builds the derivation gate. /CHALLENGE IS A KICK-BACK, NOT A SEVENTH STAGE: the skill prose's own cadence (skills/sail/intention+why.json: 'the skill's own prose sharpens by /challenge like every skill') re-enters an earlier summons on a finding — the grammar already derives back-edges to any earlier summons (PROVEME is the natural re-entry: a challenged skill is re-proved), so no new state is needed for it, same as a code-seam's kick-back."
    },
    "v2": {
      "path": [
        "THINKME",
        "TICKETME",
        "BUILDME",
        "PROVEME",
        "PROVED"
      ],
      "skippable_summons": [
        "TICKETME"
      ],
      "free_summons": [
        "WATCHME"
      ],
      "why": "Minted 2026-07-30 (ticket watchme-emits-a-probe). LEARNME LEAVES THE PATH ENTIRELY and WATCHME is not its replacement in the same seat — the shape changes, which is why this is a NEW VERSION and not a widening of v1 (versions are immutable and version-stamped; migration-by-find-and-replace is the mechanism MAP.md already names). WHAT WAS WRONG WITH v1: LEARNME sat in `path` with only TICKETME skippable, so it was MANDATORY TO CROSS for every node of this class and carried NO GATE — the one summons in the path that was forced and unchecked. Akien's rule is the exact inverse: OPTIONAL TO CARRY, MANDATORY TO SATISFY ONCE CARRIED. Under forced-and-unchecked the crossing got filled with whatever was at hand (on ticket a-node-holds-one-claim, 2026-07-29, it was close-bookkeeping — not efficacy data at all, and the node never dwelled).\n\nHOW THE INVERSION IS SPELLED: WATCHME is a FREE SUMMONS — it may appear ZERO OR MORE TIMES AT ANY POSITION, so it is absent from `path` (the backbone) and declared in `free_summons` instead. Optional to carry = the ticket author puts it in the string or does not. Mandatory to satisfy = once it IS in the string it is NOT in `skippable_summons`, so the chokepoint's forward walk cannot step over it. The two declarations are orthogonal on purpose: `skippable_summons` is the FORK a node takes at runtime, `free_summons` is the SHAPE the ticket authored.\n\nAND IT MUST NAME ITS OBJECT. `WATCHME(what-it-watches)` — a bare WATCHME is refused by _conform, because learning without an object is inert (Akien 2026-07-30): a bare token cannot state what is being learned, so nothing can check it. Forced by structure the way intention+why.json forces the why, never a blank field someone can leave empty.\n\nTHE REST IS UNCHANGED FROM v1 and deliberately so: the backbone, TICKETME as the one skippable fork, the build gate at the PROVEME crossing, the entry gate into BUILDME and the exit gate into PROVED all keep their seats. A node with a WATCHME crosses PROVEME forward INTO its watch (build gate fires there, as before — the gate is on crossing OF the PROVEME summons) and then into PROVED (exit gate fires there, as before). A node with no WATCHME reads exactly like v1 minus LEARNME.\n\nWHY THE NODE EMITS RATHER THAN BECOMES: a proved intention's efficacy data can only accumulate AFTER it rests, so a node that WAITS for it never rests and never gets measured. WATCHME's act is to EMIT A PROBE (cairn/base/probe.py — immutable, trigger/carrier/enough all open predicates); the node then rests at PROVED and the watcher stands. That deletes a state rather than adding one."
    }
  },
  "prove_gate": "derivation gate — cairnmap recompiles green: this skill renders from its intention+why.json AND no charter-less command renders. (Currently an IOU: cairnmap not built.)",
  "extra_gates": "the cairnmap recompile-gate is the skill class's distinguishing extra gate — a skill does not ship until the help that indexes it is current.",
  "kicks_back_on": "cairnmap incomplete (skill missing from the help, or a help entry with no charter) → red → fix the charter at source (the charter is the help).",
  "members_so_far": [
    "/intent",
    "/challenge",
    "/sorted",
    "/note",
    "/commit",
    "/saveslate",
    "/chart",
    "/moreabout",
    "/sail"
  ],
  "provenance": "first non-trivial node-class exercised; defined 2026-07-15 as the skill roster was built.",
  "retired_members": [
    {
      "member": "/loadslate",
      "retired": "2026-07-31",
      "superseded_by": "cairn/bin/cmd/slate, fired by a checked-in SessionStart hook",
      "disposition": "DELETED, not shelved — Akien's ruling: 'git is the record'. The charter, SKILL.md and PROOF.md are recoverable from history; this entry is the pointer that makes them findable, because a deletion nobody can find the reason for is the failure Law 7 warns about even when the bytes survive.",
      "why": "A SessionStart hook can run a command and inject its stdout, which nobody had noticed the host offered. So the first link in the workflow chain had been firing only when Akien remembered to type it, for two weeks — discipline doing physics' job (Law 4). Akien 2026-07-31: 'it should fire itself. i didn't know it could!' and, on scope, 'Full.' Finding the newest slate and printing three ratified fields is settled, so it compiled out of the skill entirely (Law 1). WHAT DID NOT COMPILE, and is now owned by nobody but the reader: the drift-check between the slate and MAP.md. The slates charter requires drift be 'named, not silently reconciled' — a program attempting that would be silent (the failure) or wrong (the worse one), so the program prints the slate's claims and says out loud, every session, that verifying them did not compile.",
      "precedent_set": "A skill retires when its question set stops needing a resolver — not when it stops being useful. The test is whether what remains is judgment. If nothing is left but judgment, the skill shrinks to that; if nothing is left AT ALL, it goes."
    }
  ],
  "roster_note": "MEASURED 2026-07-31: /chart, /moreabout and /sail were absent from this roster — the entire BUILD HALF of the workflow was never registered as belonging to its own class, though all three carry a charter (skills/<name>/intention+why.json) and are symlinked into ~/.claude/skills like every other member. Added. This is the same defect shape as the missing SKILL.md frontmatter found the same day (cairn f3f933b): the newest members of a set are the ones the set's own records forget, because registration is a step nothing enforces. The roster is a record of truth with no gate on it — an IOU against the cairnmap derivation gate this class already names in prove_gate, which would red on exactly this (a skill that renders with no roster entry)."
}
