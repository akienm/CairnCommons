{
  "id": "2026-07-25b-bus-substrate-and-the-two-gates",
  "date": "2026-07-25",
  "session": "2026-07-25, second boundary (follows 2026-07-25a-berth-migration-corrected). Long session, two arcs: (1) /sorted on BUS-COMPLETION — cast as a parent ticket with six children, after Akien collapsed CC's three proposed nouns into ONE at n=1; (2) the substrate that arc exposed got built and proven, ending in TWO GATES that each closed a fault the system had been carrying silently — the append door's shape gate and the quorum signature gate. The second of those retired the item Akien had been trying to get closed for ten days.",
  "author": "akiendelllinux_cc_0 (CC); Akien directing (the three-roles collapse; 'what the fuck is being waited on?'; 'this is what i keep trying to get sorted').",

  "at_sea": {
    "DONE + PUSHED + MEASURED — the quorum signature gate, and the first concept-piece VALIDATION in Cairn": {
      "where": "cairn fc6bfe2 (cairn/tester/quorum.py + validations_path_for_artifact + persist_validation generalized + cairn/tester/proofs/test_quorum.py 9/9) + CairnCommons 90f4d40 (the seal + the intention's header). Both pushed.",
      "what_landed": "node_classes/concept-piece.json has specified a quorum signature gate since 2026-07-15 and in that time ZERO concept-piece VALIDATIONs were written. THE CAUSE WAS AN ADDRESS, NOT A SIGNATURE: validation_store.validations_path_for derives a seal's location from a PROOF FILE path (proofs/x.py -> validations/x.json), and a concept-piece has no proof file, so the single write-door could not physically accept the record. Fixed by ADDRESSING ONLY — validations_path_for_artifact (<dir>/x.md -> <dir>/validations/x.json), persist_validation takes proof_path XOR artifact_path. Deliberately NO new artifact type and NO second record shape: a quorum seal is the SAME ratified eight fields through the SAME door (the class def already said so). quorum.seal() makes the class def's rules physics: a notary who also reviewed is REFUSED (self-seal = the hollow build in its purest form, Law 8); a signature with no `restated` is REFUSED (a rubber stamp proves someone clicked, not that anyone read); a quorum means DISTINCT hands; a red is a recorded VERDICT that kicks back (CP2), not an exception.",
      "the_seal": "CairnCommons/intentions-not-beside-code/validations/I-complete-diagnostic-on-first-pass.json — verdict GREEN, caller/reviewer `akien`, notary `cc` (different hands). Evidence is Akien's verbatim 2026-07-23 words plus the TWO corrections he made that were incorporated (build-minimal does not gate a proven tool; a forced second run is a learning signal, not a refutation) — which is what makes it a review and not merely an origin statement. The intention's header now reads PROVED.",
      "honest_about_the_seal": "The verdict was READ FROM THE RECORD of 2026-07-23, not collected fresh. The falsifier says exactly that: if Akien holds those two corrections do not amount to reviewing the final text, this seal is VOID. He overturns it by APPENDING a red beside it — never by an edit (Law 7).",
      "cc_defect_named": "CC reported this item as 'awaiting Akien's signature' across multiple sessions while the truth was that there was nowhere for a signature to land. That is a Law 4 problem (prose until there is physics) surfacing as a Law 1 defect (re-deriving the ask instead of building the door). Recorded in quorum.py's own header so the next reader gets it for free."
    },

    "DONE + PUSHED + MEASURED — the append door's SHAPE GATE, which paid for itself on the first run": {
      "where": "cairn 484e73d (cairn/charter/projector.py: UNIVERSAL_REQUIRED + RecordRefused; proofs 8 -> 12) + cairn/base/transitions.py (the fault it caught) + CairnCommons 88608ff (trouble CLEARED).",
      "what_landed": "The projector's append door now REFUSES, before any file is touched, a record missing `standing`. Ratified floor (Akien's selection, verbatim): 'Universal floor: standing only' — the door refuses any record without it; per-component declaration is FILED, not built. Measured adoption across 14 histories / 66 records: at + seq 100%, standing 98%, gate 80%, proof 56%, validation 32% — the floor was set at what the evidence already supported, not at what looked tidy. The refusal message carries the measurement and the trouble id, so the reader is not sent hunting.",
      "it_paid_immediately": "The first suite run after the gate went red in TWO places, both tracing to ONE site: cairn/base/transitions.py::emit journaled from/to/workflow/direction and NO `standing` — the one field harbor_master's register reads to place a boat. BEFORE fixing, MEASURED: zero emit-shaped records existed in any live history, so the fault had never become permanent. Fixed at the EMITTER (the one place that knows the crossing landed), not at every caller; a caller may still override with a richer line through journal_extra — the default is a floor, not a ceiling. Two new proofs in test_transitions.py hold it.",
      "trouble_closed": "CairnCommons/troubles/append-door-has-no-schema-gate.json -> standing CLEARED via TroubleDevice.clear(by='akien'), the clear door's first real use; live() is now []."
    },

    "DONE + PUSHED — the bus substrate the /sorted arc exposed": {
      "b2ca28c": "bus: the record went honest — 'PROVEN' was silently partial.",
      "784d248": "rename at sea -> at anchor -> berthed, in BOTH repos (build-to-the-tool: derivable from the native domain, no mapping to memorize).",
      "0d317e3": "diagnostic surface: the registry SEEDED and the instrument ATTACHED to the append door — the append door is now the first instrumented surface, which is why the shape gate had a place to report from.",
      "038e0c9": "callback: WHAT RIDES ALONG is a CARRIER — a callable, not a named kind (one less noun, per falsifier 10).",
      "5b8df6c": "shim: a poke per CROSSING, not per pulse — the level-flood MEASURED and cured (cairn/base/proofs/test_callback_crossing.py). A trigger parked true now pokes ONCE.",
      "788be5f": "trouble: the lane — and THE TROUBLE TICKET IS THE DAMPER. First raise writes the ticket and notifies; every raise after, while live, increments the count and notifies NOBODY. The record grows; the demand for attention does not. Clearing is the RECIPIENT'S and only the recipient's, per-recipient."
    },

    "CAST, NOTHING BUILT — bus-completion, a parent at TICKETME with six children": {
      "where": "CairnCommons/tickets/bus-completion.json (state: code-seam@v1, cursor at TICKETME). Commits 000c2f5, 8aaefff, f63e70c.",
      "akiens_collapse_at_n=1": "CC proposed three system-terms; Akien collapsed them to ONE, verbatim: 'the callback is the tool. the poke is what the callback is doing in sending a message to get the attention of something and the verb is more or less the endpoint... but we don't need that much terminology. it's just how i was using them.' So CALLBACK = the tool (already built), POKE = the act, VERB = the endpoint. Exactly one noun goes on the menu: `verb`. Explicitly NOT minted: handler, endpoint, dispatch table.",
      "the_build_consequence_that_fell_out": "Callback today carries to / channel / body — it names WHO to poke but not WHAT. The verb belongs on Callback too. CC would have missed this without the collapse.",
      "children": "a THE VERB (buildable now) · b THE ANNOUNCE MENU, and there is exactly one (waits on a) · c DELIVERY, THE POKE (waits on a) · d ONE EXCHANGE parameterized by its replies — not two shapes and not three (waits on a + c) · e external-device adapter · f the first real rider.",
      "the_measured_hole_child_a_closes": "shim.deliver's getattr(device, 'receive', None) silently returns None today. An unknown verb, a declared verb the device cannot serve, and a callback poking a verb its target does not offer must ALL go RED — never a silent poke into nothing (Law 7)."
    }
  },

  "next_direction": "BUS-COMPLETION CHILD a — THE VERB. It is the only child buildable now (b, c, d all wait on it), it closes a MEASURED Law 7 hole (the silent getattr(device,'receive',None) -> None), and it is the base shim's heavy lifting: a device declares its verbs and gets the rest free. Akien's standing order is that the bus PRECEDES the librarian. Do NOT open the librarian, the automated auto-berther, or any other big arc ahead of it without his explicit go-ahead. Before starting: /loadslate + read tickets/bus-completion.json fresh (Law 3 — this slate is a pointer, not a proof).",

  "open_threads": [
    "COINAGE AWAITING RATIFY — `crossing`, not `edge` (flagged, NOT ratified). Recorded as a distinction in bus-completion.json: 'edge' is the standard term but this codebase already spends 'filed edge' on an open design question, so overloading it costs a re-derivation on every read; 'crossing' is already native (cairn/base/diagnostic.py, system_rackmount's body={'crossed': ...}), so it introduces nothing and retires nothing. MY READ, not Akien's word — it is already in shipped code and comments, so a ratify-or-reject is owed.",
    "THE POKE is the trouble lane's largest filed edge — a raise today returns who WOULD be told and writes the record; actual delivery to an external device IS bus-completion child e. The two arcs meet there.",
    "TROUBLE RAISING IS NOT YET WIRED INTO FAILURE SITES. The lane exists and is proven; each site's identity must be chosen deliberately (the identity IS the damper key — a careless one either merges distinct faults or floods).",
    "ticket-and-task is a concept-piece at BUILDME — and as of this session it is SEALABLE, because the quorum gate now has an address. Same for any other concept-piece that has been sitting because there was nowhere to sign.",
    "FILED EDGES on bus-completion, each with the specific evidence that would open it: CHATTER DAMPING (needs a WIDTH, and no flapping signal has been measured — system_rackmount's CPU threshold is the likeliest first) and BUS RESIDUE / retention on the durable transit table (drew blood already: an end-to-end proof passed alone and went red IN THE SUITE, counting 2 pokes, because a previous run's message was still in db_domain). Both deliberately NOT guessed at (Law 3).",
    "PER-COMPONENT SCHEMA DECLARATION on the append door — filed, not built. The universal floor is `standing` only, by Akien's ratify. A component wanting a stricter shape has no way to declare it yet.",
    "state-machine-physics still at BUILDME with real children left: other node-classes' tables (concept-piece REVIEWME, host-seam), LEARNING driver-terminal + workflow-migration-by-find-replace, back-edge -> ask-Akien escalation, method-registry re-instatement. PROVEME waits on those.",
    "CARRIED FORWARD, UNTOUCHED THIS SESSION: system_rackmount FLAKE via the diagnostic-logging method; the AUTOMATED auto-berther (the hand-pass is done, the repeatable version is not); host-seam-installer name ratify + host-seam SCHEMA cast; a Cairn-NATIVE sudo-relay (today's is the UU-tree stopgap; the daemon hard-expires and needs `cairn sudorelay` if a resume needs root); intentions_model_compiler child c's hook LIVENESS confirm (out-of-turn, next real charter change — do NOT re-instruct Akien to open /hooks); the mail-notification /intent (held); web-server live-render horizon (waits on launcher wiring); harbor_master's learning surface open but unharvested.",
    "CLAUDE.md 'Rules awaiting physics' — mostly wait on the TICKETS-STORE (db/kernel physics), correctly left as tracked debt. The charter-state-history-split IOU is now effectively satisfied (the projector door is the sole append path, the split is self-hosted, and as of this session the door has a shape gate) — retire it when the tester's drift-check tooth lands."
  ],

  "measured_at_this_boundary": {
    "proofs": "33/33 proof files GREEN, run at this boundary (32 under cairn/*/proofs/ + 1 under bin/proofs/). The quorum proof is the 33rd.",
    "repos": "cairn at fc6bfe2, CairnCommons at 90f4d40. Both clean, both pushed.",
    "troubles": "live() == [] — the one trouble ever raised is cleared.",
    "concept_piece_validations": "1 (was 0 for ten days)."
  }
}
