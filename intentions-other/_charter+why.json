{
  "store": "intentions-other",
  "role": "store-charter",
  "schema_version": "v0 — migrates into the intention envelope when MAP.md Q6 lands",

  "what": "Intentions with no SINGLE code address to sit beside. Four kinds so far: host-seams (the implementation is the machine); concept-pieces (the prose IS the implementation); the ROOTS, `telos.md` and `core-values.md`, whose implementation is the entire system; and SPANNING intentions — the I-*.md, which have plenty of code but in many places at once (`I-heartbeat-callbacks-and-bus.md` is cited by ground_loop, bus, base/shim, base/callback and system_rackmount). Records here never migrate; a ticket that resolves becomes `history` in place.",

  "the_partition_question_sharpened": "The rule is NOT 'does this intention have code?' — it is 'does it have ONE code address?' A spanning intention has code in five files and therefore nowhere to be beside; putting it in any one of them would make the other four point sideways at a neighbor's charter. Homelessness comes from having no address OR from having many, and both land here (Akien, 2026-07-22: 'that's why I said they'd go in intentions-other'). Corollary worth keeping: the beside-code charter answers 'what is THIS component for'; a spanning intention answers 'what is this ARRANGEMENT for', and the second question has no directory.",

  "the_roots_are_first": "telos.md and core-values.md are the two oldest records here (both 2026-07-14, the day the repo was founded), so when `intentions/` is compiled in timestamp order they surface FIRST — the frame appears at the top of the scan by physics, not because a rule special-cases it (Akien, 2026-07-22; Law 4). This is why they need no separate `values/` container: splitting them into a sibling folder would have broken the seed claim outright, since carrying `intentions/` to a bare machine would have left the frame behind.\n\nWhy core-values belongs here and not beside `cairn/base/core_values.py`: that module is the ENFORCED form — a pin whose test trips if the words drift (`cairn/base/proofs/test_core_values.py`) — not the implementation. Law 2 holds CP1–CP6 everywhere, including in the process that builds the system, and a thing that holds everywhere has no single address to be beside. The pin is a canary, not a home.",

  "why": "Akien, 2026-07-22: 'we'd have intentions-other and intentions... and intentions is built from intentions-other AND the intentions now homed in the code.' Law 5 says intent shares its implementation's address — which silently assumes every implementation HAS an address we own. Some do not. A hook in ~/.claude/settings.json, a package on the box, a widget in ~/.local/share: real built things, fulfilled intentions, and nowhere in cairn/ to be beside. Without this store they are simply lost, and losing them loses knowledge — which is the commons test, met exactly.\n\nThe sharper reason is the seed claim: the intentions folder is meant to be the thing you carry to a new machine and regrow the system from. A seed compiled only over the beside-code charters regrows a repo that does not RUN, because everything that made the machine ready to run it was homeless. This store is what makes 'take that one folder out and rebuild the whole thing' true rather than nearly true.",

  "compiles_into": "`intentions/` — the system-scale compiled view, projected over this store PLUS the charters homed beside the code. Outer row of the three-scale projector recursion (ticket: history -> state; component: intentions+tickets -> charter; system: both sources -> intentions/). Consequence, load-bearing and not yet enforced: once `intentions/` is compiled it must never be hand-edited (CLAUDE.md, 'a compiled view is never hand-edited'). It is hand-authored TODAY. Tracked as an IOU, not a resting state (Law 4).",

  "owner": "each record has exactly one owner who gates writes to it (Law 6). The store owns the collection; the emit chokepoint validates records against the template.",

  "template": {
    "id": "string — stable unique id",
    "date": "ISO-8601 absolute date",
    "owner": "the one owner who gates writes to this node",
    "intention": "what this node is FOR (the WHAT), present tense",
    "why": "why it exists — required (traces upward)",
    "node_class": "host-seam | concept-piece. OPEN: the two roots carry no class yet — both are pre-envelope .md files that say they migrate when MAP.md Q6 lands, so Q6 assigns it rather than this store inventing a `root` class early. The concept-piece gate (quorum signature) already fits how both were ratified; core-values would additionally keep its standing pin-test gate.",
    "state": "the pipeline instance — where this node is in its class's gate-set",
    "gates": "the class's gate-set bound at casting",
    "apply": "host-seam ONLY, required — the replayable recipe. Complete enough that a bare machine reaches this state from this block alone, with nothing else from the repo. A node that merely describes its change is documentation, and documentation does not cast.",
    "verify": "host-seam ONLY, required — a re-runnable check that measures whether the claim is STILL true, plus what green looks like.",
    "falsifier": "what would show this node wrong (required to cast)",
    "horizon": "when to re-check — never optional for a host-seam, whose seal expires by nature",
    "parent": "optional — parent node id",
    "children": "array — child node ids"
  },

  "notes_on_the_store": [
    "A host-seam's seal EXPIRES. The host drifts with nothing in git changing, so a green VALIDATION here is a measurement with a date on it, not a permanent fact (Law 3).",
    "Failing loudly is the hard part. Host-config scripts conventionally fail silent (exit 0, keep the last good value) so they don't break the desktop — which means a dead node looks exactly like a healthy one. Re-verify is the only thing standing between those two states.",
    "The store is expected to stay SMALL and grow only against real need. One member at founding (usage-widget-hook). If it starts collecting every command ever run on the box, the filter has failed: a record earns its place by being something you would have to re-derive on a new machine."
  ],

  "provenance": "Founded 2026-07-22, the same afternoon the usage-widget hook was built without an intention. Akien named both the gap and the store. Demanded, not pre-seeded."
}
