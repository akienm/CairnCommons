{
  "id": "tickets-and-tasks",
  "date": "2026-07-22",
  "author": "Akien (the distinction) + CC (the test and the consequences)",
  "supersedes": "maintenance-is-an-axis-not-a-class \u2014 which had the shape half-right and the cause wrong",
  "status": "SUPERSEDED by tickets/ticket-and-task.json \u2014 cast 2026-07-22 via /sorted (concept-piece, quorum signature gate, cursor at BUILDME). This note is the working record of how the distinction was reached; the ticket is the record of truth. Berthing was scoped OUT at cast and is child (a), blocked on questions/roots-as-stations-or-shelves.",
  "the_distinction": "AKIEN, 2026-07-22: 'maybe the distinctions are TICKETS (their intentions become part of the fabric of the system immediately) and TASKS (things that need to be done by the system, but do not define the system). Both are workflows.'\n\nBoth are workflow instances \u2014 mutable strings, same machinery. What differs is whether the intention joins the FABRIC: the set of records that say what the system IS.",
  "why_this_beats_the_earlier_read": "CC had called it an AXIS \u2014 'what becomes of the ticket when it resolves: FREEZE into history, or DEPOT as a receipt.' That is the CONSEQUENCE, not the cause. Akien's version names the cause, and freeze-vs-depot then falls out of it rather than needing to be decided. Keep the cause; the disposition is derived.\n\nIt also explains the word IMMEDIATELY, which is load-bearing. A ticket moves the system's definition at CAST \u2014 before any work is done. That is exactly why its voyage must freeze beside the code: the fabric changed, so provenance is owed (Law 5). A task can be cast, run, and completed with the definition never moving \u2014 so there is no component whose charter its story is provenance OF. Depot is not a policy choice; it is what remains when nothing downstream needs the record.",
  "the_test": "THE SEED TEST \u2014 does replaying this intention on a BARE MACHINE contribute to regrowing the system? Ticket if yes, task if no.\n\n- Install the usage-widget hook: essential. The box cannot run Cairn's instrumentation without it. TICKET.\n- Yank the UU debris from ~/bin: a no-op on a fresh box; there is no UU there. TASK.\n\nThis is not a new gate. It is the test `intentions-other/` and `intentions/` already apply (the seed claim: carry that one folder to a bare machine and regrow the system). Ticket-vs-task therefore costs no new physics \u2014 /sorted asks a question the stores already ask (Law 1: don't re-derive the settled).\n\nCOROLLARY, sharp: a task's intention does NOT berth in `intentions-other/`. If it did, the seed would regrow instructions for cleaning a machine that was never dirty \u2014 the folder stops being a seed and becomes a diary. Where task intentions DO berth is open; the three-roots rule points at instance-space (~/.cairn/) for receipts and at commons only for what would lose knowledge if lost.",
  "recurrence_collapses": "Akien's two cases \u2014 'the intention might be really done' vs 'recurrent maintenance gets a new workflow instance next time its trigger rolls' \u2014 are one mechanism, not two. The standing REGIME is fabric ('host seals expire; re-verify on a horizon') and is a TICKET, cast once. Each firing is a TASK. A one-shot is the same shape with a trigger that will never fire again (UU stopped being a live source of debris). This also retires CC's earlier objection about nothing-is-terminal: no intention closes; the disposable thing was always the instance.",
  "tasks_can_graduate": "The yank is a task, but the rule it justifies \u2014 'no dead-predecessor debris on PATH' \u2014 is fabric. A task that discovers a rule SPAWNS a ticket. Without this the boundary is a trap door and knowledge found during maintenance is thrown away with the receipt.",
  "the_danger": "If TASK carries fewer gates than TICKET, gravity pulls everything toward task and the fabric quietly stops recording itself \u2014 the UU disease in a new costume. So the DEFAULT must be TICKET, and you argue your way OUT of the fabric by passing the seed test. Never into it by asserting 'this is just maintenance.' Whoever builds the /sorted gate should make ticket the fall-through and task the branch that must justify itself.",
  "confirms_the_earlier_call": "Maintenance is still not a fifth node_class. Ticket-vs-task cuts ACROSS the classes \u2014 it splits host-seam work down the middle (install the hook = ticket; yank the debris = task), which a class by definition cannot do. What Akien found sits on the workflow, not on the node.",
  "term_check": "MEASURED 2026-07-22 before coining: `task` is free. The only occurrence of the word anywhere in cairn/ or CairnCommons/ is `resolver-calls-per-solved-task` (MAP.md:190, 500), a unit of work in the demo metric \u2014 a different sense, no collision with a workflow kind.",
  "open": [
    "For a shareable recurring DEFINITION: cairn/ or CairnCommons/? The partition question applies unchanged (one code address or not), but `node_classes/operational-driver.json` never declared a `berth` field the way host-seam.json does. That is a gap in the class definition, not in the ruling.",
    "Does a task get a lighter gate-set than a ticket, or the same set with a different disposition? The danger clause says: not lighter without a fight."
  ],
  "the_flag_is_set_at_intent": "RULED (Akien, 2026-07-22: 'flag at intent, agreed'). The seed test is answerable from the WHAT alone \u2014 you do not need the approach, the typing, or the gate-set to know whether a bare machine needs this. It therefore belongs at the cheapest gate in the system (Law 1: kill what shouldn't exist before cost is spent). It also sharpens /intent's trace step: a task still traces up to a Law, but it traces to SERVING the system rather than DEFINING it.",
  "where_a_task_berths": [
    "RULED (Akien, 2026-07-22): 'if a task is a recurrent task and is required by the system, and would be required by the system on anybody else's computer as we share it, then it goes in the repo. If a task is transient and not recurring, then it goes in ~/.cairn. That leaves recurring for this specific system and/or user, but not something other users would want \u2014 that probably also goes in ~/.cairn.'\n\nCC'S READ: this collapses into the partition Cairn already has, which is why it needs no new rule. What goes in the repo for a recurrent shareable task is the DEFINITION of the recurrence \u2014 trigger, method-pointer, why \u2014 and that is an operational-driver, which is fabric, so it was already a TICKET by the fabric test. The individual FIRINGS are receipts and go to instance-space. The user-specific recurrent case is the same shape with the definition local too.\n\nSO: a task's DEFINITION is a class; a task's FIRING is an instance. class-space vs instance-space \u2014 exactly what CLAUDE.md already states ('no *runtime* state, ever'). The repo holds task definitions and never task records. And the shareability question Akien answered is the three-roots test unchanged ('if losing it loses knowledge, it's commons'). Nothing new to memorize; the ruling is the existing partition applied to a new kind of workflow."
  ],
  "the_empty_cell": "TRANSIENT BUT SHAREABLE is unfilled. The ruling covers transient-local, recurrent-local, and recurrent-shared. The fourth cell would be a one-shot every Cairn user needs \u2014 a corrective for a bad release, say. As the rule stands it lands in ~/.cairn, which means every user re-derives it: a Law 1 defect. CC's judgment is that the cell is genuinely EMPTY today \u2014 the migrations we have (charter -> state+history) change the fabric and are therefore tickets, not tasks \u2014 so the rule stands as written and the cell is FLAGGED rather than pre-built for (build minimal, grow against need). If a real member ever appears, that is the signal to revisit."
}
