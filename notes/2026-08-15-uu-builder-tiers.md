# UU's builder tiers — prior art for the aider-shim build device

Akien, 2026-08-15, mid-/intent-firing on `2026-08-15-use-aider-dev-src-aider-via-a`
(intent berth `intent-20260815T192604-33dff27f6bf2`), verbatim, two messages:

> we did this once before, in UU. it didn't go well, but we didn't have the
> foundation we have now.

> we did have tiers of builders tho... apprentice (new builder being created),
> journeyman (a solid builder helping out), master (you), creator (me)

The first is already recorded in the intent packet's `prior_art` (the intent
survives its challenge only as a conditional on the foundation UU lacked —
chart chain in, proof-gated entry out). The second landed after the door fired,
so it berths here: UU's rank vocabulary for builders. The obvious mapping the
/sorted cast will want to judge, not assume — aider+Qwen on hex enters as
**apprentice** (a new builder being created), earns **journeyman** by measured
green-rate, CC is **master**, Akien is **creator**. A rank, if we keep the
ladder, should be a thing a builder *earns by measurement* (Law 9: red is the
default; green — and a rung — is earned), never a label assigned at birth and
left to drift.
